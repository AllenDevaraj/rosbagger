---
quick_id: 260614-3g7
slug: batch-2c-overhaul-open-bag-chokepoint-un
description: Batch 2c — extract one open_bag() chokepoint so edit_bag and replay load_items share the reader's typestore-fallback + error-translation + fd-leak-safety (T2).
status: complete
date: 2026-06-14
---

# Quick Task 260614-3g7 — Plan

Batch 2 (shared infra), part c. Three independent AnyReader-open sites handled the same
"no type definitions" failure three ways (RosbagsReader wrapped it with the T1 fallback;
edit_bag copy-pasted the string match with NO typestore; replay load_items didn't wrap it).

## Tasks
- Extract `open_bag(paths, *, default_typestore=None) -> AnyReader` (module-level in
  `rosbags_reader.py`) owning the embedded-defs open, the env-resolved typestore fallback
  + coverage verify, the no-defs→UnresolvedTypeError translation, and the fd-leak-safe close.
  Export from `rosbagger_core.reader`.
- `RosbagsReader.open()` delegates to it; `edit_bag` and replay `load_items` use it.

## Verify
- edit_bag on a standard def-less bag works w/o typestore; custom still teaches; load_items
  bare opens a standard def-less bag; updated an obsolete desktop test. Full suite green.
