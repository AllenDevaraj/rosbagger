---
quick_id: 260614-37t
slug: batch-2b-overhaul-resolve-topics-chokepo
description: Batch 2b — a single resolve_topics() chokepoint that normalizes topic names and raises UnknownTopicError on a typo, killing the bagq edit --keep silent data-loss (T3).
status: complete
date: 2026-06-14
---

# Quick Task 260614-37t — Plan

Batch 2 (shared infra), part b: the topic-name chokepoint. `bagq edit big.bag --keep cmd_vel
-o out.bag` (missing the leading `/`) matched no connection and wrote an EMPTY bag while
reporting "Wrote out.bag (0 messages)" as success — the user deletes the original and keeps
nothing. Every topic-accepting surface exact-matched with no normalization and no suggestions.

## Tasks
- **errors.py** `UnknownTopicError` — the topic sibling of `UnknownTableError` (difflib
  did-you-mean over available topics, trying the slash-normalized form too).
- **new `rosbagger_core/topics.py`** `resolve_topics(requested, available)` + `normalize_topic`
  — exact match, else leading-slash-normalized match, else raise `UnknownTopicError`. Returns
  canonical names in request order. Stdlib-only.
- **`edit_bag`** resolves `keep`/`drop`/`downsample` against the bag's actual topics (after
  open, before any write) via `dataclasses.replace` — so `cmd_vel` normalizes to `/cmd_vel`
  and a typo raises. `UnknownTopicError` wired into bagq `teaching_errors`.

## Verify
- `resolve_topics` unit tests; `edit_bag` keeps /cmd_vel for `keep={'cmd_vel'}` (3 msgs, not 0);
  `keep={'cmdvel'}` raises with `/cmd_vel` suggestion; downsample normalizes + typo raises;
  CLI `--keep cmd_vel` exits 0 (no "(0 messages)"), `--keep cmdvel` exits non-zero with the
  suggestion and writes no bag. Full offline suite green.

## Out of scope (later)
- Applying resolve_topics to replay `--topics`/`--remap` and `RosbagsReader.read(topics)`
  (lower harm — empty result, not data loss); folds into Batch 2c/4.
