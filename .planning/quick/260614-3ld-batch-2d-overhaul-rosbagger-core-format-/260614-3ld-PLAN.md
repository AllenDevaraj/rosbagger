---
quick_id: 260614-3ld
slug: batch-2d-overhaul-rosbagger-core-format-
description: Batch 2d — one rosbagger_core.format module for human_size/human_dur, fixing the bagq GB-cap drift (R2/R3).
status: complete
date: 2026-06-14
---

# Quick Task 260614-3ld — Plan

Batch 2 (shared infra), part d. `_human_size` and `_human_dur` were each implemented three
times (bagq, Textual GUI, PySide6 desktop) and had DRIFTED: bagq's `_human_size` capped at
GB so a 2 TiB bag printed `2048.0 GB` while the GUIs printed `2.0 TB`.

## Tasks
- New stdlib-only `rosbagger_core/format.py`: `human_size` (1024-based, B..PB, no GB cap) +
  `human_dur` (ms/s). Single source of truth.
- The six face copies (bagq cli.py ×2; gui inspect/tf; desktop inspect_panel/tf_panel) delegate
  to it — lazy-imported INSIDE each function to honor every face's module-top import discipline.

## Verify
- `test_format.py` pins behavior incl. the 2 TiB → "2.0 TB" drift fix; updated the bagq test
  that asserted the old GB cap. Full offline suite green; offline guard green.
