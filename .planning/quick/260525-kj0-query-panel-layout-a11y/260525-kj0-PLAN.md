---
phase: quick-260525-kj0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-desktop/src/rosbagger_desktop/panels/query_panel.py
  - tests/test_desktop.py
autonomous: true
requirements: [P2-layout, P2-a11y]
must_haves:
  truths:
    - "The schema tree, results table, and history list sit inside a user-draggable QSplitter so the user controls vertical allocation"
    - "The status label is exposed to assistive tech as a status/alert region (accessible name set) and announces teaching errors"
    - "A teaching-error status is visually distinct from a neutral status (error styling applied on the error path, cleared on success)"
    - "The verbatim teaching-error text (str(e)) still appears in status_label unchanged"
    - "All existing public accessors and threaded-query behavior keep working unchanged"
    - "Offline import guard still passes (no new rosbagger_core import at module top)"
  artifacts:
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/panels/query_panel.py"
      provides: "QSplitter-wrapped resizable regions + accessible/styled status label"
      contains: "QSplitter"
    - path: "tests/test_desktop.py"
      provides: "Headless pytest-qt coverage for the splitter and a11y status behavior"
      contains: "QSplitter"
  key_links:
    - from: "QueryPanel.__init__"
      to: "QSplitter holding schema_tree / results_view / history"
      via: "splitter.addWidget(...) replacing direct layout.addWidget(...)"
      pattern: "QSplitter"
    - from: "QueryPanel status error path (_on_query_failed)"
      to: "QAccessible announcement + error stylesheet"
      via: "_set_status(text, is_error=True)"
      pattern: "setAccessibleName|QAccessible"
---

<objective>
Polish the rosbagger-desktop Query panel per the impeccable audit's two remaining P2 findings, under pyside6-ui-engineer guidance:

1. **P2-layout** — the schema tree, results table, and history list currently stack in one `QVBoxLayout` with fixed stretch factors and crush each other on a short window. Wrap the three resizable regions in a vertical `QSplitter` so the user drags to rebalance.
2. **P2-a11y** — query status/teaching errors render into a plain `QLabel` that screen readers don't announce and that gives no visual error distinction. Give the status label an accessible status/alert role + name, announce on the error path via `QAccessible`, and apply an error style that's cleared on success.

Purpose: Make the panel's visual hierarchy controllable and its teaching errors perceivable by assistive tech and sighted users alike — without changing what the panel SAYS (core still owns the teaching text) or what it DOES (threaded query intact).
Output: An updated `query_panel.py` (presentation-only changes) and extended headless `tests/test_desktop.py` coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<constraints>
THIN-FACE: presentation-only. No SQL building, no analysis, no new error message text — core still owns the teaching message; the panel only styles/announces it. Status text CONTENT stays identical (teaching errors render `str(e)` verbatim — tests assert `UnknownTableError` text appears in `status_label`).

OFFLINE-IMPORT INVARIANT (D-08): no new `rosbagger_core` imports at module top; any such import stays inside method bodies. `tests/test_offline_guard.py` must still pass (it scans only the `rosbagger_core`/`bagq` import graphs — PySide6 `QtGui`/`QAccessible` at this desktop module's top level is fine; Qt is this package's own dependency, already imported at top of `widgets/scrubber.py`).

ISOLATION: changes confined to `packages/rosbagger-desktop` + `tests/`. Do NOT touch core/bagq/record/replay/gui source.

PRESERVE every existing public property tests use: `sql_input`, `run_button`, `results_table`, `status_label`, `schema_tree`, `export_csv_button`, `export_parquet_button`, `history_list` — AND the threaded-query behavior from 260525-is6 (`BlockingWorker`, `waitUntil`-based tests, `_query_thread`/`_query_worker`/`_pending_sql` refs).

HOST: prefix all uv/pytest/ruff with `PYTHONPATH=""`. Run tests with `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest`. An intermittent "Fatal Python error: Bus error" can print at process EXIT under offscreen Qt — a known teardown artifact, not a failure; re-run to confirm a clean `passed/skipped` line.
</constraints>

<interfaces>
<!-- Current QueryPanel.__init__ layout (query_panel.py ~lines 164-205), to be modified: -->
- `self._status = QLabel("Open a bag to query")` — the status/teaching line. Status text is set in 6 places via `self._status.setText(...)`: `refresh_view` (empty-state), `_run_query` (no-reader / no-sql / running-guard / "Running…"), `_on_query_result` ("N row(s) · M column(s)"), `_on_query_failed` (verbatim teaching `message`), `_export` (export status/errors).
- Query bar (`QHBoxLayout`: `_sql_input` + `_run_button`) — fixed-height control, stays OUTSIDE the splitter.
- `self._schema_tree = QTreeWidget()` — resizable, goes IN the splitter (was `layout.addWidget(self._schema_tree, 1)`).
- `self._results_view = QTableView()` (model `self._result_model`) — resizable, goes IN the splitter (was `layout.addWidget(self._results_view, 2)`).
- Export bar (`QHBoxLayout`: `_export_csv` + `_export_parquet`) — fixed-height control, stays OUTSIDE the splitter.
- `QLabel("History")` + `self._history = QListWidget()` — the history list is resizable, goes IN the splitter (was `layout.addWidget(self._history)`).
- Public accessors that MUST keep returning the same widgets: `schema_tree`→`_schema_tree`, `results_table`→`_results_view`, `history_list`→`_history`, `status_label`→`_status`.

<!-- The teaching-error path that must keep verbatim text + gain announcement/styling: -->
- `_on_query_failed(self, message: str)` sets `self._status.setText(message)` — `message` is the verbatim `str(exc)` from the worker (the teaching contract). This is the ONLY error path for query teaching errors.
- `_on_query_result(...)` sets the neutral "N row(s) · M column(s)" status on success — this is where error styling must be CLEARED.

<!-- Existing top-of-file PySide6 imports (query_panel.py ~lines 46-61): -->
- `from PySide6.QtCore import QAbstractTableModel, QModelIndex, Qt, QThread`
- `from PySide6.QtWidgets import (QFileDialog, QHBoxLayout, QHeaderView, QLabel, QLineEdit, QListWidget, QListWidgetItem, QPushButton, QTableView, QTreeWidget, QTreeWidgetItem, QVBoxLayout, QWidget)`
- For this work: add `QSplitter` to the `QtWidgets` import, and add a new `from PySide6.QtGui import QAccessible, QAccessibleEvent` (QtGui import is fine at top — Qt is this package's own dep).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap the three resizable regions in a vertical QSplitter</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/panels/query_panel.py</files>
  <action>
Per P2-layout: replace the flat region stacking in `QueryPanel.__init__` with a vertical `QSplitter` so the user drags to allocate space.

Add `QSplitter` to the existing `PySide6.QtWidgets` import tuple. Keep the schema tree, results view, history list, and the "History" label constructed exactly as now (do NOT rename `_schema_tree`/`_results_view`/`_history` — the public accessors return them unchanged).

Build a `QSplitter(Qt.Vertical)` and add the three resizable regions as its children in current visual order: schema tree, results view, then a small history container (a `QWidget` holding a `QVBoxLayout` with the `QLabel("History")` then `self._history`, so the section title travels with the list inside the splitter). Give the splitter sensible initial proportions reflecting the old 1:2 stretch with history getting the least — e.g. `splitter.setSizes([1, 3, 1])` (results widest). Optionally `splitter.setChildrenCollapsible(False)` so a region can't be dragged to zero.

Rebuild the outer `QVBoxLayout(self)` so fixed-height controls stay OUTSIDE the splitter, in this order: `_status`, query bar (`QHBoxLayout`), the `QSplitter`, export bar (`QHBoxLayout`). Add the splitter with a stretch factor so it takes the free vertical space. Remove the old direct `layout.addWidget(self._schema_tree, 1)` / `addWidget(self._results_view, 2)` / `addWidget(QLabel("History"))` / `addWidget(self._history)` lines now that those live in the splitter.

Keep all existing signal wiring untouched. This is layout-only — no behavior, no new imports beyond `QSplitter`. Do not place fenced code in comments; keep the module docstring accurate (the layout sentence can mention the splitter).
  </action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -k "query" -q</automated>
  </verify>
  <done>A `QSplitter(Qt.Vertical)` contains the schema tree, results view, and the history container; status/query bar/export bar are fixed-height siblings outside it; `schema_tree`/`results_table`/`history_list` accessors return the same widgets; existing query tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Make the status label an accessible, error-styled live region</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/panels/query_panel.py</files>
  <action>
Per P2-a11y: keep the status text content identical (teaching errors still render `str(e)` verbatim) while making it perceivable by assistive tech and visually distinct on error.

Add `from PySide6.QtGui import QAccessible, QAccessibleEvent` at module top (QtGui is allowed at top — this is the desktop package's own Qt dep, mirroring `widgets/scrubber.py`).

In `__init__`, after constructing `self._status`, set an accessible name marking it a status region: `self._status.setAccessibleName("Query status")` (and a stable `objectName`, e.g. `self._status.setObjectName("query_status")`, so the error stylesheet can target it). Define two style constants at module level: a neutral style (empty/default) and an error style giving a clear error color/emphasis (e.g. a red foreground/border — `self._status.styleSheet()`-applied). Keep it presentation-only.

Introduce a single private helper `_set_status(self, text: str, *, is_error: bool = False) -> None` that: (1) sets `self._status.setText(text)`; (2) applies the error stylesheet when `is_error` else clears it back to neutral; (3) updates `self._status.setAccessibleDescription(text)` so the value is exposed; (4) on the error path, posts a `QAccessible` alert announcement for the label so screen readers announce it — e.g. `QAccessible.updateAccessibility(QAccessibleEvent(self._status, QAccessible.Alert))` (use the alert/announcement event available in this PySide6; if the simplest reliable call is `QAccessible.updateAccessibility` with an `Alert` event, use that). Guard the announcement so it is a no-op-safe call under the offscreen platform (it must not raise headlessly).

Route the status updates through `_set_status`: call `self._set_status(message, is_error=True)` in `_on_query_failed`, and `self._set_status(..., is_error=False)` on the success path in `_on_query_result` (this CLEARS the error style after a teaching error is later fixed). The other neutral status sets (`refresh_view`, `_run_query` guards/"Running…", `_export`) should also go through `_set_status(text)` with `is_error=False` so styling is consistently cleared — but do NOT change any of their text strings. The `_export` "Export failed: …" line MAY pass `is_error=True` (it is an error), but keep its text verbatim.

THIN-FACE: build no new message text; `message` in `_on_query_failed` stays the verbatim worker string. Verify `tests/test_offline_guard.py` still passes (no `rosbagger_core` import added at top).
  </action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -k "query" tests/test_offline_guard.py -q</automated>
  </verify>
  <done>`status_label` carries an accessible name; the teaching-error path applies error styling + posts a QAccessible alert while preserving the verbatim `str(e)` text; the success path clears the error style; offline guard still passes; no new top-level core import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend headless pytest-qt tests for the splitter and a11y status</name>
  <files>tests/test_desktop.py</files>
  <behavior>
    - Splitter exists: a `QSplitter` is reachable from the query panel and its children include the schema tree, results view, and the history list (directly or via the history container) — proving the three regions were regrouped under user-draggable control.
    - Accessible status: `panel.status_label.accessibleName()` is non-empty (the status region is named for assistive tech).
    - Error path styles + preserves text: after running `SELECT * FROM definitely_not_a_real_table`, the status carries the verbatim teaching text (existing assertion: non-empty, no "row(s)") AND an error style is set (non-empty `styleSheet()` or an error marker), and the QAccessible announcement path runs without raising headlessly.
    - Success clears error: after a subsequent successful `SELECT * FROM <table> LIMIT 1`, the status reports "row(s)" again AND the error style is cleared (empty/neutral `styleSheet()`), proving the error styling is path-scoped.
  </behavior>
  <action>
Extend `tests/test_desktop.py` (do NOT modify the module-top `QT_QPA_PLATFORM=offscreen` guard or the self-contained sys.path harness). Add two focused tests near the existing `test_query_panel_runs_real_core`:

(1) `test_query_panel_regions_live_in_splitter` — write a ROS2 sqlite fixture (`write_ros2_sqlite_bag`), open `MainWindow`, `qtbot.addWidget`, call `panel.refresh_view()`. Find the `QSplitter` via `panel.findChild(QSplitter)` (import `QSplitter` from `PySide6.QtWidgets` inside the test) and assert it is not None. Assert the schema tree and results view are descendants of the splitter (e.g. `panel.schema_tree` and `panel.results_table` are found via `splitter.findChild(...)` or their `isAncestorOf` relationship), and that the history list (`panel.history_list`) is likewise inside it. Keep the assertions robust to the history-container wrapper (use ancestor/`findChild` checks, not exact child indices).

(2) `test_query_status_announces_and_styles_errors` — open the window on the fixture, derive a real table name from `panel.schema_tree.topLevelItem(0).text(0)` (mirror the existing test so it's fixture-robust). Assert `panel.status_label.accessibleName()` is truthy. Drive the teaching-error path: set `SELECT * FROM definitely_not_a_real_table`, click Run, `qtbot.waitUntil` the status settles to a non-empty teaching string with no "row(s)" (mirror the existing teaching-leg wait), then assert the error style is applied (`panel.status_label.styleSheet()` non-empty, or whatever marker Task 2 sets) — the verbatim text assertion stays as in the existing test. Then drive a successful `SELECT * FROM <table> LIMIT 1`, `waitUntil` the model fills / status shows "row(s)", and assert the error style is cleared (neutral/empty `styleSheet()`). Use `qtbot.waitUntil` for every threaded transition (no synchronous result assumptions — the query runs on `BlockingWorker`).

Keep tests headless and assert observable state only (accessibleName, styleSheet, child relationships, label text) — never pixels or hover. After implementation, confirm a clean `passed/skipped` line (re-run if the known offscreen exit Bus-error artifact prints).
  </action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -q</automated>
  </verify>
  <done>Two new tests pass headlessly: one proves the three regions live under a QSplitter, one proves the status label is accessibly named, applies error styling + announces on the teaching-error path, and clears the error style on success. Full `tests/test_desktop.py` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user SQL → query() | Untrusted SQL string forwarded verbatim to core (unchanged by this work) |
| core teaching error → status label | `str(exc)` rendered as text + announced (presentation only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-kj0-01 | Information disclosure | status accessible description carries `str(exc)` | accept | Same verbatim teaching text already shown visually (no new content); announcement exposes nothing the screen doesn't |
| T-kj0-02 | Tampering | thin-face invariant (no SQL/analysis added) | mitigate | Changes are layout + label styling/announcement only; SQL still built by user, teaching text still built by core; offline guard test enforces no new top-level core import |
| T-kj0-03 | Denial of service | QAccessible call under offscreen Qt | mitigate | Announcement guarded so it never raises headlessly; tests run under `QT_QPA_PLATFORM=offscreen` to prove it |

No package installs in this plan (no Package Legitimacy Gate needed).
</threat_model>

<verification>
- `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py tests/test_offline_guard.py -q` → all pass (clean `passed/skipped` line; re-run if the offscreen exit Bus-error teardown artifact prints).
- `PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src/rosbagger_desktop/panels/query_panel.py tests/test_desktop.py` → clean.
- Manual grep: no new `from rosbagger_core` / `import rosbagger_core` at the top of `query_panel.py` (only inside method bodies).
</verification>

<success_criteria>
- The schema tree, results view, and history list live inside a vertical `QSplitter` the user can drag to rebalance; status/query/export bars stay fixed-height outside it.
- All public accessors (`schema_tree`, `results_table`, `history_list`, `status_label`, `sql_input`, `run_button`, `export_csv_button`, `export_parquet_button`) return the same widgets and the 260525-is6 threaded-query behavior is unchanged.
- `status_label` has an accessible name; the teaching-error path applies a distinct error style and posts a QAccessible alert while keeping `str(e)` verbatim; the success path clears the error style.
- Offline-import invariant holds (`tests/test_offline_guard.py` passes; no new top-level core import).
- Changes confined to `packages/rosbagger-desktop` + `tests/`.
</success_criteria>

<output>
Create `.planning/quick/260525-kj0-query-panel-layout-a11y/260525-kj0-SUMMARY.md` when done.
</output>
