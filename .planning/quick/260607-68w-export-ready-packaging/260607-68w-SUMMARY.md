---
quick_id: 260607-68w
slug: export-ready-packaging
description: Make rosbagger export-ready as a package (PyPI metadata, simple README, one-command installer, publish guide). GitHub push deferred.
status: complete
date: 2026-06-07
commits: 51b8129..fba3286
---

# Quick Task 260607-68w — Summary

## Outcome

rosbagger is now **export-ready as a Python package set**. All 7 packages build
valid, `twine check`-clean wheels/sdists; the README is simple and accurate; a
one-command installer and a publish guide are in place. Two clean-install crash
bugs were found and fixed along the way. GitHub push intentionally deferred to the
user. The repo was **not** turned into a colcon/ament ROS package — that would
break the zero-ROS offline guarantee (explicitly rejected).

## What changed

**1. PyPI-publishable metadata (all 7 packages)** — `51b8129`
- Added `description`, `readme = "README.md"`, `license = "MIT"` (SPDX) +
  `license-files = ["LICENSE"]`, `authors`, `[project.urls]`, `keywords`, and
  `classifiers` to every package `[project]` table.
- Wrote a concise per-package `README.md`; copied the MIT `LICENSE` into each
  package so each wheel bundles its license (`dist-info/licenses/LICENSE`).
- **Verified:** `uv build` succeeds for all 7; `twine check` PASSES on all 14
  artifacts (Metadata-Version 2.4, `License-Expression: MIT`).

**2. Simple README + complete INSTALL matrix** — `11521c9`
- Rewrote the root README: quickstart-first, one-command install, bagq usage, a
  7-package suite table, the single-transaction rationale, dev + publish pointers.
  Removed the stale `0.1.0` examples (drift fix).
- INSTALL.md: five→seven packages (added `rosbagger-desktop` + `rosbagger-rerun`),
  generalized the sibling-pin language, made the section-1 "install everything"
  git recipe actually install all seven, added a desktop path recipe + install.sh
  TL;DR.

**3. One-command installer + publish guide** — `a1bdf6a`
- `install.sh` (executable): creates/reuses a venv and installs the requested
  packages in ONE pip transaction (so bare sibling specs co-resolve). Default =
  offline CLI (core + bagq); flags `--plot/--gui/--desktop/--live/--all/--venv`.
  Resolves paths from the script dir → runs from any CWD.
- `PUBLISHING.md`: uv-native path (`uv build` per package, `uvx twine check`,
  `uv publish`), core-first ordering, TestPyPI dry run, version-bump + tagging.

**4. Clean-install bug fixes (surfaced by verification)**
- `fix(bagq)` `3b9106a`: bagq imports `click` directly (rebuilds the `--plot`
  option as a native `click.Option`) but only got it transitively via typer; newer
  in-range typer (0.26.7) no longer pulls click, so a clean `pip install bagq`
  crashed at import. Declared `click>=8.1,<9`.
- `fix(rosbagger-gui)` `ec54ae6`: `scrubber.py` imports `rich` (transitively via
  textual). Declared `rich>=13`.
- Both found via an **AST module-level-import audit** over all 7 packages —
  now reports zero undeclared module-level imports, and zero env-ROS imports at
  module level (offline invariant intact).
- `chore(deps)` `fba3286`: refreshed `uv.lock` for the new edges.

## Verification (evidence)

- `uv build` × 7 + `twine check` × 14 → all PASSED.
- Clean-room install into a throwaway venv (default path): builds, installs,
  `bagq --version` → `bagq 0.2.0`, `bagq query --help` exit 0 (the typer→click
  `--plot` rebuild works under typer 0.26.7), offline invariant holds (no
  rclpy/rosbag2_py leaked).
- AST import audit across all 7 packages → "none ✓".
- `uv sync --locked --dev` passes; offline suite **604 passed / 6 skipped, exit 0**,
  coverage gate held.
- `bash -n install.sh` clean; `install.sh` committed mode 100755.

## Deferred / handoff

- **GitHub push** (user's choice): `git push -u origin main && git push origin --tags`.
  The remote is currently empty, so nothing clones yet. The INSTALL.md git recipes
  and PUBLISHING.md tagging step activate once pushed + tagged.
- **Actual PyPI upload** is documented in PUBLISHING.md but not performed.
