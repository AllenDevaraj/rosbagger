---
phase: 08-packaging-docs-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-core/pyproject.toml
  - packages/bagq/pyproject.toml
  - packages/rosbagger-core/src/rosbagger_core/__init__.py
  - packages/bagq/src/bagq/__init__.py
  - uv.lock
  - LICENSE
  - README.md
autonomous: true
requirements:
  - "DoD: bagq installs via pip and exposes info/tables/query"
  - "DoD: offline packages import without rclpy"
  - "DoD: all v1 requirements covered by tests (CI green)"

must_haves:
  truths:
    # --- Verified LOCALLY (autonomous, this plan proves these) ---
    - "[verified-locally] Both packages report version 0.1.0: `bagq --version` prints `bagq 0.1.0` and `rosbagger_core.__version__ == '0.1.0'`"
    - "[verified-locally] `uv sync --locked --dev` succeeds after the bump (uv.lock re-locked to 0.1.0 and committed)"
    - "[verified-locally] In a FRESH throwaway venv (NOT .venv), `pip install ./packages/rosbagger-core ./packages/bagq` installs both and `bagq --help` exits 0; `bagq info|tables|query --help` each exit 0 (SC1)"
    - "[verified-locally] In that venv, from a neutral cwd, `import rosbagger_core, bagq` succeeds with NO rclpy/rosbag2_py in sys.modules, and `tools` is NOT importable (no tools/ in either wheel) (SC2)"
    - "[verified-locally] The local CI-equivalent gate is green: PYTHONPATH='' uv run -> `uv sync --locked --dev`, `ruff check .`, `ruff format --check .`, `pytest` (>=80% coverage gate)"
    - "[verified-locally] An annotated git tag `v0.1.0` exists locally"
    - "[verified-locally] An MIT LICENSE file exists at the repo root"
    - "[verified-locally] README documents the verified plain-pip install recipe, the uv dev path, the bagq[plot] extra, a per-command quickstart (info/tables/query), and the offline/no-ROS guarantee"
    # --- AWAITS PUSH (human follow-up — known, recorded gap; SC3 remote half) ---
    - "[awaits-push] `git push origin main` and `git push origin v0.1.0` then observing the GitHub Actions run go green — BLOCKED: no gh CLI / no push credential (standing STATE.md blocker). Documented in SUMMARY as the sole human follow-up; NOT a blocking checkpoint."
  artifacts:
    - path: "LICENSE"
      provides: "MIT license at repo root"
      contains: "MIT License"
    - path: "README.md"
      provides: "Install recipe + per-command usage + offline guarantee"
      contains: "pip install ./packages/rosbagger-core ./packages/bagq"
    - path: "uv.lock"
      provides: "Lockfile re-locked to 0.1.0 (matches manifests so uv sync --locked passes)"
      contains: "0.1.0"
    - path: "packages/bagq/src/bagq/__init__.py"
      provides: "bagq __version__ that `bagq --version` reads"
      contains: '__version__ = "0.1.0"'
    - path: "packages/rosbagger-core/src/rosbagger_core/__init__.py"
      provides: "rosbagger_core __version__"
      contains: '__version__ = "0.1.0"'
    - path: "packages/bagq/pyproject.toml"
      provides: "bagq distribution version 0.1.0"
      contains: 'version = "0.1.0"'
    - path: "packages/rosbagger-core/pyproject.toml"
      provides: "rosbagger-core distribution version 0.1.0"
      contains: 'version = "0.1.0"'
  key_links:
    - from: "packages/bagq/src/bagq/cli.py"
      to: "bagq.__version__"
      via: "from bagq import __version__ -> `bagq --version` prints it"
      pattern: "from bagq import __version__"
    - from: "uv.lock"
      to: "pyproject.toml version fields"
      via: "uv lock re-sync; uv sync --locked enforces match"
      pattern: "0.1.0"
    - from: "README.md install recipe"
      to: "bagq's bare `rosbagger-core` dependency"
      via: "both local packages in one pip command (pip ignores [tool.uv.sources])"
      pattern: "rosbagger-core ./packages/bagq"
---

<objective>
Ship rosbagger v0.1.0: bump both packages 0.0.0 -> 0.1.0 across all four version
sources and re-lock, add an MIT LICENSE, expand the README with the verified
install recipe and per-command quickstart, then prove the release locally —
clean-room pip install (SC1), offline import with no rclpy (SC2), the full local
CI-equivalent gate, and an annotated `v0.1.0` tag.

This is the FINAL v0.1 phase. It adds NO new dependencies and NO runtime code —
it is version/docs/license/packaging only.

Purpose: Satisfy the Definition of Done — bagq installs via pip exposing
info/tables/query, offline packages import without rclpy, the suite is green,
and the release is tagged 0.1.

Output: 0.1.0 across all sources + committed uv.lock; LICENSE; expanded README;
a local annotated `v0.1.0` tag; and a SUMMARY that distinguishes what was
verified locally from the one human follow-up (push + observe remote CI).

SC3 is SPLIT and encoded honestly: the local gate + local tag are autonomous and
done here; PUSHING the branch+tag and OBSERVING GitHub Actions go green CANNOT be
done in this environment (no `gh`, no push credential — standing STATE.md
blocker). This plan stays `autonomous: true`: it completes all local work and
records "push + observe CI green" as a clearly-stated human follow-up in the
SUMMARY (a known, recorded gap), NOT a blocking checkpoint that stalls the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-packaging-docs-release/08-RESEARCH.md

# The CLI surface to document accurately in the README (what shipped P4-P7):
@packages/bagq/src/bagq/cli.py

<interfaces>
<!-- Key facts the executor needs. Extracted from the codebase + 08-RESEARCH. -->
<!-- Use these directly — no codebase exploration needed. -->

Version lives in FOUR source sites, all currently "0.0.0" (08-RESEARCH Pitfall 3):
  - packages/rosbagger-core/pyproject.toml      line 8:  version = "0.0.0"
  - packages/bagq/pyproject.toml                line 7:  version = "0.0.0"
  - packages/rosbagger-core/src/rosbagger_core/__init__.py  line 9:  __version__ = "0.0.0"
  - packages/bagq/src/bagq/__init__.py          line 3:  __version__ = "0.0.0"

PLUS uv.lock pins version = "0.0.0" twice (08-RESEARCH Runtime State Inventory):
  - uv.lock line 121  ([[package]] name = "bagq")
  - uv.lock line 1239 ([[package]] name = "rosbagger-core")
  uv.lock is regenerated by `uv lock` after the source edits — do NOT hand-edit it.

`bagq --version` reads bagq.__init__.__version__ (NOT the wheel metadata):
  packages/bagq/src/bagq/cli.py:  `from bagq import __version__`  ->  `typer.echo(f"bagq {__version__}")`
  So bumping bagq/__init__.py is what makes `bagq --version` report 0.1.0 (Pitfall 3).

The shipped CLI surface (for the README quickstart — accurate to P4-P7, from cli.py):
  - bagq info BAG...                 # topics, msg types, counts, duration, ~Hz, size
  - bagq tables BAG...               # per-topic table name + column schema
  - bagq query "<SQL>" BAG... [-o OUT.{csv,parquet}] [--format table|csv|parquet|json] [--plot [FILE]]
      * default: rich table to stdout (OUT-01)
      * -o out.csv / -o out.parquet: write by extension (OUT-02/03); -o always wins over --format
      * --format table|csv|parquet|json: sink when no -o (csv streams to stdout; parquet w/o -o errors; json = temporal-safe records)
      * --plot: minimal headless line chart of numeric cols vs t_ns; bare = plot.png in CWD, --plot FILE = that file (OUT-04)
  - bagq --version                   # prints "bagq 0.1.0"
  - bagq --help / python -m bagq     # __main__.py exists (PATH-independent)

Verified install recipe (08-RESEARCH — the crux; pip IGNORES [tool.uv.sources]):
  END-USER (plain pip, both local packages in ONE command):
    python3 -m venv .venv && . .venv/bin/activate
    pip install ./packages/rosbagger-core ./packages/bagq
    # with the optional plotting extra (QUOTE [plot] so the shell doesn't glob it):
    pip install ./packages/rosbagger-core "./packages/bagq[plot]"
  DEVELOPER (uv workspace — editable, reproducible from uv.lock):
    uv sync --locked --dev
    uv run bagq --help
    uv run pytest
  ANTI-PATTERN: `pip install ./packages/bagq` ALONE fails ("No matching distribution
    found for rosbagger-core") — pip seeks the bare dep on PyPI where it is unpublished.
    Do NOT promise `pip install bagq` from PyPI (unpublished; v0.1 is local-install only).

The PYTHONPATH="" dev-host hazard (neutralizes a host ROS-on-PYTHONPATH leak, Phase 1
  decision) is a CONTRIBUTING/dev note specific to ROS-sourced dev shells — NOT
  user-facing README content.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version 0.0.0 -> 0.1.0 in all 4 sources, re-lock uv.lock, add MIT LICENSE</name>
  <files>packages/rosbagger-core/pyproject.toml, packages/bagq/pyproject.toml, packages/rosbagger-core/src/rosbagger_core/__init__.py, packages/bagq/src/bagq/__init__.py, uv.lock, LICENSE</files>
  <read_first>
    - packages/rosbagger-core/pyproject.toml (the `version` field, line 8)
    - packages/bagq/pyproject.toml (the `version` field, line 7)
    - packages/rosbagger-core/src/rosbagger_core/__init__.py (`__version__`, line 9)
    - packages/bagq/src/bagq/__init__.py (`__version__`, line 3)
    - 08-RESEARCH.md sections: "Pattern 2: Bump version in lockstep", "Pitfall 2: Stale uv.lock", "Pitfall 3: Version drift", "Runtime State Inventory" (uv.lock pins version=0.0.0 at lines 121/1239), "Open Questions Q1" (MIT LICENSE)
  </read_first>
  <action>
    Edit the `version` field from "0.0.0" to "0.1.0" in BOTH pyproject.toml files
    (packages/rosbagger-core/pyproject.toml and packages/bagq/pyproject.toml) and the
    `__version__` assignment from "0.0.0" to "0.1.0" in BOTH __init__.py files
    (packages/rosbagger-core/src/rosbagger_core/__init__.py and
    packages/bagq/src/bagq/__init__.py) — four edits total. Then re-lock with
    `PYTHONPATH="" uv lock` so uv.lock's two version pins (currently 0.0.0 at the bagq
    and rosbagger-core [[package]] entries) become 0.1.0; do NOT hand-edit uv.lock — let
    `uv lock` rewrite it. Per 08-RESEARCH Pitfall 2, skipping the re-lock makes the CI
    step `uv sync --locked` fail. Per Pitfall 3, `bagq --version` reads
    bagq.__init__.__version__ (not wheel metadata), so the bagq/__init__.py edit is what
    makes `bagq --version` report 0.1.0 — do not omit it. Reject the hatchling dynamic
    version alternative (08-RESEARCH Open Q2 / Alternatives): for a one-shot bump, the
    4-line edit is the lowest-risk choice and adds no new config or [build-system] deps.
    Also create an MIT LICENSE file at the repo root (08-RESEARCH Open Q1 / locked
    decision for v0.1): standard MIT License text, copyright line "Copyright (c) 2026
    rosbagger contributors" (generic project-name holder per the planning constraint —
    no individual named).
  </action>
  <acceptance_criteria>
    - All four source sites read "0.1.0": both pyproject `version` fields and both
      `__init__.py` `__version__` assignments
    - `uv.lock` no longer contains `version = "0.0.0"` for the bagq or rosbagger-core
      packages (re-locked to 0.1.0)
    - `PYTHONPATH="" uv sync --locked --dev` exits 0 (proves uv.lock matches the
      manifests — this is the CI step that would fail on a stale lock)
    - `PYTHONPATH="" uv run bagq --version` prints `bagq 0.1.0`
    - `PYTHONPATH="" uv run python -c "import rosbagger_core; assert rosbagger_core.__version__ == '0.1.0'"` exits 0
    - LICENSE exists at the repo root and its text begins with "MIT License"
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'version = "0.0.0"' uv.lock packages/rosbagger-core/pyproject.toml packages/bagq/pyproject.toml 2>/dev/null | grep -v ':0$' | wc -l)" = "0" && grep -q 'MIT License' LICENSE && PYTHONPATH="" uv sync --locked --dev && test "$(PYTHONPATH="" uv run bagq --version)" = "bagq 0.1.0" && PYTHONPATH="" uv run python -c "import rosbagger_core; assert rosbagger_core.__version__ == '0.1.0'"</automated>
  </verify>
  <done>
    Both packages are 0.1.0 in all four source sites; uv.lock is re-locked to 0.1.0 and
    `uv sync --locked` passes; `bagq --version` prints `bagq 0.1.0`; an MIT LICENSE
    exists at the repo root.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand README with the verified install recipe, per-command quickstart, and offline guarantee</name>
  <files>README.md</files>
  <read_first>
    - README.md (the current 35-line file — the plain-pip fallback at lines 28-33 currently uses `pip install -e packages/... -e packages/...` which is the DEV editable path; the END-USER recipe must be the non-editable `pip install ./packages/rosbagger-core ./packages/bagq`)
    - packages/bagq/src/bagq/cli.py (the accurate shipped CLI surface — info/tables/query signatures, the -o / --format / --plot options, `bagq --version`; see the <interfaces> block which extracted these)
    - 08-RESEARCH.md sections: "Standard Stack > Installation (the verified v0.1 recipe)", "Pattern 1: Multi-local-package pip install", "Anti-Patterns to Avoid" (bagq-alone fails; quote [plot]; do not promise PyPI bagq)
  </read_first>
  <action>
    Rewrite README.md keeping the existing intro + core-value blockquote + the Layout
    table (accurate), and replace/expand the Quickstart so it documents what actually
    shipped in P1-P7. Add these sections, all accurate to the <interfaces> block:
    (1) Install — the END-USER plain-pip recipe `pip install ./packages/rosbagger-core
    ./packages/bagq` (both local packages in ONE command, because pip ignores
    [tool.uv.sources] — 08-RESEARCH Pattern 1), the optional plotting extra with [plot]
    QUOTED (`pip install ./packages/rosbagger-core "./packages/bagq[plot]"`), and the
    developer uv path (`uv sync --locked --dev` / `uv run bagq --help` / `uv run pytest`).
    (2) Usage / quickstart — one short example per shipped capability: `bagq info BAG`,
    `bagq tables BAG`, and `bagq query "<SQL>" BAG` with its `-o out.csv|out.parquet`,
    `--format table|csv|parquet|json`, and `--plot [FILE]` options (describe each
    accurately: -o writes by extension and wins over --format; --format csv streams to
    stdout; --format parquet without -o errors; --plot writes plot.png by default).
    Mention `bagq --version` and `bagq --help`.
    (3) Errors that teach — one line noting unknown-table / unknown-column / unresolvable
    custom-msg-type produce a helpful one-line message (no traceback), per P7.
    (4) Offline / no-ROS guarantee — state that rosbagger-core and bagq import and run
    with NO ROS install (no rclpy/rosbag2_py); tests use rosbags-written fixture bags.
    Keep the README ACCURATE to what shipped — do NOT over-promise (no 3D viz, no rich
    timeseries plotting; --plot is intentionally minimal), and do NOT promise
    `pip install bagq` from PyPI (unpublished; v0.1 is local-install only). Do NOT put
    the `PYTHONPATH=""` dev-host hazard in the README — it is a ROS-sourced-dev-shell
    note that belongs in CONTRIBUTING/dev docs, not user-facing install steps. Use NO
    placeholder/"coming soon" language (remove the existing "Full documentation lands in
    a later phase." line — this IS that phase).
  </action>
  <acceptance_criteria>
    - README contains the literal end-user recipe line `pip install ./packages/rosbagger-core ./packages/bagq`
    - README documents the `bagq[plot]` extra with [plot] quoted
    - README documents the uv dev path (`uv sync` and `uv run`)
    - README has a usage example for each of `bagq info`, `bagq tables`, and `bagq query`
    - README mentions the `-o`, `--format`, and `--plot` query options
    - README states the offline / no-ROS (no rclpy) guarantee
    - README does NOT contain `pip install bagq` as a standalone PyPI instruction, does
      NOT contain `PYTHONPATH=""`, and does NOT contain "lands in a later phase"
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'pip install ./packages/rosbagger-core ./packages/bagq' README.md && grep -q 'bagq\[plot\]' README.md && grep -q 'uv sync' README.md && grep -q 'bagq info' README.md && grep -q 'bagq tables' README.md && grep -q 'bagq query' README.md && grep -Eq 'rclpy|no ROS|no-ROS' README.md && ! grep -q 'lands in a later phase' README.md && ! grep -q 'PYTHONPATH=' README.md</automated>
  </verify>
  <done>
    README documents the verified plain-pip install recipe (both packages, one command),
    the bagq[plot] extra, the uv dev path, a per-command quickstart for info/tables/query
    with the -o/--format/--plot options, the errors-that-teach behavior, and the
    offline/no-ROS guarantee — accurate to what shipped, with no placeholder language and
    no PyPI/PYTHONPATH leakage.
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean-room pip-install verification (SC1/SC2), local release gate, and local v0.1.0 tag</name>
  <files>(none — verification + local annotated git tag only; modifies no source files. SC3 remote push is a documented human follow-up, not done here.)</files>
  <read_first>
    - 08-RESEARCH.md sections: "Pattern 3: Clean-room install verification (the SC1/SC2 gate)", "Pitfall 4: Verifying in a contaminated environment", "Pitfall 5: Treating SC3 as fully autonomous", "Code Examples > The full local CI-equivalent gate" and "> Annotated tag (local; push is human-gated)", "Environment Availability" (gh/push credential = no fallback, human-gated)
    - .github/workflows/ci.yml (the CI steps this gate mirrors: uv sync --locked --dev, ruff check ., ruff format --check ., pytest)
    - .planning/STATE.md (the standing blocker: no gh CLI / no push credential; origin set to https://github.com/AllenDevaraj/rosbagger.git)
  </read_first>
  <action>
    Do the autonomous half of SC3 and the full SC1/SC2 gate; document the remote half as
    a human follow-up (do NOT add a blocking checkpoint — this plan is autonomous:true).

    Clean-room pip verification (SC1 + SC2 — 08-RESEARCH Pattern 3, Pitfall 4): with
    `export PYTHONPATH=""`, create a FRESH throwaway venv with `python3 -m venv` at a temp
    path (e.g. /tmp/bagq-verify-$$ — NOT the project .venv, which already has both
    packages editable-installed and would false-pass), then
    `<venv>/bin/pip install ./packages/rosbagger-core ./packages/bagq` (both local
    packages in one command — pip ignores [tool.uv.sources]). Assert: `<venv>/bin/bagq
    --help` exits 0; `<venv>/bin/bagq info --help`, `... tables --help`, `... query
    --help` each exit 0; then FROM A NEUTRAL CWD (cd /tmp — because '' on sys.path makes
    ./tools importable from the repo root, a false failure per Pitfall 4)
    `<venv>/bin/python -c "import rosbagger_core, bagq"` succeeds, and assert NO rclpy and
    NO rosbag2_py are resolvable
    (`importlib.util.find_spec('rclpy') is None` and `find_spec('rosbag2_py') is None`)
    and `tools` is NOT importable (`find_spec('tools') is None`) — proving no tools/ in
    either wheel (SC2). Remove the throwaway venv afterward.

    Local release gate (the autonomous half of SC3 — 08-RESEARCH Pitfall 5, Code
    Examples): from the repo root with `PYTHONPATH=""`, run the CI-equivalent steps and
    require all green: `uv sync --locked --dev`, then `uv run ruff check .`, then
    `uv run ruff format --check .`, then `uv run pytest` (the >=80% coverage gate is in
    the pyproject addopts). REMINDER from the downstream note: if `ruff format --check`
    flags anything, run `PYTHONPATH="" uv run ruff format .` to fix before re-checking
    (do this BEFORE the final commit, not after tagging).

    Local annotated tag (autonomous): create `git tag -a v0.1.0 -m "rosbagger v0.1.0 —
    bagq SQL-over-bags CLI (offline ROS1/ROS2/MCAP)"` LOCALLY and confirm `git tag` lists
    v0.1.0. Do this on the existing `main` branch (config branching_strategy is "none").

    Human follow-up (SC3 remote half — DO NOT attempt, DO NOT block): pushing the branch
    + tag (`git push origin main && git push origin v0.1.0`) and observing the GitHub
    Actions run go green CANNOT be done here (no gh CLI, no push credential — standing
    STATE.md blocker; origin = https://github.com/AllenDevaraj/rosbagger.git). Record
    this in the SUMMARY as the SOLE remaining human follow-up, clearly separated from the
    locally-verified results (mirror the must_haves [verified-locally] vs [awaits-push]
    split). Do NOT mark it a checkpoint:human that stalls the phase.
  </action>
  <acceptance_criteria>
    - Clean-room install (SC1): in a fresh non-.venv venv with PYTHONPATH="", `pip install
      ./packages/rosbagger-core ./packages/bagq` succeeds and `bagq --help` exits 0;
      `bagq info|tables|query --help` each exit 0
    - Offline import (SC2): from a neutral cwd in that venv, `import rosbagger_core, bagq`
      succeeds; `find_spec('rclpy')`, `find_spec('rosbag2_py')`, and `find_spec('tools')`
      are all None
    - Local CI-equivalent gate is green: `uv sync --locked --dev`, `ruff check .`,
      `ruff format --check .`, and `pytest` (>=80% coverage) all exit 0 under PYTHONPATH=""
    - An annotated `v0.1.0` tag exists locally (`git tag` lists it; `git cat-file -t
      v0.1.0` is `tag`)
    - The SUMMARY records "push + observe GitHub Actions green" as the only human
      follow-up, separated from the locally-verified results (SC3 split documented, not a
      stalling checkpoint)
  </acceptance_criteria>
  <verify>
    <automated>export PYTHONPATH="" && V=$(mktemp -d) && python3 -m venv "$V" && "$V/bin/pip" install -q ./packages/rosbagger-core ./packages/bagq && "$V/bin/bagq" --help >/dev/null && for s in info tables query; do "$V/bin/bagq" $s --help >/dev/null; done && (cd /tmp && "$V/bin/python" -c "import rosbagger_core, bagq; import importlib.util as u; assert u.find_spec('rclpy') is None and u.find_spec('rosbag2_py') is None and u.find_spec('tools') is None") && rm -rf "$V" && uv sync --locked --dev && uv run ruff check . && uv run ruff format --check . && uv run pytest && git cat-file -t v0.1.0 | grep -q '^tag$'</automated>
  </verify>
  <done>
    The clean-room pip install (SC1) and offline no-rclpy/no-tools import (SC2) pass in a
    fresh venv; the local CI-equivalent gate (sync --locked, ruff lint, ruff format-check,
    pytest >=80%) is green; an annotated v0.1.0 tag exists locally; and the SUMMARY
    documents push + observe-CI-green as the sole human follow-up (the SC3 remote half),
    distinguished from the locally-verified results.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install command -> pip resolver | pip resolves bagq's bare `rosbagger-core` dep; v0.1 satisfies it from LOCAL paths only (no PyPI fetch of first-party names) |
| committed uv.lock -> CI install | `uv sync --locked` installs from the pinned lockfile (tamper-evident); the bump deliberately re-locks and commits |
| repo source tree -> built wheels | src-layout scopes wheels to src/; tools/ and tests/ live outside both src/ trees |

This phase adds NO new dependencies and NO runtime code that processes untrusted
input. Surface is the published package metadata + the install recipe + a LICENSE +
README. Nothing is rated `high`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering/Spoofing | first-party dist names (`rosbagger-core`, `bagq`) | accept | Dependency-confusion is not in scope for v0.1: install is from LOCAL paths only — the multi-package pip recipe satisfies the bare dep from the locally-built wheel, never PyPI. If ever published, claim the names on PyPI (out of scope). Low risk: local-install only. |
| T-08-02 | Tampering | CI dependency install | mitigate | `uv sync --locked` installs from the committed `uv.lock` (already in CI). Task 1 deliberately re-locks on the version bump and commits the regenerated `uv.lock` so the lock stays in sync (Pitfall 2). |
| T-08-03 | Information Disclosure | built wheels (leaking tools/, tests, secrets) | mitigate | src-layout already scopes both wheels to their `src/` tree; Task 3 asserts `tools` is NOT importable from the clean-room install (no tools/ in either wheel) and there are no secrets in the repo. |
| T-08-04 | Elevation of Privilege | CI token / workflow permissions | accept | `.github/workflows/ci.yml` already sets least-privilege `permissions: contents: read` and references no secrets (verified in 08-RESEARCH Security Domain). No change needed for P8; left intact. |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a (no-op) | Phase 8 introduces ZERO new external packages — all runtime deps were vetted/approved P1-P6 (08-RESEARCH Package Legitimacy Audit: no [SLOP]/[SUS], no new installs). No legitimacy checkpoint required. The two local packages are first-party. |
</threat_model>

<verification>
Phase-level checks (the Definition of Done, all achievable except the documented
remote half of SC3):

- SC1 (pip install yields a working bagq with --help): proven by Task 3's clean-room
  `pip install ./packages/rosbagger-core ./packages/bagq` + `bagq --help` exit 0 +
  `bagq info|tables|query --help` exit 0.
- SC2 (offline packages import without rclpy): proven by Task 3's neutral-cwd
  `import rosbagger_core, bagq` with `find_spec('rclpy')`/`find_spec('rosbag2_py')`/
  `find_spec('tools')` all None.
- SC3 (CI green; version tagged 0.1) — SPLIT:
  * Autonomous (done here): the local CI-equivalent gate (`uv sync --locked --dev`,
    `ruff check .`, `ruff format --check .`, `pytest` >=80%) is green, and an annotated
    `v0.1.0` tag exists locally.
  * Human follow-up (BLOCKED — recorded gap, NOT a stalling checkpoint): `git push origin
    main && git push origin v0.1.0`, then observe the GitHub Actions run go green. No gh
    CLI / no push credential (standing STATE.md blocker).
- DoD version: `bagq --version` prints `bagq 0.1.0`; uv.lock re-locked and committed so
  `uv sync --locked` stays green (Pitfall 2/3).
- DoD hygiene: MIT LICENSE at repo root; README documents the verified install recipe +
  per-command usage + offline guarantee.

Run before the final commit (downstream reminder): `PYTHONPATH="" uv run ruff format .`
</verification>

<success_criteria>
- [ ] All 4 version sources read 0.1.0 (2 pyproject `version` + 2 `__init__.py`
      `__version__`); `bagq --version` prints `bagq 0.1.0`
- [ ] `uv.lock` re-locked to 0.1.0 and committed; `PYTHONPATH="" uv sync --locked --dev`
      exits 0
- [ ] MIT LICENSE exists at the repo root
- [ ] README documents the plain-pip recipe (both packages, one command), the bagq[plot]
      extra, the uv dev path, a per-command quickstart (info/tables/query with
      -o/--format/--plot), errors-that-teach, and the offline/no-ROS guarantee — no
      placeholder language, no PyPI/PYTHONPATH leakage
- [ ] Clean-room `pip install ./packages/rosbagger-core ./packages/bagq` in a fresh
      non-.venv venv: `bagq --help` exit 0; subcommand --help exit 0 (SC1)
- [ ] Offline import from a neutral cwd: no rclpy/rosbag2_py/tools resolvable (SC2)
- [ ] Local CI-equivalent gate green (sync --locked, ruff check, ruff format --check,
      pytest >=80%); annotated `v0.1.0` tag exists locally (SC3 autonomous half)
- [ ] SUMMARY distinguishes [verified-locally] from [awaits-push]; "push + observe CI
      green" recorded as the sole human follow-up (SC3 remote half), not a blocking
      checkpoint
</success_criteria>

<output>
Create `.planning/phases/08-packaging-docs-release/08-01-SUMMARY.md` when done.

The SUMMARY MUST clearly separate two buckets (mirror the must_haves split):
- **Verified locally (autonomous):** version 0.1.0 everywhere + `bagq --version`;
  uv.lock re-locked + `uv sync --locked` green; clean-room pip install (SC1); offline
  no-rclpy/no-tools import (SC2); local CI-equivalent gate green; MIT LICENSE; expanded
  README; local annotated `v0.1.0` tag.
- **Awaits push (human follow-up — known recorded gap, SC3 remote half):**
  `git push origin main && git push origin v0.1.0`, then observe the GitHub Actions run
  go green. Blocked by no gh CLI / no push credential (standing STATE.md blocker; origin
  = https://github.com/AllenDevaraj/rosbagger.git). This is the ONLY thing left to make
  the milestone fully shipped.
</output>
