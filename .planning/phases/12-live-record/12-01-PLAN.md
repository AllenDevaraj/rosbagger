---
phase: 12-live-record
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-record/pyproject.toml
  - packages/rosbagger-record/src/rosbagger_record/__init__.py
  - packages/rosbagger-record/src/rosbagger_record/errors.py
  - packages/rosbagger-record/src/rosbagger_record/discovery.py
  - pyproject.toml
  - tests/test_record_unit.py
  - tests/test_offline_guard.py
autonomous: true
requirements: [REC-01]

must_haves:
  truths:
    - "import rosbagger_record succeeds in the ROS-free uv venv (no rclpy/rosbag2_py in sys.modules)"
    - "rosbagger_record.record() raises a teaching RosNotAvailableError when rclpy is absent"
    - "select_topics filters a discovered {topic: type} map by positional names, --all, --regex, --exclude (pure-Python, no ROS)"
    - "import rosbagger_core and import bagq still pull NO rclpy/rosbag2_py (offline guarantee held)"
    - "uv sync --locked --dev stays green after the new workspace member is added"
  artifacts:
    - path: "packages/rosbagger-record/pyproject.toml"
      provides: "uv workspace member; only uv-resolved dep is rosbagger-core; console script rosbagger-record"
      contains: "rosbagger-record"
    - path: "packages/rosbagger-record/src/rosbagger_record/__init__.py"
      provides: "lazy _require_ros() boundary + public API re-export; NO top-level rclpy import"
      contains: "_require_ros"
    - path: "packages/rosbagger-record/src/rosbagger_record/errors.py"
      provides: "RosNotAvailableError + McapStorageUnavailableError teaching capability errors"
      contains: "class RosNotAvailableError"
    - path: "packages/rosbagger-record/src/rosbagger_record/discovery.py"
      provides: "discover_topics(node, ...) + select_topics(discovered, ...) pure-Python selection"
      exports: ["discover_topics", "select_topics"]
    - path: "tests/test_record_unit.py"
      provides: "ROS-mocked unit tests for selection + the no-ROS capability error"
      min_lines: 40
  key_links:
    - from: "packages/rosbagger-record/pyproject.toml"
      to: "rosbagger-core (workspace)"
      via: "dependencies = [\"rosbagger-core\", ...]"
      pattern: "rosbagger-core"
    - from: "tests/test_offline_guard.py"
      to: "rosbagger_record"
      via: "fresh-subprocess import assertion (no rclpy/rosbag2_py leaked)"
      pattern: "rosbagger_record"
---

<objective>
Scaffold the new `packages/rosbagger-record/` workspace package and ship its offline-safe foundation: the lazy ROS-import boundary (D-03/D-11), the teaching capability errors, the pure-Python topic discovery + subset selection (D-06/D-07), and the offline-guard extension that protects the offline↔live boundary (D-11). This is the only Phase-12 work that is fully exercisable in the ROS-free uv venv, so all of its logic is unit-coverable here; the rclpy/rosbag2_py record core (Plan 02) and the CLI + live test (Plan 03) build on top.

Purpose: Establish the offline/live package boundary as the first thing that lands, so every later task imports `rosbagger_record` cleanly in the uv venv and the offline guarantee for `rosbagger_core`/`bagq` is locked by a regression test before any ROS code exists.

Output: A `uv`-resolving `rosbagger-record` package (console script declared) whose top-level import pulls no ROS; `errors.py`, `discovery.py`; mocked unit tests; the `live` pytest marker registered; an extended offline guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-live-record/12-CONTEXT.md
@.planning/phases/12-live-record/12-RESEARCH.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase + verified research. No exploration needed. -->

The errors-that-teach pattern to MIRROR (packages/rosbagger-core/src/rosbagger_core/errors.py):
  - Each error subclasses ValueError, CARRIES structured data on attributes, and builds a plain-text teaching message in __init__ via super().__init__(...).
  - Module top imports ONLY stdlib (difflib). NEVER imports the heavy/ROS stack.

The root workspace wiring already present (pyproject.toml — DO NOT duplicate):
  - [tool.uv.workspace] members = ["packages/*"]   # globs the new package automatically
  - [tool.uv.sources] rosbagger-core = { workspace = true }   # wires the intra-workspace dep
  - [tool.pytest.ini_options] addopts = "--cov=rosbagger_core --cov=bagq --cov-report=term-missing --cov-fail-under=80"  ; testpaths = ["tests"]

The per-package manifest pattern to MIRROR (packages/bagq/pyproject.toml):
  [project] name / version="0.1.0" / requires-python=">=3.10" / dependencies
  [project.scripts] bagq = "bagq.cli:app"
  [build-system] requires=["hatchling"]; build-backend="hatchling.build"

Verified lazy-boundary skeleton (12-RESEARCH Pattern 1 — RAN clean in PYTHONPATH="" uv venv):
  __init__.py defines _require_ros() that tries `import rclpy; import rosbag2_py` inside the
  function body and raises RosNotAvailableError from ImportError; record(*a, **k) calls
  _require_ros() then `from .record import record as _r` and delegates. NO rclpy import at module top.

Verified discovery shape (12-RESEARCH Pattern 3): node.get_topic_names_and_types() ->
  List[Tuple[str, List[str]]]; take types[0] per topic; settle by spinning ~30 x 20ms first.

The offline-guard helper to REUSE (tests/test_offline_guard.py):
  _heavy_modules_after_import(*targets) spawns a FRESH interpreter with env {"PYTHONPATH": ""}
  and returns the heavy-stack modules left in sys.modules. The existing test_core_imports_without_ros
  already asserts `with pytest.raises(ImportError): importlib.import_module("rclpy"/"rosbag2_py")`
  and that rosbagger_core/bagq still import (under the `no_ros` conftest blocker).

Mock-injection technique for unit tests (12-RESEARCH Pitfall 6): because imports are lazy,
  set sys.modules["rclpy"] / sys.modules["rosbag2_py"] to a MagicMock BEFORE calling a function
  (use monkeypatch.setitem). A string-target mock.patch("rclpy.x") fails at COLLECTION in the
  ROS-free venv — do not use it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the rosbagger-record package + lazy ROS boundary + teaching errors</name>
  <files>packages/rosbagger-record/pyproject.toml, packages/rosbagger-record/src/rosbagger_record/__init__.py, packages/rosbagger-record/src/rosbagger_record/errors.py</files>
  <read_first>
    - packages/bagq/pyproject.toml (the per-package [project]/[project.scripts]/[build-system] pattern to mirror)
    - pyproject.toml (root — confirm members=["packages/*"] + sources rosbagger-core already wire the new member; do NOT edit these here)
    - packages/rosbagger-core/src/rosbagger_core/errors.py (the ValueError-subclass teaching-error pattern + the stdlib-only module-top discipline)
    - .planning/phases/12-live-record/12-RESEARCH.md (Pattern 1 lazy boundary — VERIFIED skeleton; the Installation pyproject block with env deps NOT declared)
    - .planning/phases/12-live-record/12-CONTEXT.md (D-01/D-02/D-03/D-11)
  </read_first>
  <action>
    Create packages/rosbagger-record/pyproject.toml mirroring packages/bagq/pyproject.toml: [project] name="rosbagger-record", version="0.1.0", requires-python=">=3.10", dependencies=["rosbagger-core", "typer>=0.15,<1"] (per D-03 these are the ONLY uv-resolved deps; rclpy/rosbag2_py/rosidl_runtime_py are environment-provided and DELIBERATELY absent so uv never tries to fetch nonexistent PyPI wheels); [project.scripts] rosbagger-record = "rosbagger_record.cli:app" (cli.py lands in Plan 03 — declaring the entry point now is fine, hatchling does not import it at build time); [build-system] requires=["hatchling"], build-backend="hatchling.build". Add a top comment stating "requires a sourced ROS 2 environment at runtime; rclpy/rosbag2_py are environment-provided, not uv-resolved (D-03)". Create src/rosbagger_record/__init__.py per the VERIFIED 12-RESEARCH Pattern 1: __version__="0.1.0"; from .errors import RosNotAvailableError; a _require_ros() that tries `import rclpy` then `import rosbag2_py` INSIDE the function body and raises RosNotAvailableError from the ImportError; a public record(*args, **kwargs) that calls _require_ros() then does `from .record import record as _r; return _r(*args, **kwargs)` (record.py lands in Plan 02); a list_topics(*args, **kwargs) shaped the same way (also delegates to .record after _require_ros). NO top-level rclpy/rosbag2_py/rosidl_runtime_py import anywhere in __init__.py. Create src/rosbagger_record/errors.py importing ONLY stdlib (no difflib needed here unless used): RosNotAvailableError(RuntimeError) — message teaches "rclpy/rosbag2_py not importable — source your ROS 2 environment (e.g. `source /opt/ros/humble/setup.bash`) before recording; the offline bagq/inspect/query tools need no ROS"; McapStorageUnavailableError(RuntimeError) taking (requested: str, available: list[str]) carrying .requested/.available and building a message naming the requested storage id, the registered writers, and the remedy "install the storage plugin (e.g. `sudo apt install ros-humble-rosbag2-storage-mcap`) or pass --storage sqlite3". Use RuntimeError (not ValueError) for these capability errors — they are environment/capability conditions, distinct from the core ValueError teaching-data errors. Module top stays import-light.
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run python -c "import rosbagger_record; import sys; assert not [m for m in sys.modules if m.split('.')[0] in {'rclpy','rosbag2_py','rosidl_runtime_py'}]; print('clean import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - ENV=offline (PYTHONPATH="" uv run): `import rosbagger_record` succeeds and leaves NO rclpy/rosbag2_py/rosidl_runtime_py in sys.modules (the command above prints "clean import OK").
    - ENV=offline: `PYTHONPATH="" uv run python -c "import rosbagger_record as r; r.record()"` raises RosNotAvailableError (NOT a bare ModuleNotFoundError) whose message mentions sourcing ROS 2.
    - ENV=offline: `PYTHONPATH="" uv sync --locked --dev` exits 0 (the new member's only external dep, rosbagger-core, is already locked; no new PyPI resolution). If uv reports the lock is out of date because the new member is now globbed in, re-lock with `PYTHONPATH="" uv lock` and confirm `uv sync --locked --dev` then exits 0 — commit the updated uv.lock.
    - packages/rosbagger-record/pyproject.toml does NOT list rclpy, rosbag2_py, or rosidl_runtime_py in dependencies (D-03): `grep -E "rclpy|rosbag2_py|rosidl" packages/rosbagger-record/pyproject.toml` returns nothing.
    - errors.py imports no ROS and no heavy stack at module top: `PYTHONPATH="" uv run python -c "import sys; import rosbagger_record.errors; assert not [m for m in sys.modules if m.split('.')[0] in {'rclpy','rosbag2_py','duckdb','pyarrow','sqlglot'}]"` exits 0.
  </acceptance_criteria>
  <done>The package imports cleanly in the ROS-free venv with no ROS modules pulled, record() raises the teaching capability error, uv sync --locked --dev is green, and rclpy/rosbag2_py are not declared as deps.</done>
</task>

<task type="auto">
  <name>Task 2: Topic discovery + pure-Python subset selection (D-06/D-07)</name>
  <files>packages/rosbagger-record/src/rosbagger_record/discovery.py</files>
  <read_first>
    - packages/rosbagger-record/src/rosbagger_record/__init__.py (Task 1 — the lazy-import discipline this module follows)
    - .planning/phases/12-live-record/12-RESEARCH.md (Pattern 3 discovery-with-settle VERIFIED; the Architectural Responsibility Map row "Topic subset selection — pure Python, unit-testable with mocks"; Pitfall 4 external-discovery settle)
    - .planning/phases/12-live-record/12-CONTEXT.md (D-06 discover via get_topic_names_and_types after settle; D-07 subset: positional / --all / --regex / --exclude)
  </read_first>
  <action>
    Create src/rosbagger_record/discovery.py, top level importing ONLY __future__ + stdlib re (NO rclpy at module top). Define discover_topics(node, *, settle_iters: int = 30, settle_dt: float = 0.02) -> dict[str, str]: lazy-import rclpy INSIDE the body, spin `for _ in range(settle_iters): rclpy.spin_once(node, timeout_sec=settle_dt)` to let DDS discovery populate (Pattern 3 — external publishers need the settle, Pitfall 4), then return `{name: types[0] for name, types in node.get_topic_names_and_types() if types}` (take the first type per topic; multi-type topics are rare). Define select_topics(discovered: dict[str, str], *, topics: list[str] | None = None, all_topics: bool = False, regex: str | None = None, exclude: str | None = None) -> dict[str, str] as a PURE function (no ROS, no lazy import) implementing D-07: if all_topics is True start from every discovered topic; else start from the positional `topics` that exist in discovered (silently drop names not present — they are simply not published; the CLI in Plan 03 may warn); if regex is given, keep only names matching re.search(regex, name); if exclude is given, drop names matching re.search(exclude, name); return the {topic: type_str} subset preserving discovered's types. Treat topic names purely as DATA — never interpolate them into a shell or eval; regex is compiled via re only (T-12-02). Make select_topics deterministic (stable iteration over discovered). Decide and document the precedence (all_topics OR positional as the base set, then regex include filter, then exclude filter) in the docstring.
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_record_unit.py -k select -q</automated>
  </verify>
  <acceptance_criteria>
    - ENV=offline (PYTHONPATH="" uv run): `import rosbagger_record.discovery` pulls no rclpy into sys.modules (lazy import inside discover_topics only).
    - select_topics is unit-tested (Task 3) WITHOUT any ROS: positional names select exactly the named present topics; a positional name not in `discovered` is dropped (not an error); all_topics=True returns every discovered topic; regex="^/cmd" keeps only matching names; exclude="image" drops matching names; combining all_topics + exclude yields everything-minus-excluded. Each returned value is the discovered type string for that topic.
    - discover_topics is structured so a MagicMock node (node.get_topic_names_and_types returns a list of (name, [type]) tuples; rclpy.spin_once mocked) drives it in a unit test with rclpy injected via sys.modules — verified by Task 3's `-k discover` test.
    - No shell interpolation / eval of topic names anywhere: `grep -nE "os\.system|subprocess|eval\(|exec\(" packages/rosbagger-record/src/rosbagger_record/discovery.py` (filtered of comments) returns nothing.
  </acceptance_criteria>
  <done>discover_topics returns {topic: type_str} after a settle and select_topics is a pure, deterministic, ROS-free filter implementing positional/--all/--regex/--exclude, both covered by Task 3's mocked unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mocked unit tests + offline-guard extension + live marker registration</name>
  <files>tests/test_record_unit.py, tests/test_offline_guard.py, pyproject.toml</files>
  <behavior>
    - select_topics(positional): given discovered={"/a":"T","/b":"U","/c":"V"}, topics=["/a","/c","/zzz"] -> {"/a":"T","/c":"V"} (missing /zzz dropped).
    - select_topics(all): all_topics=True -> the full discovered map.
    - select_topics(regex): regex="^/cmd" over {"/cmd_vel":"...","/imu":"..."} -> only {"/cmd_vel":...}.
    - select_topics(exclude): exclude="image" over {"/cmd_vel":...,"/image":...} -> {"/cmd_vel":...}.
    - discover_topics(mocked): a MagicMock node returning [("/telemetry",["std_msgs/msg/String"]),("/empty",[])] with rclpy.spin_once mocked -> {"/telemetry":"std_msgs/msg/String"} (the typeless /empty dropped); spin_once called settle_iters times.
    - no_ros: rosbagger_record.record() raises RosNotAvailableError when rclpy is unimportable.
    - offline guard: `import rosbagger_record` in a fresh interpreter leaks no rclpy/rosbag2_py; `import rosbagger_core` / `import bagq` still leak no rclpy/rosbag2_py.
  </behavior>
  <read_first>
    - tests/test_offline_guard.py (the EXACT existing helpers to reuse: _heavy_modules_after_import with env {"PYTHONPATH": ""}; test_core_imports_without_ros uses the `no_ros` conftest fixture; the per-module fresh-subprocess rosbags-leak pattern in test_import_tf_does_not_pull_rosbags)
    - tests/conftest.py (the `no_ros` sys.meta_path blocker fixture — BLOCKED includes rclpy/rosbag2_py/rosidl_runtime_py/ament_index_python)
    - packages/rosbagger-record/src/rosbagger_record/discovery.py (Task 2 — select_topics signature + discover_topics signature)
    - packages/rosbagger-record/src/rosbagger_record/__init__.py (Task 1 — record() + _require_ros())
    - .planning/phases/12-live-record/12-RESEARCH.md (Pitfall 6 mock-injection via sys.modules; Pitfall 5 offline-guard regression; the Phase Requirements -> Test Map rows for discover/select/no_ros; Open Q3 register the `live` marker)
    - .planning/phases/12-live-record/12-CONTEXT.md (D-10 two-tier testing; D-11 offline guarantee)
  </read_first>
  <action>
    Write tests/test_record_unit.py (a self-contained harness; prepend the rosbagger-record + rosbagger-core src trees to sys.path at the top if pytest does not already resolve the new member in the uv venv — verify which is needed and pick the minimal one). RED first: write the select_topics cases (-k select), the discover_topics mocked case (-k discover), and the no-ROS capability case (-k no_ros) per <behavior>, then implement nothing new (Tasks 1+2 already provide the code, so these should pass GREEN immediately — if any fails, it reveals a Task 1/2 contract bug to fix there, not here). For the discover_topics + no_ros cases use monkeypatch.setitem(sys.modules, "rclpy", MagicMock()) (and "rosbag2_py" where _require_ros needs it) BEFORE calling, per Pitfall 6 — do NOT use mock.patch("rclpy....") string targets (they fail at collection in the ROS-free venv). The no_ros case asserts pytest.raises(RosNotAvailableError) when rclpy import fails — reuse the `no_ros` conftest fixture (its blocker raises ImportError for rclpy) so _require_ros() converts it to the teaching error. Then EXTEND tests/test_offline_guard.py with: (a) test_import_record_does_not_pull_ros — uses a fresh-subprocess check (mirror test_import_tf_does_not_pull_rosbags, but assert no rclpy/rosbag2_py rather than rosbags; env={"PYTHONPATH": ""}) that `import rosbagger_record` leaves no rclpy/rosbag2_py in sys.modules; (b) widen the EXISTING core/bagq leak assertions if needed so they explicitly cover rclpy/rosbag2_py (test_core_imports_without_ros + test_no_ros_leaked_into_sys_modules already do — confirm rosbagger_record being installed in the venv does not regress them; add an explicit assertion only if a gap exists). NOTE: the fresh subprocess for `import rosbagger_record` must be able to FIND the package — if `PYTHONPATH=""` hides the uv-venv site-packages where the member installed, set the subprocess env so the rosbagger-record src tree is importable while rclpy/rosbag2_py stay absent (e.g. PYTHONPATH pointing ONLY at the two src trees, NOT a ROS path); document the exact env used in the test docstring. Finally, register the `live` marker in root pyproject.toml: add `markers = ["live: integration tests that require a sourced ROS 2 environment (rclpy + rosbag2_py); skipped in the offline CI"]` under [tool.pytest.ini_options] (silences the unknown-marker warning Plan 03's live test would raise). Do NOT change addopts/--cov here (the record-core coverage decision is handled in Plan 02/03).
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_record_unit.py tests/test_offline_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - ENV=offline (PYTHONPATH="" uv run): `pytest tests/test_record_unit.py tests/test_offline_guard.py -q` passes; the select/discover/no_ros cases and the new test_import_record_does_not_pull_ros all green.
    - ENV=offline: the full offline suite `PYTHONPATH="" uv run pytest` stays green at >=80% coverage (the new package's pure-Python discovery/selection are covered; the gate is unchanged from the prior 387-passed baseline plus the new tests).
    - The `live` marker is registered: `grep -n "live:" pyproject.toml` shows the marker line under [tool.pytest.ini_options]; running the suite emits NO "Unknown pytest.mark.live" warning (Plan 03 relies on this).
    - ENV=offline: `import rosbagger_core` and `import bagq` still leak no rclpy/rosbag2_py (the existing offline-guard tests remain green — the offline guarantee is intact with rosbagger-record present in the workspace).
    - The unit tests inject mocks via sys.modules (monkeypatch.setitem), NOT mock.patch string targets: `grep -nE "mock\.patch\([\"']rclpy" tests/test_record_unit.py` returns nothing.
  </acceptance_criteria>
  <done>The mocked unit tests for selection + discovery + the no-ROS capability error pass in the uv venv, the offline guard is extended to assert `import rosbagger_record` pulls no ROS (and core/bagq stay clean), the `live` marker is registered, and the full offline suite is green at >=80%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uv venv ↔ ROS runtime | The offline package boundary: `rosbagger_record` must import cleanly in the ROS-free venv; rclpy/rosbag2_py exist only behind lazy function-body imports. A leak here breaks the offline guarantee for `core`/`bagq`. |
| CLI args / regex ↔ selection | Topic names and `--regex`/`--exclude` patterns are user-supplied strings consumed by `select_topics`. Treated as DATA (re.search only), never shell/eval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | offline import boundary (`__init__.py`/`errors.py`/`discovery.py`) | mitigate | All rclpy/rosbag2_py imports inside function bodies; offline-guard test (Task 3) asserts `import rosbagger_record` leaks no ROS and core/bagq stay clean. Regression-locked. |
| T-12-02 | Tampering / DoS | `select_topics` regex + topic-name handling | mitigate | Topic names handled as data; `--regex`/`--exclude` compiled via stdlib `re.search` only — no shell interpolation, no eval (grep-gated in Task 2). A pathological regex is the local trusted user's own input (accept the ReDoS risk for a local CLI). |
| T-12-SC | Tampering | uv package installs | mitigate | 12-RESEARCH Package Legitimacy Audit: only `rosbagger-core` (workspace, vetted) + `typer` (pre-vetted bagq dep) are uv-resolved. rclpy/rosbag2_py/rosidl_runtime_py are environment-provided ROS distro packages on disk (`/opt/ros/humble`), NOT registry installs — slopcheck/PyPI auditing does not apply. No new third-party PyPI package introduced. No [ASSUMED]/[SUS]/[SLOP] packages → no legitimacy checkpoint required. |

No HIGH threats: no network service, no auth/session/crypto surface, no SQL/identifier-injection (unlike the query module). Local developer tool recording data the user already owns.
</threat_model>

<verification>
- ENV=offline: `PYTHONPATH="" uv run pytest tests/test_record_unit.py tests/test_offline_guard.py -q` green.
- ENV=offline: `PYTHONPATH="" uv sync --locked --dev` exit 0; `PYTHONPATH="" uv run python -c "import rosbagger_record"` clean (no ROS in sys.modules).
- ENV=offline: `PYTHONPATH="" uv run ruff check packages/rosbagger-record tests/test_record_unit.py && PYTHONPATH="" uv run ruff format --check packages/rosbagger-record tests/test_record_unit.py` clean.
- The `live` marker registered; no unknown-marker warning.
</verification>

<success_criteria>
- `packages/rosbagger-record/` exists as a uv workspace member whose only uv-resolved deps are `rosbagger-core` + `typer`; console script `rosbagger-record` declared (cli.py lands Plan 03).
- `import rosbagger_record` succeeds in the ROS-free uv venv pulling no rclpy/rosbag2_py; `record()` raises RosNotAvailableError when ROS is absent.
- `select_topics` implements positional/--all/--regex/--exclude as a pure ROS-free function; `discover_topics` returns `{topic: type_str}` after a settle (mocked-rclpy unit-tested).
- Offline guard extended: `import rosbagger_record` leaks no ROS; `import rosbagger_core`/`import bagq` stay ROS-free.
- Full offline suite green at >=80%; `uv sync --locked --dev` green; ruff clean; `live` marker registered.
</success_criteria>

<output>
Create `.planning/phases/12-live-record/12-01-SUMMARY.md` when done. Record: the exact `pyproject.toml` deps shipped (and whether uv.lock needed a re-lock); the chosen select_topics precedence; the precise fresh-subprocess env used in the new offline-guard test for `import rosbagger_record`; and the offline-suite pass count + coverage.
</output>
