---
phase: 12-live-record
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - packages/rosbagger-record/src/rosbagger_record/cli.py
  - tests/test_record_live.py
  - tests/test_record_unit.py
autonomous: true
requirements: [REC-01]

must_haves:
  truths:
    - "rosbagger-record list (or --list) prints discoverable topics + their types and exits (SC1)"
    - "rosbagger-record /a /b -o OUT records the named subset; --all / --regex / --exclude / --duration / --max-messages / --storage flags route to record() (D-07/D-08/D-09)"
    - "the live integration test records a publisher's topic to a bag and re-opens + iterates it via the v1 RosbagsReader(default_typestore=ROS2_HUMBLE), proving SC2+SC3 in the ROS-sourced lane"
    - "the live test is SKIPPED in the offline CI (pytest.importorskip + live marker); the MCAP-specific assertion is skipif-guarded on get_registered_writers(); SC2/SC3 are proven via the available sqlite3 path"
    - "importing bagq.cli still pulls no rclpy/rosbag2_py (bagq's offline import graph is untouched by this phase)"
  artifacts:
    - path: "packages/rosbagger-record/src/rosbagger_record/cli.py"
      provides: "thin typer app: `list` verb + record verb over the record() API; console-script target rosbagger-record"
      contains: "app = typer.Typer"
    - path: "tests/test_record_live.py"
      provides: "LIVE integration test: external publisher -> bounded record -> re-open via v1 reader (SC1/SC2/SC3)"
      contains: "importorskip"
  key_links:
    - from: "packages/rosbagger-record/src/rosbagger_record/cli.py"
      to: "rosbagger_record.record / list_topics"
      via: "lazy import inside the command bodies; CLI is a thin face (API-first, D-02)"
      pattern: "from rosbagger_record import|import rosbagger_record"
    - from: "tests/test_record_live.py"
      to: "rosbagger_core.reader.rosbags_reader.RosbagsReader"
      via: "with RosbagsReader(out, default_typestore=ROS2_HUMBLE) as r: list(r.read()) — the offline↔live re-open contract (SC3)"
      pattern: "RosbagsReader"
    - from: "tests/test_record_live.py"
      to: "rosbag2_py.get_registered_writers()"
      via: "skipif guard on the MCAP-specific assertion; sqlite3 path proves the mechanism here"
      pattern: "get_registered_writers"
---

<objective>
Ship the thin `rosbagger-record` CLI (`list` verb + record verb over the Plan-02 `record()` API, D-02/D-07/D-08/D-09) and the LIVE integration test that closes the offline↔live loop: an external publisher → bounded `record()` → re-open + iterate via the v1 `RosbagsReader` (SC1/SC2/SC3). The live test runs in the ROS-sourced lane (system `python3` with ROS sourced, src trees prepended to PYTHONPATH), is gated by `pytest.importorskip("rclpy")` + the `live` marker so the offline CI skips it cleanly, proves SC2/SC3 via the available sqlite3 storage path, and `skipif`-guards the MCAP-specific assertion on `get_registered_writers()`. Run the phase gate (full offline suite + ruff + offline guard) and execute the live lane on this box.

Purpose: Delivers the user-facing console script and the end-to-end proof of REC-01 — the recorded bag genuinely re-opens through the existing v1 reader, with no new read code. This is the offline↔live closing loop the whole phase exists to establish.

Output: `cli.py` (console-script `rosbagger-record` now resolves); `test_record_live.py`; a CLI unit test (mocked); the phase gate green; the live lane run + its result recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-live-record/12-CONTEXT.md
@.planning/phases/12-live-record/12-RESEARCH.md

@packages/rosbagger-record/src/rosbagger_record/__init__.py
@packages/rosbagger-record/src/rosbagger_record/record.py
@packages/rosbagger-record/src/rosbagger_record/errors.py
@packages/rosbagger-core/src/rosbagger_core/reader/rosbags_reader.py

<interfaces>
<!-- Contracts for the CLI + live test. Extracted from the codebase + verified research. -->

The thin-CLI / console-script pattern to MIRROR (packages/bagq/src/bagq/cli.py):
  app = typer.Typer(add_completion=False, help="...", no_args_is_help=True)
  @app.command() bodies LAZY-import the core API (keeps `--help` fast / offline-light).
  Args via Annotated[T, typer.Argument(...)] / options via Annotated[T, typer.Option("-o","--output", ...)].
  Console script entry: rosbagger-record = "rosbagger_record.cli:app" (declared in Plan 01's pyproject).

From Plan 02 (record.py): record(topics: list[str], out: str, *, storage="mcap", max_messages=None, duration=None) -> int;
  list_topics() -> dict[str, str].  __init__ re-exports both behind _require_ros().

SC3 re-open contract (rosbagger_core.reader.rosbags_reader.RosbagsReader — VERIFIED in 12-RESEARCH):
  from rosbagger_core.reader.rosbags_reader import RosbagsReader
  from rosbags.typesys import get_typestore, Stores
  ts = get_typestore(Stores.ROS2_HUMBLE)            # harmless no-op for self-describing MCAP; REQUIRED for sqlite3
  with RosbagsReader(out, default_typestore=ts) as r:
      msgs = list(r.read())                          # each Message has .topic / .t_ns / .msgtype
  assert len(msgs) == expected_count and all(m.topic == expected_topic for m in msgs)

Live-test gating (12-RESEARCH "Live integration test skeleton" + Open Q2/Q3 — VERIFIED primitives):
  rclpy = pytest.importorskip("rclpy")               # skips in the offline CI / ROS-free venv
  pytestmark = pytest.mark.live                       # the `live` marker (registered in Plan 01)
  def _mcap_available(): import rosbag2_py; return "mcap" in rosbag2_py.get_registered_writers()
  @pytest.mark.skipif(not _mcap_available(), reason="ros-humble-rosbag2-storage-mcap not installed")  # MCAP-specific assertion only

Live-lane invocation recipe (VERIFIED on box):
  source /opt/ros/humble/setup.bash
  PYTHONPATH="packages/rosbagger-record/src:packages/rosbagger-core/src:$PYTHONPATH" \
    python3 -m pytest tests/test_record_live.py -m live -v
  (system python3 already has pytest + rosbags + rclpy + rosbag2_py + numpy; PREPEND the src trees, do NOT replace PYTHONPATH)

External publisher for the live test (12-RESEARCH end-to-end probe used this shape): a second rclpy node OR a
  subprocess publishing a simple topic (e.g. std_msgs/msg/String on /telemetry) at a steady rate, so the recorder's
  discovery settle finds it and the bounded record (max_messages=N) captures N frames deterministically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thin rosbagger-record CLI (`list` + record verb over the API)</name>
  <files>packages/rosbagger-record/src/rosbagger_record/cli.py, tests/test_record_unit.py</files>
  <read_first>
    - packages/bagq/src/bagq/cli.py (the typer.Typer app + @app.command + Annotated Argument/Option pattern + the LAZY core-import-in-body discipline to mirror)
    - packages/rosbagger-record/src/rosbagger_record/__init__.py (record()/list_topics() the CLI calls; _require_ros() raises RosNotAvailableError when ROS absent)
    - packages/rosbagger-record/src/rosbagger_record/record.py (record() signature: topics/out/storage/max_messages/duration)
    - packages/rosbagger-record/src/rosbagger_record/errors.py (RosNotAvailableError / McapStorageUnavailableError — the CLI presents these as clean messages, no traceback)
    - .planning/phases/12-live-record/12-CONTEXT.md (D-02 console script not a bagq subcommand; D-06 list; D-07 subset flags; D-08 default MCAP + path; D-09 SIGINT + bounded)
    - .planning/phases/12-live-record/12-RESEARCH.md (the recommended cli.py shape: `list` + record verbs; Code Examples)
  </read_first>
  <action>
    Create src/rosbagger_record/cli.py mirroring bagq/cli.py: top level imports ONLY __future__ + typer (+ Annotated from typing); NO rclpy/rosbag2_py and NO eager rosbagger_record.record import at module top — import the API lazily inside command bodies (keeps `rosbagger-record --help` fast and the import graph clean). Build app = typer.Typer(add_completion=False, help="rosbagger-record — discover live ROS 2 topics and record a selected subset to a bag. Requires a sourced ROS 2 environment.", no_args_is_help=True). Add a `list` command (SC1, D-06): body lazy-imports list_topics from rosbagger_record, calls it, prints each `topic    type` line (sorted); empty -> a "no topics discovered" line. Add the record command (D-07/D-08/D-09): positional `topics: Annotated[list[str], typer.Argument(help="Topics to record; omit with --all to record everything")]` (allow empty when --all); options `-o/--output OUT` (required path), `--all` (bool, record everything published), `--regex` (str, include filter), `--exclude` (str, exclude filter), `--storage` (str, default "mcap"; choices mcap|sqlite3 — D-08 default preserved, sqlite3 escape), `--duration` (float seconds, optional), `--max-messages` (int, optional). The body lazy-imports record() and the selection helper; it resolves the selection by passing topics/--all/--regex/--exclude through to the API (record() already calls select_topics with positional topics; if --all/--regex/--exclude need to reach select_topics, thread them through record()'s signature OR call discover+select in the CLI then pass the resolved topic list — pick the path that keeps record() the single orchestrator and document it). Wrap both command bodies so RosNotAvailableError and McapStorageUnavailableError print a single clean stderr line + exit(1) with NO traceback (mirror bagq's teaching_errors mechanism — either reuse a small local decorator or a try/except in each body; do not dump a Python traceback for these capability errors). Keep the CLI THIN (API-first, D-02): no recording logic, no rclpy calls, no SQL — it parses args and calls the package API. EXTEND tests/test_record_unit.py with typer CliRunner tests (ENV=offline): `rosbagger-record --help` and `list --help` / record `--help` exit 0; invoking `list` or record with ROS absent (the uv venv has no rclpy) surfaces the RosNotAvailableError as a clean Exit(1) (CliRunner sees SystemExit, NOT a raw RosNotAvailableError escaping) — assert isinstance(result.exception, SystemExit) and that it is not the raw RuntimeError. Use typer.testing.CliRunner against the typer app (mirror bagq's CLI tests).
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_record_unit.py -k cli -q</automated>
  </verify>
  <acceptance_criteria>
    - ENV=offline (PYTHONPATH="" uv run): `pytest tests/test_record_unit.py -k cli -q` passes — `--help` for the app + both verbs exit 0; `list`/record with ROS absent exit cleanly via Exit(1) with no traceback (CliRunner result.exception is SystemExit, not the raw RosNotAvailableError).
    - ENV=offline: `import bagq.cli` (and `import bagq`) still pull NO rclpy/rosbag2_py — bagq's offline import graph is untouched (the offline-guard tests from Plan 01 stay green; `rosbagger-record` is a SEPARATE console script per D-02, never a bagq subcommand). `grep -rn "rosbagger_record" packages/bagq/` returns nothing.
    - cli.py top level imports no ROS and no eager record import: `grep -nE "^(import|from) (rclpy|rosbag2_py|rosidl)" packages/rosbagger-record/src/rosbagger_record/cli.py` returns nothing; the rosbagger_record API import is inside command bodies.
    - The record verb exposes --storage with mcap the default (D-08): `rosbagger-record record --help` (run via CliRunner) shows `--storage` and the default mcap.
    - The CLI is thin (D-02): cli.py contains no SequentialWriter/create_subscription/get_topic_names_and_types calls (`grep -nE "SequentialWriter|create_subscription|get_topic_names_and_types" cli.py` returns nothing) — all of that is in record.py.
  </acceptance_criteria>
  <done>The thin `rosbagger-record` CLI ships `list` + a record verb over the Plan-02 API with mcap-default --storage and bounded/selection flags, presents capability errors cleanly (no traceback), keeps bagq's import graph ROS-free, and its offline CliRunner tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Live integration test — publisher → bounded record → re-open via v1 reader (SC1/SC2/SC3)</name>
  <files>tests/test_record_live.py</files>
  <read_first>
    - packages/rosbagger-record/src/rosbagger_record/record.py (record() + list_topics() — the functions under live test)
    - packages/rosbagger-core/src/rosbagger_core/reader/rosbags_reader.py (RosbagsReader(paths, *, default_typestore) + read() yielding Message(.topic/.t_ns/.msgtype); the SC3 re-open contract)
    - tests/test_tf.py (the self-contained-harness pattern: own sys.path insert, own session fixtures, parametrization — a good shape model for a standalone test file)
    - .planning/phases/12-live-record/12-RESEARCH.md (the "Live integration test skeleton" + "SC3 re-open assertion" VERIFIED code; Pitfall 1 MCAP-missing skipif; Pitfall 2 sqlite3 needs default_typestore=ROS2_HUMBLE; Pitfall 3 venv/ROS interpreter split; Pitfall 4 settle; the VERIFIED live-lane invocation recipe; Open Q1 pass typestore defensively; Open Q2 skipif on get_registered_writers)
    - .planning/phases/12-live-record/12-CONTEXT.md (D-09 bounded mode makes the test deterministic; D-10 tier-2 live test gated/skippable; the specific "record then `with RosbagsReader(out) as r: list(r.read())` and assert" idea)
  </read_first>
  <action>
    Create tests/test_record_live.py as a self-contained harness. At the very top: `import pytest; rclpy = pytest.importorskip("rclpy")` so the offline suite (PYTHONPATH="" uv run, no rclpy) SKIPS the whole module cleanly; then `pytestmark = pytest.mark.live` (the marker registered in Plan 01). If the rosbagger_record/rosbagger_core src trees are not already importable in the ROS-sourced lane, prepend them to sys.path at the top of the file so the live lane (system python3) resolves them (the recipe prepends them on PYTHONPATH; a sys.path insert is belt-and-suspenders — verify which the lane needs and keep it minimal). Implement the SC2+SC3 test: spin up an EXTERNAL publisher (a second rclpy node created in the test, or a subprocess) publishing a simple message (e.g. std_msgs/msg/String) on a topic like /telemetry at a steady rate; call record(topics=["/telemetry"], out=str(tmp_path/"rec"), storage=<the available storage>, max_messages=N) so it discovers the publisher (after the settle), captures exactly N frames, and finalizes; then re-open via the SC3 contract: `from rosbags.typesys import get_typestore, Stores; with RosbagsReader(tmp_path/"rec", default_typestore=get_typestore(Stores.ROS2_HUMBLE)) as r: msgs = list(r.read())` and assert len(msgs) == N and all(m.topic == "/telemetry" for m in msgs) (SC2 = N frames recorded while publishing; SC3 = re-opens + iterates via the v1 reader). Pass default_typestore=ROS2_HUMBLE DEFENSIVELY (Open Q1 — harmless no-op for self-describing MCAP, REQUIRED for sqlite3, Pitfall 2). Storage selection: prove the mechanism via the storage that IS registered on this box. Write a TWO-variant approach — (a) a test that records via sqlite3 (registered here) and asserts SC2/SC3 (this is the one that RUNS on this box and proves the pipeline); (b) a test recording via mcap, decorated `@pytest.mark.skipif(not _mcap_available(), reason="ros-humble-rosbag2-storage-mcap not installed")` where `_mcap_available()` lazy-imports rosbag2_py and checks `"mcap" in get_registered_writers()` — this proves the LOCKED D-08 format where the plugin is present and skips cleanly here. Also add an SC1 test: call list_topics() (or the CLI `list`) with the publisher running and assert /telemetry appears in the discovered map with its type. Clean up rclpy (shutdown) and the publisher node in a finally so the test does not leak a live context into other live tests. Keep this test OUT of the offline coverage path — it never runs in PYTHONPATH="" uv run (the importorskip guarantees the skip), so it cannot and must not count toward the offline gate.
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_record_live.py -q</automated>
  </verify>
  <acceptance_criteria>
    - ENV=offline (PYTHONPATH="" uv run): `pytest tests/test_record_live.py -q` reports the module SKIPPED (via importorskip("rclpy")) — zero failures, the offline CI stays green. This is the gate the offline CI sees.
    - ENV=offline: the FULL offline suite `PYTHONPATH="" uv run pytest` is green at >=80% with test_record_live.py collected-and-skipped (no unknown-marker warning, since `live` is registered) and contributing nothing to coverage.
    - ENV=live (ROS-sourced lane — the VERIFIED recipe `source /opt/ros/humble/setup.bash && PYTHONPATH="packages/rosbagger-record/src:packages/rosbagger-core/src:$PYTHONPATH" python3 -m pytest tests/test_record_live.py -m live -v`): the SC1 test discovers /telemetry with its type; the sqlite3 SC2/SC3 test records exactly N frames from the external publisher and the recorded bag re-opens via `RosbagsReader(out, default_typestore=ROS2_HUMBLE)` yielding N messages all on /telemetry; the mcap variant is SKIPPED here with the "mcap not installed" reason (this box lacks the plugin). Record the exact command run + its output (passed/skipped counts) in the SUMMARY.
    - The MCAP-specific assertion is skipif-guarded on get_registered_writers(): `grep -n "get_registered_writers" tests/test_record_live.py` matches inside the skip helper; the sqlite3 path carries the SC2/SC3 proof on this box.
    - The SC3 re-open uses the EXISTING v1 reader with default_typestore=ROS2_HUMBLE (no new read code): `grep -n "RosbagsReader" tests/test_record_live.py` matches and the test passes default_typestore.
  </acceptance_criteria>
  <done>The live integration test proves SC1 (discover), SC2 (record N frames while publishing), and SC3 (re-open + iterate via the v1 reader with default_typestore=ROS2_HUMBLE) in the ROS-sourced lane via the sqlite3 path, skips the MCAP-specific assertion where the plugin is absent, and SKIPS entirely in the offline CI — proven by running both lanes.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full offline suite + ruff + offline guard + live-lane run</name>
  <files>tests/test_record_unit.py</files>
  <read_first>
    - .planning/phases/12-live-record/12-RESEARCH.md (Validation Architecture: the per-task / per-wave / phase-gate sampling commands; the live-lane recipe; the coverage note on the live-only wiring)
    - .planning/phases/12-live-record/12-CONTEXT.md (the hard constraints: offline CI green; uv sync green; offline guard green; recorded bag re-opens via v1 reader)
    - tests/test_offline_guard.py (the guards that must all stay green: core/bagq + the new rosbagger_record guard from Plan 01)
    - the prior Phase-11 SUMMARY pattern in STATE.md (phase-gate reporting: full-suite pass count + coverage + ruff clean + offline guard count)
  </read_first>
  <action>
    Run and record the phase gate. (1) Offline gate: `PYTHONPATH="" uv run pytest` — the FULL offline suite must be green at >=80% coverage (the prior baseline was 387 passed @ 97.23%; this phase adds the unit + CLI tests and the collected-skipped live test). (2) Lint/format: `PYTHONPATH="" uv run ruff check .` and `PYTHONPATH="" uv run ruff format --check .` clean (the new package src + the two new test files). (3) Offline guard: confirm tests/test_offline_guard.py is fully green — `import rosbagger_core`/`import bagq` leak no rclpy/rosbag2_py AND `import rosbagger_record` leaks no rclpy/rosbag2_py (the Plan-01 guard). (4) uv: `PYTHONPATH="" uv sync --locked --dev` exit 0 (re-lock + commit uv.lock if the new member changed it — note in SUMMARY). (5) LIVE lane: run the VERIFIED recipe `source /opt/ros/humble/setup.bash && PYTHONPATH="packages/rosbagger-record/src:packages/rosbagger-core/src:$PYTHONPATH" python3 -m pytest tests/test_record_live.py -m live -v` and capture the result (the sqlite3 SC2/SC3 + SC1 tests pass; the mcap variant skips on this box). If this command requires sourcing ROS (a different shell environment than the uv venv) and cannot be run autonomously in this harness, run it via a single bash invocation that sources setup.bash first; if sourcing is genuinely unavailable in the harness, record that the live lane was prepared + the exact command, and flag running it as the human-follow-up (mirroring the Phase-08 SC3 split precedent) — but ATTEMPT it first. (6) If any addopts/coverage adjustment for rosbagger_record was made in Plan 02, confirm the final --cov-fail-under is unchanged (>=80%) and the gate is still meaningful. Do not add new production code in this task — it is the verification capstone; the only file edit allowed is a small docstring/marker fix in test_record_unit.py if the gate surfaces a lint nit on this phase's own test edits.
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest && PYTHONPATH="" uv run ruff check . && PYTHONPATH="" uv run ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - ENV=offline: `PYTHONPATH="" uv run pytest` is green at >=80% coverage; test_record_live.py is collected-and-skipped (no failures, no unknown-marker warning).
    - ENV=offline: `PYTHONPATH="" uv run ruff check .` and `PYTHONPATH="" uv run ruff format --check .` both clean across the new package src + the two new test files.
    - ENV=offline: tests/test_offline_guard.py is fully green — the offline guarantee holds: `import rosbagger_core`/`import bagq`/`import rosbagger_record` pull no rclpy/rosbag2_py.
    - ENV=offline: `PYTHONPATH="" uv sync --locked --dev` exit 0 (uv.lock committed if changed).
    - ENV=live (ROS-sourced lane): the live recipe was RUN (or, if sourcing is unavailable in the harness, prepared with the exact command recorded as a documented human follow-up — Phase-08 SC3 split precedent). When run: SC1 discovers /telemetry; the sqlite3 SC2/SC3 test passes (N frames recorded + re-opened via the v1 reader); the mcap variant skips with the plugin-missing reason. The exact command + passed/skipped counts are recorded in the SUMMARY.
    - The final coverage gate is unchanged (`grep -n "cov-fail-under=80" pyproject.toml` still present) — the gate was never weakened to accommodate the live-only wiring.
  </acceptance_criteria>
  <done>The full offline suite is green at >=80% with the live test collected-and-skipped, ruff is clean, the offline guard (core+bagq+record) is green, uv sync --locked --dev is green, and the live lane was run on this box (or prepared with the exact command as a recorded human follow-up) proving SC1/SC2/SC3 via the sqlite3 path with the MCAP assertion skipif-guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bagq import graph ↔ rosbagger-record | `rosbagger-record` is a SEPARATE console script (D-02), never a bagq subcommand — bagq must remain ROS-free. The boundary is enforced by the offline guard (no rosbagger_record import in bagq). |
| offline CI lane ↔ live test lane | The live test runs only in the ROS-sourced lane; `pytest.importorskip("rclpy")` + the `live` marker keep the offline CI skipping it so the no-ROS gate stays green. |
| CLI args ↔ record() API | Topic names / `--regex` / `--exclude` / `-o` path / `--storage` are user CLI input passed to the API as data (no shell interpolation; storage constrained to mcap|sqlite3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | cli.py / bagq import boundary | mitigate | cli.py imports no ROS at top (grep-gated); the API is lazy in command bodies; bagq imports no rosbagger_record (grep-gated); offline-guard tests stay green. |
| T-12-06 | Spoofing / Tampering | live test reads back a different bag than recorded | mitigate | SC3 asserts BOTH count (== N) AND topic (all == /telemetry) on the re-opened bag, so a stale/empty/wrong bag fails the assertion. Bounded `max_messages=N` makes the count deterministic. |
| T-12-02 | Tampering | CLI `--storage`/topic/regex inputs | mitigate | `--storage` constrained to {mcap, sqlite3}; topic names + regex passed as data to the API (re.search only, established in Plan 01); `-o` is a local path handled by rosbag2_py. No shell/eval. |
| T-12-SC | Tampering | uv package installs | mitigate | No new PyPI package this plan (typer already vetted; rosbagger-core workspace). rclpy/rosbag2_py environment-provided on disk. 12-RESEARCH Package Legitimacy Audit clean — no [ASSUMED]/[SUS]/[SLOP], no legitimacy checkpoint required. |

No HIGH threats: local CLI + a gated live test; no network/auth/session/crypto/injection surface.
</threat_model>

<verification>
- ENV=offline: `PYTHONPATH="" uv run pytest` green at >=80%; test_record_live.py collected-and-skipped; no unknown-marker warning.
- ENV=offline: `PYTHONPATH="" uv run ruff check . && PYTHONPATH="" uv run ruff format --check .` clean; `PYTHONPATH="" uv sync --locked --dev` exit 0.
- ENV=offline: tests/test_offline_guard.py green (core+bagq+record ROS-free); `import bagq.cli` pulls no ROS.
- ENV=live (ROS-sourced lane): `source /opt/ros/humble/setup.bash && PYTHONPATH="packages/rosbagger-record/src:packages/rosbagger-core/src:$PYTHONPATH" python3 -m pytest tests/test_record_live.py -m live -v` — SC1 discovers; sqlite3 SC2/SC3 passes; mcap variant skips.
</verification>

<success_criteria>
- `rosbagger-record list` prints discoverable topics + types (SC1); the record verb records a subset with --all/--regex/--exclude/--duration/--max-messages/--storage (mcap default), presenting capability errors cleanly (D-02/D-07/D-08/D-09).
- The live test proves SC2 (record N frames while a publisher runs) and SC3 (re-open + iterate via the v1 `RosbagsReader(default_typestore=ROS2_HUMBLE)`) in the ROS-sourced lane via the sqlite3 path; the MCAP-specific assertion is skipif-guarded; the test skips entirely in the offline CI.
- bagq's import graph stays ROS-free; the offline guard (core+bagq+record) is green; the full offline suite is green at >=80%; ruff + `uv sync --locked --dev` clean.
- The live lane was run on this box (or prepared + recorded as a human follow-up per the Phase-08 split precedent).
</success_criteria>

<output>
Create `.planning/phases/12-live-record/12-03-SUMMARY.md` when done. Record: the chosen selection-threading path (CLI threads --all/--regex/--exclude through record() vs CLI resolves then passes a topic list); the EXACT live-lane command run + its passed/skipped counts (and whether the mcap variant skipped on this box); whether the live lane ran autonomously or is a recorded human follow-up; the final full-suite pass count + coverage; and confirmation that REC-01 / SC1 / SC2 / SC3 are all satisfied. Then mark REC-01 complete in REQUIREMENTS.md traceability and ROADMAP.md Phase 12.
</output>
