---
phase: 02-bag-reader-layer
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - tests/test_reader.py
autonomous: true
requirements: [READ-01, READ-02, READ-03, READ-04, READ-05]
must_haves:
  truths:
    - "A fixture-backed test opens all three formats (ROS1 .bag, ROS2 sqlite3, ROS2 MCAP) through RosbagsReader and iterates messages"
    - "Iterating yields Message records carrying topic, t/t_ns, stamp, msgtype, and the deserialized msg for each format"
    - "stamp is derived correctly: None for headerless /cmd_vel (Twist), 1_000_000_000 for header-bearing /imu and /image"
    - "Two same-format ROS2 bags open as one logical dataset: message_count sums and timestamps are globally ascending (the merge test that resolves research Open Question 1)"
    - "Two ROS1 bags also merge as one logical dataset"
  artifacts:
    - path: "tests/test_reader.py"
      provides: "Fixture-backed RosbagsReader test suite across all 3 formats + multi-bag merge"
      contains: "RosbagsReader"
      min_lines: 80
  key_links:
    - from: "tests/test_reader.py"
      to: "rosbagger_core.reader.RosbagsReader"
      via: "import + open fixtures + assert Message stream"
      pattern: "from rosbagger_core.reader import"
    - from: "tests/test_reader.py"
      to: "tools.make_fixtures"
      via: "make_all_fixtures / write_ros2_sqlite_bag for the merge fixtures"
      pattern: "make_fixtures import"
---

<objective>
Prove the phase's success criteria with a fixture-backed test suite for `RosbagsReader`: all three formats open and iterate (READ-01/02/03), each yields `Message` records with the four derived fields and correct `stamp` (READ-04), and multiple same-format bag paths read as one time-ordered logical dataset (READ-05) — including the two-ROS2-bag merge case the research flagged as Open Question 1.

Purpose: Turn the research's verified-by-probe findings into committed regression tests, and resolve assumption A1 / Open Question 1 (multi-ROS2 merge) into a verified fact backed by a fixture test. The tests reuse the Phase 1 fixture generator (`tools/make_fixtures.py`) so the whole suite runs with NO ROS install.

Output: `tests/test_reader.py` — the reader's regression suite, raising `rosbagger_core` coverage above the ≥80% gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bag-reader-layer/02-RESEARCH.md
@packages/rosbagger-core/src/rosbagger_core/reader/base.py
@packages/rosbagger-core/src/rosbagger_core/reader/rosbags_reader.py
@tests/test_fixtures.py
@tools/make_fixtures.py

<interfaces>
<!-- The reader under test (from 02-01/02-02) and the fixture helpers (Phase 1). -->

Under test (rosbagger_core.reader):
  RosbagsReader(paths: str | Path | Iterable[str|Path], *, default_typestore=None)  # context manager via BagReader
  RosbagsReader.read() -> Iterator[Message]            # lazy, time-ordered
  RosbagsReader.topics -> Mapping[str, TopicInfo]
  RosbagsReader.connections -> Sequence[Connection]
  Message(topic: str, t: int, t_ns: int, stamp: int|None, msgtype: str, msg: object)

Fixture helpers (tools/make_fixtures.py — Phase 1):
  make_all_fixtures(dest_dir) -> {"ros1": Path(.bag), "ros2_sqlite": Path(dir), "ros2_mcap": Path(dir)}
  write_ros1_bag(dest_dir) -> Path
  write_ros2_sqlite_bag(dest_dir) -> Path     # writes a "ros2_sqlite" dir under dest_dir
  write_ros2_mcap_bag(dest_dir) -> Path       # writes a "ros2_mcap" dir under dest_dir

Verified fixture facts (02-RESEARCH.md): each single bag has 9 messages across topics {/cmd_vel: Twist x3, /imu: Imu x3, /image: Image x3}. /cmd_vel has NO header -> stamp None. /imu and /image headers have sec=1,nanosec=0 for msg 0 -> stamp 1_000_000_000. Log timestamps t_ns = 1_000_000_000, 1_100_000_000, 1_200_000_000. Two bags merged -> 18 messages, timestamps ascending (heapq.merge).

Test-harness conventions (tests/test_fixtures.py): import tools.make_fixtures by inserting the repo root on sys.path inside the test file (tools is a dev-only package, not installed). Use a session-scoped tmp_path_factory fixture for the shared single-bag set. Local runs need `PYTHONPATH=""` (host ROS leak); CI is ROS-free.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Single-format reader tests across ROS1 / ROS2-sqlite / ROS2-MCAP (READ-01/02/03/04)</name>
  <files>tests/test_reader.py</files>
  <read_first>
    - tests/test_fixtures.py (COPY its harness conventions: the `sys.path.insert(0, repo_root)` block to import `tools.make_fixtures`, the `@pytest.fixture(scope="session")` over `tmp_path_factory.mktemp(...)`, the `FORMATS`/`EXPECTED_TOPICS` parametrize style)
    - packages/rosbagger-core/src/rosbagger_core/reader/rosbags_reader.py (the RosbagsReader behavior under test)
    - packages/rosbagger-core/src/rosbagger_core/reader/base.py (Message field names for assertions)
    - .planning/phases/02-bag-reader-layer/02-RESEARCH.md (Code Examples §1/§2 for expected counts/topics/stamps; phase_requirements table READ-01..04; Pitfall 5 for the PYTHONPATH note to record in the module docstring)
  </read_first>
  <behavior>
    - Opening each format (ros1, ros2_sqlite, ros2_mcap) via `with RosbagsReader(path) as r: msgs = list(r.read())` yields exactly 9 Message instances (READ-01/02/03).
    - For every format, the set of `m.topic` equals {"/cmd_vel", "/imu", "/image"} and every record is a `Message` instance with int `t` and `t_ns` and a non-None `msg` (READ-04).
    - For every format, all `/cmd_vel` records have `stamp is None` (headerless Twist), and all `/imu` and `/image` records have integer `stamp` (header-bearing); the first `/imu` record's `stamp == 1_000_000_000` (READ-04 stamp derivation).
    - `t == t_ns` for every record (the reader sets both to the same log-time ns).
    - `r.topics` exposes the three topics without iterating messages (metadata path), and `r.connections` is non-empty — accessed on an opened reader.
    - Calling `read()` before `open()` (i.e. `RosbagsReader(path).read()` then advancing it without entering the context) raises (not-opened guard) — assert it raises when the generator is advanced.
  </behavior>
  <action>
    Create `tests/test_reader.py` with a module docstring noting (a) what it proves (RosbagsReader over all 3 formats + READ-04 record/stamp derivation) and (b) the `PYTHONPATH=""` local-run requirement from 02-RESEARCH.md Pitfall 5 (CI is ROS-free, so no prefix there). Reproduce the `tests/test_fixtures.py` repo-root `sys.path` insert so `from tools.make_fixtures import make_all_fixtures` works, with the `# noqa: E402` after the path setup. Import `from rosbagger_core.reader import RosbagsReader, Message` (and `BagReader` if asserting subclass).

    Add a `@pytest.fixture(scope="session")` named `fixture_bags` that calls `make_all_fixtures(tmp_path_factory.mktemp("reader_bags"))` and returns the dict — mirror the existing fixture so the bags are generated once per session (do NOT reuse the one in test_fixtures.py; keep this file self-contained). Define `FORMATS = ("ros1", "ros2_sqlite", "ros2_mcap")` and `EXPECTED_TOPICS = {"/cmd_vel", "/imu", "/image"}`.

    Write parametrized tests over `FORMATS`:
    - `test_reader_opens_and_yields_nine_messages`: open via the context manager, `msgs = list(r.read())`, assert `len(msgs) == 9` and every element `isinstance(m, Message)`.
    - `test_reader_yields_all_topics_with_record_fields`: assert `{m.topic for m in msgs} == EXPECTED_TOPICS`; for each record assert `isinstance(m.t, int)`, `m.t == m.t_ns`, `m.msg is not None`, and `m.msgtype` is a non-empty str.
    - `test_reader_stamp_derivation`: assert all `/cmd_vel` records have `stamp is None`; assert all `/imu` and `/image` records have `isinstance(stamp, int)`; assert the first `/imu` record (lowest `t_ns`) has `stamp == 1_000_000_000`.
    - `test_reader_topics_metadata_without_iteration`: open the reader, assert `set(r.topics) == EXPECTED_TOPICS` and `len(list(r.connections)) >= 3` WITHOUT calling `read()`.

    Add one non-parametrized `test_read_before_open_raises`: construct `RosbagsReader(fixture_bags["ros1"])` WITHOUT entering the context, then assert advancing its `read()` generator raises (use `pytest.raises(...)` around `next(reader.read())`; match the not-opened guard's exception type from 02-02, e.g. `RuntimeError`).

    Use `Path` inputs (the fixture dict already returns `Path`s). Do not add coverage pragmas. Keep assertions exact (the numbers above are verified facts, not approximations).
  </action>
  <acceptance_criteria>
    - `tests/test_reader.py` exists and `PYTHONPATH="" uv run pytest tests/test_reader.py -q` passes with the single-format tests collected (≥ 13 cases: 4 parametrized x 3 formats + the read-before-open case).
    - The stamp test specifically asserts `/cmd_vel` -> `None` and first `/imu` -> `1_000_000_000` (grep: `grep -nE "stamp is None|1_000_000_000" tests/test_reader.py` matches both).
    - All three FORMATS are exercised (grep: `grep -nE "ros1|ros2_sqlite|ros2_mcap" tests/test_reader.py` matches all three).
    - `uv run ruff check tests/test_reader.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_reader.py -q</automated>
  </verify>
  <done>tests/test_reader.py opens ROS1/ROS2-sqlite/ROS2-MCAP fixtures through RosbagsReader, asserts 9 Message records per format with the three expected topics, correct field shape, and exact stamp derivation (None for /cmd_vel, 1_000_000_000 for the first /imu), plus a metadata-only topics/connections check and a read-before-open guard test — all green under PYTHONPATH="".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-bag merge tests — two ROS2 bags + two ROS1 bags as one dataset (READ-05)</name>
  <files>tests/test_reader.py</files>
  <read_first>
    - tests/test_reader.py (extend the file from Task 1 — reuse its sys.path setup and imports)
    - .planning/phases/02-bag-reader-layer/02-RESEARCH.md (Code Examples §3 multi-bag merge; Pattern 3 pass-through; Open Question 1 + Assumption A1 — THIS test resolves them: open two ROS2 bags, assert merged count + ascending timestamps; Pitfall 1 — do NOT mix ROS1 and ROS2 in one reader)
    - tools/make_fixtures.py (write_ros2_sqlite_bag / write_ros1_bag write into a dest dir; generate a SECOND copy into a separate tmp dir as the research probes did)
  </read_first>
  <behavior>
    - Opening TWO same-format ROS2 sqlite bags (each generated into its own tmp dir) in one `RosbagsReader([pathA, pathB])` yields 18 Message records (9 + 9 summed across bags) — READ-05.
    - The `t_ns` sequence over the merged stream is globally non-decreasing (ascending), proving `AnyReader`'s heapq merge ordering is preserved through the reader (resolves Open Question 1 / A1 for multi-ROS2).
    - `r.topics` over the merged ROS2 dataset still reports the three expected topics, with summed counts across the two bags (per-topic msgcount doubles vs a single bag).
    - Opening TWO ROS1 `.bag` files in one reader likewise yields 18 records with ascending `t_ns` (READ-05 for ROS1).
  </behavior>
  <action>
    Extend `tests/test_reader.py` with multi-bag fixtures and tests. Add a `@pytest.fixture(scope="session")` `two_ros2_sqlite_bags(tmp_path_factory)` that calls `write_ros2_sqlite_bag` twice into two distinct `tmp_path_factory.mktemp(...)` directories (e.g. "rec0"/"rec1") and returns `[pathA, pathB]` — both same format, mirroring 02-RESEARCH.md §3's "second copy into a separate tmp dir" approach (do NOT write both into the same dir; the dir name "ros2_sqlite" would collide). Add an analogous `two_ros1_bags` fixture using `write_ros1_bag` into two separate tmp dirs. Import `write_ros2_sqlite_bag`/`write_ros1_bag` from `tools.make_fixtures`.

    Write:
    - `test_two_ros2_bags_merge_as_one_dataset`: `with RosbagsReader(two_ros2_sqlite_bags) as r: msgs = list(r.read())`; assert `len(msgs) == 18`; assert `[m.t_ns for m in msgs] == sorted(m.t_ns for m in msgs)` (globally ascending). This is the assertion that turns research A1 into a verified fact — add a comment citing 02-RESEARCH.md Open Question 1.
    - `test_two_ros2_bags_topics_sum_counts`: open the merged reader; assert `set(r.topics) == EXPECTED_TOPICS`; assert each topic's msgcount is 6 (3 per bag x 2 bags) using the TopicInfo `.msgcount` attribute (per 02-RESEARCH.md TopicInfo(msgtype, msgdef, msgcount, connections)).
    - `test_two_ros1_bags_merge_as_one_dataset`: same shape as the ROS2 merge test but with `two_ros1_bags`; assert `len(msgs) == 18` and ascending `t_ns`.

    Do NOT add a mixed ROS1+ROS2 test that expects success — Pitfall 1 documents that `AnyReader` raises on a mixed list; if you want to assert the constraint, add an OPTIONAL `test_mixed_formats_raise` using `pytest.raises(Exception)` around `RosbagsReader([ros1_path, ros2_path]).open()` (acceptable since v1 lets the raw `AnyReaderError` surface). Keep counts/ordering assertions exact.
  </action>
  <acceptance_criteria>
    - `PYTHONPATH="" uv run pytest tests/test_reader.py -q` passes including the merge tests; the two-ROS2 merge test asserts `len(msgs) == 18` and ascending `t_ns` (grep: `grep -nE "== 18|sorted\(" tests/test_reader.py` matches).
    - The topics-sum test references the `msgcount` attribute (grep: `grep -n "msgcount" tests/test_reader.py` matches).
    - Both a two-ROS2 and a two-ROS1 merge test exist (grep: `grep -nE "two_ros2|two_ros1" tests/test_reader.py` matches both).
    - Full suite green: `PYTHONPATH="" uv run pytest -q` passes (reader tests + existing fixture/offline tests) and the ≥80% coverage gate in pyproject `addopts` is met (no `--cov-fail-under` failure).
    - `uv run ruff check tests/test_reader.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_reader.py -q && PYTHONPATH="" uv run pytest -q</automated>
  </verify>
  <done>The reader suite proves multi-bag READ-05: two same-format ROS2 bags and two ROS1 bags each open as one logical dataset yielding 18 time-ordered Message records, ROS2 topic counts sum across bags, and the full test suite passes the ≥80% coverage gate — resolving research Open Question 1 (multi-ROS2 merge) into a verified, committed fact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture bag -> RosbagsReader (test harness) | The bags here are self-generated by `tools/make_fixtures.py` (trusted, deterministic content) — not attacker-controlled. The untrusted-input boundary itself lives in 02-02; this plan only exercises it with known-good fixtures. No network, auth, or persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-D | Denial of Service | session fixtures generating bags repeatedly | accept | Bags are tiny (9 msgs) and generated once per session via `tmp_path_factory` (mirrors the existing test_fixtures.py pattern); negligible resource cost, throwaway tmp dirs, no committed binaries. |
| T-02-03-I | Information Disclosure | test harness importing ROS via host PYTHONPATH leak | mitigate | Document + run with `PYTHONPATH=""` (02-RESEARCH.md Pitfall 5) so the host ROS-on-PYTHONPATH leak cannot pull ROS into the test process; CI is ROS-free. The committed test file bakes in NO `PYTHONPATH=""` (per project instruction) — it is a run-time prefix only. |
| T-02-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installed; tests use already-locked `rosbags` + the Phase 1 fixture generator. No supply-chain surface. |

> This is a test-only plan exercising the 02-02 boundary with trusted fixtures. The real untrusted-bag-parsing threats are registered against 02-02. Nothing here rises to `high`.
</threat_model>

<verification>
- `PYTHONPATH="" uv run pytest tests/test_reader.py -q` passes (all single-format + multi-bag cases).
- `PYTHONPATH="" uv run pytest -q` passes end-to-end and meets the ≥80% coverage gate (pyproject `--cov-fail-under=80`).
- The two-ROS2-bag merge test asserts 18 records and ascending `t_ns` (resolves research Open Question 1).
- A two-ROS1-bag merge test asserts the same for ROS1.
- Stamp derivation asserted exactly: `/cmd_vel` -> `None`, first `/imu` -> `1_000_000_000`, across all three formats.
- `uv run ruff check tests/test_reader.py` passes.
</verification>

<success_criteria>
The phase's three success criteria are proven by committed, ROS-free tests: (1) `RosbagsReader` opens ROS2 sqlite, ROS2 MCAP, and ROS1 bags (READ-01/02/03); (2) iterating yields `(topic, t, stamp, msgtype, msg)` records with correct `stamp` derivation (READ-04); (3) multiple same-format bag paths read as one time-ordered logical dataset for both ROS2 and ROS1 (READ-05), turning research Assumption A1 / Open Question 1 into a verified fact. The full suite passes the ≥80% coverage gate.
</success_criteria>

<output>
Create `.planning/phases/02-bag-reader-layer/02-03-SUMMARY.md` when done.
</output>
