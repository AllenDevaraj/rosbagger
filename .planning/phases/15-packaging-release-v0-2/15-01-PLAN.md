---
phase: 15-packaging-release-v0-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-core/pyproject.toml
  - packages/bagq/pyproject.toml
  - packages/rosbagger-record/pyproject.toml
  - packages/rosbagger-replay/pyproject.toml
  - packages/rosbagger-gui/pyproject.toml
  - packages/rosbagger-core/src/rosbagger_core/__init__.py
  - packages/bagq/src/bagq/__init__.py
  - packages/rosbagger-record/src/rosbagger_record/__init__.py
  - packages/rosbagger-replay/src/rosbagger_replay/__init__.py
  - packages/rosbagger-gui/src/rosbagger_gui/__init__.py
  - uv.lock
autonomous: true
requirements: [SC1, SC4, SC5]
user_setup: []

must_haves:
  truths:
    - "All five packages report version 0.2.0 (5 pyproject + 5 __init__)"
    - "Each sibling dependency line pins >=0.2,<0.3 (bagq, record, replay, gui depending on rosbagger-core)"
    - "rosbagger-gui declares [project.optional-dependencies] live = rosbagger-record + rosbagger-replay (both pinned >=0.2,<0.3)"
    - "uv.lock is re-locked at 0.2.0 so uv sync --locked passes"
    - "bagq --version prints bagq 0.2.0"
    - "Base import rosbagger_gui leaks no rclpy/rosbag2_py after the live extra is added"
  artifacts:
    - path: "packages/rosbagger-core/pyproject.toml"
      provides: "rosbagger-core dist version 0.2.0"
      contains: 'version = "0.2.0"'
    - path: "packages/rosbagger-gui/pyproject.toml"
      provides: "gui version 0.2.0 + sibling pin + live extra"
      contains: '[project.optional-dependencies]'
    - path: "uv.lock"
      provides: "re-locked 0.2.0 lockfile"
      contains: "0.2.0"
  key_links:
    - from: "packages/bagq/pyproject.toml"
      to: "rosbagger-core>=0.2,<0.3"
      via: "[project] dependencies sibling pin"
      pattern: "rosbagger-core>=0.2,<0.3"
    - from: "packages/bagq/src/bagq/__init__.py"
      to: "bagq --version"
      via: "__version__ drives the CLI version string"
      pattern: '__version__ = "0.2.0"'
---

<objective>
Stamp a coherent v0.2.0 release across all five workspace packages and make the
inter-package dependencies resolve by version spec (D-01/D-03), then re-lock.

This is the manifest+version layer of Phase 15. After this plan: every package
declares version 0.2.0, every sibling dependency is pinned `>=0.2,<0.3`,
rosbagger-gui gains the declarative `live` extra (D-02), and uv.lock is
re-locked so `uv sync --locked` stays green. No runtime code changes.

Purpose: close the wheel-resolution gap at the source (bare `rosbagger-core`
deps cannot resolve outside the workspace) and ship coherent versions — the
prerequisite for the external-venv proof in Plan 02.
Output: 10 version sites at 0.2.0 + 4 pinned sibling lines + 1 live extra +
re-locked uv.lock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-packaging-release-v0-2/15-CONTEXT.md
@.planning/phases/15-packaging-release-v0-2/15-RESEARCH.md
@.planning/phases/08-packaging-docs-release/08-01-SUMMARY.md

<host_constraints>
- ROS is sourced globally on this box: bare `uv run` / `uv lock` may leak host
  ROS onto PYTHONPATH. ALWAYS prefix uv commands with `PYTHONPATH=""`.
- Invoke ruff as `uv run ruff` (never bare `ruff`).
</host_constraints>

<interfaces>
<!-- Exact version sites (RESEARCH.md Version Source Inventory, verified this session). -->
<!-- All currently "0.1.0"; all become "0.2.0". -->

pyproject `version` lines (5):
  packages/rosbagger-core/pyproject.toml      line 8  : version = "0.1.0"
  packages/bagq/pyproject.toml                line 7  : version = "0.1.0"
  packages/rosbagger-record/pyproject.toml    line 13 : version = "0.1.0"
  packages/rosbagger-replay/pyproject.toml    line 15 : version = "0.1.0"
  packages/rosbagger-gui/pyproject.toml       line 13 : version = "0.1.0"

__init__ `__version__` lines (5):
  packages/rosbagger-core/src/rosbagger_core/__init__.py      line 9  : __version__ = "0.1.0"
  packages/bagq/src/bagq/__init__.py                          line 3  : __version__ = "0.1.0"
  packages/rosbagger-record/src/rosbagger_record/__init__.py  line 32 : __version__ = "0.1.0"
  packages/rosbagger-replay/src/rosbagger_replay/__init__.py  line 31 : __version__ = "0.1.0"
  packages/rosbagger-gui/src/rosbagger_gui/__init__.py        line 12 : __version__ = "0.1.0"

Sibling dependency lines (4 packages → rosbagger-core; bare today):
  bagq             dependencies = ["rosbagger-core", "typer>=0.15,<1", "rich>=13"]
  rosbagger-record dependencies = ["rosbagger-core", "typer>=0.15,<1"]
  rosbagger-replay dependencies = ["rosbagger-core", "typer>=0.15,<1"]
  rosbagger-gui    dependencies = ["rosbagger-core", "textual>=8,<9"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump all 10 version sites to 0.2.0</name>
  <files>packages/rosbagger-core/pyproject.toml, packages/bagq/pyproject.toml, packages/rosbagger-record/pyproject.toml, packages/rosbagger-replay/pyproject.toml, packages/rosbagger-gui/pyproject.toml, packages/rosbagger-core/src/rosbagger_core/__init__.py, packages/bagq/src/bagq/__init__.py, packages/rosbagger-record/src/rosbagger_record/__init__.py, packages/rosbagger-replay/src/rosbagger_replay/__init__.py, packages/rosbagger-gui/src/rosbagger_gui/__init__.py</files>
  <read_first>
    - packages/rosbagger-core/pyproject.toml (and the other four pyproject.toml)
    - packages/bagq/src/bagq/__init__.py (and the other four __init__.py)
    - 15-RESEARCH.md "Version Source Inventory" (the 10-site table) and "Pitfall 2"
    - .planning/phases/08-packaging-docs-release/08-01-SUMMARY.md (the v0.1 4-site bump precedent)
  </read_first>
  <action>
    Change the literal string "0.1.0" to "0.2.0" at exactly the 10 sites listed
    in the interfaces block: the 5 `version = "0.1.0"` lines in the package
    pyproject.toml files, and the 5 `__version__ = "0.1.0"` lines in the package
    __init__.py files. Change ONLY the version literal — touch no other line,
    add no comments. Do NOT adopt hatchling dynamic versioning (Phase 8 chose
    hand-bump as the lowest-risk path; mirror it). The root pyproject.toml is a
    virtual workspace with no [project] table and no version — do not touch it.
  </action>
  <verify>
    <automated>grep -rn 'version = "0.2.0"' packages/*/pyproject.toml | grep -v '^#' | grep -c 'version = "0.2.0"' | grep -qx 5 && grep -rn '__version__ = "0.2.0"' packages/*/src/*/__init__.py | grep -c '__version__' | grep -qx 5 && echo "10 SITES OK"</automated>
  </verify>
  <done>All 5 pyproject `version` and all 5 `__version__` read "0.2.0"; zero "0.1.0" version literals remain in those 10 files.</done>
</task>

<task type="auto">
  <name>Task 2: Pin sibling deps >=0.2,<0.3 and add the gui live extra</name>
  <files>packages/bagq/pyproject.toml, packages/rosbagger-record/pyproject.toml, packages/rosbagger-replay/pyproject.toml, packages/rosbagger-gui/pyproject.toml</files>
  <read_first>
    - packages/bagq/pyproject.toml, packages/rosbagger-record/pyproject.toml, packages/rosbagger-replay/pyproject.toml, packages/rosbagger-gui/pyproject.toml
    - 15-RESEARCH.md "Inter-package dependencies after this phase" table + "Pattern 1" + "Pattern 3" + "Anti-Patterns to Avoid"
    - 15-CONTEXT.md D-01, D-02, D-03
    - tests/test_offline_guard.py (test_import_gui_does_not_pull_ros) — the invariant the live extra must not regress
  </read_first>
  <action>
    Per D-01/D-03, in each of bagq, rosbagger-record, rosbagger-replay, and
    rosbagger-gui pyproject.toml, replace the bare `"rosbagger-core"` entry in
    `[project] dependencies` with `"rosbagger-core>=0.2,<0.3"`. Keep all other
    deps unchanged (typer/rich/textual pins stay). Do NOT bake any git or path
    URL into [project] dependencies — version spec ONLY (D-01 source-agnostic;
    baking a URL is the explicit anti-pattern). rosbagger-core itself has no
    sibling deps — leave its dependencies line untouched.

    Per D-02, add to packages/rosbagger-gui/pyproject.toml a new
    `[project.optional-dependencies]` table with one entry:
    `live = ["rosbagger-record>=0.2,<0.3", "rosbagger-replay>=0.2,<0.3"]`.
    Place it after the `dependencies` line / before `[project.scripts]`. This
    is purely declarative — add NO import to rosbagger_gui/__init__.py; the live
    panels already lazy-import the siblings in method bodies (Phase 14), so the
    base import stays rclpy-free.
  </action>
  <verify>
    <automated>grep -c 'rosbagger-core>=0.2,<0.3' packages/bagq/pyproject.toml packages/rosbagger-record/pyproject.toml packages/rosbagger-replay/pyproject.toml packages/rosbagger-gui/pyproject.toml | grep -v ':0' | grep -c ':1' | grep -qx 4 && grep -q 'live = \["rosbagger-record>=0.2,<0.3", "rosbagger-replay>=0.2,<0.3"\]' packages/rosbagger-gui/pyproject.toml && echo "PINS + EXTRA OK"</automated>
  </verify>
  <done>The four sibling lines read `rosbagger-core>=0.2,<0.3`; rosbagger-gui has a `live` extra naming both record and replay pinned `>=0.2,<0.3`; no git/path URL appears in any `[project] dependencies`.</done>
</task>

<task type="auto">
  <name>Task 3: Re-lock uv.lock at 0.2.0 and confirm bagq --version</name>
  <files>uv.lock</files>
  <read_first>
    - uv.lock (search for the existing "0.1.0" version entries for the five members near revision = 3)
    - 15-RESEARCH.md "Pitfall 2" + "Runtime State Inventory" (uv.lock carries 0.1.0 strings, must re-lock) + "Local CI-equivalent gate"
  </read_first>
  <action>
    Run `PYTHONPATH="" uv lock` to regenerate uv.lock against the bumped
    manifests so every workspace member resolves at 0.2.0 with the new sibling
    pins. Then run `PYTHONPATH="" uv sync --locked --dev` to prove the lockfile
    is consistent (it must NOT report "lockfile is out of date"). Finally run
    `PYTHONPATH="" uv run bagq --version` and confirm it prints exactly
    `bagq 0.2.0`. Do not edit uv.lock by hand — let `uv lock` own it.
  </action>
  <verify>
    <automated>PYTHONPATH="" uv sync --locked --dev >/dev/null 2>&1 && PYTHONPATH="" uv run bagq --version 2>/dev/null | grep -qx "bagq 0.2.0" && echo "LOCK + VERSION OK"</automated>
  </verify>
  <done>`uv sync --locked --dev` succeeds (lockfile current); `bagq --version` prints `bagq 0.2.0`; uv.lock no longer carries 0.1.0 for the five members.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest → built wheel | `[project] dependencies` is the only inter-package contract that ships; a baked URL here would leak into every consumer lockfile |
| live extra → base import | the `live` extra must remain declarative; an eager import would pull ROS into the base GUI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `[project] dependencies` URL injection | mitigate | Version spec only (D-01); Task 2 verify greps for the bare pin and the absence of git/path is enforced by review of the dependencies line |
| T-15-02 | Information disclosure | live extra causing ROS eager-import | mitigate | Declarative extra only; no `__init__` edit; offline guard re-run in Plan 03 phase gate |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new external packages introduced this phase (RESEARCH Package Legitimacy Audit); only first-party sibling pins + version bumps. No install-task surface. |
</threat_model>

<verification>
- `grep` confirms 10 sites at 0.2.0 and 4 sibling pins + 1 live extra.
- `uv sync --locked --dev` passes; `bagq --version` → `bagq 0.2.0`.
- Each `python -c "import rosbagger_X; print(rosbagger_X.__version__)"` (run under
  `PYTHONPATH=""`) prints 0.2.0 for core/record/replay/gui and bagq.
</verification>

<success_criteria>
- SC4: versions coherent at 0.2.0 across all five packages + re-locked uv.lock.
- SC1 prerequisite: manifests are wheel-buildable (Plan 03 phase gate runs `uv build`).
- SC5 prerequisite: live extra is declarative; base import invariant preserved
  (asserted in Plan 03 phase gate).
</success_criteria>

<output>
Create `.planning/phases/15-packaging-release-v0-2/15-01-SUMMARY.md` when done.
</output>
