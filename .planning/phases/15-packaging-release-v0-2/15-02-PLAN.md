---
phase: 15-packaging-release-v0-2
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - scripts/proof_external_install.sh
  - INSTALL.md
autonomous: true
requirements: [SC1, SC2, SC3]
user_setup: []

must_haves:
  truths:
    - "All five wheels build via uv build --all-packages"
    - "A fresh EXTERNAL venv (not .venv, neutral cwd) installs all five from local paths in one transaction with rosbagger-core resolving by spec"
    - "rosbagger-gui --help exits 0 and import rosbagger_gui works outside the monorepo"
    - "Base import rosbagger_gui leaks no rclpy/rosbag2_py; tools/ is not importable from the installed venv"
    - "INSTALL.md documents the one-transaction git+subdirectory meta recipe, per-package snippets, path install, consumer-side [tool.uv.sources], the live note, and the proof recipe"
    - "The literal git+@v0.2.0 recipe is documented and flagged awaits-push (NOT a blocking checkpoint — remote is empty)"
  artifacts:
    - path: "scripts/proof_external_install.sh"
      provides: "autonomous path-based external-venv proof"
      min_lines: 20
    - path: "INSTALL.md"
      provides: "central install + usage docs (D-04)"
      contains: "subdirectory=packages/rosbagger-core"
  key_links:
    - from: "scripts/proof_external_install.sh"
      to: "fresh venv outside .venv"
      via: "mktemp -d + python3 -m venv + PYTHONPATH= pip install ./packages/*"
      pattern: "venv"
    - from: "INSTALL.md"
      to: "rosbagger-gui[live]"
      via: "live-panel install note (D-02)"
      pattern: "rosbagger-gui\\[live\\]"
---

<objective>
Prove the wheel-resolution gap is actually closed and document every install
path (D-04).

This is the proof+docs layer of Phase 15. After this plan: a committed
`scripts/proof_external_install.sh` builds/installs all five packages into a
throwaway venv outside the monorepo and smoke-checks CLI + import + offline
invariant + tools-not-in-wheel; and a central `INSTALL.md` documents the
one-transaction meta recipe, per-package git+subdirectory snippets, path
installs, the consumer-side `[tool.uv.sources]` recipe, and the `live` GUI note.

The literal `git+…@v0.2.0` recipe is DOCUMENTED but flagged awaits-push (the
GitHub remote is public-but-empty; the git recipe cannot run end-to-end
locally — Phase 8 honest-split precedent). The path-based proof is the
autonomous, verifiable-now gate.

Purpose: SC2 (external resolution proven) + SC3 (per-package install/usage docs).
Output: one proof script + one INSTALL.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-packaging-release-v0-2/15-CONTEXT.md
@.planning/phases/15-packaging-release-v0-2/15-RESEARCH.md
@.planning/phases/15-packaging-release-v0-2/15-01-SUMMARY.md
@.planning/phases/08-packaging-docs-release/08-01-SUMMARY.md
@README.md

<host_constraints>
- ROS is sourced globally: every command in the proof script and every test/lint
  invocation MUST be prefixed `PYTHONPATH=""` (neutralizes the host ROS leak;
  the offline assertion is meaningless without it — RESEARCH Pitfall 4).
- The proof MUST use a FRESH venv via `python3 -m venv` (NOT the workspace
  `.venv`, NOT `uv run`) and `cd` to a neutral cwd OUTSIDE the monorepo before
  the smoke checks — otherwise it "passes" via workspace source and proves
  nothing (RESEARCH Pitfall 3).
</host_constraints>

<interfaces>
<!-- The verified autonomous proof recipe (RESEARCH "Code Examples"). Use as the script body. -->
<!-- Co-installing all five in ONE pip invocation is what makes the bare sibling spec resolve. -->

  set -euo pipefail
  TMP=$(mktemp -d); python3 -m venv "$TMP/venv"
  PYTHONPATH="" "$TMP/venv/bin/pip" install \
    ./packages/rosbagger-core ./packages/bagq \
    ./packages/rosbagger-record ./packages/rosbagger-replay \
    "./packages/rosbagger-gui[live]"
  cd /tmp                                              # neutral cwd OUTSIDE the monorepo
  PYTHONPATH="" "$TMP/venv/bin/rosbagger-gui" --help >/dev/null
  PYTHONPATH="" "$TMP/venv/bin/bagq" --version         # -> bagq 0.2.0
  PYTHONPATH="" "$TMP/venv/bin/python" -c "import rosbagger_gui, rosbagger_core, bagq, rosbagger_record, rosbagger_replay"
  # offline invariant + tools-not-in-wheel asserts also run here

<!-- The literal git+subdirectory meta recipe for INSTALL.md (awaits-push; RESEARCH Pattern 1). -->
<!-- Remote: https://github.com/AllenDevaraj/rosbagger  tag: v0.2.0  subdir: packages/<pkg> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit scripts/proof_external_install.sh (autonomous path-based proof)</name>
  <files>scripts/proof_external_install.sh</files>
  <read_first>
    - 15-RESEARCH.md "Code Examples" → "Autonomous external-venv PROOF (path-based)" + "Build all wheels at once (SC1)"
    - 15-RESEARCH.md "Pitfall 3" (don't run from inside the monorepo / .venv) + "Pitfall 4" (PYTHONPATH="" everywhere) + Open Question 2 (tools-not-in-wheel assertion)
    - .planning/phases/08-packaging-docs-release/08-01-SUMMARY.md (the v0.1 clean-room recipe being generalized)
    - 15-01-SUMMARY.md (confirms versions are at 0.2.0 so the script can assert `bagq 0.2.0`)
  </read_first>
  <action>
    Write an executable bash script (`#!/usr/bin/env bash`, `set -euo pipefail`)
    that proves external spec resolution with zero network/remote dependency,
    following the verified recipe in the interfaces block. The script MUST,
    using a directory resolved relative to the script location so it runs from
    any cwd (compute the repo root from the script path, not a hardcoded
    absolute path):
      1. Create a throwaway venv via `mktemp -d` + `python3 -m venv` (NEVER the
         workspace .venv) and register a trap to `rm -rf` it on exit.
      2. `PYTHONPATH="" <venv>/bin/pip install` all FIVE packages by local path
         in ONE invocation — `./packages/rosbagger-core ./packages/bagq
         ./packages/rosbagger-record ./packages/rosbagger-replay` and
         `"./packages/rosbagger-gui[live]"` — so the bare sibling spec resolves
         in-transaction (the resolution crux).
      3. `cd` to a neutral directory OUTSIDE the monorepo (e.g. the temp dir or
         /tmp) before any smoke check.
      4. Assert `<venv>/bin/rosbagger-gui --help` exits 0.
      5. Assert `<venv>/bin/bagq --version` equals exactly `bagq 0.2.0`.
      6. Assert `import rosbagger_gui, rosbagger_core, bagq, rosbagger_record,
         rosbagger_replay` succeeds.
      7. Offline invariant: assert `import rosbagger_gui` leaves no module whose
         top-level name is in {rclpy, rosbag2_py} in sys.modules.
      8. tools-not-in-wheel: assert `import tools` raises ModuleNotFoundError
         from the installed venv (RESEARCH Open Q2 — the wheels stay clean).
      Every venv-python / venv-pip / CLI invocation is prefixed `PYTHONPATH=""`.
      On all checks passing, print exactly `PROOF OK` as the final line and exit 0;
      any failed assertion aborts via `set -e` with a nonzero exit.
    Do NOT use `uv run` or reference the workspace .venv anywhere in the script.
    `chmod +x` the script.
  </action>
  <verify>
    <automated>test -x scripts/proof_external_install.sh && PYTHONPATH="" bash scripts/proof_external_install.sh 2>&1 | tail -1 | grep -qx "PROOF OK" && echo "PROOF SCRIPT OK"</automated>
  </verify>
  <done>`bash scripts/proof_external_install.sh` builds/installs into a fresh external venv, all six smoke checks pass, and the final line is `PROOF OK`; the script never touches .venv or `uv run`.</done>
</task>

<task type="auto">
  <name>Task 2: Write central INSTALL.md (D-04 — every install path + proof + live note)</name>
  <files>INSTALL.md</files>
  <read_first>
    - 15-CONTEXT.md D-01, D-02, D-04 (the INSTALL.md content contract)
    - 15-RESEARCH.md "Three valid consumer recipes", "Pattern 1/2/3", "Consumer-side [tool.uv.sources] recipe", "Pitfall 1" (lead with the one-transaction recipe), "Pitfall 5" (repo empty → git recipe awaits push)
    - README.md (existing v0.1 install section + the "why one command" callout to mirror tone)
  </read_first>
  <action>
    Write a top-level INSTALL.md (Claude's discretion on exact section wording/
    order) covering ALL of:
      - A "why one command" callout up front: external installs MUST name every
        needed package in ONE `pip`/`uv pip install` invocation because the
        sibling spec (`rosbagger-core>=0.2,<0.3`) has no index to resolve from
        (we publish to none). Leading with a single-package install fails
        (RESEARCH Pitfall 1) — do NOT present one as primary.
      - The one-line META recipe: all five packages as
        `pkg @ git+https://github.com/AllenDevaraj/rosbagger@v0.2.0#subdirectory=packages/<pkg>`
        in a single command (both `uv pip install` and `pip install` forms).
      - Per-package git+subdirectory snippets (e.g. gui+core; the `[live]`
        variant co-naming record+replay+core).
      - Local path-install snippet: `pip install ./packages/rosbagger-core
        ./packages/bagq …` (the autonomous path equivalent — generalized Phase 8
        recipe).
      - Consumer-side `[tool.uv.sources]` recipe (a downstream uv project's own
        pyproject declaring `rosbagger-core`/`rosbagger-gui` git sources with
        `tag = "v0.2.0"`, subdirectory) — note this lives in the CONSUMER's
        project, never baked into our wheels (D-01 compatible).
      - The live-GUI note: base `rosbagger-gui` is offline (inspect/query/tf);
        `rosbagger-gui[live]` adds the rclpy-backed record/replay panels (D-02).
      - The external-venv PROOF recipe: point to `scripts/proof_external_install.sh`
        and inline the fresh-venv → path-install → `--help`/import smoke steps.
      - An explicit AWAITS-PUSH flag on the git+`@v0.2.0` recipes: the remote
        (https://github.com/AllenDevaraj/rosbagger) is public but currently empty;
        the literal git recipe runs once the repo is pushed and a v0.2.0 tag
        exists. The path-based proof is the locally-verified gate today
        (Phase 8 honest-split precedent).
    Use the exact repo URL, the `@v0.2.0` tag, and `subdirectory=packages/<pkg>`
    for each of the five packages.
  </action>
  <verify>
    <automated>test -f INSTALL.md && grep -q 'subdirectory=packages/rosbagger-core' INSTALL.md && grep -q 'subdirectory=packages/rosbagger-gui' INSTALL.md && grep -q 'rosbagger-gui\[live\]' INSTALL.md && grep -q 'pip install ./packages/rosbagger-core' INSTALL.md && grep -q 'tool.uv.sources' INSTALL.md && grep -qiE 'await|empty|not yet pushed|push' INSTALL.md && echo "INSTALL.md OK"</automated>
  </verify>
  <done>INSTALL.md exists with the one-transaction meta recipe (all five git+subdirectory URLs at @v0.2.0), per-package snippets, the path-install snippet, the consumer-side `[tool.uv.sources]` recipe, the `rosbagger-gui[live]` note, a pointer to the proof script, and an explicit awaits-push flag on the git recipes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof venv → host env | the proof must run in an isolated fresh venv with PYTHONPATH="" so host ROS cannot mask an offline regression |
| INSTALL.md recipe → consumer | a documented recipe that bakes a URL into [project] deps would leak into consumer lockfiles; INSTALL.md keeps URLs in install commands / consumer-side sources only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Spoofing | proof passing via workspace source instead of installed wheel | mitigate | Script uses a fresh `python3 -m venv` (not .venv), neutral cwd outside the repo, path install (not `-e`), PYTHONPATH="" — RESEARCH Pitfall 3 |
| T-15-04 | Information disclosure | offline assertion masked by host ROS leak | mitigate | All proof commands prefixed PYTHONPATH=""; assertion checks sys.modules for rclpy/rosbag2_py — RESEARCH Pitfall 4 |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | The proof installs only first-party local-path packages + their already-vetted PyPI deps (Phases 1-14). No new external package surface. |
</threat_model>

<verification>
- `bash scripts/proof_external_install.sh` prints `PROOF OK`.
- INSTALL.md grep checks (Task 2 verify) all pass.
- INSTALL.md leads with the one-transaction recipe; no single-package install is
  presented as primary.
</verification>

<success_criteria>
- SC1: all five wheels build (the proof's path install exercises the build for
  each; Plan 03 also runs `uv build --all-packages`).
- SC2: fresh external venv resolves the bare sibling spec by path/transaction and
  `rosbagger-gui --help` + imports work outside the monorepo — proven by the script.
- SC3: per-package install + usage docs exist with git+subdirectory and
  `[tool.uv.sources]` snippets and the `rosbagger-gui[live]` note.
</success_criteria>

<output>
Create `.planning/phases/15-packaging-release-v0-2/15-02-SUMMARY.md` when done.
</output>
