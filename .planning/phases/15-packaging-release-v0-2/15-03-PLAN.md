---
phase: 15-packaging-release-v0-2
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - .planning/phases/15-packaging-release-v0-2/15-03-SUMMARY.md
autonomous: true
requirements: [SC1, SC2, SC5]
user_setup: []

must_haves:
  truths:
    - "uv build --all-packages builds all five wheels with no hatch config errors"
    - "uv sync --locked --dev passes (lockfile current at 0.2.0)"
    - "uv run ruff check . and ruff format --check . are clean"
    - "PYTHONPATH= uv run pytest passes (>=80% gate; offline guard included)"
    - "scripts/proof_external_install.sh prints PROOF OK"
    - "A local annotated v0.2.0 tag exists; push + observe-CI documented as the sole human follow-up"
  artifacts:
    - path: ".planning/phases/15-packaging-release-v0-2/15-03-SUMMARY.md"
      provides: "phase-gate evidence + honest local/awaits-push split"
      min_lines: 20
  key_links:
    - from: "uv build --all-packages"
      to: "dist/ five wheels"
      via: "hatchling auto-detect src-layout (no config)"
      pattern: "all-packages"
    - from: "PYTHONPATH= uv run pytest"
      to: "tests/test_offline_guard.py"
      via: "offline invariant gate under the live extra"
      pattern: "offline_guard"
---

<objective>
Run the full v0.2 release gate and tag locally — the SC1/SC2/SC5 sign-off layer.

This is the gate layer of Phase 15. After this plan: `uv build --all-packages`
proves all five wheels build (SC1); the path-based external-venv proof passes
(SC2); and `uv sync --locked` + ruff + the full offline pytest suite + offline
guard are green under the new versions and the live extra (SC5). A local
annotated `v0.2.0` tag is created; the remote push + GitHub-Actions observation
is documented as the sole human follow-up (the standing no-push blocker;
Phase 8 honest-split precedent).

Purpose: prove the release is coherent and the offline invariant survived the
live extra, then stamp the local tag. No code/manifest changes here — this plan
verifies and records.
Output: a green phase gate + local v0.2.0 tag + SUMMARY recording the
autonomous-vs-awaits-push split.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-packaging-release-v0-2/15-CONTEXT.md
@.planning/phases/15-packaging-release-v0-2/15-RESEARCH.md
@.planning/phases/15-packaging-release-v0-2/15-01-SUMMARY.md
@.planning/phases/15-packaging-release-v0-2/15-02-SUMMARY.md
@.planning/phases/08-packaging-docs-release/08-01-SUMMARY.md

<host_constraints>
- ROS is sourced globally: every gate command MUST be prefixed `PYTHONPATH=""`.
  Bare `uv run pytest` CRASHES on this box (RESEARCH Pitfall 4 / MEMORY note).
- Invoke ruff as `uv run ruff` (never bare `ruff`).
- The offline guard (tests/test_offline_guard.py, incl. test_import_gui_does_not_pull_ros)
  MUST stay green AFTER the live extra was added — it is the SC5 anchor.
</host_constraints>

<interfaces>
<!-- The Phase-15 release gate (RESEARCH "Local CI-equivalent gate" + "Build all wheels at once"). -->
  PYTHONPATH="" uv build --all-packages -o dist/      # SC1: all five wheels
  PYTHONPATH="" uv sync --locked --dev                # lockfile current
  PYTHONPATH="" uv run ruff check .
  PYTHONPATH="" uv run ruff format --check .
  PYTHONPATH="" uv run pytest                         # >=80% gate + offline guard
  PYTHONPATH="" bash scripts/proof_external_install.sh # SC2: PROOF OK
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full v0.2 release gate (SC1/SC2/SC5)</name>
  <files>(verification only — no source edits)</files>
  <read_first>
    - 15-RESEARCH.md "Code Examples" → "Build all wheels at once" + "Local CI-equivalent gate"
    - 15-RESEARCH.md "Pitfall 4" (PYTHONPATH="" everywhere) + Open Question 1 (proof script is the SC2 gate)
    - 15-01-SUMMARY.md + 15-02-SUMMARY.md (confirm versions bumped + proof script exists)
    - tests/test_offline_guard.py (the SC5 invariant; especially test_import_gui_does_not_pull_ros)
  </read_first>
  <action>
    Run the release gate in order, each command prefixed `PYTHONPATH=""`:
      1. `uv build --all-packages -o dist/` — confirm five wheels are produced
         (rosbagger_core, bagq, rosbagger_record, rosbagger_replay,
         rosbagger_gui) with no hatchling config errors (SC1). Auto-detection
         needs no `[tool.hatch.*]` — add none.
      2. `uv sync --locked --dev` — must succeed (lockfile current at 0.2.0; no
         "out of date").
      3. `uv run ruff check .` and `uv run ruff format --check .` — both clean.
      4. `uv run pytest` — full offline suite passes at >=80% coverage, INCLUDING
         tests/test_offline_guard.py (the live extra must not have regressed the
         base `import rosbagger_gui` ROS-free invariant — SC5).
      5. `bash scripts/proof_external_install.sh` — must print `PROOF OK` (SC2).
    If any step fails, STOP and report the failure (do not paper over it). Do not
    push to the remote or run live (`-m live`) tests — those are out of scope /
    human follow-up.
  </action>
  <verify>
    <automated>PYTHONPATH="" uv build --all-packages -o dist/ >/dev/null 2>&1 && ls dist/rosbagger_core-0.2.0*.whl dist/bagq-0.2.0*.whl dist/rosbagger_record-0.2.0*.whl dist/rosbagger_replay-0.2.0*.whl dist/rosbagger_gui-0.2.0*.whl >/dev/null 2>&1 && PYTHONPATH="" uv sync --locked --dev >/dev/null 2>&1 && PYTHONPATH="" uv run ruff check . >/dev/null 2>&1 && PYTHONPATH="" uv run ruff format --check . >/dev/null 2>&1 && PYTHONPATH="" uv run pytest >/dev/null 2>&1 && PYTHONPATH="" bash scripts/proof_external_install.sh 2>&1 | tail -1 | grep -qx "PROOF OK" && echo "GATE GREEN"</automated>
  </verify>
  <done>Five 0.2.0 wheels build; `uv sync --locked --dev` passes; ruff check + format-check clean; full pytest suite green at >=80% incl. the offline guard; proof script prints `PROOF OK`.</done>
</task>

<task type="auto">
  <name>Task 2: Create local v0.2.0 tag and record the honest local/awaits-push split</name>
  <files>.planning/phases/15-packaging-release-v0-2/15-03-SUMMARY.md</files>
  <read_first>
    - 15-RESEARCH.md "Repo state caveat" + "Pitfall 5" (remote empty → git recipe + push await human follow-up)
    - .planning/phases/08-packaging-docs-release/08-01-SUMMARY.md (the v0.1 local-tag + push-as-sole-follow-up split being mirrored)
    - @$HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    After Task 1's gate is green, create a local annotated git tag
    `v0.2.0` with a message naming the milestone (e.g. "rosbagger v0.2.0 —
    Modular cockpit: git/path-installable packages"). Do NOT push it — the
    standing no-push-credential blocker means push + GitHub-Actions observation
    + the literal git+subdirectory recipe execution are the SOLE human
    follow-ups (mirror Phase 8). Record in the SUMMARY: which SCs are verified
    locally now (SC1/SC2/SC4/SC5 + SC3 docs exist), and that SC2's literal
    git+`@v0.2.0` recipe is documented-but-awaits-push (the path-based proof is
    the locally-verified equivalent — the resolution MECHANISM is proven).
    Note that the local CI-equivalent gate is the strongest available proxy for
    the remote CI run.
  </action>
  <verify>
    <automated>git tag -l v0.2.0 | grep -qx v0.2.0 && git cat-file -t "$(git rev-parse v0.2.0)" | grep -qx tag && test -f .planning/phases/15-packaging-release-v0-2/15-03-SUMMARY.md && echo "TAG + SUMMARY OK"</automated>
  </verify>
  <done>A local annotated `v0.2.0` tag exists (not pushed); 15-03-SUMMARY.md records the verified-locally SCs and the push/git-recipe human follow-up split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local gate → remote CI | the local CI-equivalent gate is a proxy for the unpushable remote run; honesty about the split is the boundary |
| live extra → offline guard | the gate's pytest run is the SC5 enforcement point that the live extra did not regress the base import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Repudiation | claiming the git+@v0.2.0 recipe "works" when the remote is empty | mitigate | SUMMARY records it as awaits-push; only the path-based proof (autonomous) is claimed verified — RESEARCH Pitfall 5 |
| T-15-06 | Information disclosure | offline regression slipping through under host ROS leak | mitigate | Gate runs `PYTHONPATH="" uv run pytest` incl. test_offline_guard.py — RESEARCH Pitfall 4 |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new external packages; gate only builds first-party wheels + re-locks already-vetted deps. |
</threat_model>

<verification>
- The Task 1 verify chain returns `GATE GREEN` (all six gate steps pass).
- `git tag -l v0.2.0` shows an annotated tag; SUMMARY documents the split.
- No push and no `-m live` run performed (out of scope this plan).
</verification>

<success_criteria>
- SC1: `uv build --all-packages` produces five 0.2.0 wheels.
- SC2: `scripts/proof_external_install.sh` prints `PROOF OK` (external resolution
  by spec proven via the path transaction).
- SC5: full offline suite + offline guard green under the live extra and 0.2.0.
- Local `v0.2.0` tag created; remote push + literal git recipe = documented human
  follow-up (the only remaining step, per the standing no-push blocker).
</success_criteria>

<output>
Create `.planning/phases/15-packaging-release-v0-2/15-03-SUMMARY.md` when done.
</output>
