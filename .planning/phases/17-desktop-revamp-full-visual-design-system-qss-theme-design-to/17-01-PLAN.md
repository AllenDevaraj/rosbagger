---
phase: 17-desktop-revamp-full-visual-design-system-qss-theme-design-to
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-desktop/src/rosbagger_desktop/theme/__init__.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/theme/tokens.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/theme/qss.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/theme/manager.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/cli.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py
  - tests/test_desktop.py
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "build_qss(DARK) and build_qss(LIGHT) are pure strings (no QApplication) each containing that theme's baked token hex values"
    - "ThemeManager.apply() sets a non-empty QApplication stylesheet containing the active theme's bg hex"
    - "ThemeManager.toggle() flips the QSettings ui/theme value AND the applied stylesheet to the other theme without relaunch"
    - "A persisted ui/theme value chosen on a prior launch is honored on the next ThemeManager construction"
    - "A View menu in the shell exposes a checkable Dark/Light toggle action wired to ThemeManager.toggle"
    - "import rosbagger_core / import bagq still pull no PySide6 (offline+Qt-free guard green)"
  artifacts:
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/theme/tokens.py"
      provides: "Frozen Tokens dataclass + DARK and LIGHT instances (OKLCH-authored, baked sRGB hex), Qt-free pure data"
      contains: "class Tokens"
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/theme/qss.py"
      provides: "Pure build_qss(tokens) -> str function — the single place QSS strings live (D-03)"
      contains: "def build_qss"
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/theme/manager.py"
      provides: "ThemeManager owning setStyleSheet + QSettings persistence + live unpolish/polish refresh"
      contains: "class ThemeManager"
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/theme/__init__.py"
      provides: "Re-exports ThemeManager, Tokens, DARK, LIGHT"
  key_links:
    - from: "theme/manager.py"
      to: "theme/qss.py + theme/tokens.py"
      via: "build_qss(DARK if name == dark else LIGHT)"
      pattern: "build_qss\\("
    - from: "main_window.py"
      to: "ThemeManager.toggle"
      via: "View menu checkable action triggered signal"
      pattern: "toggle"
    - from: "cli.py main()"
      to: "QSettings org/app name + ThemeManager.apply()"
      via: "setOrganizationName/setApplicationName before constructing the manager, apply() before window.show()"
      pattern: "setOrganizationName"
---

<objective>
Build the design-token + QSS theme foundation for rosbagger-desktop: a Qt-free `theme/` subpackage (frozen `Tokens` dataclass with DARK + LIGHT palettes, a pure `build_qss(tokens) -> str` function, and a `ThemeManager` that applies/toggles/persists), wire `QSettings` org/app identity and an initial apply into `cli.main`, and add a live View-menu Dark/Light toggle to the shell.

Purpose: This is the foundation every panel's visual rollout (17-03) consumes — tokens are the single source of styling (D-03), the toggle flips dark↔light live and persists (D-06), and the mechanism stays pure-Python with zero new deps inside the package (D-04, D-08).
Output: `theme/` subpackage, themed `QApplication`, a persisted live toggle, and headless tests proving the token-driven pure-string contract + live flip + persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-desktop-revamp-full-visual-design-system-qss-theme-design-to/17-CONTEXT.md
@.planning/phases/17-desktop-revamp-full-visual-design-system-qss-theme-design-to/17-RESEARCH.md

# The shell + entry where QSettings identity, the manager, and the toggle hook in.
@packages/rosbagger-desktop/src/rosbagger_desktop/cli.py
@packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py
# Existing offline/Qt-free guard that MUST stay green (D-08).
@tests/test_offline_guard.py

<interfaces>
<!-- Contracts the executor implements against. The shell already builds a &File menu in
     MainWindow._build_menu (main_window.py line ~145); add the View menu alongside it. -->

theme/tokens.py (NEW — pure data, NO Qt import — must stay offline-clean):
```python
from dataclasses import dataclass

@dataclass(frozen=True)
class Tokens:
    bg: str; surface: str; text: str; text_muted: str
    accent: str; error: str; border: str
    space_sm: int; space_md: int; radius: int
    font_size_base: int; font_size_mono: int

DARK: Tokens   # neutral-warm dark default (NOT category-reflex navy/cyan), per D-07
LIGHT: Tokens  # calm low-chroma light surface
```

theme/qss.py (NEW — pure function, NO QApplication needed):
```python
def build_qss(t: Tokens) -> str: ...  # f-strings every token into ONE stylesheet keyed by class/objectName
```

theme/manager.py (NEW — the only tier touching QApplication/QSettings):
```python
class ThemeManager(QObject):
    def __init__(self) -> None: ...        # reads persisted name from QSettings().value("ui/theme", "dark")
    @property
    def name(self) -> str: ...             # "dark" | "light" (test accessor)
    def apply(self) -> None: ...           # setStyleSheet(build_qss(active)) + unpolish/polish/update over topLevelWidgets
    def toggle(self) -> None: ...          # flip name, persist via QSettings.setValue, then apply()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Token module + pure build_qss (the D-03/D-04 mechanism)</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/theme/__init__.py, packages/rosbagger-desktop/src/rosbagger_desktop/theme/tokens.py, packages/rosbagger-desktop/src/rosbagger_desktop/theme/qss.py, tests/test_desktop.py</files>
  <behavior>
    - build_qss(DARK) returns a non-empty str containing DARK.bg, DARK.surface, DARK.text, DARK.accent, DARK.error, and DARK.border hex values.
    - build_qss(LIGHT) returns a non-empty str containing the LIGHT counterparts; DARK and LIGHT outputs differ.
    - build_qss output targets QTableView (gridline-color, selection-background-color), QHeaderView::section, and QSplitter::handle selectors (Pitfall 3 — those don't inherit from QWidget styling).
    - tokens.py and qss.py import NO PySide6 (pure data + string); a fresh-interpreter import of rosbagger_desktop.theme.tokens leaves no PySide6 in sys.modules.
  </behavior>
  <action>Create the `theme/` subpackage. In `tokens.py` define a `@dataclass(frozen=True) Tokens` per the interface block and two instances DARK + LIGHT (per D-07: a calm, focused robotics-engineer-at-a-desk scene — a neutral-warm dark default, a low-chroma light; AVOID the category-reflex navy/cyan "tool" look). Author each color in OKLCH reasoning and store the baked sRGB `#rrggbb` with the source `oklch(...)` in a trailing comment — design-time bake only, NO runtime color conversion (D-07, RESEARCH OKLCH section). tokens.py imports ONLY `dataclasses` — NO Qt (D-08, RESEARCH Pitfall 6). In `qss.py` write the pure `build_qss(t: Tokens) -> str` that f-strings tokens into ONE stylesheet — the SINGLE place QSS strings live (D-03) — explicitly targeting `QTableView { gridline-color / selection-background-color }`, `QHeaderView::section`, `QSplitter::handle`, and a `#status_error` objectName selector for the error status color (this replaces query_panel's inline `_STATUS_ERROR_STYLE` literal in 17-03). qss.py imports NO Qt. `theme/__init__.py` re-exports `Tokens, DARK, LIGHT, build_qss`. Add pure-string tests to tests/test_desktop.py (a `-k qss` group) that call build_qss with NO QApplication and assert each token hex appears (RESEARCH Pitfall 4 — the cleanest proof the system is token-driven).</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -k qss -x</automated>
  </verify>
  <done>build_qss(DARK)/build_qss(LIGHT) pure-string tests pass with no QApplication; tokens.py + qss.py import no PySide6; DARK and LIGHT outputs each contain their own token hex and differ.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThemeManager apply/toggle/persist + cli identity wiring</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/theme/manager.py, packages/rosbagger-desktop/src/rosbagger_desktop/theme/__init__.py, packages/rosbagger-desktop/src/rosbagger_desktop/cli.py, tests/test_desktop.py</files>
  <behavior>
    - After ThemeManager().apply(), QApplication.instance().styleSheet() is non-empty and contains the active theme's bg hex.
    - After toggle(), QSettings().value("ui/theme") has flipped to the other name AND styleSheet() now contains the OTHER theme's bg hex (live flip, no relaunch — RESEARCH Pattern 2).
    - A ThemeManager constructed when ui/theme is already persisted as "light" reports name == "light" and applies the LIGHT stylesheet.
  </behavior>
  <action>Create `theme/manager.py` with `ThemeManager(QObject)` per the interface block (RESEARCH Pattern 2): `__init__` reads `QSettings().value("ui/theme", "dark")`; `apply()` does `QApplication.instance().setStyleSheet(build_qss(DARK if self._name == "dark" else LIGHT))` THEN iterates `app.topLevelWidgets()` calling `w.style().unpolish(w); w.style().polish(w); w.update()` so already-shown widgets flush cached style (RESEARCH Pitfall 2 — without this the flip is partial/stale); `toggle()` flips the name, `QSettings().setValue("ui/theme", ...)` immediately (persist), then `apply()`. Expose a `name` read-only property for tests. Re-export ThemeManager from `theme/__init__.py`. In `cli.main` (cli.py), AFTER constructing `QApplication` but BEFORE constructing MainWindow: call `QCoreApplication.setOrganizationName("rosbagger")` and `QCoreApplication.setApplicationName("rosbagger-desktop")` (→ ~/.config/rosbagger/rosbagger-desktop.conf, RESEARCH verified), then construct a `ThemeManager`, call `.apply()`, and hand the manager to MainWindow (constructor arg) so the View-menu toggle (Task 3) can reach it. Keep the QApplication/MainWindow imports INSIDE main() (cli.py Pitfall-6 discipline — the --help path stays Qt-free; test_import_desktop_cli_does_not_pull_pyside6_or_ros must stay green). Add qtbot tests (`-k theme_apply` / `-k theme_toggle`) that set org/app name to a TEMP scope (use a tmp_path QSettings file or a unique org name) so the dev box's real ~/.config is NOT polluted (RESEARCH Wave 0 note), then assert apply() stylesheet contents and the toggle() flip of both QSettings + styleSheet.</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -k "theme_apply or theme_toggle" -x</automated>
  </verify>
  <done>apply() sets a non-empty stylesheet with the active bg hex; toggle() flips both the persisted QSettings value and the applied stylesheet to the other theme; a pre-persisted "light" value is honored on construction; cli sets org/app identity and applies the theme before show; cli stays Qt-free on import.</done>
</task>

<task type="auto">
  <name>Task 3: View-menu live toggle in the shell</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py, tests/test_desktop.py</files>
  <action>Wire the toggle into the shell (RESEARCH Open Question 1 recommendation — a checkable View-menu action, lowest-friction, no new toolbar chrome). Add a `theme_manager` parameter to `MainWindow.__init__` (default None so existing test constructions and the bag-only path still work; when None, MainWindow constructs its own ThemeManager so a directly-constructed window is still themeable). In `_build_menu` add a `&View` menu alongside the existing `&File` menu with a checkable "Dark theme" (or a "Theme ▸ Dark/Light" pair) action whose `triggered`/`toggled` calls `self._theme_manager.toggle()`; reflect the manager's current `name` in the action's checked state. Expose the action and manager via read-only properties (`theme_action`, `theme_manager`) for tests. Add a qtbot test asserting: clicking/toggling the action flips `window.theme_manager.name` and the live `QApplication.styleSheet()` to the other theme (use the temp QSettings scope from Task 2). Do NOT touch panel internals or add any inline color here — only menu wiring (D-03; panel styling is 17-03).</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -k "theme or view_menu" -x</automated>
  </verify>
  <done>MainWindow has a View menu with a checkable theme toggle action wired to ThemeManager.toggle; triggering it flips theme_manager.name and the live app stylesheet; existing MainWindow constructions (no theme_manager arg) still build and pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QSettings file ↔ app | Persisted `ui/theme` value read on launch; an unexpected value must degrade safely, not crash |
| offline import graph ↔ Qt | A stray top-level PySide6 import in theme/tokens.py or theme/qss.py would breach the Qt-free invariant (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | QSettings `ui/theme` value | mitigate | `apply()` selects DARK unless the stored name equals "dark"/"light"; any other/missing value falls back to the dark default — no crash on a hand-edited conf file |
| T-17-02 | Information Disclosure | dev box ~/.config pollution by tests | mitigate | theme tests scope QSettings to a temp org/app or tmp_path file so the real user conf is never written (RESEARCH Wave 0 note) |
| T-17-03 | Elevation/Isolation | theme/tokens.py + theme/qss.py imports | mitigate | both modules import ONLY dataclasses/stdlib (no Qt); test_offline_guard.py::test_import_core_does_not_pull_pyside6 stays green |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | ZERO new packages this phase (RESEARCH Package Legitimacy Audit — N/A); nothing to slopcheck |
</threat_model>

<verification>
- `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -x` — full desktop suite green (re-run once if the benign offscreen exit Bus-error appears at teardown — confirm the clean pass line).
- `PYTHONPATH="" uv run pytest tests/test_offline_guard.py -x` — offline + Qt-free guard green (D-08).
- `PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src/rosbagger_desktop/theme/` — clean.
</verification>

<success_criteria>
- A `theme/` subpackage exists with Qt-free tokens.py + qss.py and a PySide6-only manager.py.
- build_qss(DARK)/build_qss(LIGHT) are unit-tested as pure strings carrying their baked token hex.
- ThemeManager.apply() themes the running QApplication; toggle() flips live AND persists via QSettings.
- A View-menu checkable action flips the theme live with no relaunch.
- Offline/Qt-free guard and the existing desktop suite stay green.
</success_criteria>

<output>
Create `.planning/phases/17-desktop-revamp-full-visual-design-system-qss-theme-design-to/17-01-SUMMARY.md` when done.
</output>
