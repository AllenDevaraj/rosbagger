---
phase: 17-desktop-revamp-full-visual-design-system-qss-theme-design-to
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-desktop/src/rosbagger_desktop/widgets/__init__.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/widgets/result_model.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/widgets/rows_model.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/widgets/status.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/panels/query_panel.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/panels/inspect_panel.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/panels/tf_panel.py
  - tests/test_desktop.py
autonomous: true
requirements: [D-02, D-08, D-09]
must_haves:
  truths:
    - "_ResultTableModel lives in widgets/result_model.py and query_panel imports it from there (query tests still green)"
    - "A generic RowsTableModel(headers, rows) renders list-of-tuple rows lazily via str() in widgets/rows_model.py"
    - "inspect renders its bag-info and schemas data through QTableView + RowsTableModel (not QTableWidgetItem)"
    - "tf renders its edges and gaps data through QTableView + RowsTableModel"
    - "inspect and tf run their core collect_* calls off the UI thread on BlockingWorker (no UI-thread block on the heavy path)"
    - "A shared accessible set_status helper preserves the platformName() != offscreen QAccessible guard (D-09)"
    - "All existing panel test accessors still resolve (bag_info_table/schemas_table/edges_table/gaps_table/status_label)"
  artifacts:
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/widgets/result_model.py"
      provides: "Lifted _ResultTableModel (pyarrow-backed, lazy) shared by query"
      contains: "class _ResultTableModel"
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/widgets/rows_model.py"
      provides: "Generic RowsTableModel(headers, rows) over list[tuple[str, ...]] for inspect/tf"
      contains: "class RowsTableModel"
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/widgets/status.py"
      provides: "Accessible set_status helper generalizing query's _set_status (keeps offscreen QAccessible guard)"
      contains: "offscreen"
  key_links:
    - from: "panels/query_panel.py"
      to: "widgets/result_model.py"
      via: "import _ResultTableModel from ..widgets"
      pattern: "from \\.\\.widgets"
    - from: "panels/inspect_panel.py"
      to: "BlockingWorker + RowsTableModel"
      via: "collect_bag_info/collect_table_schemas on a worker, rows fed to RowsTableModel"
      pattern: "BlockingWorker"
    - from: "panels/tf_panel.py"
      to: "BlockingWorker + RowsTableModel + shared set_status"
      via: "collect_tf_report on a worker; NoTransformsError routed through the accessible status helper"
      pattern: "BlockingWorker"
---

<objective>
Close the cross-panel engineering-parity gap (D-02) for the two offline panels that lag the query-panel bar. Lift the lazy `_ResultTableModel` out of `query_panel.py` into a shared `widgets/result_model.py`; add a generic `RowsTableModel(headers, rows)` for dataclass-row panels; generalize query's accessible `_set_status` into a shared `widgets/status.py` helper (keeping the offscreen-`QAccessible` guard, D-09); then migrate **inspect** and **tf** to `QTableView` + a model and move their synchronous `collect_*` core calls onto a `BlockingWorker`.

Purpose: Bring inspect + tf up to the query panel's robustness bar — off the UI thread, model/view, accessible status (D-02) — with ZERO new analysis/bag/SQL logic (D-09, thin-face). Record/replay are already threaded; their status/a11y + visual treatment is 17-03.
Output: Two shared models + a shared status helper in `widgets/`; inspect/tf converted to model/view + worker; updated panel tests asserting the new model API and off-thread refresh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-desktop-revamp-full-visual-design-system-qss-theme-design-to/17-CONTEXT.md
@.planning/phases/17-desktop-revamp-full-visual-design-system-qss-theme-design-to/17-RESEARCH.md

# The reference implementation — the bar inspect/tf must reach (model, worker, _set_status).
@packages/rosbagger-desktop/src/rosbagger_desktop/panels/query_panel.py
# The off-thread scaffolding inspect/tf adopt.
@packages/rosbagger-desktop/src/rosbagger_desktop/workers.py
# The two panels to convert.
@packages/rosbagger-desktop/src/rosbagger_desktop/panels/inspect_panel.py
@packages/rosbagger-desktop/src/rosbagger_desktop/panels/tf_panel.py
@tests/test_offline_guard.py

<interfaces>
<!-- The shared models/status the executor creates and the panels consume.
     _ResultTableModel exists verbatim in query_panel.py lines 89–152 — MOVE it, do not rewrite. -->

widgets/result_model.py (lifted from query_panel.py — pyarrow-backed, lazy, str()-per-cell):
```python
class _ResultTableModel(QAbstractTableModel):  # moved verbatim; preserves the temporal-safe str() rule
    def set_table(self, table: "pyarrow.Table | None") -> None: ...
```

widgets/rows_model.py (NEW — generic, for inspect/tf dataclass rows):
```python
class RowsTableModel(QAbstractTableModel):
    def __init__(self, headers: list[str], rows: list[tuple[str, ...]] | None = None) -> None: ...
    def set_rows(self, rows: list[tuple[str, ...]]) -> None: ...  # begin/endResetModel; str() every cell
```

widgets/status.py (NEW — generalized from query_panel._set_status, lines 302–325):
```python
def set_status(label: QLabel, text: str, *, is_error: bool = False) -> None:
    # sets text + accessibleDescription; toggles objectName "status_error" (color comes from theme QSS, NOT inline);
    # on error AND QGuiApplication.platformName() != "offscreen": post QAccessible.Alert (best-effort, guarded)
```

workers.py (existing — unchanged): BlockingWorker(work, teaching_errors=(), label=...), run_on_thread(owner, worker, on_result, on_failed, on_finished), stop_thread(thread).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift shared models + accessible status helper into widgets/</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/widgets/result_model.py, packages/rosbagger-desktop/src/rosbagger_desktop/widgets/rows_model.py, packages/rosbagger-desktop/src/rosbagger_desktop/widgets/status.py, packages/rosbagger-desktop/src/rosbagger_desktop/widgets/__init__.py, packages/rosbagger-desktop/src/rosbagger_desktop/panels/query_panel.py, tests/test_desktop.py</files>
  <behavior>
    - RowsTableModel(["a","b"], [("1","2")]).rowCount()==1, columnCount()==2, data(index(0,0))=="1", headerData(0,Horizontal)=="a".
    - RowsTableModel.set_rows(...) swaps rows inside begin/endResetModel so a bound QTableView refreshes.
    - query_panel imports _ResultTableModel from ..widgets (not a local class) and the existing query tests (-k query) stay green.
    - set_status sets label text + accessibleDescription; under offscreen the error path is a no-op announce (no segfault) but still sets the status_error objectName.
  </behavior>
  <action>Create `widgets/result_model.py` by MOVING `_ResultTableModel` verbatim out of query_panel.py (lines 89–152) — preserve the lazy `str()`-per-cell temporal-safe rule and `set_table`/begin-endResetModel exactly (RESEARCH anti-pattern: do NOT convert ns→datetime). Create `widgets/rows_model.py` with a generic `RowsTableModel(QAbstractTableModel)` over `list[tuple[str, ...]]` per the interface block — lazy `str()` per cell, `headers` for `headerData`, `set_rows` wrapped in begin/endResetModel. Create `widgets/status.py` with `set_status(label, text, *, is_error=False)` generalizing query_panel's `_set_status` (lines 302–325): set text + accessibleDescription, toggle the `status_error` objectName (NOT an inline stylesheet literal — the COLOR comes from theme QSS owned by 17-01's qss.py `#status_error` selector, D-03), and KEEP the `QGuiApplication.platformName() != "offscreen"` guard around the `QAccessible.updateAccessibility(QAccessibleEvent(label, Alert))` announce inside `contextlib.suppress(Exception)` (D-09 / RESEARCH Pitfall 1 / commit 7e99a5f — posting it under offscreen segfaults). Re-export `_ResultTableModel, RowsTableModel, set_status` from `widgets/__init__.py` (keep widgets/ offline+Qt-clean: PySide6 + stdlib only). Update query_panel.py to import `_ResultTableModel` from `..widgets` and have its `_set_status` delegate to the shared `set_status` (keeping its public `status_label`/`results_table` accessors and behavior identical). Add `-k rows_model` + `-k set_status` unit/qtbot tests to tests/test_desktop.py.</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -k "rows_model or set_status or query" -x</automated>
  </verify>
  <done>widgets/ exports _ResultTableModel + RowsTableModel + set_status; query_panel imports the lifted model and uses the shared status helper; query tests stay green; RowsTableModel + set_status have passing tests; the offscreen QAccessible guard is preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inspect panel → QTableView + RowsTableModel + off-thread worker</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/panels/inspect_panel.py, tests/test_desktop.py</files>
  <behavior>
    - After a bag opens and the worker finishes, inspect.bag_info_table.model().rowCount() > 0 (real collect_bag_info rows reach the view).
    - The schemas view model has one row per (table, column) from collect_table_schemas.
    - collect_bag_info/collect_table_schemas run on a BlockingWorker, not synchronously on the UI thread; a test waits via qtbot.waitUntil for the model to populate.
    - With no reader open, both views' models are empty and the header shows the empty-state line.
  </behavior>
  <action>Convert inspect_panel.py to the query-panel bar (D-02). Replace the two `QTableWidget`/`QTableWidgetItem` tables with `QTableView`s bound to `RowsTableModel` instances (bag-info headers `["topic","msgtype","count","hz"]`; schemas headers `["table","column","type","lazy"]`). Move the `collect_bag_info` + `collect_table_schemas` calls (currently synchronous in `refresh_view`, lines 104–108) ONTO a `BlockingWorker` via `run_on_thread` (mirror query_panel `_run_query`/`_on_query_result`/`_on_query_finished`/`closeEvent`): the worker's `work()` returns the `(info, schemas)` pair, an `on_result` UI-thread slot formats rows (KEEP the existing `_human_size`/`_EM_DASH`/`_MIXED` presentation helpers — presentation only, no new analysis, D-09) and calls `set_rows` on both models, and `closeEvent` calls `stop_thread`. Add a re-entry guard (refuse a refresh while a worker runs, mirror query). Keep the lazy `rosbagger_core.inspect` import INSIDE the worker `work()` body (D-08 offline invariant — top level stays PySide6-only). PRESERVE the `bag_info_table` and `schemas_table` public properties (tests use them) — they now return `QTableView`; route status through the shared `set_status` helper on the header label. Note `showEvent` currently calls `refresh_view` synchronously — keep that trigger but make `refresh_view` dispatch to the worker. UPDATE the existing `test_inspect_panel_shows_real_topics` to assert `.model().rowCount() > 0` and to `qtbot.waitUntil` for worker completion (RESEARCH Wave 0: migrate the QTableWidget rowCount assertion to the model API).</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -k "inspect" -x</automated>
  </verify>
  <done>inspect renders both tables via QTableView + RowsTableModel; collect_* runs off the UI thread on BlockingWorker; the empty-state path clears both models; bag_info_table/schemas_table accessors still resolve; the inspect test asserts model rowCount after waitUntil and passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TF panel → QTableView + RowsTableModel + off-thread worker</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/panels/tf_panel.py, tests/test_desktop.py</files>
  <behavior>
    - A /tf fixture bag yields edges_table.model().rowCount() > 0 after the worker finishes.
    - A no-/tf bag raises NoTransformsError → routed through the shared accessible set_status as the teaching text; both view models empty; no crash.
    - collect_tf_report runs on a BlockingWorker (off the UI thread).
    - The empty-gaps case still surfaces the "no gaps detected" note row in the gaps model.
  </behavior>
  <action>Convert tf_panel.py to the query-panel bar (D-02), mirroring Task 2. Replace the two `QTableWidget`s with `QTableView` + `RowsTableModel` (edges headers `["parent","child","kind","count","rate(Hz)","max gap","gaps"]`; gaps headers `["parent","child","gap","at (bag t)","at (abs ns)"]`). Move `collect_tf_report` (synchronous in `refresh_view`, line 120) ONTO a `BlockingWorker` with `teaching_errors=(NoTransformsError,)` so the teaching message arrives on the `failed` slot as `str(exc)` and is rendered VERBATIM through the shared `set_status(self._status, msg, is_error=True)` helper (THIN-FACE — core owns the message, D-09). KEEP the `_human_dur`/`_EM_DASH` presentation formatting and the per-edge rate/span computation EXACTLY (presentation only, no new analysis) — just move row-building into the `on_result` UI-thread slot feeding `set_rows`. Preserve the empty-gaps "no gaps detected" note-row behavior in the gaps model. Keep the lazy `rosbagger_core.tf`/`rosbagger_core.errors` imports INSIDE the worker `work()` body (D-08). Add the re-entry guard + `stop_thread` in `closeEvent`. PRESERVE the `edges_table`/`gaps_table`/`status_label` public properties (now QTableView for the two tables). UPDATE the existing `test_tf_panel_renders_or_teaches` to assert `.model().rowCount()` and `qtbot.waitUntil` for both the populated and teaching paths.</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -k "tf" -x</automated>
  </verify>
  <done>tf renders edges/gaps via QTableView + RowsTableModel; collect_tf_report runs off the UI thread; NoTransformsError is routed through the accessible shared status helper without crashing; the no-gaps note row persists; edges_table/gaps_table/status_label accessors still resolve; the tf test asserts model rowCount after waitUntil and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker thread ↔ UI thread | core collect_* results cross from a BlockingWorker to UI-thread slots via signals; widgets are touched only on the UI thread |
| offscreen Qt ↔ QAccessible | posting an Alert under offscreen segfaults (uncatchable) — the shared status helper must keep the platform guard |
| offline import graph ↔ Qt | widgets/ models must stay PySide6+stdlib only; core APIs imported lazily inside worker bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Denial of Service | inspect/tf refresh on a large bag | mitigate | collect_* runs on BlockingWorker so the UI event loop never blocks (D-02 parity bar) |
| T-17-05 | Tampering/Crash | QAccessible Alert under offscreen | mitigate | shared set_status keeps `platformName() != "offscreen"` guard + contextlib.suppress (D-09, commit 7e99a5f) |
| T-17-06 | Repudiation/Teardown | live worker thread destroyed mid-run | mitigate | closeEvent calls stop_thread; run_on_thread keeps refs (workers.py Pitfall 2 discipline) |
| T-17-07 | Elevation/Isolation | core import reachable at module top | mitigate | rosbagger_core.inspect/tf imported INSIDE worker work() bodies; widgets/ stays Qt-only — offline guard green |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | ZERO new packages (RESEARCH Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -x` — full desktop suite green (re-run once if the benign offscreen exit Bus-error appears).
- `PYTHONPATH="" uv run pytest tests/test_offline_guard.py -x` — offline + Qt-free guard green (D-08); widgets/ added no Qt to the core graph.
- `PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src/rosbagger_desktop/` — clean.
- Confirm every preserved accessor resolves: `grep -nE "def (bag_info_table|schemas_table|edges_table|gaps_table|status_label)" packages/rosbagger-desktop/src/rosbagger_desktop/panels/inspect_panel.py packages/rosbagger-desktop/src/rosbagger_desktop/panels/tf_panel.py`.
</verification>

<success_criteria>
- _ResultTableModel lifted to widgets/result_model.py; query still green.
- Generic RowsTableModel + shared accessible set_status exist in widgets/.
- inspect + tf render via QTableView + RowsTableModel and run collect_* off the UI thread on BlockingWorker.
- The offscreen QAccessible guard is preserved in the shared helper.
- Every existing public panel accessor still resolves; offline/Qt-free guard stays green.
</success_criteria>

<output>
Create `.planning/phases/17-desktop-revamp-full-visual-design-system-qss-theme-design-to/17-02-SUMMARY.md` when done.
</output>
