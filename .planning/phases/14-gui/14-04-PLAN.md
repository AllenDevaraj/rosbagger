---
phase: 14-gui
plan: 04
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - packages/rosbagger-gui/src/rosbagger_gui/panels/query.py
autonomous: true
requirements: [GUI-01]

must_haves:
  truths:
    - "The query panel runs the user's SQL via the real query(sql, reader) backend and shows the result rows in a DataTable (D-06)."
    - "A schema/topic Tree browser (from collect_table_schemas) lets the user click a column to insert its quoted name into the SQL input (D-06)."
    - "Query history + re-run is available (D-06)."
    - "CSV / Parquet export buttons write the last result via the real write_table path (D-06)."
    - "The panel calls only the existing query()/collect_table_schemas/write_table APIs — no SQL/format logic in the GUI (thin-face rule)."
  artifacts:
    - path: "packages/rosbagger-gui/src/rosbagger_gui/panels/query.py"
      provides: "QueryPanel(Widget) → SQL Input + results DataTable + schema Tree + history + CSV/Parquet export"
      contains: "query"
      min_lines: 40
  key_links:
    - from: "packages/rosbagger-gui/src/rosbagger_gui/panels/query.py"
      to: "rosbagger_core.backend.query.query"
      via: "Run handler passes the SQL string + shared reader to query()"
      pattern: "import query"
    - from: "packages/rosbagger-gui/src/rosbagger_gui/panels/query.py"
      to: "rosbagger_core.output.export.write_table"
      via: "export buttons write the last result table by chosen path/extension"
      pattern: "write_table"
    - from: "packages/rosbagger-gui/src/rosbagger_gui/panels/query.py"
      to: "rosbagger_core.inspect.collect_table_schemas"
      via: "schema/topic Tree browser source (click column → insert into SQL)"
      pattern: "collect_table_schemas"
---

<objective>
Fill the query panel stub (D-06): a SQL `Input`/`TextArea` + a results `DataTable` over the real `query(sql, reader)` backend, PLUS all of — query history + re-run, a schema/topic `Tree` browser (sourced from `collect_table_schemas`; click a column → insert its quoted name into the SQL box), and CSV/Parquet export `Button`s over the real `write_table` path. Zero SQL/format/selection logic in the panel — it collects a string + a path and calls the core API verbatim.

Purpose: The richest offline panel and the second half of the SC3 proof (Plan 14-07 drives this panel against a fixture bag and asserts real query() rows reach the DataTable).
Output: A populated QueryPanel with results table, schema tree, history, and export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-gui/14-CONTEXT.md
@.planning/phases/14-gui/14-RESEARCH.md

<interfaces>
<!-- rosbagger_core.backend.query (read from source) -->
query(sql: str, reader: BagReader, *, alias: bool = True, backend=None) -> pyarrow.Table
  raises UnknownTableError / UnknownColumnError / UnresolvedTypeError (all ValueError subclasses, teaching messages)
NOTE: import LAZILY inside the method: from rosbagger_core.backend.query import query

<!-- rosbagger_core.output.export (read from source) -->
write_table(table: pyarrow.Table, path: str) -> None   # format chosen by file extension (.csv / .parquet); LIST/STRUCT-safe DuckDB COPY; the T-06-01 quote-escape lives here
NOTE: import LAZILY: from rosbagger_core.output.export import write_table

<!-- schema browser source -->
collect_table_schemas(reader) -> list[TableSchema]  # .table_name + .columns[*].name — the Tree nodes; clicking a column inserts the quoted column name into the SQL Input

<!-- DataTable fill (14-RESEARCH Pattern 4) -->
dt.clear(columns=True); dt.add_columns(*table.column_names); for row in table.to_pylist(): dt.add_row(*(row[c] for c in table.column_names))
NOTE (14-RESEARCH Pitfall: temporal columns): query() may return t/stamp as timestamp[ns]; render via str() of to_pylist() values is fine for display (bounded by the user's SQL); do NOT to_pylist() an unbounded raw blob stream — SELECT * over a heavy-blob topic is the user's explicit choice and query()'s blob gating (QURY-07) already bounds it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QueryPanel — SQL input + query() results DataTable + history/re-run</name>
  <files>packages/rosbagger-gui/src/rosbagger_gui/panels/query.py</files>
  <read_first>
    - packages/rosbagger-gui/src/rosbagger_gui/panels/query.py (the stub being filled — created in 14-02)
    - packages/rosbagger-gui/src/rosbagger_gui/app.py (the shared-reader attribute + active-panel refresh callback)
    - packages/rosbagger-core/src/rosbagger_core/backend/query.py (the query() signature + the teaching error types it raises — the ONLY query API this panel calls; see <interfaces>)
    - 14-RESEARCH.md Pattern 4 (pyarrow.Table → DataTable) + Code Example 2 (the SC3 query test the panel must satisfy: an Input id `sql-input`, a DataTable id `results`, Enter runs the query) + Pitfall: temporal columns
  </read_first>
  <action>
    In `QueryPanel(Widget)`, compose a SQL `Input` (id `sql-input`) and a results `DataTable` (id `results`), plus a query-history widget (a `ListView`/`OptionList` id `history` of previously-run SQL strings) and a Run affordance (Enter on the input, or a `Button`). On Run: read the SQL string from `sql-input` and the App's shared open reader, lazily import `from rosbagger_core.backend.query import query` INSIDE the handler, call `query(sql, reader)`, and fill the `results` DataTable via the Pattern 4 mapping (`add_columns(*table.column_names)` + a row per `table.to_pylist()` entry, `str()`-rendering temporal/list cell values for display). Append each successfully-run SQL to the `history` list; selecting a history entry repopulates `sql-input` (re-run by pressing Run again — D-06). Catch the teaching errors (`UnknownTableError`/`UnknownColumnError`/`UnresolvedTypeError` from `rosbagger_core.errors`, all ValueError subclasses) and render `str(e)` into a `Static` status line (id `query-status`) instead of crashing — the GUI presents the message; the message CONTENT is built in core (thin-face). Keep the panel's last result `pyarrow.Table` on an attribute for the export task. Empty-state `Static("Open a bag to query")` when no bag. The panel imports ONLY textual + rosbagger_core.backend.query/errors (lazy) for this task.
  </action>
  <verify>
    <automated>grep -n "from rosbagger_core.backend.query import query\|sql-input\|results" packages/rosbagger-gui/src/rosbagger_gui/panels/query.py</automated>
    <automated>PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.query"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import query" packages/rosbagger-gui/src/rosbagger_gui/panels/query.py` >= 1 and the import is INSIDE a handler body (module-top `grep -vn '^#' ...query.py | grep -c '^from rosbagger_core'` is 0).
    - The Input id is `sql-input` and the results DataTable id is `results` (exact ids — the SC3 test in 14-07 queries them; see Code Example 2).
    - A `history` widget is present and selecting an entry repopulates `sql-input` (D-06 history + re-run).
    - The teaching errors are caught and rendered to a status `Static`, not raised (no traceback on a bad query).
    - `PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.query"` exits 0.
    - (Behavior — proven in 14-07 SC3) running `SELECT ...` against a fixture bag fills the `results` DataTable with row_count >= 1 carrying real query() output.
  </acceptance_criteria>
  <done>QueryPanel runs user SQL through the real query() backend, renders results in the `results` DataTable, keeps a re-runnable history, and teaches on bad queries without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Schema/topic Tree browser (click-to-insert) + CSV/Parquet export buttons</name>
  <files>packages/rosbagger-gui/src/rosbagger_gui/panels/query.py</files>
  <read_first>
    - packages/rosbagger-gui/src/rosbagger_gui/panels/query.py (the panel from Task 1 — being extended)
    - packages/rosbagger-core/src/rosbagger_core/inspect.py (collect_table_schemas → TableSchema.table_name + columns[*].name — the Tree source; see <interfaces>)
    - packages/rosbagger-core/src/rosbagger_core/output/export.py (write_table — format chosen by extension; the ONLY export API; see <interfaces>)
    - 14-RESEARCH.md Don't-Hand-Roll (CSV/Parquet export = write_table; the one SQL-literal escape lives there) + Tree widget reference
  </read_first>
  <action>
    Extend `QueryPanel` with a schema/topic `Tree` (id `schema-tree`): on bag-load, lazily `from rosbagger_core.inspect import collect_table_schemas`, build one Tree branch per `TableSchema.table_name` with a leaf per `columns[*].name`. On a column-leaf select, insert that column name (quoted/dotted as it appears in the schema — the panel inserts the string verbatim from `col.name`, it does NOT construct new SQL) into the `sql-input` at the cursor (D-06 click-to-insert). Add two export `Button`s (`export-csv`, `export-parquet`): each writes the panel's last-result `pyarrow.Table` via a lazily-imported `from rosbagger_core.output.export import write_table`, passing a path with the matching extension (the panel collects the destination path — a default like `query_result.csv`/`.parquet` in CWD, or a path Input the user supplies — and calls `write_table(last_result, path)` verbatim; format is chosen by `write_table` from the extension, NOT by the panel). Disable the export buttons (or render a status hint) when there is no last result. Catch and surface any export error to the `query-status` Static. The panel still imports ONLY textual + rosbagger_core.inspect/output.export (lazy) — no SQL/format logic added.
  </action>
  <verify>
    <automated>grep -n "collect_table_schemas\|write_table\|schema-tree\|export-csv\|export-parquet" packages/rosbagger-gui/src/rosbagger_gui/panels/query.py</automated>
    <automated>PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.query"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "collect_table_schemas" packages/rosbagger-gui/src/rosbagger_gui/panels/query.py` >= 1 (schema Tree source) and a `Tree` with id `schema-tree` is composed.
    - `grep -c "write_table" packages/rosbagger-gui/src/rosbagger_gui/panels/query.py` >= 1 (export goes through the real writer); two export buttons (`export-csv`, `export-parquet`) exist.
    - The panel builds NO SQL string and NO COPY/format string itself: `grep -c "COPY\|FORMAT CSV\|FORMAT PARQUET" packages/rosbagger-gui/src/rosbagger_gui/panels/query.py` is 0 (format is write_table's job; column insert is a verbatim string from col.name).
    - All `rosbagger_core` imports are inside method bodies (module-top scan stays 0).
    - `PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.query"` exits 0.
  </acceptance_criteria>
  <done>QueryPanel has a schema/topic Tree whose column leaves insert into the SQL box, and CSV/Parquet export buttons that write the last result via the real write_table path — no SQL/format logic in the GUI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL input → query() | The query panel runs ARBITRARY user SQL BY DESIGN over the local DuckDB backend (that is the feature). It reads local bag files only — no network, no remote DB. The user is the trusted local operator (Phase 5 disposition: accept). |
| export path → write_table | Export writes CSV/Parquet to a local path the user chooses; the path quote-escape (T-06-01) lives in write_table, reused verbatim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Injection | User SQL forwarded to query() | accept | The SQL IS the intended interface (Phase 5 T-05-04). No new interpolation: the panel passes the string verbatim; the schema-tree click inserts a column name verbatim from col.name (no SQL construction). |
| T-14-04-02 | Tampering | Export path with embedded quote / traversal | mitigate/accept | Path goes through write_table's existing '→'' escape (T-06-01); traversal is the local user's own filesystem (accept, as in Phase 6). |
| T-14-04-03 | Denial of Service | SELECT * materializing a heavy-blob column into a DataTable | mitigate | query()'s heavy-blob gating (QURY-07) already bounds results; the panel maps query()'s already-bounded result, never an unbounded raw stream (14-RESEARCH Pattern 4 note). |
</threat_model>

<verification>
- `PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.query"` exits 0.
- `PYTHONPATH="" uv run ruff check packages/rosbagger-gui/src` clean.
- Behavior proven by Plan 14-07's SC3 query test (real query() rows reach the `results` DataTable).
</verification>

<success_criteria>
QueryPanel is a thin face over query()/collect_table_schemas/write_table delivering SQL input + results table + schema-tree click-to-insert + history/re-run + CSV/Parquet export, with no SQL/format logic in the GUI.
</success_criteria>

<output>
Create `.planning/phases/14-gui/14-04-SUMMARY.md` when done.
</output>
