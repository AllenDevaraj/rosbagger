---
phase: 14-gui
plan: 07
type: execute
wave: 4
depends_on: ["14-02", "14-03", "14-04", "14-05", "14-06"]
files_modified:
  - tests/test_gui.py
  - tests/test_gui_live.py
  - tests/test_offline_guard.py
autonomous: true
requirements: [GUI-01]

must_haves:
  truths:
    - "SC1: an App.run_test() test asserts the launched app exposes all five panels (inspect/query/tf/record/replay) over module APIs."
    - "SC2: a test asserts the live panels (record/replay) are disabled and the offline panels (inspect/query/tf) are enabled when ROS is not available; offline panels work without ROS."
    - "SC3: App.run_test()/Pilot tests drive the inspect AND query panels against a make_fixtures bag and assert REAL rosbagger_core output reaches the widgets (inspect topic rows; query result rows) — ROS-free in CI."
    - "The offline-import invariant is extended: import rosbagger_gui (and its app/panels) pulls no rclpy/rosbag2_py (a fresh-interpreter subprocess test, mirroring test_import_replay_does_not_pull_ros)."
    - "A live-marked GUI test exercises the record/replay panels on a ROS-sourced lane (importorskip rclpy) and is skipped in offline CI."
    - "The full offline suite + ruff + offline guard are green (phase gate)."
  artifacts:
    - path: "tests/test_gui.py"
      provides: "SC1/SC2/SC3 headless App.run_test()/Pilot tests"
      contains: "run_test"
    - path: "tests/test_gui_live.py"
      provides: "live-marked record/replay GUI integration (ROS-sourced lane)"
      contains: "importorskip"
    - path: "tests/test_offline_guard.py"
      provides: "test_import_gui_does_not_pull_ros (extends the existing guard)"
      contains: "test_import_gui_does_not_pull_ros"
  key_links:
    - from: "tests/test_gui.py"
      to: "rosbagger_gui.app.RosbaggerApp"
      via: "async App.run_test() driving the panels against a fixture bag"
      pattern: "RosbaggerApp"
    - from: "tests/test_offline_guard.py"
      to: "rosbagger_gui"
      via: "fresh-interpreter subprocess scan asserting no rclpy/rosbag2_py leak"
      pattern: "test_import_gui_does_not_pull_ros"
---

<objective>
Prove the three success criteria and lock the offline invariant: SC1 (five panels exposed), SC2 (capability-gating), SC3 (inspect + query drive real `rosbagger_core` against a `make_fixtures` bag via `App.run_test()`/`Pilot`, ROS-free in CI). Extend `tests/test_offline_guard.py` with `test_import_gui_does_not_pull_ros`. Add a `live`-marked GUI integration test for the record/replay panels (ROS-sourced lane, `importorskip("rclpy")`). Run the phase gate (full offline suite + ruff + offline guard).

Purpose: The proof layer — this is where GUI-01's three success criteria become passing automated tests, and where the no-ROS promise is regression-locked for the new package.
Output: tests/test_gui.py (SC1/SC2/SC3), tests/test_gui_live.py (live lane), an extended tests/test_offline_guard.py, and a green phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-gui/14-CONTEXT.md
@.planning/phases/14-gui/14-RESEARCH.md

<interfaces>
<!-- The App under test (created in 14-02; panels filled 14-03..14-06) -->
RosbaggerApp(bag_path=...)  # async with app.run_test() as pilot: ...
sidebar nav ids: nav-inspect / nav-query / nav-tf / nav-record / nav-replay
panel ids (ContentSwitcher): inspect / query / tf / record / replay
query panel: Input id `sql-input`, results DataTable id `results`
inspect panel: bag-info DataTable id `bag-info`

<!-- Pilot driving (14-RESEARCH Pattern 2 / Code Example 2) -->
async with app.run_test() as pilot:
    await pilot.pause()                 # flush messages — ALWAYS pause before asserting (Pitfall 4)
    await pilot.click("#nav-query"); await pilot.pause()
    app.query_one("#sql-input").value = "SELECT topic, t_ns FROM imu LIMIT 1"
    await pilot.press("enter"); await pilot.pause()
    table = app.query_one("#results", DataTable); assert table.row_count == 1
NOTE (14-RESEARCH Code Example 2 / Tertiary source): the exact 8.2.7 DataTable column-introspection accessor (table.columns vs table.ordered_columns) shifted across versions — VERIFY against the installed package when writing the assertion; do not trust memory. row_count is stable.

<!-- Fixture bags (tools/make_fixtures.py, read from source) -->
write_ros2_sqlite_bag(dest_dir) -> Path   # ROS 2 sqlite3 bag dir named "ros2_sqlite"; topics include /imu, /cmd_vel (-> tables imu, cmd_vel)
NOTE: the ROS 2 sqlite3 reader needs default_typestore=get_typestore(Stores.ROS2_HUMBLE) — the App opens it that way (14-02). Use the SAME fixture writer the reader/query suites use; assert against its known topics.

<!-- async config -->
pytest-asyncio with asyncio_mode = "auto" (added to root pyproject in 14-02) — tests are plain `async def`.

<!-- the existing offline-guard helper to mirror -->
_ros_modules_after_import(*targets) -> list[str]  # fresh interpreter, env={"PYTHONPATH":""}; used by test_import_replay_does_not_pull_ros
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SC1/SC2/SC3 headless tests + the offline-guard extension</name>
  <files>tests/test_gui.py, tests/test_offline_guard.py</files>
  <read_first>
    - tests/test_gui.py (will create)
    - tests/test_offline_guard.py (the file being extended — the _ros_modules_after_import helper + test_import_replay_does_not_pull_ros is the TEMPLATE to mirror for the GUI)
    - packages/rosbagger-gui/src/rosbagger_gui/app.py (RosbaggerApp ctor + the nav/panel ids + ros_available gating — what the tests drive; created in 14-02)
    - packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py + panels/query.py (the bag-info / results DataTable ids the SC3 assertions query; created 14-03/14-04)
    - tools/make_fixtures.py (write_ros2_sqlite_bag — the SC3 fixture; its known topics /imu, /cmd_vel)
    - 14-RESEARCH.md Pattern 2 + Code Example 2 (the run_test()/Pilot SC3 shape) + Pitfall 3 (async plugin) + Pitfall 4 (pause before assert) + the DataTable-accessor version caveat
  </read_first>
  <behavior>
    - SC1: launching RosbaggerApp(bag_path=fixture) and entering run_test() exposes all five panels — the five nav ids and the five ContentSwitcher panel ids are all queryable.
    - SC2 (offline): when ros_available is False, the record + replay sidebar items / panels are `disabled`; inspect/query/tf are NOT disabled. (Force the no-ROS condition deterministically — e.g. monkeypatch rosbagger_gui._detect_ros to return False, or construct the app under the no_ros blocker — so the test is meaningful on this ROS-equipped dev box.)
    - SC3 inspect: after selecting the inspect panel against the fixture bag, the `bag-info` DataTable row_count >= 1 (real collect_bag_info topics rendered).
    - SC3 query: after typing a SELECT over a known fixture table (e.g. `SELECT topic, t_ns FROM imu LIMIT 1`) into `sql-input` and running, the `results` DataTable row_count == 1 (real query() output reached the widget).
    - offline guard: a fresh interpreter importing rosbagger_gui (and rosbagger_gui.app) leaks no rclpy/rosbag2_py.
  </behavior>
  <action>
    Create `tests/test_gui.py` as a self-contained async test module (mirror the reader/tf suites' self-contained harness shape — own fixture-writing into tmp_path via tools.make_fixtures). Write `async def` tests (asyncio_mode=auto, set in 14-02) using `async with RosbaggerApp(bag_path=bag).run_test() as pilot`, always `await pilot.pause()` after each interaction before asserting (Pitfall 4): (a) `test_app_has_five_panels` (SC1) queries the five nav ids + five panel ids; (b) `test_live_panels_disabled_without_ros` (SC2) forces no-ROS (monkeypatch `rosbagger_gui._detect_ros` → False) and asserts record/replay disabled, inspect/query/tf enabled; (c) `test_inspect_panel_shows_real_topics` (SC3) selects inspect and asserts `#bag-info` DataTable row_count >= 1; (d) `test_query_panel_runs_real_core` (SC3) selects query, sets `#sql-input`, presses enter, asserts `#results` DataTable row_count == 1. VERIFY the 8.2.7 DataTable accessor when writing any column-name assertion (prefer row_count, which is stable). Extend `tests/test_offline_guard.py` with `test_import_gui_does_not_pull_ros` using the existing `_ros_modules_after_import` helper (assert `_ros_modules_after_import("rosbagger_gui", "rosbagger_gui.app") == []`) — mirror the docstring + mechanism of `test_import_replay_does_not_pull_ros`.
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_gui.py -x</automated>
    <automated>PYTHONPATH="" uv run pytest tests/test_offline_guard.py::test_import_gui_does_not_pull_ros -x</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH="" uv run pytest tests/test_gui.py -x` passes with at least the four named tests (SC1/SC2/SC3-inspect/SC3-query) collected and green — NOT skipped (asyncio_mode=auto is active; Pitfall 3).
    - `test_query_panel_runs_real_core` asserts the `#results` DataTable row_count == 1 against a real `query()` over the fixture bag (SC3 — real rosbagger_core output reached the widget).
    - `test_inspect_panel_shows_real_topics` asserts the `#bag-info` DataTable row_count >= 1 (SC3).
    - `test_live_panels_disabled_without_ros` asserts record/replay disabled AND inspect/query/tf enabled under forced no-ROS (SC2).
    - `PYTHONPATH="" uv run pytest tests/test_offline_guard.py::test_import_gui_does_not_pull_ros -x` passes (offline invariant extended to rosbagger_gui).
  </acceptance_criteria>
  <done>SC1/SC2/SC3 are passing headless tests against a fixture bag, ROS-free; the offline guard covers rosbagger_gui.</done>
</task>

<task type="auto">
  <name>Task 2: live-marked record/replay GUI integration test + phase gate</name>
  <files>tests/test_gui_live.py</files>
  <read_first>
    - tests/test_gui_live.py (will create)
    - tests/test_record_live.py and tests/test_replay_live.py (the Phase 12/13 LIVE-lane templates: importorskip("rclpy") + @pytest.mark.live + a publisher fixture / ROS-sourced recipe; mirror their gating so offline CI stays green)
    - packages/rosbagger-gui/src/rosbagger_gui/panels/record.py + panels/replay.py (the live panels under test; created 14-05/14-06)
    - .planning/phases/12-live-record/12-VERIFICATION.md + .planning/phases/13-live-replay/13-CONTEXT.md (the ROS-sourced live-lane recipe)
  </read_first>
  <action>
    Create `tests/test_gui_live.py` guarded by `pytest.importorskip("rclpy")` and `@pytest.mark.live` (the `live` marker is already registered in root pyproject). Drive the record/replay panels via `RosbaggerApp(...).run_test()` on the ROS-sourced lane: for record, start a publisher (mirror the Phase 12 live test's publisher fixture), open the record panel, assert the discovery worker populates the checklist with the published topic (await the worker / poll a reactive), run a bounded record, and assert the count/teaching path; for replay, replay a fixture bag through the panel's transport worker and assert a subscriber receives (mirror Phase 13 SC1). Keep these SKIPPED in offline CI (importorskip). Then run the phase gate: the full offline suite, ruff check + format --check, and the offline guard — all green, coverage gate (`--cov=rosbagger_core --cov=bagq`) unchanged and still ≥80% (rosbagger_gui stays OUT of it).
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_gui_live.py --co -q</automated>
    <automated>PYTHONPATH="" uv run pytest</automated>
    <automated>PYTHONPATH="" uv run ruff check . && PYTHONPATH="" uv run ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_gui_live.py` is collectable and its tests are SKIPPED (not errored) in the offline environment via `importorskip("rclpy")` + `@pytest.mark.live` — `PYTHONPATH="" uv run pytest tests/test_gui_live.py --co -q` lists them without import errors.
    - `PYTHONPATH="" uv run pytest` (full offline suite) passes with the coverage gate ≥80% on `rosbagger_core + bagq` (unchanged gate; `rosbagger_gui` excluded).
    - `PYTHONPATH="" uv run ruff check .` and `ruff format --check .` are clean across the new GUI package + tests.
    - The Phase-13 replay tests are still green in the full run (no regression from the Plan 14-01 sink refactor).
  </acceptance_criteria>
  <done>A live-marked GUI integration test exercises record/replay on the ROS-sourced lane (skipped offline); the full offline suite + ruff + offline guard pass as the phase gate, coverage gate unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → App | Tests drive the App headless against fixture bags written into tmp_path; no external input, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07-01 | Tampering | An SC3 test that passes without executing (async not wired) | mitigate | asyncio_mode=auto (14-02) + await pilot.pause() before every assert (Pitfall 3/4); acceptance criteria require the tests be GREEN not skipped. |
| T-14-07-02 | Elevation | A future edit re-leaks ROS into the offline GUI import graph | mitigate | test_import_gui_does_not_pull_ros regression-locks it (fresh-interpreter subprocess scan, mirrors the record/replay guards). |
| T-14-07-SC | Tampering | npm/pip/cargo installs | mitigate | No NEW package installs in this plan (pytest-asyncio/textual/textual-dev were installed + slopcheck [OK] in 14-02). No [ASSUMED]/[SUS] package — no blocking checkpoint needed. |
</threat_model>

<verification>
- `PYTHONPATH="" uv run pytest` (full offline suite) green; coverage ≥80% on rosbagger_core+bagq.
- `PYTHONPATH="" uv run pytest tests/test_gui.py tests/test_offline_guard.py -x` green.
- `PYTHONPATH="" uv run ruff check . && ruff format --check .` clean.
- `tests/test_gui_live.py` skipped (offline) / runnable on the ROS-sourced lane.
</verification>

<success_criteria>
GUI-01's three success criteria are passing automated tests (SC1 five panels, SC2 capability-gating, SC3 inspect+query over real core against a fixture bag); the offline invariant covers rosbagger_gui; the live lane is gated; the full phase gate is green with the coverage gate unchanged.
</success_criteria>

<output>
Create `.planning/phases/14-gui/14-07-SUMMARY.md` when done.
</output>
