---
phase: 14-gui
plan: 06
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - packages/rosbagger-gui/src/rosbagger_gui/panels/replay.py
  - packages/rosbagger-gui/src/rosbagger_gui/widgets/__init__.py
  - packages/rosbagger-gui/src/rosbagger_gui/widgets/scrubber.py
autonomous: true
requirements: [GUI-01]

must_haves:
  truths:
    - "The replay panel exposes the six transport controls (play/pause/step/seek/rate/loop) wired to the pure Replayer driven over the SHARED build_publish_sink from Plan 14-01 (D-09/D-09a) — no second publish path."
    - "A custom scrubber Widget reflects Replayer position and seeks via Replayer.seek() on click/drag (D-09)."
    - "Jump-to-event markers from list_events(bag) overlay the scrubber and seek to the event time (D-09)."
    - "The Replayer.run() drive loop runs in a @work(thread=True) worker; transport methods are called from UI handlers between run segments — the event loop never blocks."
    - "rosbagger_replay / rclpy are lazy-imported inside method bodies only — import rosbagger_gui stays ROS-free."
  artifacts:
    - path: "packages/rosbagger-gui/src/rosbagger_gui/panels/replay.py"
      provides: "ReplayPanel(Widget) → pure Replayer + shared build_publish_sink in a thread worker, six controls, scrubber, event markers"
      contains: "Replayer"
      min_lines: 50
    - path: "packages/rosbagger-gui/src/rosbagger_gui/widgets/scrubber.py"
      provides: "Custom timeline Widget: reactive position + event-marker overlay; click → seek fraction"
      contains: "class"
  key_links:
    - from: "packages/rosbagger-gui/src/rosbagger_gui/panels/replay.py"
      to: "rosbagger_replay.Replayer + build_publish_sink"
      via: "panel drives the pure Replayer over the shared sink in a thread worker (D-09a)"
      pattern: "build_publish_sink"
    - from: "packages/rosbagger-gui/src/rosbagger_gui/panels/replay.py"
      to: "rosbagger_core.events.list_events"
      via: "jump-to-event markers sourced from the events sidecar (D-09)"
      pattern: "list_events"
    - from: "packages/rosbagger-gui/src/rosbagger_gui/widgets/scrubber.py"
      to: "Replayer.seek"
      via: "scrubber click/drag → panel calls replayer.seek(int(t_offset_ns))"
      pattern: "seek"
---

<objective>
Fill the live replay panel stub (D-09): full transport controls (play/pause/step/seek/rate/loop) wired to the PURE `Replayer` driven over the SHARED `build_publish_sink` from Plan 14-01 (D-09a — single production publish path), a custom scrubber `Widget` (no built-in slider) reflecting position and seeking via `Replayer.seek()`, and jump-to-event markers from `list_events(<bag>.events.parquet)`. The `Replayer.run()` loop runs in a `@work(thread=True)` worker; transport methods fire from UI handlers between run segments. `rosbagger_replay`/`rclpy` are lazy-imported inside method bodies only.

Purpose: The richest live panel; the realization of the jump-points Phase 13 deferred to this phase. It is the one place a panel is more than a one-liner — but it drives the module API (the injectable Replayer + the shared sink), never re-implementing pacing/seek/publish.
Output: A populated ReplayPanel + a custom scrubber widget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-gui/14-CONTEXT.md
@.planning/phases/14-gui/14-RESEARCH.md
@.planning/phases/14-gui/14-01-PLAN.md

<interfaces>
<!-- rosbagger_replay (read from source; build_publish_sink added by Plan 14-01) -->
load_items(bag_paths, *, topics=None, default_typestore=None) -> list[ReplayItem(t_ns:int, topic:str, msgtype:str, cdr:bytes)]
Replayer(items, sink, *, clock=time.monotonic, sleep=time.sleep, rate=1.0, loop=False, max_messages=None, duration=None)
  controls: play() / pause() / step() / set_rate(x>0) / seek(t_offset_ns) / .loop (attr)
  introspection: .state (State.PLAYING/PAUSED/STEPPING/DONE), .cursor, .rate
  run() -> None   # BLOCKING drive loop — run in a thread worker; returns when PAUSED (after step), DONE, or a bound trips
build_publish_sink(node) -> (sink, published)   # the shared rclpy publish closure + the {"n":0} count dict (Plan 14-01); lazy-imported via the rosbagger_replay front door
RosNotAvailableError / NoMessagesToReplayError   # teaching errors
NOTE (D-09a): the panel creates its OWN rclpy context+node (the Replayer/replay design is re-entrant-context-safe — Phase 13 WR-04), builds the sink via build_publish_sink(node), constructs the Replayer over load_items(...), and runs Replayer.run() in a thread worker. seek() is the ONLY position-setter; --start seconds → seek(int(s*1e9)). A loop wrap rewinds to index 0, NOT the last seek (WR-02). Seek-past-end lands cursor==len(items) (clean DONE — Pitfall 6).
Lazy-import rosbagger_replay + rclpy INSIDE the worker/handler bodies.

<!-- rosbagger_core.events (read from source) -->
list_events(bag) -> pyarrow.Table   # columns: t_start_ns, t_end_ns, label, note (empty fixed-schema table if no sidecar)
NOTE (14-RESEARCH Code Example 4): marker fraction = (row["t_start_ns"] - bag_start_ns) / max(1, bag_end_ns - bag_start_ns); label = row["label"]. import LAZILY.

<!-- Custom scrubber (14-RESEARCH: no built-in slider) -->
A Widget rendering a bar + a reactive position (0..1) + event-marker overlay; on click, compute the fraction and the panel maps it to seek(int(fraction * bag_span_ns)).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Custom scrubber Widget (reactive position + event-marker overlay + click→fraction)</name>
  <files>packages/rosbagger-gui/src/rosbagger_gui/widgets/__init__.py, packages/rosbagger-gui/src/rosbagger_gui/widgets/scrubber.py, packages/rosbagger-gui/src/rosbagger_gui/app.tcss</files>
  <read_first>
    - packages/rosbagger-gui/src/rosbagger_gui/widgets/scrubber.py (will create)
    - packages/rosbagger-gui/src/rosbagger_gui/app.tcss (created in 14-02 — add scrubber styling here or in a scoped block)
    - 14-RESEARCH.md Alternatives Considered (custom scrubber vs textual-slider — custom chosen, keeps deps to textual only) + Don't-Hand-Roll (scrubber row) + Code Example 4 (event_marks fractions) + Pitfall 6 (loop wraparound / seek-past-end / DONE)
  </read_first>
  <action>
    Create `rosbagger_gui/widgets/__init__.py` exporting the scrubber class, and `rosbagger_gui/widgets/scrubber.py` with a custom `Widget` (e.g. `Scrubber(Widget)`) that renders a horizontal bar with a reactive `position: float` (0..1) playhead and an overlaid set of event markers (a list of `(fraction, label)` tuples set by the panel). On a click, compute the click fraction along the bar's width and post a custom message (e.g. `Scrubber.Seeked(fraction)`) OR call a panel-provided callback — the WIDGET itself contains NO Replayer/seek logic; it only emits a fraction (the panel maps fraction → `seek(int(fraction * bag_span_ns))`). Markers reflect event positions; the playhead reflects `position`. Pure textual/stdlib — NO rosbagger import in this file. Add scrubber CSS (bar + playhead + marker styling).
  </action>
  <verify>
    <automated>grep -n "class\|position\|Seeked\|render" packages/rosbagger-gui/src/rosbagger_gui/widgets/scrubber.py</automated>
    <automated>PYTHONPATH="" uv run python -c "import rosbagger_gui.widgets.scrubber"</automated>
  </verify>
  <acceptance_criteria>
    - `rosbagger_gui/widgets/scrubber.py` defines a `Widget` subclass with a reactive `position` and a marker overlay; `grep -c "position" ...scrubber.py` >= 1.
    - The widget emits a click fraction (a custom Message or callback) and contains NO Replayer/seek call itself: `grep -c "Replayer\|\.seek(" ...scrubber.py` is 0 (transport mapping is the panel's job).
    - `grep -c "rosbagger" ...scrubber.py` is 0 (pure textual widget — no module API import).
    - `PYTHONPATH="" uv run python -c "import rosbagger_gui.widgets.scrubber"` exits 0.
  </acceptance_criteria>
  <done>A reusable custom scrubber Widget renders a playhead + event markers and emits a click fraction, with zero transport/module logic.</done>
</task>

<task type="auto">
  <name>Task 2: ReplayPanel — six controls over the pure Replayer + shared sink (thread worker) + scrubber + event markers</name>
  <files>packages/rosbagger-gui/src/rosbagger_gui/panels/replay.py</files>
  <read_first>
    - packages/rosbagger-gui/src/rosbagger_gui/panels/replay.py (the stub being filled — created in 14-02; mounts the disabled teaching hint when ROS absent, D-03)
    - packages/rosbagger-gui/src/rosbagger_gui/widgets/scrubber.py (the scrubber from Task 1 the panel embeds + wires)
    - packages/rosbagger-replay/src/rosbagger_replay/scheduler.py (the Replayer's six controls + state/cursor introspection + run() semantics + the loop/seek/DONE edge behavior — see <interfaces> and Pitfall 6)
    - packages/rosbagger-replay/src/rosbagger_replay/replay.py (build_publish_sink from Plan 14-01 + the created_ctx/rclpy.ok() context discipline the panel mirrors for its OWN context — WR-04)
    - packages/rosbagger-replay/src/rosbagger_replay/__init__.py (the lazy front door: _require_ros, load_items, Replayer, build_publish_sink re-export, RosNotAvailableError/NoMessagesToReplayError)
    - packages/rosbagger-core/src/rosbagger_core/events.py (list_events → the jump-to-event marker source; see <interfaces> + Code Example 4)
    - 14-RESEARCH.md Pattern 5 + Code Example 3 (@work(thread=True) + call_from_thread) + Code Example 4 (event_marks) + Pitfall 1 (event-loop block) + Pitfall 6 (loop/seek-past-end/DONE)
  </read_first>
  <action>
    In `ReplayPanel(Widget)`, compose: the `Scrubber` widget, Play/Pause/Step `Button`s, a rate control (`Input`/`Select` → `set_rate`), a loop `Switch`/`Checkbox` (→ `replayer.loop`), and a `Static` status (id `replay-status`). On panel show (ROS available), lazily build the transport: create the panel's OWN rclpy context+node (lazy `import rclpy` inside the worker, guarding `rclpy.ok()` and only initializing/shutting down the context the panel created — mirror replay.py WR-04), build `sink, published = build_publish_sink(node)` (the SHARED Plan-14-01 sink — D-09a, NO inlined publish mechanics), `items = load_items(bag, topics=..., default_typestore=...)`, and `replayer = Replayer(items, sink, rate=..., loop=...)`. Run `replayer.run()` in a `@work(exclusive=True, thread=True)` worker so the drive loop never blocks the event loop (Pitfall 1); Play calls `replayer.play()` then (re)starts the run worker, Pause calls `replayer.pause()`, Step calls `replayer.step()` then runs the worker once (one-then-pause), set_rate/loop forward to the Replayer methods/attr. A scrubber click fraction maps to `replayer.seek(int(fraction * bag_span_ns))` (bag_span from `items[0].t_ns`/`items[-1].t_ns`); treat `State.DONE` / `cursor == len(items)` as end-of-track (Pitfall 6), and remember a loop wrap rewinds to index 0, not the last seek (WR-02). Poll `replayer.state`/`replayer.cursor` to update the scrubber `position` via `call_from_thread`. Source jump-to-event markers: lazily `from rosbagger_core.events import list_events`, map rows to `(fraction, label)` via the Code Example 4 formula, set them on the scrubber; a marker click seeks to that event's time. Catch `RosNotAvailableError`/`NoMessagesToReplayError` and surface to `replay-status`. Every `rosbagger_replay`/`rclpy` import is INSIDE a worker/handler body.
  </action>
  <verify>
    <automated>grep -n "@work\|Replayer\|build_publish_sink\|list_events\|seek\|set_rate" packages/rosbagger-gui/src/rosbagger_gui/panels/replay.py</automated>
    <automated>PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.replay"</automated>
    <automated>PYTHONPATH="" uv run python -c "import sys; import rosbagger_gui.panels.replay; leaked=[m for m in sys.modules if m.split('.')[0] in {'rclpy','rosbag2_py'}]; assert leaked==[], leaked; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@work" ...replay.py` >= 1 with `thread=True` (Replayer.run() runs off the event loop — Pitfall 1).
    - `grep -c "build_publish_sink" ...replay.py` >= 1 (drives the SHARED Plan-14-01 sink — D-09a) AND `grep -c "create_publisher\|deserialize_message" ...replay.py` is 0 (no inlined/duplicated publish mechanics — single production publish path).
    - All six controls are wired: `grep -c "\.play(\|\.pause(\|\.step(\|set_rate\|\.seek(\|\.loop" ...replay.py` covers play/pause/step/set_rate/seek/loop (each present).
    - `grep -c "list_events" ...replay.py` >= 1 (jump-to-event markers sourced from the events sidecar — D-09) and the scrubber's markers are set from them.
    - `grep -c "call_from_thread" ...replay.py` >= 1 (thread-safe scrubber/position updates).
    - Every `rosbagger_replay`/`rclpy` import is inside a worker/method body: module-top scan `grep -vn '^#' ...replay.py | grep -c '^import rclpy\|^from rosbagger_replay\|^import rosbagger_replay\|^from rosbagger_core.events'` is 0.
    - The fresh-interpreter scan after `import rosbagger_gui.panels.replay` (PYTHONPATH="") leaks no rclpy/rosbag2_py.
    - `PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.replay"` exits 0.
  </acceptance_criteria>
  <done>ReplayPanel drives the pure Replayer over the shared build_publish_sink in a thread worker with all six transport controls, a position-reflecting scrubber that seeks, and event-sidecar jump markers — no duplicated publish path, offline-clean import graph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay panel → rclpy publish | The panel publishes the bag's CDR to live ROS topics via the shared sink — same surface as the shipped replay CLI; the panel owns its own rclpy context (WR-04 re-entrant-safe). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Denial of Service | Replayer.run() blocking the event loop | mitigate | run() runs in @work(thread=True); transport methods fire between run segments; scrubber updates via call_from_thread (Pitfall 1). |
| T-14-06-02 | Tampering | A second/divergent publish path in the GUI | mitigate | The panel imports the SHARED build_publish_sink (Plan 14-01); acceptance criteria grep-gate that no create_publisher/deserialize_message mechanics are inlined here (single production publish path — D-09a). |
| T-14-06-03 | Elevation | rclpy/rosbagger_replay leaking into the offline import graph | mitigate | Lazy imports inside worker/handler bodies; fresh-interpreter scan in acceptance criteria + the 14-07 offline guard. |
| T-14-06-04 | Denial of Service | Seek-past-end / loop-wrap edge crashing the panel | mitigate | Treat State.DONE / cursor==len(items) as end-of-track; a loop wrap rewinds to 0 not the last seek (WR-02 / Pitfall 6) — handled per the scheduler contract. |
</threat_model>

<verification>
- `PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.replay, rosbagger_gui.widgets.scrubber"` exits 0; fresh-interpreter scan leaks no ROS.
- `PYTHONPATH="" uv run ruff check packages/rosbagger-gui/src` clean.
- Live replay behavior is exercised by the `live`-marked GUI integration test in Plan 14-07 (ROS-sourced lane, importorskip("rclpy")) — skipped in offline CI.
</verification>

<success_criteria>
ReplayPanel delivers the six transport controls + scrubber + event-jump markers over the pure Replayer and the SHARED publish sink, all blocking work in a thread worker, with no duplicated publish path and an offline-clean import graph.
</success_criteria>

<output>
Create `.planning/phases/14-gui/14-06-SUMMARY.md` when done.
</output>
