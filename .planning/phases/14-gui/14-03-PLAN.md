---
phase: 14-gui
plan: 03
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py
  - packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py
autonomous: true
requirements: [GUI-01]

must_haves:
  truths:
    - "The inspect panel renders the real collect_bag_info / collect_table_schemas output (topics, types, counts, duration, approx Hz, size) for the shared loaded bag (D-05)."
    - "The tf panel renders the real collect_tf_report output (parent→child edge summary + per-edge gap timeline) for the shared loaded bag (D-07)."
    - "Both panels call EXACTLY their one rosbagger_core API and contain no analysis logic (thin-face rule)."
  artifacts:
    - path: "packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py"
      provides: "InspectPanel(Widget) → collect_bag_info + collect_table_schemas, rendered into DataTables"
      contains: "collect_bag_info"
    - path: "packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py"
      provides: "TfPanel(Widget) → collect_tf_report, rendered into edge + gap DataTables"
      contains: "collect_tf_report"
  key_links:
    - from: "packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py"
      to: "rosbagger_core.inspect.collect_bag_info"
      via: "panel reads the App's shared reader and calls the API"
      pattern: "collect_bag_info"
    - from: "packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py"
      to: "rosbagger_core.tf.collect_tf_report"
      via: "panel reads the App's shared reader and calls the API"
      pattern: "collect_tf_report"
---

<objective>
Fill the inspect (D-05) and tf (D-07) offline panel stubs: each is a thin renderer over exactly one `rosbagger_core` API, driven against the App's shared open reader. Inspect renders `collect_bag_info` (topics/types/counts/duration/Hz/size) + `collect_table_schemas` (per-topic table + columns). TF renders `collect_tf_report` (parent→child edge summary table + per-edge gap timeline). Zero analysis logic — the GUI is a face.

Purpose: Two of the three always-on offline panels; inspect is part of the SC3 proof (Plan 14-07).
Output: A populated InspectPanel and TfPanel rendering real core output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-gui/14-CONTEXT.md
@.planning/phases/14-gui/14-RESEARCH.md

<interfaces>
<!-- rosbagger_core.inspect (read from source) -->
collect_bag_info(reader) -> BagInfo:
  .topics: list[TopicInfo(topic:str, msgtype:str|None, count:int, hz:float|None)]  (sorted by topic)
  .message_count:int, .start_time_ns:int|None, .end_time_ns:int|None, .duration_ns:int|None, .size_bytes:int
collect_table_schemas(reader) -> list[TableSchema]  # each: .table_name, .columns[*].name / .arrow_type / .is_heavy_blob
NOTE: import LAZILY inside the panel method (from rosbagger_core.inspect import collect_bag_info, collect_table_schemas) — keep panel module top stdlib/textual-only.

<!-- rosbagger_core.tf (read from source) -->
collect_tf_report(reader, *, gap_multiplier=5.0, gap_ms=None) -> TfReport:
  .frames: frozenset[str], .start_ns:int|None, .end_ns:int|None
  .edges: list[EdgeReport(parent, child, static:bool, samples:int, first_ns, last_ns, expected_ns:int|None, max_gap_ns:int|None, gap_count:int)]
  .gaps: list[GapReport(parent, child, gap_ns:int, at_ns:int, at_rel_ns:int, at_rel_s:float, expected_ns:int)]
NOTE: raises NoTransformsError (a ValueError subclass) when the bag has no /tf or /tf_static — render the teaching message, do not crash.

<!-- DataTable fill pattern (14-RESEARCH Pattern 4) -->
dt.clear(columns=True); dt.add_columns(*names); dt.add_row(*values)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InspectPanel — collect_bag_info + collect_table_schemas into DataTables (D-05)</name>
  <files>packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py</files>
  <read_first>
    - packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py (the stub being filled — created in 14-02)
    - packages/rosbagger-gui/src/rosbagger_gui/app.py (how the App exposes the shared open reader the panel must read — created in 14-02)
    - packages/rosbagger-core/src/rosbagger_core/inspect.py (collect_bag_info / collect_table_schemas — the ONLY APIs this panel calls; BagInfo/TopicInfo/TableSchema field shapes per <interfaces>)
    - 14-RESEARCH.md Pattern 4 (pyarrow/dataclass → DataTable mapping) and the empty-state guidance
  </read_first>
  <action>
    In `InspectPanel(Widget)`, compose a `DataTable` (id `bag-info`) for the per-topic overview and a `DataTable` (id `table-schemas`) for per-topic table names + columns (plus a `Static` header line for whole-bag duration/size/message_count). Add a `refresh_view()` method that reads the App's shared open reader (the attribute exposed by `app.py`), lazily imports `from rosbagger_core.inspect import collect_bag_info, collect_table_schemas` INSIDE the method, calls both, and fills the DataTables: `bag-info` columns = `topic / msgtype / count / hz`, one row per `BagInfo.topics` entry (render `None` msgtype as `<mixed>`, `None` hz as an em dash — mirror the bagq CLI's rendering shape); `table-schemas` shows each `TableSchema.table_name` and its `columns[*].name` / `str(col.arrow_type)` / heavy-blob marker. Render the whole-bag `duration_ns`/`size_bytes`/`message_count` in the header `Static` (human-format duration/size in the panel — display formatting is presentation, allowed; NO new analysis). Call `refresh_view()` from `on_mount` and whenever the panel is shown / the bag is switched (the App calls back the active panel on `open_bag` — wire to that). If no bag is loaded, render an empty-state `Static("Open a bag to inspect")`. The panel imports ONLY textual + rosbagger_core.inspect (lazy) — no other module API.
  </action>
  <verify>
    <automated>grep -n "collect_bag_info\|collect_table_schemas" packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py</automated>
    <automated>PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.inspect"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "collect_bag_info" packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py` >= 1 and `grep -c "collect_table_schemas" ...inspect.py` >= 1 (calls the real APIs).
    - `grep -c "DataTable" packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py` >= 1 (renders into a DataTable with id `bag-info`).
    - The inspect import (`from rosbagger_core.inspect import ...`) is INSIDE a method body, not at module top: `grep -vn '^#' packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py | grep -c '^from rosbagger_core'` is 0 at module scope (verify the import is indented inside a def).
    - `PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.inspect"` exits 0.
    - (Behavior — proven in 14-07 SC3) the `bag-info` DataTable row_count >= 1 against a fixture bag.
  </acceptance_criteria>
  <done>InspectPanel renders real collect_bag_info + collect_table_schemas output into DataTables for the shared bag, with an empty-state and bag-switch refresh.</done>
</task>

<task type="auto">
  <name>Task 2: TfPanel — collect_tf_report into edge + gap DataTables (D-07)</name>
  <files>packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py</files>
  <read_first>
    - packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py (the stub being filled — created in 14-02)
    - packages/rosbagger-gui/src/rosbagger_gui/app.py (the shared-reader attribute + the active-panel refresh callback)
    - packages/rosbagger-core/src/rosbagger_core/tf.py (collect_tf_report + TfReport/EdgeReport/GapReport field shapes per <interfaces>; NoTransformsError teaching error)
    - packages/bagq/src/bagq/cli.py (the existing `bagq tf` rich-table renderer — the edge-summary + gap-timeline column shape to MIRROR, NOT re-implement; grep for "TF edges"/"TF gaps")
  </read_first>
  <action>
    In `TfPanel(Widget)`, compose a `DataTable` (id `tf-edges`) for the per-edge summary and a `DataTable` (id `tf-gaps`) for the gap timeline. Add `refresh_view()` that reads the App's shared open reader, lazily imports `from rosbagger_core.tf import collect_tf_report` (and `NoTransformsError` from `rosbagger_core.errors`) INSIDE the method, calls `collect_tf_report(reader)`, and fills: `tf-edges` columns = `parent / child / kind / count / rate(Hz) / max gap / gaps` (mirror the bagq tf renderer's columns; `kind` = static/dynamic from `EdgeReport.static`; em dash for `None` rate/max_gap), one row per `TfReport.edges`; `tf-gaps` columns = `parent / child / gap / at (bag t) / at (abs ns)`, one row per `TfReport.gaps` (empty -> a `Static("no gaps detected")`). Catch `NoTransformsError` and render its teaching message as a `Static` (do not crash). Wire `refresh_view()` to `on_mount` + the bag-switch refresh callback; empty-state `Static("Open a bag with /tf to analyze")` when no bag. The panel imports ONLY textual + rosbagger_core.tf/errors (lazy).
  </action>
  <verify>
    <automated>grep -n "collect_tf_report\|NoTransformsError" packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py</automated>
    <automated>PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.tf"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "collect_tf_report" packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py` >= 1 (calls the real API).
    - `grep -c "NoTransformsError" packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py` >= 1 (the no-TF teaching path is handled, not a crash).
    - The tf import is INSIDE a method body, not module top: `grep -vn '^#' packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py | grep -c '^from rosbagger_core'` is 0 at module scope.
    - `grep -c "DataTable" packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py` >= 1 (renders into a DataTable with id `tf-edges`).
    - `PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.tf"` exits 0.
  </acceptance_criteria>
  <done>TfPanel renders real collect_tf_report edge-summary + gap-timeline into DataTables for the shared bag, with a NoTransformsError teaching path and an empty-state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → panel render | The panels read O(1) metadata (inspect) and stream only /tf + /tf_static (tf) off the shared reader; the bag is the trusted local user's own data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Denial of Service | A huge/hostile bag | accept | collect_bag_info is O(1) metadata (never reader.read()); collect_table_schemas reads only declared msgtypes; collect_tf_report streams only the two /tf topics. No unbounded materialization in these panels. |
| T-14-03-02 | Denial of Service | A bag with no /tf | mitigate | TfPanel catches NoTransformsError and renders the teaching message instead of crashing. |
</threat_model>

<verification>
- `PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.inspect, rosbagger_gui.panels.tf"` exits 0.
- `PYTHONPATH="" uv run ruff check packages/rosbagger-gui/src` clean.
- Behavior proven by Plan 14-07's SC3 inspect test (and tf rendering against the tf fixture bag).
</verification>

<success_criteria>
InspectPanel and TfPanel are thin renderers over collect_bag_info/collect_table_schemas and collect_tf_report respectively, driving the shared reader with empty-state and teaching-error handling, no analysis logic.
</success_criteria>

<output>
Create `.planning/phases/14-gui/14-03-SUMMARY.md` when done.
</output>
