---
phase: 14-gui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-replay/src/rosbagger_replay/replay.py
  - packages/rosbagger-replay/src/rosbagger_replay/__init__.py
  - tests/test_replay.py
autonomous: true
requirements: [GUI-01]

must_haves:
  truths:
    - "Both replay_bag() and the GUI can import ONE shared rclpy publish-sink builder; there is exactly one production publish path."
    - "The Phase-13 replay tests (and the live SC1 lane) still pass after the refactor — no behavior regression."
  artifacts:
    - path: "packages/rosbagger-replay/src/rosbagger_replay/replay.py"
      provides: "A reusable, importable publish-sink builder factored out of replay()'s body"
      contains: "def build_publish_sink"
  key_links:
    - from: "packages/rosbagger-replay/src/rosbagger_replay/replay.py:replay()"
      to: "build_publish_sink"
      via: "internal call (replay no longer inlines the get_message/deserialize_message/create_publisher mechanics)"
      pattern: "build_publish_sink"
    - from: "packages/rosbagger-replay/src/rosbagger_replay/__init__.py"
      to: "build_publish_sink"
      via: "lazy re-export behind _require_ros (the GUI imports it through the package front door)"
      pattern: "build_publish_sink"
---

<objective>
Refactor the ~15-line rclpy publish sink out of `rosbagger_replay/replay.py`'s `replay()` body into ONE reusable function (`build_publish_sink`) that BOTH `replay_bag()` and the Phase-14 GUI replay panel import (D-09a). This keeps a SINGLE production publish path: the GUI will drive the pure `Replayer` directly (for live transport) using the SAME sink mechanics `replay_bag()` uses — never a duplicated publish path.

Purpose: D-09's full live transport (play/pause/step/seek/rate/loop + scrubber) requires the GUI to drive the pure `Replayer` directly, because `replay_bag()` runs to completion and exposes no mid-flight control. To honor the "never re-implement the publish path" rule, the publish mechanics (`get_message` -> `create_publisher` -> `deserialize_message` -> `publish`) must live in one importable function.
Output: `build_publish_sink` in `replay.py`, lazily re-exported from `__init__`; `replay()` rewired to use it; Phase-13 replay tests green (no regression).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-gui/14-CONTEXT.md
@.planning/phases/14-gui/14-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract build_publish_sink, rewire replay(), and re-export it lazily</name>
  <files>packages/rosbagger-replay/src/rosbagger_replay/replay.py, packages/rosbagger-replay/src/rosbagger_replay/__init__.py, tests/test_replay.py</files>
  <read_first>
    - packages/rosbagger-replay/src/rosbagger_replay/replay.py (the file being modified — the inlined sink closure lives in replay()'s body, lines ~100-121: pubs dict, get_message, create_publisher, deserialize_message, publish, published["n"] counter)
    - packages/rosbagger-replay/src/rosbagger_replay/__init__.py (the lazy front door + _require_ros + replay_bag alias; where the lazy re-export is added)
    - packages/rosbagger-replay/src/rosbagger_replay/scheduler.py (the Replayer the sink feeds — confirms the sink signature is sink(item) -> None and item has .topic/.msgtype/.cdr)
    - tests/test_replay.py (the Phase-13 replay test suite that must still pass — understand what it asserts before changing replay.py)
  </read_first>
  <action>
    In `replay.py`, add a module-level function `build_publish_sink(node)` that returns a `(sink, published)` pair where `sink(item) -> None` is the publish closure currently inlined in `replay()` and `published` is the `{"n": 0}` count dict (the WR-07 source-of-truth counter). Move the lazy `from rclpy.serialization import deserialize_message` and `from rosidl_runtime_py.utilities import get_message` imports INSIDE `build_publish_sink`'s body (preserving the offline invariant — NO module-top ROS import). Keep the per-topic `pubs` dict, the depth-10 RELIABLE-VOLATILE `node.create_publisher(cls, item.topic, 10)` QoS, the `get_message(item.msgtype)` resolve, `deserialize_message(item.cdr, cls)`, `pub.publish(msg)`, and the `published["n"] += 1` increment EXACTLY as they are today (verbatim mechanics — no behavior change). Rewire `replay()` to call `sink, published = build_publish_sink(node)` instead of defining the closure inline; everything else in `replay()` (the `created_ctx`/`rclpy.ok()` guard, `Replayer` construction, `start` seek, `play()`/`run()`, the best-effort `finally` teardown, `return published["n"]`) stays unchanged. In `__init__.py`, re-export `build_publish_sink` through a lazy accessor that calls `_require_ros()` first (mirror the `replay` front-door pattern) and add it to `__all__`, so the GUI imports it via the package front door without breaking `import rosbagger_replay` offline-cleanliness.
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_replay.py -x</automated>
    <automated>grep -n "def build_publish_sink" packages/rosbagger-replay/src/rosbagger_replay/replay.py</automated>
    <automated>PYTHONPATH="" uv run pytest tests/test_offline_guard.py::test_import_replay_does_not_pull_ros -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def build_publish_sink" packages/rosbagger-replay/src/rosbagger_replay/replay.py` returns exactly one match (the extracted function exists).
    - `replay()` no longer inlines the publish mechanics: `grep -c "create_publisher" packages/rosbagger-replay/src/rosbagger_replay/replay.py` is 1 (only inside `build_publish_sink`, not duplicated in `replay()`).
    - `PYTHONPATH="" uv run pytest tests/test_replay.py -x` passes with the SAME number of tests as before the refactor (no regression — the Phase-13 replay/transport tests stay green).
    - `PYTHONPATH="" uv run pytest tests/test_offline_guard.py::test_import_replay_does_not_pull_ros -x` passes (the extracted sink keeps every rclpy/rosidl import inside the function body; `import rosbagger_replay` still leaks no rclpy/rosbag2_py).
    - `grep -n "build_publish_sink" packages/rosbagger-replay/src/rosbagger_replay/__init__.py` shows the lazy re-export + `__all__` entry.
  </acceptance_criteria>
  <done>build_publish_sink is the single source of publish mechanics; replay() uses it; Phase-13 replay tests pass unchanged; offline guard for replay still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI/CLI → rclpy publish sink | The shared sink publishes deserialized bag CDR bytes onto live ROS topics; the bag content is the trusted local user's own data (same trust model as the shipped replay CLI). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | build_publish_sink refactor introduces a behavior drift vs the verified replay() path | mitigate | Move the mechanics VERBATIM (no logic change); Phase-13 replay test suite regression-gates it (acceptance criteria run tests/test_replay.py). |
| T-14-01-02 | Elevation | rclpy import leaks into the offline import graph via the extracted function | mitigate | Keep deserialize_message/get_message imports INSIDE build_publish_sink's body; gate with test_import_replay_does_not_pull_ros. |
</threat_model>

<verification>
- `PYTHONPATH="" uv run pytest tests/test_replay.py tests/test_offline_guard.py -x` green.
- The published-count semantics (WR-07) preserved: `replay()` still returns `published["n"]`.
</verification>

<success_criteria>
One importable `build_publish_sink` is the single publish-mechanics source for both `replay_bag()` and the GUI; Phase-13 replay behavior is unchanged (tests green).
</success_criteria>

<output>
Create `.planning/phases/14-gui/14-01-SUMMARY.md` when done.
</output>
