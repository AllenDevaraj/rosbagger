---
phase: 14-gui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-gui/pyproject.toml
  - packages/rosbagger-gui/src/rosbagger_gui/__init__.py
  - packages/rosbagger-gui/src/rosbagger_gui/app.py
  - packages/rosbagger-gui/src/rosbagger_gui/cli.py
  - packages/rosbagger-gui/src/rosbagger_gui/app.tcss
  - packages/rosbagger-gui/src/rosbagger_gui/panels/__init__.py
  - packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py
  - packages/rosbagger-gui/src/rosbagger_gui/panels/query.py
  - packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py
  - packages/rosbagger-gui/src/rosbagger_gui/panels/record.py
  - packages/rosbagger-gui/src/rosbagger_gui/panels/replay.py
  - pyproject.toml
autonomous: true
requirements: [GUI-01]

must_haves:
  truths:
    - "rosbagger-gui is a uv workspace member; `import rosbagger_gui` succeeds offline (pulls no rclpy/rosbag2_py)."
    - "`rosbagger-gui --help` exits 0 and `rosbagger-gui <bag>` launches an App with a sidebar listing all five panels (inspect/query/tf/record/replay) over a ContentSwitcher."
    - "Live panels (record/replay) render disabled with a teaching hint when rclpy is not importable; offline panels (inspect/query/tf) are always enabled (D-03)."
    - "ROS detection is a cheap, top-level-import-free rclpy check (D-04 tier 1)."
    - "The App owns ONE shared open reader (D-02): launched with <bag>, panels read from it; an in-TUI picker can open/switch bags."
  artifacts:
    - path: "packages/rosbagger-gui/pyproject.toml"
      provides: "Workspace-member manifest (deps = rosbagger-core + textual) + the rosbagger-gui console script"
      contains: "rosbagger-gui = \"rosbagger_gui.cli:"
    - path: "packages/rosbagger-gui/src/rosbagger_gui/app.py"
      provides: "RosbaggerApp(App): sidebar ListView + ContentSwitcher shell, shared reader lifecycle, ros_available gate"
      contains: "class RosbaggerApp"
      min_lines: 40
    - path: "packages/rosbagger-gui/src/rosbagger_gui/cli.py"
      provides: "console-script entry point parsing the <bag-path> launch arg"
    - path: "packages/rosbagger-gui/src/rosbagger_gui/__init__.py"
      provides: "ROS-free package top + the cheap _detect_ros helper (rclpy import INSIDE the function)"
  key_links:
    - from: "packages/rosbagger-gui/src/rosbagger_gui/app.py"
      to: "rosbagger_core.reader.RosbagsReader"
      via: "App opens one shared reader on bag-load (D-02)"
      pattern: "RosbagsReader"
    - from: "packages/rosbagger-gui/src/rosbagger_gui/app.py"
      to: "ContentSwitcher"
      via: "sidebar ListView.Selected sets switcher.current = panel_id (D-01)"
      pattern: "ContentSwitcher"
    - from: "pyproject.toml"
      to: "packages/rosbagger-gui/src"
      via: "[tool.ruff] src list + textual-dev/pytest-asyncio dev group"
      pattern: "rosbagger-gui/src"
---

<objective>
Create the `rosbagger-gui` uv workspace member and the Textual App shell: a sidebar `ListView` + `ContentSwitcher` (D-01), the shared open-reader lifecycle (D-02), the cheap tier-1 ROS-availability gate (D-04) that disables the live panels with a teaching hint (D-03), and the `rosbagger-gui` console script (launch-arg `<bag-path>`). Register all five panels as STUB widgets in the switcher; the offline-panel and live-panel plans (Waves 2-3) fill the stubs. Also add the dev-group async test plugin (pytest-asyncio, slopcheck-verified [OK]) and `asyncio_mode = "auto"` so the SC3 headless tests in Plan 14-07 can run.

Purpose: Establish the shell, packaging, capability-gating, and shared-reader seam every panel sits on, with the offline-import invariant locked from the start.
Output: A launchable `rosbagger-gui` with five (stubbed) panels, the disabled-live gating, and the test/packaging scaffolding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-gui/14-CONTEXT.md
@.planning/phases/14-gui/14-RESEARCH.md

<interfaces>
<!-- The shared reader the App owns (D-02) — extracted from packages/rosbagger-core/src/rosbagger_core/reader/rosbags_reader.py -->
RosbagsReader(paths: str|Path|Iterable, *, default_typestore: object = None)
  .open() / .close() / context-manager (__enter__ returns self, __exit__ never swallows)
  properties (all O(1), valid after open()): topics, connections, message_count, duration, start_time, end_time, typestore, paths
NOTE: the ROS 2 sqlite3 fixture needs default_typestore=get_typestore(Stores.ROS2_HUMBLE) (from rosbags.typesys); MCAP / ROS 1 do not (self-describing).

<!-- The cheap tier-1 detect from 14-RESEARCH Pattern 3 — rclpy import INSIDE the function -->
def _detect_ros() -> bool:  # try: import rclpy; return True / except ImportError: return False
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace-member packaging + root pyproject wiring + dev async plugin</name>
  <files>packages/rosbagger-gui/pyproject.toml, pyproject.toml</files>
  <read_first>
    - packages/rosbagger-replay/pyproject.toml (the workspace-member TEMPLATE: [project] with deps = rosbagger-core + (here) textual, [project.scripts] console entry, [build-system] hatchling — mirror this shape, swap typer for textual)
    - packages/rosbagger-replay/src/rosbagger_replay/__init__.py (the lazy-import + offline-clean __init__ TEMPLATE)
    - pyproject.toml (root virtual workspace: [tool.uv.workspace] members, [tool.uv.sources], [dependency-groups] dev, [tool.ruff] src list, [tool.pytest.ini_options] addopts/testpaths/markers — the files being modified)
  </read_first>
  <action>
    Create `packages/rosbagger-gui/pyproject.toml` mirroring `rosbagger-replay/pyproject.toml`: `[project] name = "rosbagger-gui"`, `version = "0.1.0"`, `requires-python = ">=3.10"`, `dependencies = ["rosbagger-core", "textual>=8,<9"]` (NOTHING ROS — the live panels lazy-import `rosbagger_record`/`rosbagger_replay` as already-installed workspace siblings, D-03), `[project.scripts] rosbagger-gui = "rosbagger_gui.cli:main"` (or `:app` if cli.py exposes a Typer app — pick one and keep it consistent with Task 3), and `[build-system]` hatchling. In the ROOT `pyproject.toml`: add `packages/rosbagger-gui/src` to the `[tool.ruff] src` list; add `pytest-asyncio>=0.23` and `textual-dev>=1,<2` to `[dependency-groups] dev`; add `asyncio_mode = "auto"` to `[tool.pytest.ini_options]`. Do NOT change the `--cov=rosbagger_core --cov=bagq` addopts — keep `rosbagger_gui` OUT of the coverage gate (mirror Phase 13 D-12; the GUI is near-zero pure logic by the thin-face rule). Run `uv sync` so the editable `.pth` lands in site-packages (so `import rosbagger_gui` resolves under `PYTHONPATH=""`).
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run python -c "import rosbagger_gui"</automated>
    <automated>grep -n "rosbagger-gui/src" pyproject.toml</automated>
    <automated>grep -n "asyncio_mode" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `packages/rosbagger-gui/pyproject.toml` exists; `grep -c 'rclpy\|rosbag2_py\|rosidl' packages/rosbagger-gui/pyproject.toml` is 0 (no ROS dep declared).
    - `PYTHONPATH="" uv run python -c "import rosbagger_gui"` exits 0 (member installed, top level resolves offline).
    - Root `pyproject.toml` `[tool.ruff] src` includes `packages/rosbagger-gui/src`; `[dependency-groups] dev` includes `pytest-asyncio` and `textual-dev`; `[tool.pytest.ini_options]` has `asyncio_mode = "auto"`.
    - `grep -c 'cov=rosbagger_gui' pyproject.toml` is 0 (GUI stays out of the coverage gate).
    - `PYTHONPATH="" uv sync` completes without resolving any ROS wheel.
  </acceptance_criteria>
  <done>rosbagger-gui is an installed workspace member; textual + dev async plugin resolve; offline import works; coverage gate unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: App shell — sidebar + ContentSwitcher, shared reader, ROS gate, panel stubs</name>
  <files>packages/rosbagger-gui/src/rosbagger_gui/__init__.py, packages/rosbagger-gui/src/rosbagger_gui/app.py, packages/rosbagger-gui/src/rosbagger_gui/cli.py, packages/rosbagger-gui/src/rosbagger_gui/app.tcss, packages/rosbagger-gui/src/rosbagger_gui/panels/__init__.py, packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py, packages/rosbagger-gui/src/rosbagger_gui/panels/query.py, packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py, packages/rosbagger-gui/src/rosbagger_gui/panels/record.py, packages/rosbagger-gui/src/rosbagger_gui/panels/replay.py</files>
  <read_first>
    - packages/rosbagger-gui/src/rosbagger_gui/__init__.py (will create — the ROS-free package top + _detect_ros)
    - 14-RESEARCH.md Pattern 1 (Sidebar + ContentSwitcher shell), Pattern 3 (disabled reactive gating + _detect_ros), Pitfall 5 (shared reader lifecycle), Pitfall 2 (offline import boundary)
    - packages/rosbagger-core/src/rosbagger_core/reader/rosbags_reader.py (RosbagsReader open/close/context-manager + properties — the shared reader the App owns; see <interfaces>)
    - packages/rosbagger-replay/src/rosbagger_replay/__init__.py (the offline-clean __init__ + lazy-import discipline to mirror in rosbagger_gui/__init__.py)
  </read_first>
  <action>
    Create `rosbagger_gui/__init__.py` with the ROS-free package top and a module-level `_detect_ros() -> bool` helper that does `try: import rclpy; return True / except ImportError: return False` (import INSIDE the function — never at module top, per Pitfall 2). Create `rosbagger_gui/app.py` with `class RosbaggerApp(App)`: `CSS_PATH = "app.tcss"`, a `compose()` that yields a `Horizontal(ListView(...), ContentSwitcher(...))` — the sidebar `ListView` has one `ListItem` per panel with ids `nav-inspect`/`nav-query`/`nav-tf`/`nav-record`/`nav-replay`, and the `ContentSwitcher` (id `content`) holds the five panel widgets with ids `inspect`/`query`/`tf`/`record`/`replay`. Implement `on_list_view_selected` to set `self.query_one("#content", ContentSwitcher).current = event.item.id.removeprefix("nav-")` (D-01). Accept a `bag_path` ctor arg (the D-02 launch-arg); in `on_mount`, if `bag_path` is set, open ONE shared `RosbagsReader(bag_path, default_typestore=...)` (pass `get_typestore(Stores.ROS2_HUMBLE)` so the ROS 2 sqlite3 fixture loads — see Pitfall 5) and expose it as an App attribute the panels read; provide an `open_bag(path)` method that closes any existing reader and opens the new one then refreshes the active panel (the in-TUI picker seam, D-02). Compute `ros_available = _detect_ros()` once; set the record/replay sidebar `ListItem`s and panel widgets `disabled = not ros_available` (D-03). Create stub panel widgets in `rosbagger_gui/panels/` — `inspect.py` (`InspectPanel(Widget)`), `query.py` (`QueryPanel(Widget)`), `tf.py` (`TfPanel(Widget)`), `record.py` (`RecordPanel(Widget)`), `replay.py` (`ReplayPanel(Widget)`) — each a minimal `Widget` subclass that for now mounts a `Static` placeholder; the live-panel stubs (record/replay) mount the teaching-hint `Static("Source your ROS 2 environment to enable …")` when disabled (D-03). Create `panels/__init__.py` exporting the five panel classes. Create `app.tcss` with the Horizontal layout + the disabled dim styling. Create `rosbagger_gui/cli.py`: a `main()` (or Typer `app`) that parses an optional `<bag-path>` positional arg and calls `RosbaggerApp(bag_path=...).run()` (D-02) — keep it consistent with the `[project.scripts]` entry from Task 1. Keep EVERY `rosbagger_record`/`rosbagger_replay`/`rclpy` import out of module top everywhere (the live panel stubs do NOT import the live packages yet — that lands in Waves 3).
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run rosbagger-gui --help</automated>
    <automated>grep -n "class RosbaggerApp\|ContentSwitcher\|RosbagsReader" packages/rosbagger-gui/src/rosbagger_gui/app.py</automated>
    <automated>PYTHONPATH="" uv run python -c "import sys; import rosbagger_gui, rosbagger_gui.app; leaked=[m for m in sys.modules if m.split('.')[0] in {'rclpy','rosbag2_py'}]; assert leaked==[], leaked; print('offline-clean')"</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH="" uv run rosbagger-gui --help` exits 0 (console script wired).
    - `grep -c "ContentSwitcher" packages/rosbagger-gui/src/rosbagger_gui/app.py` >= 1 and `grep -c "RosbagsReader" packages/rosbagger-gui/src/rosbagger_gui/app.py` >= 1 (D-01 shell + D-02 shared reader present).
    - The fresh-interpreter scan (`PYTHONPATH=""`) after `import rosbagger_gui, rosbagger_gui.app` leaks no rclpy/rosbag2_py (offline boundary held — formal guard test lands in 14-07).
    - `grep -c '^import rclpy\|^import rosbagger_record\|^import rosbagger_replay' packages/rosbagger-gui/src/rosbagger_gui/*.py packages/rosbagger-gui/src/rosbagger_gui/panels/*.py` is 0 (no module-top live/ROS imports anywhere).
    - The five sidebar `ListItem` ids (`nav-inspect`/`nav-query`/`nav-tf`/`nav-record`/`nav-replay`) and the five panel ids (`inspect`/`query`/`tf`/`record`/`replay`) are present in `app.py`.
  </acceptance_criteria>
  <done>rosbagger-gui launches a sidebar+ContentSwitcher shell with five panels, a shared open reader, the disabled-live gating, and a clean offline import graph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launch-arg / picker → reader | A bag path supplied at launch or via the in-TUI picker is opened by the App's RosbagsReader; the user's own local filesystem (no traversal concern beyond their own files). |
| package top → ROS | `import rosbagger_gui` must NOT pull rclpy/rosbag2_py; the live packages are lazy-imported only inside live-panel method bodies (Waves 3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Denial of Service | A missing / non-bag launch path | mitigate | The App opens the reader inside on_mount and surfaces a teaching error / empty-state prompt rather than crashing (the bag-open error path; offline panels prompt to open a bag). |
| T-14-02-02 | Elevation | rclpy leaking into the offline import graph via app/panels module-top imports | mitigate | _detect_ros imports rclpy INSIDE the function; live panel stubs import no live package; acceptance scan + the 14-07 offline-guard test gate it. |
| T-14-02-03 | Tampering | Supply chain: pytest-asyncio / textual-dev dev-group installs | accept | Both slopcheck [OK] (pytest-asyncio verified this session; textual + textual-dev verified in RESEARCH). No [ASSUMED]/[SUS] package — no blocking checkpoint needed. |
</threat_model>

<verification>
- `PYTHONPATH="" uv run rosbagger-gui --help` exits 0.
- `PYTHONPATH="" uv run python -c "import rosbagger_gui"` exits 0; fresh-interpreter scan leaks no ROS.
- `PYTHONPATH="" uv run ruff check packages/rosbagger-gui/src` clean.
</verification>

<success_criteria>
A launchable five-panel sidebar+ContentSwitcher TUI with shared-reader lifecycle, disabled-live capability gating, and an offline-clean import graph; the dev async test plugin + asyncio_mode wired for Plan 14-07.
</success_criteria>

<output>
Create `.planning/phases/14-gui/14-02-SUMMARY.md` when done.
</output>
