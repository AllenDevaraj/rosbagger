---
phase: 14-gui
plan: 05
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - packages/rosbagger-gui/src/rosbagger_gui/panels/record.py
autonomous: true
requirements: [GUI-01]

must_haves:
  truths:
    - "When ROS is available and the record panel is opened, a tier-2 discover_topics scan runs in a thread worker and populates a topic checklist (D-04/D-08)."
    - "Selecting topics + Start records them via the real record() API in a thread worker; Stop ends a bounded record (D-08)."
    - "All blocking ROS work (discovery scan, record loop) runs in @work(thread=True) workers updating widgets via call_from_thread — the event loop never blocks (RESEARCH landmine #1)."
    - "rosbagger_record is lazy-imported inside method bodies only — import rosbagger_gui stays ROS-free."
  artifacts:
    - path: "packages/rosbagger-gui/src/rosbagger_gui/panels/record.py"
      provides: "RecordPanel(Widget) → lazy rosbagger_record discover_topics/select_topics/record in thread workers"
      contains: "@work"
      min_lines: 40
  key_links:
    - from: "packages/rosbagger-gui/src/rosbagger_gui/panels/record.py"
      to: "rosbagger_record.discover_topics"
      via: "tier-2 on-open scan in a thread worker populating the checklist (D-04)"
      pattern: "discover_topics"
    - from: "packages/rosbagger-gui/src/rosbagger_gui/panels/record.py"
      to: "rosbagger_record.record"
      via: "Start handler drives record() in a thread worker (D-08)"
      pattern: "record"
---

<objective>
Fill the live record panel stub (D-04/D-08): on open (when ROS is available — tier-1 enabled it in 14-02), run a tier-2 live `discover_topics` scan in a thread worker to populate a topic checklist; Start records the selected subset via the real `record()` API in a thread worker; Stop ends a bounded record. Every blocking ROS call runs in `@work(thread=True)` with `call_from_thread` widget updates — the event loop never freezes. `rosbagger_record` is lazy-imported inside method bodies only.

Purpose: The first of the two live panels; the thin face over the shipped record API.
Output: A populated RecordPanel with discovery checklist + start/stop over record()/discover_topics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-gui/14-CONTEXT.md
@.planning/phases/14-gui/14-RESEARCH.md

<interfaces>
<!-- rosbagger_record public API (read from source) -->
discover_topics(node, *, settle_iters=30, settle_dt=0.02) -> dict[str, str]   # {topic: type}; needs a live rclpy node + spin (blocking ~0.6s) — run in a thread worker
select_topics(discovered, *, topics=None, all_topics=False, regex=None, exclude=None) -> dict[str, str]   # PURE filter (no ROS)
record(topics: list[str], out: str, *, all_topics=False, regex=None, exclude=None, storage="mcap", max_messages=None, duration=None) -> int   # blocking record loop; storage="sqlite3" is the box escape; returns captured count
list_topics() -> dict[str, str]   # lazy front door; requires ROS
RosNotAvailableError / NoTopicsMatchedError   # teaching errors (from rosbagger_record)
NOTE: record() is the SINGLE discover→select→record orchestrator (it calls discover_topics + select_topics itself). The PANEL'S own on-open discover_topics scan is ONLY to populate the checklist UI (D-04); Start then calls record() with the chosen topics. Lazy-import rosbagger_record INSIDE method bodies.

<!-- Textual worker pattern (14-RESEARCH Pattern 5 / Code Example 3) -->
@work(exclusive=True, thread=True) def scan(self): ...  # lazy import inside; self.app.call_from_thread(self._on_result, data)
from textual.worker import get_current_worker  # worker.is_cancelled for Stop
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecordPanel — tier-2 discovery scan (thread worker) → checklist + start/stop record() (thread worker)</name>
  <files>packages/rosbagger-gui/src/rosbagger_gui/panels/record.py</files>
  <read_first>
    - packages/rosbagger-gui/src/rosbagger_gui/panels/record.py (the stub being filled — created in 14-02; already mounts the disabled teaching hint when ROS is absent, D-03)
    - packages/rosbagger-gui/src/rosbagger_gui/app.py (the ros_available flag + how panels learn they were opened/shown)
    - packages/rosbagger-record/src/rosbagger_record/discovery.py (discover_topics + select_topics signatures — the checklist source; see <interfaces>)
    - packages/rosbagger-record/src/rosbagger_record/record.py (the record() signature + the discover→select→record pipeline it owns — the Start API)
    - packages/rosbagger-record/src/rosbagger_record/__init__.py (the lazy front-door + RosNotAvailableError/NoTopicsMatchedError teaching errors to catch)
    - 14-RESEARCH.md Pattern 5 + Code Example 3 (the @work(thread=True) + call_from_thread worker shape) + Pitfall 1 (blocking the event loop)
  </read_first>
  <action>
    In `RecordPanel(Widget)`, compose a topic checklist (a `SelectionList`, or a `ListView` of `Checkbox`es, id `topic-checklist`), a destination-path `Input` (id `record-out`), a storage selector (a `Select`/`RadioSet` for `mcap`/`sqlite3`, default `mcap` — sqlite3 is the box escape), Start (`record-start`) and Stop (`record-stop`) `Button`s, and a `Static` status (id `record-status`). When the panel is shown AND `ros_available`, trigger a `@work(exclusive=True, thread=True)` discovery worker that lazily `from rosbagger_record import discover_topics` (plus creating a short-lived rclpy node — lazy `import rclpy` inside the worker, mirroring the record core's node lifecycle, OR call `list_topics()` if it returns the same `{topic: type}` map without a caller-managed node — prefer the simplest correct path that needs no duplicated node mechanics) and posts the discovered `{topic: type}` map back via `self.app.call_from_thread(self._populate_checklist, mapping)`. Show the panel `loading` reactive while the scan runs. On Start: read the checked topics + the out path + the storage choice, then run a `@work(exclusive=True, thread=True)` record worker that lazily `from rosbagger_record import record` and calls `record(selected_topics, out, storage=storage, ...)`, posting the captured count back via `call_from_thread` to the status line; Stop sets a cancel flag the worker honors (the record() bounded-stop / a `worker.is_cancelled` check) — use a bounded `duration`/`max_messages` or the worker-cancel path so Stop actually ends the loop. Catch `RosNotAvailableError`/`NoTopicsMatchedError` and surface the teaching message to `record-status` (an empty graph after enabling still teaches — D-04). NEVER call discover_topics/record on the event loop directly (Pitfall 1). Every `rosbagger_record`/`rclpy` import is INSIDE a worker/method body.
  </action>
  <verify>
    <automated>grep -n "@work\|discover_topics\|record\|call_from_thread" packages/rosbagger-gui/src/rosbagger_gui/panels/record.py</automated>
    <automated>PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.record"</automated>
    <automated>PYTHONPATH="" uv run python -c "import sys; import rosbagger_gui.panels.record; leaked=[m for m in sys.modules if m.split('.')[0] in {'rclpy','rosbag2_py'}]; assert leaked==[], leaked; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@work" packages/rosbagger-gui/src/rosbagger_gui/panels/record.py` >= 1 AND the worker decorators include `thread=True` (no blocking ROS call on the event loop — Pitfall 1).
    - `grep -c "discover_topics\|list_topics" ...record.py` >= 1 (tier-2 scan populates the checklist) and `grep -c "call_from_thread" ...record.py` >= 1 (thread-safe widget updates).
    - `grep -c "record(" ...record.py` >= 1 (Start drives the real record() API).
    - Every `rosbagger_record`/`rclpy` import is inside a method/worker body: module-top scan `grep -vn '^#' ...record.py | grep -c '^import rclpy\|^from rosbagger_record\|^import rosbagger_record'` is 0.
    - The fresh-interpreter scan after `import rosbagger_gui.panels.record` (PYTHONPATH="") leaks no rclpy/rosbag2_py.
    - `grep -c "RosNotAvailableError\|NoTopicsMatchedError" ...record.py` >= 1 (teaching paths handled).
    - `PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.record"` exits 0.
  </acceptance_criteria>
  <done>RecordPanel runs a tier-2 discovery scan in a thread worker to fill a topic checklist and drives the real record() API on Start/Stop in a thread worker, with teaching-error handling and a ROS-free import graph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| record panel → rclpy/rosbag2_py | The panel discovers + records live ROS topics — the same rclpy surface as the already-shipped rosbagger-record CLI; topic/regex strings pass through select_topics' stdlib re (never shell/eval). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Denial of Service | A blocking discover_topics/record call freezing the TUI | mitigate | All blocking ROS work in @work(thread=True) with call_from_thread updates (Pitfall 1); acceptance criteria grep-gate the worker decorators. |
| T-14-05-02 | Elevation | rclpy/rosbagger_record leaking into the offline import graph | mitigate | Lazy imports inside worker/method bodies only; fresh-interpreter scan in acceptance criteria + the 14-07 offline guard. |
| T-14-05-03 | Injection | Topic include/exclude regex strings | accept | select_topics uses stdlib re only, never shell/eval (T-12-02); the panel passes strings verbatim. Local trusted user. |
</threat_model>

<verification>
- `PYTHONPATH="" uv run python -c "import rosbagger_gui.panels.record"` exits 0; fresh-interpreter scan leaks no ROS.
- `PYTHONPATH="" uv run ruff check packages/rosbagger-gui/src` clean.
- Live behavior is exercised by the `live`-marked GUI integration test in Plan 14-07 (ROS-sourced lane, importorskip("rclpy")) — skipped in offline CI.
</verification>

<success_criteria>
RecordPanel is a thin face over discover_topics/record running entirely in thread workers, with a topic checklist + start/stop, teaching-error handling, and an offline-clean import graph.
</success_criteria>

<output>
Create `.planning/phases/14-gui/14-05-SUMMARY.md` when done.
</output>
