---
phase: 20-replay-rviz-fidelity-clock-and-static-republish
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - packages/rosbagger-replay/src/rosbagger_replay/replay.py
  - tests/test_replay_fidelity_live.py
autonomous: true
requirements: [REP-04]
must_haves:
  truths:
    - "build_publish_sink(node) with NO new kwargs behaves EXACTLY as today: the existing 2-tuple unpacking `sink, published = build_publish_sink(node)` (replay.py) and `sink, self._published = build_publish_sink(self._node)` (replay_panel.py) keep working byte-for-byte behaviorally; published is still {'n': 0}; no /clock publisher, no tracker created"
    - "build_publish_sink(node, *, publish_clock=True) lazily resolves rosgraph_msgs/msg/Clock, creates a /clock publisher ONCE, and on each sink(item) publishes a Clock whose .clock.sec/.nanosec == clock_stamp_ns(item.t_ns) — proven by a live test whose external subscriber receives /clock"
    - "build_publish_sink(node, *, static_topics=frozenset({'/tf_static'})) builds a StaticTracker and calls tracker.record(item) AFTER each normal publish; a republish helper pushes tracker.republish_items() back through the SAME sink after a seek so a fresh subscriber re-primes — proven by a live test (seek then the subscriber receives the re-published /tf_static)"
    - "When the new kwargs are passed, the tracker is reachable by callers (via a back-compatible return shape) so a republish-after-seek can be invoked WITHOUT breaking the 2-tuple unpacking when the kwargs are absent"
    - "build_publish_sink stays the SINGLE production publish path — the /clock + tracker-record logic is layered INSIDE it (no second sink, no second publisher-build/deserialize mechanics)"
    - "Every rclpy / rosidl_runtime_py import for /clock stays LAZY inside the function body (the offline invariant); import rosbagger_replay stays ROS-free; tests/test_offline_guard.py green"
    - "The live test is -m live (skipped in offline CI via importorskip rclpy) and is NOT required for the offline gate; the offline gate stays green unchanged (SC2 is satisfied by the 20-01 pure StaticTracker unit test)"
  artifacts:
    - path: "packages/rosbagger-replay/src/rosbagger_replay/replay.py"
      provides: "build_publish_sink(node, *, publish_clock=False, static_topics=frozenset()) — keyword-only, DEFAULT OFF (no-kwargs call unchanged); when static_topics non-empty builds a StaticTracker and records each published item; when publish_clock lazily resolves rosgraph_msgs/msg/Clock, creates a /clock publisher once, and publishes a Clock(.clock=clock_stamp_ns(item.t_ns)) per publish; a back-compatible return exposing the tracker; a republish_static(...) helper pushing tracker.republish_items() through the sink after a seek"
      contains: "publish_clock"
    - path: "tests/test_replay_fidelity_live.py"
      provides: "Live (-m live, importorskip rclpy) tests mirroring test_replay_live.py: (a) publish_clock -> external subscriber subprocess receives /clock (rosgraph_msgs/msg/Clock); (b) static_topics -> after a seek the re-published /tf_static (from write_tf_bag) reaches a fresh subscriber. Skipped in offline CI."
      contains: "pytest.mark.live"
  key_links:
    - from: "replay.py build_publish_sink sink(item)"
      to: "tracker.record(item) + the /clock Clock publish"
      via: "after the normal pub.publish(msg) + published['n'] += 1: if tracker is not None: tracker.record(item); if clock_pub is not None: build a Clock msg, set .clock.sec/.nanosec = clock_stamp_ns(item.t_ns), clock_pub.publish(clock_msg)"
      pattern: "clock_stamp_ns\\(item\\.t_ns\\)"
    - from: "replay.py build_publish_sink"
      to: "lazy get_message('rosgraph_msgs/msg/Clock') + node.create_publisher(..., '/clock', 10) ONCE"
      via: "when publish_clock: resolve the Clock class + create the /clock publisher a single time (outside sink, captured in the closure)"
      pattern: "rosgraph_msgs/msg/Clock"
    - from: "replay.py republish_static helper"
      to: "tracker.republish_items() -> the SAME sink"
      via: "for item in tracker.republish_items(): sink(item) — called by a caller AFTER replayer.seek(...) so a fresh scene re-primes"
      pattern: "republish_items\\(\\)"
    - from: "build_publish_sink no-kwargs return"
      to: "the existing 2-tuple `sink, published = build_publish_sink(node)`"
      via: "the new return element is OPTIONAL/back-compatible: the 2-tuple unpacking keeps working when no kwargs are passed (e.g. tracker exposed via a sink attribute, or a NamedTuple whose first two fields are sink, published)"
      pattern: "sink, published = build_publish_sink"
---

<objective>
Wire the LIVE rclpy boundary for Phase-20 RViz fidelity onto the SINGLE existing publish path: extend `build_publish_sink` (the one production sink shared by `replay()` and the desktop panel) to optionally (a) publish `/clock` from the current item's `t_ns` and (b) feed each published item to a `StaticTracker` and re-publish the tracked set after a seek. Both behaviors are opt-in keyword args that DEFAULT OFF — the no-kwargs call (and its existing 2-tuple unpacking) stays behaviorally identical.

Mechanism (RESEARCH §2b, the decided design — extend the SINGLE sink, do not add a second): `build_publish_sink(node, *, publish_clock=False, static_topics=frozenset())` (keyword-only). When `static_topics` is non-empty, build a `StaticTracker(static_topics)` (from 20-01) and call `tracker.record(item)` after each normal publish. When `publish_clock`, lazily `get_message("rosgraph_msgs/msg/Clock")`, create a `/clock` publisher ONCE, and in `sink(item)` publish a `Clock` whose `.clock.sec/.nanosec = clock_stamp_ns(item.t_ns)` (piggy-back on each publish — the simplest correct cadence; a fixed-Hz timer is a deferred nicety). Provide a `republish_static(...)` helper (or a sink attribute/method) that callers invoke AFTER `replayer.seek(...)` to push `tracker.republish_items()` through the SAME sink so a fresh subscriber re-primes. CRITICAL back-compat: keep `sink, published = build_publish_sink(node)` working UNCHANGED when no kwargs are passed — make the tracker reachable via a back-compatible return shape (e.g. expose the tracker as an attribute on the `sink` closure, OR return a NamedTuple whose first two iterated fields stay `(sink, published)`), so the TWO existing 2-tuple unpackings (replay.py:154 and replay_panel.py:289) do not break.

Purpose: This is the LIVE tier of the Phase-12/13 two-tier strategy — the thin rclpy boundary on top of the 20-01 pure logic. It proves SC1 (`/clock` published during replay) and SC2-live (static re-published after a seek) via `-m live` tests, and the library half of SC3 (both behaviors opt-in via library params, defaults off). The offline gate stays green unchanged (SC2's CI proof is the 20-01 pure StaticTracker unit test); the live test is the SC1 + SC2-live proof to be run in the ROS-sourced lane. 20-03 (desktop toggles) threads these opt-ins from the UI.
Output: an extended `replay.py` (lazy ROS, defaults off, single publish path) + a `-m live` fidelity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-replay-rviz-fidelity-clock-and-static-republish/20-CONTEXT.md
@.planning/phases/20-replay-rviz-fidelity-clock-and-static-republish/20-RESEARCH.md
@.planning/phases/20-replay-rviz-fidelity-clock-and-static-republish/20-01-SUMMARY.md

# The SINGLE publish path being extended (the ONLY production file this plan changes).
# build_publish_sink (~:39-87) returns (sink, published); replay() unpacks it at ~:154 — the
# 2-tuple contract that MUST stay back-compatible when no new kwargs are passed.
@packages/rosbagger-replay/src/rosbagger_replay/replay.py
# The pure tier this plan consumes (20-01): StaticTracker + clock_stamp_ns, re-exported here.
@packages/rosbagger-replay/src/rosbagger_replay/fidelity.py
# The front door (lazy build_publish_sink delegator — _require_ros guard); StaticTracker/
# clock_stamp_ns re-exported here (20-01). No change needed unless the return shape needs it.
@packages/rosbagger-replay/src/rosbagger_replay/__init__.py
# The OTHER 2-tuple call site that MUST keep working: `sink, self._published =
# build_publish_sink(self._node)` (~:351). This plan must NOT break it (20-03 threads the kwargs).
@packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py
# The live-test pattern to MIRROR: importorskip rclpy (~:54) + pytestmark = pytest.mark.live
# (~:56) + an in-process production front door publisher + an EXTERNAL subscriber subprocess
# (own rclpy context) counting received msgs on stdout (~:87-176). Reuse this recipe verbatim.
@tests/test_replay_live.py
# Offline/ROS-free guard that MUST stay green (the /clock + tracker-record code stays lazy).
@tests/test_offline_guard.py

<interfaces>
<!-- Contracts: the 20-01 pure tier this plan consumes + the existing publish path it extends. -->

From 20-01 (rosbagger_replay.fidelity — pure, re-exported from the front door):
```python
class StaticTracker:
    def __init__(self, static_topics=frozenset({"/tf_static"})) -> None: ...
    def record(self, item) -> None: ...            # latest-per-static-topic
    def republish_items(self) -> list: ...          # one latest item per tracked topic
    def clear(self) -> None: ...

def clock_stamp_ns(t_ns: int) -> tuple[int, int]: ...   # divmod split -> (sec, nanosec)
```

EXISTING build_publish_sink (replay.py — the SINGLE publish path, this plan EXTENDS it):
```python
def build_publish_sink(node):
    from rclpy.serialization import deserialize_message     # LAZY (offline invariant)
    from rosidl_runtime_py.utilities import get_message     # LAZY
    published = {"n": 0}
    pubs: dict[str, tuple] = {}
    def sink(item) -> None:
        # get_message(item.msgtype) -> create_publisher(cls, item.topic, 10) once per topic;
        # deserialize_message(item.cdr, cls); pub.publish(msg); published["n"] += 1
        ...
    return sink, published          # <-- the 2-tuple contract that MUST stay back-compatible
```
TWO call sites unpack this 2-tuple (do NOT break when kwargs absent):
  - replay.py:~154  `sink, published = build_publish_sink(node)`
  - replay_panel.py:~351  `sink, self._published = build_publish_sink(self._node)`

The /clock message shape (rosgraph_msgs/msg/Clock): a single field `clock` of type builtin_interfaces/
Time with int fields `.sec` and `.nanosec`. Build via `Clock = get_message("rosgraph_msgs/msg/Clock");
msg = Clock(); msg.clock.sec, msg.clock.nanosec = clock_stamp_ns(item.t_ns)`. The /clock publisher uses
the same sane default QoS depth-10 (RViz/use_sim_time subscribers tolerate it for the tests).

NEW build_publish_sink signature (this plan):
```python
def build_publish_sink(node, *, publish_clock: bool = False, static_topics=frozenset()):
    # DEFAULT OFF: when both args are at their defaults the function is behaviorally identical
    # to today AND `sink, published = build_publish_sink(node)` still unpacks (back-compatible).
    # tracker = StaticTracker(static_topics) if static_topics else None
    # clock_pub = (lazy get_message Clock + node.create_publisher(Clock,"/clock",10)) if publish_clock else None
    # sink(item): normal publish; then if tracker: tracker.record(item); if clock_pub: publish Clock(clock_stamp_ns(item.t_ns))
    ...
```
Pick the LEAST-disruptive back-compatible return (RESEARCH §2b): EITHER keep returning the
2-tuple `(sink, published)` and attach the tracker to the sink (e.g. `sink.tracker = tracker`)
OR return a small NamedTuple whose iteration order keeps `(sink, published)` first so the
existing unpackings still bind sink+published. Provide `republish_static(sink_or_tracker)` (a
module function or a sink method) that does `for it in tracker.republish_items(): sink(it)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend build_publish_sink with opt-in /clock + static-tracking (lazy ROS, defaults off, back-compatible return)</name>
  <files>packages/rosbagger-replay/src/rosbagger_replay/replay.py</files>
  <behavior>
    - The no-kwargs contract is PRESERVED: `build_publish_sink(node)` returns a value whose 2-tuple unpacking `sink, published = build_publish_sink(node)` still binds `sink` (callable) and `published` (`{"n": 0}`); no /clock publisher is created and no tracker is built (the offline guard + the existing live test + the desktop panel all rely on this). This is verifiable at the offline tier with a fake node (the function body's ROS imports — `deserialize_message`/`get_message` — are only hit on first `sink(item)`, not at build time, so a no-op build with a stub node that records `create_publisher` calls proves no `/clock` publisher is made when `publish_clock=False`). If hitting ROS in CI is undesirable, the back-compat ASSERTION is proven by the live test's plain replay path (it calls the front door which still unpacks the 2-tuple) — the executor MAY add a tiny offline structural check (signature has keyword-only `publish_clock`/`static_topics` with the documented defaults; the no-kwargs path builds no tracker) without importing ROS.
    - LIVE (proven in Task 2's -m live test, restated here as the behavioral contract): with `publish_clock=True`, each `sink(item)` publishes a `/clock` `rosgraph_msgs/msg/Clock` whose `.clock` time equals `clock_stamp_ns(item.t_ns)`; with `static_topics={"/tf_static"}`, `tracker.record(item)` runs after each publish and `republish_static(...)` re-pushes `tracker.republish_items()` through the SAME sink.
  </behavior>
  <action>Extend `build_publish_sink` per RESEARCH §2b — DO NOT add a second sink; layer the new behavior INSIDE the existing one. Change the signature to `def build_publish_sink(node, *, publish_clock: bool = False, static_topics=frozenset()):` (keyword-only via the `*`). Keep the existing lazy `from rclpy.serialization import deserialize_message` / `from rosidl_runtime_py.utilities import get_message` at the top of the body (offline invariant — still lazy). Before defining `sink`: build `tracker = None` and, if `static_topics`, `tracker = StaticTracker(static_topics)` (import from `.fidelity import StaticTracker` — stdlib-only, can be a module-top import since fidelity.py is ROS-free; or import inside the body for consistency). Build `clock_pub = None` and, if `publish_clock`, lazily resolve the Clock class ONCE (`Clock = get_message("rosgraph_msgs/msg/Clock")`) and `clock_pub = node.create_publisher(Clock, "/clock", 10)` (a single publisher captured in the closure — NEVER per-item); also `from .fidelity import clock_stamp_ns`. In `sink(item)`: keep the existing get_message/create_publisher/deserialize_message/publish/`published["n"] += 1` mechanics UNCHANGED; THEN add — `if tracker is not None: tracker.record(item)` and `if clock_pub is not None: cmsg = Clock(); cmsg.clock.sec, cmsg.clock.nanosec = clock_stamp_ns(item.t_ns); clock_pub.publish(cmsg)`. Choose the LEAST-disruptive back-compatible return (Pitfall 2): keep `return sink, published` (the 2-tuple) and attach the tracker as `sink.tracker = tracker` (a function attribute) so both existing 2-tuple unpackings keep working AND callers that opted into static_topics can reach the tracker via `sink.tracker`. Add a module-level helper `def republish_static(sink) -> int:` (or a method) that does `tracker = getattr(sink, "tracker", None); if tracker is None: return 0; items = tracker.republish_items(); for it in items: sink(it); return len(items)` — callers invoke it AFTER `replayer.seek(...)` to re-prime a fresh scene (the scheduler is NOT involved — Pitfall 5: this is a publish-path concern layered around seek). Update the build_publish_sink docstring: document the keyword-only opt-ins (defaults off => today's behavior + the 2-tuple unpacking unchanged), the /clock piggy-back cadence, the static-tracking + republish_static helper, and that the /clock build stays LAZY (Pitfall 4) and this remains the SINGLE publish path (Pitfall — no second sink). Keep docstrings under 100 cols. Per CONTEXT/RESEARCH §3 Pitfalls 1/2/4/5/7: defaults off; back-compatible return; /clock lazy; static-republish layered around seek (scheduler.py UNCHANGED); thin boundary.</action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_offline_guard.py tests/test_replay_unit.py -x -q && PYTHONPATH="" uv run ruff check packages/rosbagger-replay/src/rosbagger_replay/replay.py && PYTHONPATH="" uv run ruff format --check packages/rosbagger-replay/src/rosbagger_replay/replay.py</automated>
  </verify>
  <done>build_publish_sink takes keyword-only publish_clock/static_topics (default off); the no-kwargs 2-tuple unpacking still works (both existing call sites unaffected); when enabled it records to a StaticTracker + publishes /clock (Clock.clock = clock_stamp_ns(item.t_ns)) inside the SAME sink; republish_static re-pushes tracker.republish_items() through the sink; all /clock + tracker ROS imports stay lazy in the body; the offline guard + the full scheduler/source unit suite are green; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Live (-m live) test — /clock received (SC1) + static re-published after a seek (SC2 live)</name>
  <files>tests/test_replay_fidelity_live.py</files>
  <action>Create `tests/test_replay_fidelity_live.py` MIRRORING `tests/test_replay_live.py` exactly: `rclpy = pytest.importorskip("rclpy")` at the top (skips the whole module in the ROS-free offline CI), `pytestmark = pytest.mark.live`, the belt-and-suspenders `sys.path` src-tree prepend, and the external-subscriber-subprocess recipe (each subscriber runs in its OWN process/rclpy context, prints READY then COUNT on stdout — Pitfall 4 subscriber-before-publisher + a ~1s DDS discovery settle). Two tests: (1) `test_clock_published_external_subscriber_receives` (SC1) — start an external subscriber subprocess that subscribes to `/clock` (`from rosgraph_msgs.msg import Clock`) and counts received msgs; then drive the publish path with `publish_clock=True` enabled. Since `replay_bag()` does not yet expose the clock flag at the front door, exercise the sink DIRECTLY in-process the way the panel will: `import rclpy; rclpy.init(); node = rclpy.create_node(...); from rosbagger_replay import build_publish_sink, load_items; sink, published = build_publish_sink(node, publish_clock=True); items = load_items(bag, default_typestore=get_typestore(Stores.ROS2_HUMBLE)); for it in items: sink(it)` then assert the external subscriber received >= 1 `/clock` message (one per published item). Use `write_ros2_sqlite_bag(tmp_path)` (the 3-topic fixture) and `default_typestore=get_typestore(Stores.ROS2_HUMBLE)` (Pitfall 6 — sqlite3 needs it). Tear down node/context in a finally. (2) `test_static_republished_after_seek_external_subscriber_receives` (SC2 live) — use `write_tf_bag(tmp_path, ros1=False)` (writes a latched `/tf_static` map->odom + dynamic `/tf`); start an external subscriber subprocess on `/tf_static` (`from tf2_msgs.msg import TFMessage`); in-process build `sink, published = build_publish_sink(node, static_topics=frozenset({"/tf_static"}))`, publish the items via the sink (so the tracker records the latest `/tf_static`), then simulate a seek by calling the republish helper `from rosbagger_replay import republish_static; republish_static(sink)` (re-pushing the tracked `/tf_static` through the sink); assert the external subscriber received the re-published `/tf_static` message. Reuse the `_SUBSCRIBER_SRC`-style template (parametrize the msg type/topic). Mark BOTH `-m live`. Add a module docstring noting these are TIER-2 LIVE (SC1 + SC2-live) proofs run in the ROS-sourced lane (`source /opt/ros/humble/setup.bash` + `uv run --with pyyaml`), skipped in offline CI; the offline gate does NOT need them (SC2's CI proof is the 20-01 pure StaticTracker unit test). Keep imports of `rosgraph_msgs`/`tf2_msgs` inside the subscriber subprocess source + after the importorskip in the parent (they resolve only in the sourced lane). NOTE per CONTEXT: this live test is NOT required to run for the offline gate — it is the ROS-lane proof.</action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_replay_fidelity_live.py -q --collect-only && PYTHONPATH="" uv run ruff check tests/test_replay_fidelity_live.py && PYTHONPATH="" uv run ruff format --check tests/test_replay_fidelity_live.py</automated>
  </verify>
  <done>tests/test_replay_fidelity_live.py exists, is collected-and-skipped under the offline PYTHONPATH="" lane (importorskip rclpy), is marked -m live, mirrors the external-subscriber-subprocess recipe, and contains the SC1 (/clock received) + SC2-live (static re-published after a seek, using write_tf_bag) tests; ruff check + format clean. The offline gate is unaffected (the live module is skipped). [The ROS-sourced lane runs it for SC1 + SC2-live sign-off — out of scope for the offline gate.]</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build_publish_sink return shape ↔ existing 2-tuple callers | replay.py:154 and replay_panel.py:351 unpack `sink, published`; a breaking return shape would crash both production callers — the new return MUST stay back-compatible when no kwargs are passed |
| rosbagger_replay ↔ ROS runtime | The /clock Clock build + the publisher creation are rclpy/rosidl touches; a stray module-top import would breach the offline invariant — they stay lazy inside the function body |
| publish path ↔ scheduler | Static re-publish is a publish-path concern layered AROUND seek; pushing it into the scheduler would add ROS to the pure scheduler.py (forbidden) — republish_static drives the sink, not the scheduler |
| bag bytes (untrusted) → deserialize/publish | item.cdr is deserialized + published; a malformed Clock stamp could mis-set sim time — clock_stamp_ns guarantees a well-formed (sec, nanosec) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-A | Tampering | a non-back-compatible return shape breaking `sink, published = build_publish_sink(node)` | mitigate | the 2-tuple `(sink, published)` return is preserved; the tracker is exposed via `sink.tracker` (a function attribute), so both existing 2-tuple unpackings keep working when no kwargs are passed — the offline scheduler/source suite + the existing live test exercise the no-kwargs path |
| T-20-02-B | Elevation/Isolation | a stray module-top rclpy/rosidl import for the /clock build | mitigate | get_message / create_publisher / Clock build stay LAZY inside the build_publish_sink body (Pitfall 4); test_offline_guard.py (test_import_replay_does_not_pull_ros) stays green |
| T-20-02-C | Denial of Service | creating a new /clock publisher per published item (publisher churn) | mitigate | the /clock publisher + the Clock class are resolved ONCE before the sink closure and captured; the sink only publishes, never re-creates — proven by the live test's bounded run |
| T-20-02-D | Tampering | static re-publish leaking ROS into the pure scheduler (scheduler.py change) | mitigate | republish_static is a publish-path helper driving the sink AFTER seek; scheduler.py is UNCHANGED (Pitfall 5) — the scheduler stays generic over .t_ns items |
| T-20-02-E | Repudiation | a flaky live test (DDS discovery race) giving a false pass/fail | mitigate | mirror the verified test_replay_live.py recipe: subscriber-before-publisher, READY handshake, ~1s discovery settle, bounded spin window, COUNT on stdout — the deterministic recipe Phase 13 already proved on the box |
| T-20-02-SC | Tampering | npm/pip/cargo installs | accept | ZERO new packages this plan — rclpy/rosgraph_msgs/tf2_msgs ship with the sourced ROS distro (not pip-installed); rosbags is the existing locked dep; no Package Legitimacy Audit needed; nothing to slopcheck |
</threat_model>

<verification>
- `PYTHONPATH="" uv run pytest tests/test_offline_guard.py tests/test_replay_unit.py -x -q` — `import rosbagger_replay` stays ROS-free; the full scheduler/source unit suite green (the no-kwargs build_publish_sink path unchanged).
- `PYTHONPATH="" uv run pytest tests/test_replay_fidelity_live.py -q --collect-only` — the live module is COLLECTED-AND-SKIPPED in the offline lane (importorskip rclpy), contributing nothing to the offline gate.
- `PYTHONPATH="" uv run ruff check ...` + `ruff format --check ...` on replay.py + tests/test_replay_fidelity_live.py — clean (line length 100; docstrings under 100).
- Phase gate (blended, OFFLINE): `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest --cov=rosbagger_core --cov=bagq --cov=rosbagger_desktop --cov-fail-under=80` — green at ≥80% (re-run once if the benign offscreen-exit Bus-error appears at teardown — Qt teardown artifact, not a failure; confirm the clean pass line or use --junitxml). The live test is NOT required for this gate.
- ROS-sourced lane (SC1 + SC2-live sign-off, NOT part of the offline gate): `source /opt/ros/humble/setup.bash && uv run --with pyyaml pytest tests/test_replay_fidelity_live.py -m live -v` — the external subscriber receives `/clock` (SC1) and the re-published `/tf_static` after a seek (SC2-live).
</verification>

<success_criteria>
- With `publish_clock=True`, `/clock` (`rosgraph_msgs/msg/Clock`) is published carrying bag-relative time (`clock_stamp_ns(item.t_ns)`) during replay — proven by the `-m live` subscriber test — SC1.
- With `static_topics` set, the latest `/tf_static` seen before a seek is re-published through the SAME sink after the seek so a fresh subscriber re-primes — proven by the `-m live` test (CI proof is the 20-01 pure StaticTracker unit) — SC2.
- Both behaviors are opt-in keyword args on `build_publish_sink` (the SINGLE publish path) with defaults OFF; the no-kwargs 2-tuple unpacking stays back-compatible (both existing call sites unaffected) — SC3 (library half).
- All /clock + tracker-record ROS imports stay lazy; `import rosbagger_replay` stays ROS-free; scheduler.py is UNCHANGED; the offline gate is green at ≥80% — SC4.
</success_criteria>

<output>
Create `.planning/phases/20-replay-rviz-fidelity-clock-and-static-republish/20-02-SUMMARY.md` when done.
</output>
