---
phase: 03-message-table-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-core/src/rosbagger_core/schema/model.py
  - packages/rosbagger-core/src/rosbagger_core/schema/names.py
  - tests/test_schema_names.py
autonomous: true
requirements: [QURY-01]
must_haves:
  truths:
    - "A topic /camera/image_raw maps to table name camera_image_raw"
    - "Two topics that sanitize to the same name get distinct, deterministic table names"
    - "Each flattened column is described by a ColumnDef carrying its name, Arrow type, ROS path, and heavy-blob flag"
    - "A whole topic's mapping is described by a TableSchema carrying table_name, topic, msgtype, and its ColumnDef list"
  artifacts:
    - path: "packages/rosbagger-core/src/rosbagger_core/schema/names.py"
      provides: "sanitize_table_name + TableNameResolver (collision-resolving)"
      contains: "def sanitize_table_name"
    - path: "packages/rosbagger-core/src/rosbagger_core/schema/model.py"
      provides: "ColumnDef + TableSchema backend-neutral model"
      contains: "class TableSchema"
    - path: "tests/test_schema_names.py"
      provides: "Unit tests for sanitization + collision resolution + model shape"
  key_links:
    - from: "schema/names.py TableNameResolver"
      to: "schema/names.py sanitize_table_name"
      via: "resolver calls sanitize then de-duplicates"
      pattern: "sanitize_table_name"
---

<objective>
Establish Phase 3's backend-neutral contracts: the topic-to-table-name sanitizer + a collision-resolving `TableNameResolver` (QURY-01), and the `ColumnDef` / `TableSchema` dataclasses that every later plan in this phase produces and that Phase 4 (`bagq tables`) and Phase 5 (`QueryBackend`) consume.

This is the interface-defining wave: it ships the public model and the name logic with zero `pyarrow`/`rosbags` dependency, so the next plans implement the AST→Arrow translation against a fixed contract instead of inventing it.

Purpose: Map each topic to one uniquely-named table (QURY-01) and pin the public schema model so downstream code (this phase's 03-02/03-03, then Phase 4/5) builds against a stable shape.
Output: `schema/names.py`, `schema/model.py`, and `tests/test_schema_names.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Research is VERIFIED — transcribe its sanitization rule and collision-resolver design; do not reinvent.
@.planning/phases/03-message-table-schema/03-RESEARCH.md

# The model fields are specified in the research's Component Responsibilities table.
@packages/rosbagger-core/src/rosbagger_core/reader/base.py

<interfaces>
<!-- Conventions the executor must mirror (no codebase exploration needed). -->

reader/__init__.py re-export style (light subpackage public API) — mirror for schema/__init__.py in 03-03:
```python
from .base import BagReader, Message
from .rosbags_reader import RosbagsReader
__all__ = ["BagReader", "Message", "RosbagsReader"]
```

Message record this phase turns into rows (reader/base.py), for ros_path/value awareness:
```python
@dataclass(frozen=True, slots=True)
class Message:
    topic: str
    t: int
    t_ns: int
    stamp: int | None
    msgtype: str
    msg: object
```

Research-specified model fields (Component Responsibilities table, schema/model.py):
- ColumnDef(name: str, arrow_type, ros_path: tuple[str, ...], is_heavy_blob: bool)
- TableSchema(table_name: str, topic: str, msgtype: str, columns: list[ColumnDef])
  helpers: arrow_schema(include=...), column_names(include=...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the ColumnDef / TableSchema model contract</name>
  <files>packages/rosbagger-core/src/rosbagger_core/schema/model.py</files>
  <read_first>
    - packages/rosbagger-core/src/rosbagger_core/schema/__init__.py (the empty seam — the package this lands in)
    - packages/rosbagger-core/src/rosbagger_core/reader/base.py (Message shape; mirror its `from __future__ import annotations` + frozen-dataclass style)
    - .planning/phases/03-message-table-schema/03-RESEARCH.md — "Recommended Project Structure" + "Component Responsibilities" table (the ColumnDef/TableSchema field list) and "Claude's Discretion" (dataclass vs NamedTuple is the executor's call)
  </read_first>
  <behavior>
    - A ColumnDef stores name (str), arrow_type (object — typed loosely so model.py needs no pyarrow import), ros_path (tuple[str, ...]), is_heavy_blob (bool).
    - A TableSchema stores table_name (str), topic (str), msgtype (str), columns (list[ColumnDef]).
    - TableSchema.column_names() returns the column names in declared order; with no include argument it omits columns where is_heavy_blob is True.
    - TableSchema.column_names(include={"data"}) re-includes a heavy-blob column whose name is in the include set.
    - The include key is the dotted column name (matches research Open Question 2 recommendation); document this in the method docstring.
    - arrow_schema(include=...) is declared as the seam Phase 5 drives; in THIS plan it MAY raise NotImplementedError or be a thin stub — the pyarrow build lands in 03-03. Document that 03-03 fills it. (Do not import pyarrow here.)
  </behavior>
  <action>
    Create `model.py` as a stdlib-only module (`from __future__ import annotations`; `from dataclasses import dataclass, field`). Define `ColumnDef` and `TableSchema` as frozen dataclasses matching the research Component Responsibilities table; type `arrow_type` as `object` so model.py imports NO pyarrow (keeps the offline-guard import graph and unit tests light). Implement `TableSchema.column_names(self, include: set[str] | None = None) -> list[str]` filtering out `is_heavy_blob` columns whose `name` is not in `include`. Declare `arrow_schema(self, include: set[str] | None = None)` and have it `raise NotImplementedError("filled in 03-03")` — the Arrow build is intentionally deferred so this plan stays pyarrow-free. Add a module docstring stating this is the backend-neutral public model consumed by Phase 4 (`bagq tables`) and Phase 5 (`QueryBackend`), and that `include` keys on the dotted column name (research Open Q2). Do NOT import duckdb anywhere (Phase 5 concern). NO fenced code in the shipped module beyond normal Python.
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_schema_names.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `model.py` defines `class ColumnDef` and `class TableSchema` with exactly the research fields; `arrow_type` typed as `object`; module imports neither `pyarrow` nor `duckdb` nor `rosbags`.
    - Behavior: `TableSchema(...).column_names()` omits a heavy-blob column by default and includes it when its name is passed in `include`.
    - Test command: `PYTHONPATH="" uv run pytest tests/test_schema_names.py -q` passes (the model tests live in this file per Task 3).
  </acceptance_criteria>
  <done>ColumnDef + TableSchema exist with the specified fields; column_names() honors the include set; no heavy-stack import in model.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sanitize_table_name + collision-resolving TableNameResolver</name>
  <files>packages/rosbagger-core/src/rosbagger_core/schema/names.py</files>
  <read_first>
    - packages/rosbagger-core/src/rosbagger_core/schema/model.py (the TableSchema this feeds; import nothing heavy)
    - .planning/phases/03-message-table-schema/03-RESEARCH.md — "Pattern 2: Table-name sanitization + deterministic collision resolution", "Code Examples §1", and "Pitfall 7" (collision + SQL-identifier edge cases)
  </read_first>
  <behavior>
    - sanitize_table_name("/camera/image_raw") == "camera_image_raw" (the canonical spec example).
    - sanitize_table_name strips exactly ONE leading "/", replaces remaining "/" with "_": "/a/b" -> "a_b", "/tf" -> "tf", "/imu" -> "imu", "/cmd_vel" -> "cmd_vel".
    - Any character outside [0-9A-Za-z_] becomes "_": "/a.b" -> "a_b".
    - An empty result (topic "/") becomes "topic"; a leading-digit result gets a "t_" prefix.
    - TableNameResolver.resolve(topic) returns a sanitized name; a second topic that sanitizes to an already-used name (case-insensitive comparison, since SQL folds case) gets a deterministic suffix ("a_b", then "a_b_2", ...).
    - The resolver records the topic -> table_name mapping (exposed for Phase 4's `bagq tables`) and is idempotent: resolving the SAME topic twice returns the SAME name.
  </behavior>
  <action>
    Create `names.py` as stdlib-only (`from __future__ import annotations`; `import re`). Implement `sanitize_table_name(topic: str) -> str` per research Pattern 2 / Code Examples §1: drop one leading "/", `replace("/", "_")`, `re.sub(r"[^0-9A-Za-z_]", "_", name)`, empty -> `"topic"`, leading-digit -> `"t_" + name`. Implement a stateful `TableNameResolver` class holding a `dict[str, str]` topic->name plus a set of used names compared case-insensitively (`name.lower()`); `resolve(topic)` returns the existing name if the topic was already resolved (idempotent), else sanitizes and, on a case-insensitive collision, appends `_2`, `_3`, ... until unique; expose the recorded mapping via a read accessor (e.g. a `mapping` property returning a copy) so Phase 4 can print it. Do NOT quote identifiers here — quoting via `sqlglot` is wired in 03-03 (Pattern 6); this module produces the raw safe-charset names only. NO pyarrow/rosbags/duckdb import.
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_schema_names.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `names.py` defines `def sanitize_table_name(topic: str) -> str` and `class TableNameResolver`; imports only `re` + stdlib (no pyarrow/rosbags/duckdb).
    - Behavior: `sanitize_table_name("/camera/image_raw") == "camera_image_raw"`; `/a/b` and `/a.b` resolve through one resolver to `a_b` and `a_b_2`; resolving the same topic twice is stable.
    - CLI assertion: the canonical example and the collision case are asserted by name in `tests/test_schema_names.py`.
  </acceptance_criteria>
  <done>sanitize_table_name matches the spec rule and edge cases; TableNameResolver de-duplicates case-insensitively and deterministically, idempotently, and records the topic->name mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit-test the model + names against the spec examples and fixtures' topics</name>
  <files>tests/test_schema_names.py</files>
  <read_first>
    - packages/rosbagger-core/src/rosbagger_core/schema/names.py (the module under test)
    - packages/rosbagger-core/src/rosbagger_core/schema/model.py (the model under test)
    - tests/test_reader.py (lines 29-62 — the established `from __future__ import annotations` + repo-root sys.path-insert harness convention this file MUST mirror; though this plan needs NO fixture bags, follow the import-ordering + `# noqa: E402` style)
    - .planning/phases/03-message-table-schema/03-RESEARCH.md — "Pitfall 7" (collision cases to assert)
  </read_first>
  <behavior>
    - Asserts sanitize_table_name on the fixtures' real topics: "/cmd_vel"->"cmd_vel", "/imu"->"imu", "/image"->"image", and the canonical "/camera/image_raw"->"camera_image_raw".
    - Asserts edge cases: "/" -> "topic"; a leading-digit topic ("/2d_scan") gains a "t_" prefix; "/a.b" -> "a_b".
    - Asserts TableNameResolver: feeding "/a/b" then "/a.b" yields "a_b" then "a_b_2"; resolving "/imu" twice returns "imu" both times; the recorded mapping includes every resolved topic.
    - Asserts the model: a ColumnDef round-trips its four fields; TableSchema.column_names() omits an is_heavy_blob=True column by default and includes it when its name is in the include set.
  </behavior>
  <action>
    Create `tests/test_schema_names.py` mirroring the harness style of `tests/test_reader.py` (module docstring; `from __future__ import annotations`; import `sanitize_table_name`, `TableNameResolver` from `rosbagger_core.schema.names` and `ColumnDef`, `TableSchema` from `rosbagger_core.schema.model`). Write the assertions enumerated in the behavior block as small, named test functions (one per concern: canonical sanitize, edge cases, resolver collisions, resolver idempotency, model column_names include/exclude). No fixture bags are needed (pure-Python units). Keep tests independent of pyarrow. These tests are what keeps the coverage gate (`--cov-fail-under=80`) satisfied for the new `names.py`/`model.py` lines within this plan.
  </action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_schema_names.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/test_schema_names.py` exists; imports the two new modules; no pyarrow import.
    - Behavior: all enumerated assertions pass, including `camera_image_raw`, the `a_b`/`a_b_2` collision, resolver idempotency, and the model include/exclude.
    - Test command: `PYTHONPATH="" uv run pytest tests/test_schema_names.py -q` is green.
  </acceptance_criteria>
  <done>The names + model are covered by passing unit tests asserting the spec examples, fixture topics, collision determinism, and the heavy-blob include/exclude on column_names().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bag content -> table name | Topic strings are untrusted external input that become SQL table identifiers in Phase 5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `sanitize_table_name` / `TableNameResolver` | mitigate | Restrict the output charset to `[0-9A-Za-z_]` via the `re.sub` allow-list and reject leading digits/empties, so a hostile topic (`/x"; DROP TABLE y;--`) cannot escape into a raw identifier; final SQL-time quoting via `sqlglot` is wired in 03-03 (Pattern 6) — this plan produces only quotable safe-charset names, never concatenates into SQL. |
| T-03-02 | Spoofing/Tampering | `TableNameResolver` collision handling | mitigate | Case-insensitive collision detection + deterministic suffixing prevents two distinct topics from silently aliasing to one table (which Phase 5 would resolve ambiguously); resolver is idempotent so the same topic never yields two names. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — `rosbags`/`pyarrow`/`sqlglot`/`duckdb` were locked + audited in Phase 1 (RESEARCH "Package Legitimacy Audit": all Approved, no `[ASSUMED]`/`[SUS]`/`[SLOP]`). Nothing to install or vet here. |
</threat_model>

<verification>
- `PYTHONPATH="" uv run pytest tests/test_schema_names.py -q` passes.
- `PYTHONPATH="" uv run python -c "import rosbagger_core"` still succeeds and pulls in NO pyarrow/rosbags (offline-guard import graph unchanged — model.py/names.py are stdlib-only and not imported by the top-level package).
- `sanitize_table_name("/camera/image_raw")` returns exactly `camera_image_raw`.
</verification>

<success_criteria>
- QURY-01 satisfied: every topic maps to one sanitized, collision-resolved table name (`/camera/image_raw` -> `camera_image_raw`); the topic->name mapping is recorded for Phase 4.
- The public `ColumnDef` / `TableSchema` model exists with the research-specified fields and a working `column_names(include=...)` heavy-blob filter.
- `names.py` and `model.py` import no heavy stack (pyarrow/rosbags/duckdb); the offline invariant is untouched.
- New code is covered by `tests/test_schema_names.py` (coverage gate `--cov-fail-under=80` held within the plan).
</success_criteria>

<output>
Create `.planning/phases/03-message-table-schema/03-01-SUMMARY.md` when done.
</output>
