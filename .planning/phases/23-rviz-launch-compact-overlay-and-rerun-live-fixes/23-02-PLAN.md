---
phase: 23-rviz-launch-compact-overlay-and-rerun-live-fixes
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py
  - tests/test_desktop.py
autonomous: true
requirements: [VIZ-SKIP]
must_haves:
  truths:
    - "ReplayPanel gains `‹ 5s` and `5s ›` time-skip buttons on the control bar (near the existing Step button). Each calls a new `_skip(delta_s: float)` that ensure_transport, reads the live time cursor (position_fraction * _bag_span_ns), seeks to clamp(cur + delta_s*1e9, 0, _bag_span_ns) via the thread-safe Replayer.seek, and updates the playhead. Distinct from Step (one message); works while playing OR paused."
    - "_skip re-primes static topics after the seek when the 'Re-publish static on seek' toggle is on (same as _on_seeked) so RViz/Rerun re-prime, and sets a teaching status like 'Skipped to {pct}%.'"
    - "ReplayPanel exposes a small REMOTE-CONTROL API for the overlay (plan 23-04): `positionChanged = Signal(float)` emitted inside _update_position right after scrubber.set_position; `toggle_play()` (play if not genuinely playing else pause); `skip_back()`/`skip_forward()` (call _skip(-5)/_skip(+5)); `seek_fraction(f)` (delegates to _on_seeked); `current_fraction()` (position_fraction or 0.0). These are thin wrappers over existing handlers — no new transport logic."
    - "New buttons have accessors (skip_back_button/skip_forward_button) mirroring the existing play_button/step_button @property pattern; wired via clicked.connect in the existing connect block."
    - "Offline desktop tests prove: the two skip buttons exist + are clickable; clicking 5s› after building a transport moves position_fraction forward by ~5s/span (within tolerance) and ‹5s moves it back, clamped at [0,1]; toggle_play flips play/pause; positionChanged fires when _update_position runs."
  artifacts:
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py"
      provides: "_skip(delta_s); ‹5s/5s› QPushButtons + skip_back_button/skip_forward_button accessors; positionChanged Signal emitted in _update_position; toggle_play/skip_back/skip_forward/seek_fraction/current_fraction public methods"
      contains: "def _skip"
    - path: "tests/test_desktop.py"
      provides: "offline: skip buttons present+clickable; ±5s seek moves the playhead the right direction (clamped); toggle_play flips state; positionChanged emitted"
      contains: "skip_forward_button"
  key_links:
    - from: "5s› / ‹5s buttons"
      to: "relative time seek"
      via: "_skip(delta): cur=position_fraction*_bag_span_ns; new=min(max(cur+delta*1e9,0),_bag_span_ns); _replayer.seek(int(new)); _update_position()"
      pattern: "def _skip"
    - from: "_update_position"
      to: "overlay sync signal"
      via: "self._scrubber.set_position(fraction); self.positionChanged.emit(fraction)"
      pattern: "self\\.positionChanged\\.emit"
---

<objective>
Add `‹ 5s` / `5s ›` time-skip buttons to the Replay control bar (you asked for these on the normal
GUI too), and expose a small **remote-control API** on `ReplayPanel` (`positionChanged` signal +
`toggle_play`/`skip_back`/`skip_forward`/`seek_fraction`/`current_fraction`) that the compact overlay
(plan 23-04) will drive. Skip = a relative time seek over the thread-safe `Replayer.seek`; the API
methods are thin wrappers over existing handlers — no new transport logic, panel stays a thin face.

Requirement **VIZ-SKIP**: ±5 s skip works playing or paused and re-primes statics; the overlay-facing
API is ready for 23-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-rviz-launch-compact-overlay-and-rerun-live-fixes/23-CONTEXT.md
@.planning/phases/23-rviz-launch-compact-overlay-and-rerun-live-fixes/23-RESEARCH.md

# The panel. Control-bar build (~:141-149), connect block (~:196-207), button accessors (~:256-284),
# _on_seeked (~:733-768) [the seek + static-republish path to mirror in _skip], _update_position
# (~:770-780) [add positionChanged.emit here], _play/_pause/_genuinely_playing (~:643-676, 626-641)
# [toggle_play uses these], _ensure_transport (~:370-467), _bag_span_ns.
@packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py
# Scrubber.set_position / position / seeked — the overlay (23-04) reuses this widget against the API.
@packages/rosbagger-desktop/src/rosbagger_desktop/widgets/scrubber.py
@tests/test_desktop.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ±5s skip buttons + _skip + remote-control API</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py, tests/test_desktop.py</files>
  <read_first>packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py, packages/rosbagger-desktop/src/rosbagger_desktop/widgets/scrubber.py, tests/test_desktop.py</read_first>
  <action>In `replay_panel.py`: (1) Import `Signal` from PySide6.QtCore (it currently imports Qt/QThread/QTimer — add Signal). Declare a class-level `positionChanged = Signal(float)` on ReplayPanel (after the docstring, before __init__, like Scrubber declares `seeked`). (2) Buttons — after `self._step_button` (~:133) add `self._skip_back_button = QPushButton("‹ 5s")` and `self._skip_fwd_button = QPushButton("5s ›")`; in the control_bar build add them right after the Step button (`control_bar.addWidget(self._skip_back_button)`, `control_bar.addWidget(self._skip_fwd_button)`) so the order is Play/Pause/Step/‹5s/5s›/rate/loop/…/Rerun. (3) Wire — in the connect block add `self._skip_back_button.clicked.connect(self.skip_back)` and `self._skip_fwd_button.clicked.connect(self.skip_forward)`. (4) Accessors — add `skip_back_button`/`skip_forward_button` @property (mirror `step_button`). (5) `_skip(self, delta_s: float)`: `if not self._ensure_transport(): return`; `cur = self._replayer.position_fraction * self._bag_span_ns`; `new = min(max(cur + delta_s * 1e9, 0.0), float(self._bag_span_ns))`; `self._replayer.seek(int(new))`; `self._update_position()`; if `self._static_seek_checkbox.isChecked() and self._sink is not None: from rosbagger_replay import republish_static; republish_static(self._sink)`; `set_status(self._status, f"Skipped to {self._replayer.position_fraction*100:.0f}%.")`. (6) Public API: `skip_back(self)` → `self._skip(-5.0)`; `skip_forward(self)` → `self._skip(5.0)`; `toggle_play(self)` → if `self._genuinely_playing(): self._pause()` else `self._play()`; `seek_fraction(self, f: float)` → `self._on_seeked(f)`; `current_fraction(self) -> float` → `self._replayer.position_fraction if self._replayer is not None else 0.0`. (7) In `_update_position`, after `self._scrubber.set_position(fraction)`, add `self.positionChanged.emit(fraction)` (so the overlay scrubber tracks live). Keep everything ROS-lazy (no new module-top imports beyond Signal/QPushButton which are already Qt). Add offline tests to `tests/test_desktop.py`: `test_replay_skip_buttons_present` — `skip_back_button`/`skip_forward_button` exist with the right labels and are enabled; `test_replay_skip_seeks_relative` — build a transport on a fixture bag (reuse the live-or-fake setup the existing seek test uses; if the existing offline tests fake the replayer, fake one with a settable position_fraction + a recording seek), set the playhead mid-bag, `panel.skip_forward()` advances `position_fraction` by ~5s/span, `panel.skip_back()` reduces it, both clamped to [0,1]; `test_replay_toggle_play_flips` — toggle_play from idle plays (status 'Playing…'), called again pauses; `test_replay_position_changed_emitted` — connect a slot to `panel.positionChanged`, call `_update_position` with a built transport, assert the slot received the fraction. Use `panel.show()` + `button.click()` for the button-present test. (If the offline suite has no ROS to build a real Replayer, gate the seek-math tests behind the same fake-replayer approach already used by existing offline replay tests — read test_desktop.py first to match the established pattern, e.g. a monkeypatched _ensure_transport that installs a fake _replayer/_bag_span_ns.)</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -x -q && PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py tests/test_desktop.py && PYTHONPATH="" uv run ruff format --check packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py && PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run python -c "import rosbagger_desktop.panels.replay_panel, sys; assert 'rclpy' not in sys.modules"</automated>
  </verify>
  <done>‹5s/5s› buttons seek ±5s relative over the thread-safe Replayer.seek (clamped, static-reprimed when enabled), distinct from Step; positionChanged fires in _update_position; toggle_play/skip_back/skip_forward/seek_fraction/current_fraction are thin public wrappers; offline desktop tests + ruff green; replay_panel stays ROS-free at module top.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-23-02-A | Tampering | _skip racing the drive worker (mutating cursor while run() reads it) | mitigate | seek is thread-safe since Phase 18-01 (lock + wake); _skip only calls the same seek the live scrubber already uses mid-play |
| T-23-02-B | Correctness | skip past bag ends / before start | mitigate | clamp to [0, _bag_span_ns]; seek-past-end lands a clean DONE (documented) |
| T-23-02-C | Isolation | adding a ROS import at module top for republish_static | mitigate | republish_static imported lazily inside _skip (same as _on_seeked) |
</threat_model>

<verification>
- `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -x -q` — skip buttons present; ±5s relative seek (clamped); toggle_play flips; positionChanged emitted; module ROS-free.
- Phase-partial gate (OFFLINE): `... --cov=rosbagger_desktop --cov-fail-under=80` green.
- ROS lane: `uv run rosbagger-desktop <bag>` → Replay → Play → `5s ›`/`‹ 5s` jump the playhead ±5s and RViz/Rerun (if open) reflect it.
</verification>

<success_criteria>
- ±5s skip buttons on the normal Replay bar seek relative time (playing or paused), clamped, static-reprimed when enabled (VIZ-SKIP).
- ReplayPanel exposes positionChanged + toggle_play/skip_back/skip_forward/seek_fraction/current_fraction for the overlay (23-04), all thin wrappers; offline suite green; panel stays a thin ROS-free face.
</success_criteria>

<output>
Create `.planning/phases/23-rviz-launch-compact-overlay-and-rerun-live-fixes/23-02-SUMMARY.md` when done.
</output>
