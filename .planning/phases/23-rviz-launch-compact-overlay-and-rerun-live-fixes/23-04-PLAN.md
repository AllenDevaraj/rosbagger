---
phase: 23-rviz-launch-compact-overlay-and-rerun-live-fixes
plan: 04
type: execute
wave: 4
depends_on: [23-02, 23-03]
files_modified:
  - packages/rosbagger-desktop/src/rosbagger_desktop/widgets/overlay.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/widgets/__init__.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py
  - tests/test_desktop.py
autonomous: true
requirements: [VIZ-OVERLAY]
must_haves:
  truths:
    - "A frameless, always-on-top OverlayWindow(QWidget) mini-player: flags Qt.FramelessWindowHint | Qt.WindowStaysOnTopHint | Qt.Tool; layout = `‹ 5s` · `⏯` play/pause · `5s ›` · a Scrubber · `⛶` restore · `✕` close. It is constructed with (replay_panel, main_window) and remote-controls the EXISTING Replayer through 23-02's API — no second transport."
    - "Overlay wiring: its Scrubber.seeked → panel.seek_fraction; ‹5s/⏯/5s› buttons → panel.skip_back/toggle_play/skip_forward; panel.positionChanged → overlay scrubber.set_position (live playhead). ⛶ → main_window.exit_overlay(); ✕ → main_window.close() (QUIT — existing teardown stops replay + closes RViz/Rerun via atexit/closeEvent). The frameless window is draggable (mousePress stores the offset, mouseMove calls self.move)."
    - "The trigger is a menu-bar TOP-RIGHT corner control (menuBar().setCornerWidget(btn, Qt.TopRightCorner)). On click: if the active stacked panel is the Replay panel → main_window.enter_overlay() (collapse to overlay); otherwise → self.showMinimized() (textbook OS minimize, like any other window)."
    - "enter_overlay(): save main-window geometry, build the overlay lazily (once), sync its scrubber to panel.current_fraction(), hide the main window, show the overlay (positioned near the bottom-center of the screen). exit_overlay(): hide the overlay, showNormal() + restoreGeometry() + raise the main window. MainWindow.closeEvent also closes the overlay so it never lingers and the app can quit."
    - "Dragging the overlay scrubber seeks the live Replayer → republished to ROS → RViz/Rerun update live (inherited from 23-02's seek path); the overlay needs no ROS/transport code of its own."
    - "Invariants: overlay.py imports ONLY PySide6 + stdlib + the local Scrubber (no ROS, no rerun); main_window stays ROS-free at module top; offline-import guard still green."
  artifacts:
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/widgets/overlay.py"
      provides: "OverlayWindow(panel, main_window): frameless stay-on-top mini-player (‹5s/⏯/5s›/Scrubber/⛶/✕) wired to the panel API + draggable"
      contains: "class OverlayWindow"
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py"
      provides: "menu-bar corner-widget trigger; _on_minimize_clicked (overlay on Replay tab / showMinimized elsewhere); enter_overlay/exit_overlay (geometry save/restore); closeEvent closes the overlay"
      contains: "def enter_overlay"
    - path: "tests/test_desktop.py"
      provides: "offline: trigger enters overlay on Replay tab + minimizes elsewhere; overlay controls call the panel API; positionChanged moves the overlay scrubber; restore returns to the window; close quits"
      contains: "OverlayWindow"
  key_links:
    - from: "menu-bar corner button click"
      to: "context-dependent collapse vs minimize"
      via: "_on_minimize_clicked: if self._stack.currentWidget() is self.replay_panel: self.enter_overlay() else: self.showMinimized()"
      pattern: "def _on_minimize_clicked"
    - from: "overlay controls"
      to: "the existing Replayer (via 23-02 API)"
      via: "scrubber.seeked→panel.seek_fraction; buttons→panel.skip_back/toggle_play/skip_forward; panel.positionChanged→overlay.scrubber.set_position"
      pattern: "panel\\.positionChanged"
---

<objective>
Add the **compact overlay** mini-player: a frameless, always-on-top window with `‹ 5s` / play-pause /
`5s ›` / the Scrubber / restore / close that remote-controls the *existing* Replayer via 23-02's API.
Entered from a **menu-bar top-right corner control** that collapses to the overlay on the Replay tab and
does a normal `showMinimized()` everywhere else. You open RViz/Rerun, collapse the GUI to this thin
slider, and scrub — RViz updates live (the seek republishes to ROS). `✕` quits; `⛶` restores the full
window.

Requirement **VIZ-OVERLAY**: the overlay drives the live Replayer with no second transport; the trigger
is context-dependent; restore/quit work; invariants hold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-rviz-launch-compact-overlay-and-rerun-live-fixes/23-CONTEXT.md
@.planning/phases/23-rviz-launch-compact-overlay-and-rerun-live-fixes/23-RESEARCH.md

# The panel API the overlay drives (added in 23-02): positionChanged signal, toggle_play, skip_back,
# skip_forward, seek_fraction, current_fraction. The overlay holds a ref to the panel + calls these.
@packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py
# The Scrubber the overlay reuses (its own instance): seeked signal + set_position. Theme-styled.
@packages/rosbagger-desktop/src/rosbagger_desktop/widgets/scrubber.py
@packages/rosbagger-desktop/src/rosbagger_desktop/widgets/__init__.py
# The shell: _nav/_stack, self.replay_panel, _build_menu (~:220-238) [add the corner widget after the
# menus], closeEvent (~:302-307) [close the overlay too]. self.window() reaches this from the panel.
@packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py
@tests/test_desktop.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: OverlayWindow mini-player widget</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/widgets/overlay.py, packages/rosbagger-desktop/src/rosbagger_desktop/widgets/__init__.py, tests/test_desktop.py</files>
  <read_first>packages/rosbagger-desktop/src/rosbagger_desktop/widgets/scrubber.py, packages/rosbagger-desktop/src/rosbagger_desktop/widgets/__init__.py, packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py, tests/test_desktop.py</read_first>
  <action>Create `widgets/overlay.py` (module top: PySide6 + stdlib + `from .scrubber import Scrubber` only — NO ROS, NO rerun). `class OverlayWindow(QWidget)` with `__init__(self, panel, main_window, parent=None)`: store `self._panel = panel`, `self._main = main_window`; set window flags `Qt.FramelessWindowHint | Qt.WindowStaysOnTopHint | Qt.Tool`; set a small fixed height (~56px) and a sensible min width (~520px); objectName 'overlay_window' (so the theme can style it). Build a horizontal layout: `self._back = QToolButton(text='‹ 5s')`, `self._play = QToolButton(text='⏯')`, `self._fwd = QToolButton(text='5s ›')`, `self._scrubber = Scrubber()`, `self._restore = QToolButton(text='⛶')`, `self._close = QToolButton(text='✕')`; scrubber gets stretch. Wire: `self._back.clicked.connect(panel.skip_back)`, `self._play.clicked.connect(panel.toggle_play)`, `self._fwd.clicked.connect(panel.skip_forward)`, `self._scrubber.seeked.connect(panel.seek_fraction)`, `self._restore.clicked.connect(main_window.exit_overlay)`, `self._close.clicked.connect(main_window.close)`, and `panel.positionChanged.connect(self._scrubber.set_position)` (live playhead). Add `sync()` → `self._scrubber.set_position(panel.current_fraction())` (called on show by enter_overlay). Implement frameless dragging: `mousePressEvent` stores `self._drag_offset = event.globalPosition().toPoint() - self.frameGeometry().topLeft()` on left-button; `mouseMoveEvent` if dragging `self.move(event.globalPosition().toPoint() - self._drag_offset)`; `mouseReleaseEvent` clears it. Expose accessors `scrubber`, `play_button`, `skip_back_button`, `skip_forward_button`, `restore_button`, `close_button` (@property) for tests. Re-export OverlayWindow from `widgets/__init__.py`. Offline tests in `tests/test_desktop.py`: `test_overlay_controls_drive_panel` — build a MainWindow (offscreen) on a fixture bag (or a fake panel exposing the 23-02 API: skip_back/skip_forward/toggle_play/seek_fraction recording calls + a positionChanged Signal + current_fraction); construct OverlayWindow(panel, main); assert clicking back/play/fwd calls panel.skip_back/toggle_play/skip_forward; emitting `panel.positionChanged.emit(0.5)` moves `overlay.scrubber.position` to ~0.5; `overlay.scrubber.seeked.emit(0.25)` (or a real drag) calls panel.seek_fraction(0.25). Use real ReplayPanel from a MainWindow if simplest (it already exposes the API after 23-02) and just assert the wiring via monkeypatching the panel methods to recorders.</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -x -q -k "overlay" && PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src/rosbagger_desktop/widgets/overlay.py && PYTHONPATH="" uv run ruff format --check packages/rosbagger-desktop/src/rosbagger_desktop/widgets/overlay.py && PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run python -c "import rosbagger_desktop.widgets.overlay, sys; assert not {'rclpy','rerun'} & set(sys.modules)"</automated>
  </verify>
  <done>OverlayWindow is a frameless stay-on-top mini-player (‹5s/⏯/5s›/Scrubber/⛶/✕) wired to the panel's 23-02 remote-control API, draggable, with a live scrubber via positionChanged; it imports no ROS/rerun; offline overlay tests + ruff green.</done>
</task>

<task type="auto">
  <name>Task 2: Corner-widget trigger + enter/exit overlay + smart minimize</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py, tests/test_desktop.py</files>
  <read_first>packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py, packages/rosbagger-desktop/src/rosbagger_desktop/widgets/overlay.py, tests/test_desktop.py</read_first>
  <action>In `main_window.py`: (1) After `_build_menu()` in __init__ (or at the end of `_build_menu`), add a top-right corner control: `self._overlay_button = QToolButton(); self._overlay_button.setText("⤓"); self._overlay_button.setToolTip("Compact overlay (Replay) / minimize"); self.menuBar().setCornerWidget(self._overlay_button, Qt.TopRightCorner); self._overlay_button.clicked.connect(self._on_minimize_clicked)`. Import QToolButton from PySide6.QtWidgets and Qt is already imported. (2) Add `self._overlay = None` and `self._saved_geometry = None` in __init__. (3) `_on_minimize_clicked(self)`: `if self._stack.currentWidget() is self.replay_panel: self.enter_overlay()` else `self.showMinimized()`. (4) `enter_overlay(self)`: `self._saved_geometry = self.saveGeometry()`; if `self._overlay is None`: `from .widgets.overlay import OverlayWindow; self._overlay = OverlayWindow(self.replay_panel, self)`; `self._overlay.sync()`; position it near bottom-center of the available screen (use `self.screen().availableGeometry()` to compute x/y; guard for None screen in headless → just move(100,100)); `self.hide(); self._overlay.show(); self._overlay.raise_(); self._overlay.activateWindow()`. (5) `exit_overlay(self)`: `if self._overlay is not None: self._overlay.hide()`; `self.showNormal()`; `if self._saved_geometry is not None: self.restoreGeometry(self._saved_geometry)`; `self.raise_(); self.activateWindow()`. (6) closeEvent — before the existing reader-close, add `if self._overlay is not None: self._overlay.close(); self._overlay = None`. Add `overlay`/`overlay_button` @property accessors for tests (overlay returns self._overlay). Offline tests in `tests/test_desktop.py`: `test_corner_button_enters_overlay_on_replay` — build MainWindow on a fixture bag, select the Replay nav row (`window._nav.setCurrentRow(<replay index>)` or set current widget), `window.overlay_button.click()`; assert `window.overlay is not None` and `window.overlay.isVisible()` and `not window.isVisible()`; `test_corner_button_minimizes_off_replay` — select the Inspect row, monkeypatch `window.showMinimized` to a recorder, click → recorder called once and `window.overlay is None`; `test_exit_overlay_restores_window` — after entering overlay, `window.exit_overlay()` → overlay hidden + `window.isVisible()`; `test_overlay_close_button_quits` — after entering overlay, monkeypatch `window.close` to a recorder, `window.overlay.close_button.click()` → recorder called (✕ = quit). Use offscreen; visibility asserts may be flaky offscreen — prefer asserting object state (`window.overlay is not None`, hide/show called) and use the monkeypatch-recorder approach for showMinimized/close where visibility is unreliable.</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -x -q && PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py tests/test_desktop.py && PYTHONPATH="" uv run ruff format --check packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py && PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run python -c "import rosbagger_desktop.main_window, sys; assert 'rclpy' not in sys.modules"</automated>
  </verify>
  <done>A menu-bar top-right corner control collapses to the overlay on the Replay tab and does a normal showMinimized() elsewhere; enter/exit_overlay save+restore geometry and hide/show the main window; ✕ quits, ⛶ restores; closeEvent closes the overlay; offline tests + ruff green; main_window stays ROS-free at module top.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-23-04-A | Availability | overlay lingering as an orphan top-level window keeping the app alive after the main window closes | mitigate | MainWindow.closeEvent closes self._overlay; overlay is parented logically via main_window ref |
| T-23-04-B | Correctness | overlay scrubber feedback loop (set_position re-emitting seeked) | mitigate | Scrubber.set_position suppresses the seeked emit (existing _suppress_emit guard) — same as the panel |
| T-23-04-C | Isolation | overlay.py importing ROS/rerun and breaking the offline graph | mitigate | overlay imports only PySide6 + the local Scrubber; offline-guard python -c asserts no rclpy/rerun |
| T-23-04-D | Correctness | second transport / duplicated Replayer in the overlay | mitigate | overlay holds NO replayer — it calls the panel's API which owns the single Replayer |
</threat_model>

<verification>
- `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -x -q` — overlay controls drive the panel API; positionChanged moves the overlay scrubber; trigger enters overlay on Replay / minimizes elsewhere; restore + ✕-quit; ROS-free imports.
- Phase gate (OFFLINE, full suite): `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest --cov=rosbagger_core --cov=bagq --cov=rosbagger_desktop --cov-fail-under=80` green (re-run once on the benign offscreen Bus-error).
- Offline-import guard: `import rosbagger_desktop`, `rosbagger_desktop.widgets.overlay`, `rosbagger_desktop.main_window` pull no rclpy/rosbags/rerun.
- ROS lane (manual, VIZ-OVERLAY sign-off): `uv run rosbagger-desktop <bag>` → Replay → Open in RViz → click the top-right ⤓ → the GUI collapses to the floating overlay → drag the slider → RViz translates live; ⛶ restores the window; on a non-Replay tab ⤓ minimizes normally; ✕ on the overlay quits (rviz2 closes too).
</verification>

<success_criteria>
- A frameless always-on-top overlay (‹5s/⏯/5s›/Scrubber/⛶/✕) remote-controls the existing Replayer; dragging its scrubber seeks live → RViz/Rerun update (VIZ-OVERLAY).
- The top-right corner control collapses to overlay on the Replay tab and minimizes normally elsewhere; ⛶ restores, ✕ quits; the overlay never orphans the app.
- overlay.py + main_window stay ROS/rerun-free at module top; full offline suite green at >=80%.
</success_criteria>

<output>
Create `.planning/phases/23-rviz-launch-compact-overlay-and-rerun-live-fixes/23-04-SUMMARY.md` when done.
</output>
