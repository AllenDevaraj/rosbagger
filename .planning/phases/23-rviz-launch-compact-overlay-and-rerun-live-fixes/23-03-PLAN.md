---
phase: 23-rviz-launch-compact-overlay-and-rerun-live-fixes
plan: 03
type: execute
wave: 3
depends_on: [23-02]
files_modified:
  - packages/rosbagger-desktop/src/rosbagger_desktop/rviz_config.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/rviz_session.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py
  - packages/rosbagger-desktop/pyproject.toml
  - tests/test_rviz_config.py
  - tests/test_desktop.py
  - tests/test_desktop_live.py
autonomous: true
requirements: [VIZ-RVIZ]
must_haves:
  truths:
    - "A PURE, offline-testable rviz_config.py (stdlib + yaml only, NO ROS, NO Qt) builds a valid .rviz config string from (topics, fixed_frame): build_rviz_config(topics: list[tuple[str,str]], fixed_frame: str) -> str. msgtype→display map: Image/CompressedImage→rviz_default_plugins/Image, PointCloud2→PointCloud2, LaserScan→LaserScan, TFMessage→TF, OccupancyGrid→Map, Marker→Marker, MarkerArray→MarkerArray, Odometry→Odometry, Path→Path; always prepend Grid + TF; each topic-bound display carries the topic name + Reliable/Volatile QoS matching the publisher; top-level Visualization Manager.Global Options.Fixed Frame=fixed_frame. A pick_fixed_frame(topics, tf_frames=None) helper picks map>odom>base_link>first-frame>'map'."
    - "rviz_session.py launches + tracks rviz2 MIRRORING rosbagger_rerun/session.py: rviz_available()=shutil.which('rviz2') is not None; open_rviz(config_path)->subprocess.Popen(['rviz2','-d',config_path]) spawned non-detached with the NVIDIA-dGPU PRIME-offload env (_prefer_gpu reused/duplicated) + /proc child-PID tracking; close_rviz() SIGTERM tracked PIDs (idempotent) + atexit backstop. All stdlib (subprocess/shutil/os/signal) — ROS-free."
    - "ReplayPanel gains a checkable 'Open in RViz' button (near 'Open in Rerun'). Toggling ON: ROS-gate; if not rviz_available() -> teaching status 'RViz 2 not found — source your ROS 2 environment.' + uncheck (NO pip path). Else: AUTO-FIDELITY (check _clock_checkbox + _static_seek_checkbox; rebuild the transport if one already exists without a static tracker, modeled on closeEvent's pause+stop_thread); read the bag's topics OFF the UI thread (BlockingWorker over collect_bag_info); build_rviz_config; write a temp .rviz; rviz_session.open_rviz(cfg); then a delayed one-shot republish_static so the late-joining RViz re-primes /tf_static. Toggling OFF: rviz_session.close_rviz()."
    - "RViz subscribes to the topics the existing Replayer/build_publish_sink already publishes — NO second publish path is built; the config + launch is pure glue. open_rviz/close_rviz/rviz_available are reached via the module attribute so tests monkeypatch them."
    - "Auto-fidelity 'just works': because /clock + static tracking are on and republish_static fires after launch AND on every subsequent seek/skip (23-02 already re-primes on seek when static is on), a late-launched RViz gets /tf_static + sim time without manual toggling."
    - "Invariants: rviz_config.py is ROS-free AND Qt-free (offline-import guard extended); replay_panel stays ROS-free at module top (rviz_session/rviz_config imported lazily inside the handler); rviz2 process dies with the GUI (close on toggle-off + closeEvent + atexit)."
  artifacts:
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/rviz_config.py"
      provides: "build_rviz_config(topics, fixed_frame)->str (yaml); _DISPLAY_FOR msgtype map; pick_fixed_frame(...); pure (no ROS/Qt)"
      contains: "def build_rviz_config"
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/rviz_session.py"
      provides: "rviz_available(); open_rviz(config_path)->Popen (non-detached, GPU env, /proc PID tracking); close_rviz() SIGTERM+atexit"
      contains: "def open_rviz"
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py"
      provides: "self._rviz_button (checkable) + rviz_button accessor; _toggle_rviz/_open_rviz/_close_rviz; auto-fidelity rebuild; off-UI-thread topic read; delayed republish_static; closeEvent closes rviz"
      contains: "_toggle_rviz"
    - path: "tests/test_rviz_config.py"
      provides: "offline: each msgtype maps to the right display Class with its Topic; Grid+TF always present; Fixed Frame set; pick_fixed_frame heuristic; yaml round-trips"
      contains: "build_rviz_config"
    - path: "packages/rosbagger-desktop/pyproject.toml"
      provides: "dependencies += pyyaml>=6 (pure, ROS-free; for rviz_config)"
      contains: "pyyaml"
  key_links:
    - from: "_toggle_rviz(True), rviz available"
      to: "auto-fidelity + config + launch"
      via: "self._enable_rviz_fidelity(); topics=collect_bag_info(reader).topics (in a worker); cfg=write build_rviz_config(...); rviz_session.open_rviz(cfg); QTimer.singleShot(2500, lambda: republish_static(self._sink))"
      pattern: "rviz_session.open_rviz\\("
    - from: "msgtype"
      to: "rviz display class"
      via: "_DISPLAY_FOR = {'sensor_msgs/msg/Image':'rviz_default_plugins/Image', ...}; topic-bound displays carry Topic+QoS"
      pattern: "rviz_default_plugins/"
---

<objective>
Add an **Open in RViz** toggle to the Replay control bar that auto-launches `rviz2` subscribed to the
bag's viz topics. The Replayer already publishes real ROS 2 topics (Phase 13/20: `/clock` + `/tf_static`
republish for fidelity), so RViz just subscribes — no second publish path. We (1) generate a `.rviz`
config from the bag's topics (a **pure, offline-tested** builder), (2) launch + track `rviz2` exactly
like the Rerun viewer, and (3) **auto-enable fidelity** (`/clock` + static tracking + re-prime) so a
late-joining RViz "just works".

Requirement **VIZ-RVIZ**: one click opens RViz showing the bag's sensors/TF live; the viewer dies with
the GUI; missing `rviz2` teaches; invariants (offline graph ROS-free AND Qt-free; thin face) hold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-rviz-launch-compact-overlay-and-rerun-live-fixes/23-CONTEXT.md
@.planning/phases/23-rviz-launch-compact-overlay-and-rerun-live-fixes/23-RESEARCH.md

# The lifecycle to MIRROR for rviz_session.py: open_viewer/close_viewer/_child_pids/_track_viewers/
# _terminate_pid/_prefer_gpu/_nvidia_vulkan_present (non-detached spawn, /proc diff, SIGTERM, atexit,
# NVIDIA PRIME offload). rviz_session is the rviz2 analog (subprocess.Popen instead of rec.spawn).
@packages/rosbagger-rerun/src/rosbagger_rerun/session.py
# The panel. Add the button near _rerun_button (~:137-149), wire in the connect block (~:196-207),
# fidelity checkboxes _clock_checkbox/_static_seek_checkbox (~:177-185) [auto-check these], _ensure_transport
# (~:370-467) [publish_clock/static_topics baked here; sink.tracker], _on_seeked republish_static (~:765-768),
# closeEvent (~:299-316) [pause+stop_thread rebuild pattern + add _close_rviz], _sink, _drive_thread/_worker.
@packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py
# collect_bag_info(reader).topics -> TopicInfo(.topic, .msgtype). Run in a BlockingWorker (inspect does).
@packages/rosbagger-desktop/src/rosbagger_desktop/panels/inspect_panel.py
@packages/rosbagger-desktop/src/rosbagger_desktop/workers.py
@packages/rosbagger-desktop/pyproject.toml
@tests/test_desktop.py
@tests/test_desktop_live.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure rviz_config.py builder + offline unit tests</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/rviz_config.py, packages/rosbagger-desktop/pyproject.toml, tests/test_rviz_config.py</files>
  <read_first>packages/rosbagger-desktop/src/rosbagger_desktop/rviz_config.py, packages/rosbagger-rerun/src/rosbagger_rerun/converters.py, packages/rosbagger-desktop/pyproject.toml, tests/test_rviz_config.py</read_first>
  <action>Create `rviz_config.py` (module top: stdlib + `import yaml` only — NO PySide6, NO ROS). Define `_DISPLAY_FOR: dict[str,str]` mapping msgtype→rviz display class (sensor_msgs/msg/Image & sensor_msgs/msg/CompressedImage → "rviz_default_plugins/Image"; sensor_msgs/msg/PointCloud2 → "rviz_default_plugins/PointCloud2"; sensor_msgs/msg/LaserScan → "rviz_default_plugins/LaserScan"; tf2_msgs/msg/TFMessage → "rviz_default_plugins/TF"; nav_msgs/msg/OccupancyGrid → "rviz_default_plugins/Map"; visualization_msgs/msg/Marker → "rviz_default_plugins/Marker"; visualization_msgs/msg/MarkerArray → "rviz_default_plugins/MarkerArray"; nav_msgs/msg/Odometry → "rviz_default_plugins/Odometry"; nav_msgs/msg/Path → "rviz_default_plugins/Path"). Implement `_topic_display(name, cls, topic)` returning a display dict with at least `Class`, `Name`, `Enabled: True`, and `Topic: {"Value": topic, "Depth": 10, "Durability Policy": "Volatile", "Reliability Policy": "Reliable"}` (matching the publisher's RELIABLE/VOLATILE depth-10 QoS). TF display takes no Topic. Implement `pick_fixed_frame(topics, tf_frames=None) -> str`: if tf_frames given, return the first of ('map','odom','base_link') present, else the first frame; with no tf_frames, default 'map'. Implement `build_rviz_config(topics: list[tuple[str,str]], fixed_frame: str) -> str`: build a Displays list = [Grid display, TF display] + one display per topic whose msgtype is in _DISPLAY_FOR (skip unknown msgtypes; de-dup); wrap in `{"Panels": [...minimal Displays panel...], "Visualization Manager": {"Class": "", "Global Options": {"Fixed Frame": fixed_frame, "Frame Rate": 30}, "Displays": displays, "Tools": [{"Class": "rviz_default_plugins/MoveCamera"}], "Views": {"Current": {"Class": "rviz_default_plugins/Orbit"}}}}` and return `yaml.safe_dump(cfg, sort_keys=False)`. Add `"pyyaml>=6"` to rosbagger-desktop `[project].dependencies` in pyproject.toml (pure, ROS/Qt-free). Create `tests/test_rviz_config.py` (pure, no qtbot/ROS): assert each msgtype in the map yields a display with the correct Class and the topic in its Topic.Value; assert Grid + TF are always present even for an empty/unknown topic list; assert an unknown msgtype (e.g. std_msgs/msg/String) produces NO display (but Grid+TF remain); assert `build_rviz_config([...], 'odom')` sets Global Options.Fixed Frame to 'odom' and `yaml.safe_load` round-trips it; assert pick_fixed_frame prefers map>odom>base_link and falls back to 'map'.</action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_rviz_config.py -x -q && PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src/rosbagger_desktop/rviz_config.py tests/test_rviz_config.py && PYTHONPATH="" uv run ruff format --check packages/rosbagger-desktop/src/rosbagger_desktop/rviz_config.py && PYTHONPATH="" uv run python -c "import rosbagger_desktop.rviz_config, sys; assert not {'rclpy','PySide6','rosbags'} & set(sys.modules)"</automated>
  </verify>
  <done>build_rviz_config maps each supported msgtype to the right rviz_default_plugins display with QoS-matched Topic, always includes Grid+TF, sets Fixed Frame, skips unknown msgtypes, and yaml-round-trips; pick_fixed_frame heuristic correct; the module is ROS-free AND Qt-free; pyyaml is a desktop dep; offline tests + ruff green.</done>
</task>

<task type="auto">
  <name>Task 2: rviz_session.py launcher (mirror rerun session lifecycle)</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/rviz_session.py, tests/test_desktop.py</files>
  <read_first>packages/rosbagger-rerun/src/rosbagger_rerun/session.py, packages/rosbagger-desktop/src/rosbagger_desktop/rviz_session.py, tests/test_desktop.py</read_first>
  <action>Create `rviz_session.py` mirroring `rosbagger_rerun/session.py` for rviz2 (all stdlib; ROS-free). `rviz_available() -> bool`: `shutil.which("rviz2") is not None` (import shutil inside). `open_rviz(config_path: str) -> subprocess.Popen`: `_prefer_gpu()` (copy the NVIDIA Vulkan PRIME-offload helper + _nvidia_vulkan_present + _GPU_OFFLOAD_ENV/_NVIDIA_VULKAN_ICDS from rerun session, or import-and-reuse them from rosbagger_rerun.session if cleaner — duplicating keeps the desktop independent of rerun being installed; prefer duplication), then `proc = subprocess.Popen(["rviz2", "-d", config_path], start_new_session=False)`, track `proc.pid` in a module `_TRACKED_RVIZ_PIDS: set[int]` + install the `atexit.register(close_rviz)` backstop once (mirror `_track_viewers`), return proc. `close_rviz() -> None`: SIGTERM every tracked pid (idempotent, mirror `close_viewer`/`_terminate_pid`). Keep imports (subprocess/shutil/os/signal/atexit) inside bodies or module-top stdlib — never ROS. Add offline tests to `tests/test_desktop.py`: `test_rviz_available_reflects_which` — monkeypatch `shutil.which` to None → rviz_available() False, to a path → True; `test_open_rviz_invokes_popen` — monkeypatch `rviz_session.subprocess.Popen` to a fake recording the argv, call `open_rviz('/tmp/x.rviz')`, assert argv == `['rviz2','-d','/tmp/x.rviz']` and the fake pid is tracked; `test_close_rviz_terminates_tracked` — with a tracked fake pid, monkeypatch `os.kill` to record, `close_rviz()` SIGTERMs it and clears the set (idempotent on a second call).</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -x -q -k "rviz" && PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src/rosbagger_desktop/rviz_session.py && PYTHONPATH="" uv run ruff format --check packages/rosbagger-desktop/src/rosbagger_desktop/rviz_session.py && PYTHONPATH="" uv run python -c "import rosbagger_desktop.rviz_session, sys; assert 'rclpy' not in sys.modules"</automated>
  </verify>
  <done>rviz_session launches rviz2 -d cfg non-detached with GPU env + /proc-style PID tracking, closes via SIGTERM + atexit (idempotent), mirrors the rerun viewer lifecycle, stays ROS-free; offline tests (monkeypatched which/Popen/kill) + ruff green.</done>
</task>

<task type="auto">
  <name>Task 3: 'Open in RViz' button + auto-fidelity + launch + delayed re-prime + close</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py, tests/test_desktop.py, tests/test_desktop_live.py</files>
  <read_first>packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py, packages/rosbagger-desktop/src/rosbagger_desktop/panels/inspect_panel.py, packages/rosbagger-desktop/src/rosbagger_desktop/workers.py, tests/test_desktop.py, tests/test_desktop_live.py</read_first>
  <action>In `replay_panel.py`: (1) Button — after `self._rerun_button` add `self._rviz_button = QPushButton("Open in RViz"); self._rviz_button.setCheckable(True)`; place it in the control bar next to the Rerun button (both behind the addStretch so they read as 'view' actions). Wire `self._rviz_button.toggled.connect(self._toggle_rviz)`. Add a `rviz_button` @property. Add `self._rviz_topics_thread=None`, `self._rviz_topics_worker=None`, `self._rviz_proc=None` to __init__. (2) `_toggle_rviz(checked)`: if not checked → `self._close_rviz(); return`. ROS-gate (`if not self._ros_available(): teach + uncheck + return`). `from .. import rviz_session` (lazy) — `if not rviz_session.rviz_available(): set_status('RViz 2 not found — source your ROS 2 environment.', is_error=True); self._rviz_button.setChecked(False); return`. Call `self._enable_rviz_fidelity()` (below). Then read topics OFF the UI thread: build a BlockingWorker over `lambda: __import__('rosbagger_core.inspect', fromlist=['collect_bag_info']).collect_bag_info(reader).topics` (reader from `self.window().reader`; if None → teach + uncheck) — actually call a small local `_read_topics()` method that imports collect_bag_info inside and returns `[(ti.topic, ti.msgtype) for ti in collect_bag_info(reader).topics]`; run it via `run_on_thread(self, BlockingWorker(self._read_topics), on_result=self._on_rviz_topics, on_failed=..., on_finished=...)` keeping BOTH refs. (3) `_enable_rviz_fidelity()`: `self._clock_checkbox.setChecked(True); self._static_seek_checkbox.setChecked(True)`; if `self._replayer is not None and getattr(self._sink, "tracker", None) is None`: REBUILD modeled on closeEvent — `was_playing = self._genuinely_playing(); self._pending_action = None; if was_playing: self._replayer.pause(); self._position_timer.stop(); stop_thread(self._drive_thread); self._drive_thread=None; self._drive_worker=None`; `self._teardown_transport()`; `if not self._ensure_transport(): return`; `if was_playing: self._replayer.play(); self._start_drive()`. (4) `_on_rviz_topics(topics)`: `from ..rviz_config import build_rviz_config, pick_fixed_frame`; `cfg = build_rviz_config(topics, pick_fixed_frame(topics))`; write to a tempfile `.rviz` (keep the path on self for cleanup: `self._rviz_cfg_path`); `from .. import rviz_session; self._rviz_proc = rviz_session.open_rviz(path)`; `set_status('Opened RViz — subscribing to the bag topics (press Play).')`; schedule the re-prime: `QTimer.singleShot(2500, self._reprime_rviz_static)`. (5) `_reprime_rviz_static()`: `if self._static_seek_checkbox.isChecked() and self._sink is not None: from rosbagger_replay import republish_static; republish_static(self._sink)`. (6) `_close_rviz()`: `from .. import rviz_session; rviz_session.close_rviz()`; `self._rviz_proc = None`; clean up the temp cfg file (best-effort). (7) closeEvent — add `self._close_rviz()` and `stop_thread(self._rviz_topics_thread)` next to the existing rerun/install teardown. open_rviz/close_rviz/rviz_available reached via the `rviz_session` module attribute (monkeypatchable). Keep all rviz_session/rviz_config/rosbagger_core imports LAZY inside handlers (module top stays ROS-free). Offline desktop tests in `tests/test_desktop.py`: `test_rviz_button_present` — checkable `rviz_button` labelled 'Open in RViz'; `test_rviz_toggle_unavailable_teaches` — monkeypatch `rviz_session.rviz_available`→False, click → teaching status + unchecked + no Popen; `test_rviz_open_builds_config_and_launches` — monkeypatch `rviz_session.rviz_available`→True, `rviz_session.open_rviz`→a fake recording the config path, give the panel a window with a fake reader whose collect_bag_info returns Image+LaserScan+TF topics (monkeypatch collect_bag_info), `panel.rviz_button.click()`, `qtbot.waitUntil(lambda: panel._rviz_proc is not None)`, read the written cfg file and assert it contains rviz_default_plugins/Image + /LaserScan + /TF + the topic names; `test_rviz_enable_fidelity_checks_boxes` — clicking checks both fidelity checkboxes. In `tests/test_desktop_live.py` add `test_rviz_launch_smoke` (-m live, skipif `shutil.which('rviz2') is None`): on a real bag, monkeypatch nothing — `panel.rviz_button.click()`; wait until `_rviz_proc is not None`; assert `_rviz_proc.poll() is None` (process started) then `_close_rviz()`; (a full visual check is manual — rviz2 needs a display).</action>
  <verify>
    <automated>uv sync >/dev/null 2>&1 && PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py tests/test_rviz_config.py -x -q && PYTHONPATH="" uv run pytest tests/test_desktop_live.py -q --collect-only && PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py tests/test_desktop.py tests/test_desktop_live.py && PYTHONPATH="" uv run ruff format --check packages/rosbagger-desktop/src/rosbagger_desktop/panels/replay_panel.py && PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run python -c "import rosbagger_desktop.panels.replay_panel, sys; assert 'rclpy' not in sys.modules"</automated>
  </verify>
  <done>'Open in RViz' auto-enables fidelity (rebuilding the transport seamlessly if needed), reads topics off the UI thread, generates+writes a .rviz config, launches+tracks rviz2, schedules a /tf_static re-prime, and closes rviz on toggle-off/close; missing rviz2 teaches; no second publish path; offline desktop+config tests green, live smoke collected; replay_panel ROS-free at module top.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-23-03-A | DoS | collect_bag_info blocking the UI on a huge bag | mitigate | topic read runs in a BlockingWorker (kept refs), like inspect |
| T-23-03-B | Tampering | the auto-fidelity rebuild racing the drive worker | mitigate | rebuild uses the proven closeEvent pattern (pause wakes run() → worker returns → stop_thread joins) before teardown+ensure |
| T-23-03-C | Resource | leaked rviz2 process / temp .rviz on close | mitigate | close_rviz SIGTERM + atexit (mirror rerun); temp cfg cleaned in _close_rviz |
| T-23-03-D | Isolation | a module-top ROS/Qt import in rviz_config (it's offline-tested) | mitigate | rviz_config = stdlib + yaml only; offline-guard python -c asserts no rclpy/PySide6/rosbags |
| T-23-03-E | Elevation | launching an arbitrary 'rviz2' from PATH | accept | rviz2 from the user's sourced ROS env on explicit click; gated by shutil.which; no shell=True (argv list) |
</threat_model>

<verification>
- `PYTHONPATH="" uv run pytest tests/test_rviz_config.py -x -q` — pure builder: msgtype→display, Grid+TF always, Fixed Frame, unknown skipped, yaml round-trip, pick_fixed_frame.
- `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -x -q` — button present; unavailable teaches; open builds+launches (fake Popen) with the right config; fidelity boxes auto-checked; ROS-free import.
- `PYTHONPATH="" uv run pytest tests/test_desktop_live.py -q --collect-only` — live rviz smoke collects-and-skips offline.
- Phase-partial gate (OFFLINE): `... --cov=rosbagger_desktop --cov-fail-under=80` green.
- ROS lane (VIZ-RVIZ sign-off, manual): `uv run rosbagger-desktop <bag>` → Replay → Open in RViz → an rviz2 window opens with Image/PointCloud/LaserScan/TF displays bound to the bag topics; press Play (or scrub) → the displays update live; closing the GUI kills rviz2.
</verification>

<success_criteria>
- One click opens rviz2 auto-subscribed (via a generated .rviz) to the bag's viz topics, which the Replayer already publishes — no second publish path (VIZ-RVIZ).
- Fidelity auto-enables (/clock + static re-prime) so a late-launched RViz just works; missing rviz2 teaches; the rviz2 process dies with the GUI.
- rviz_config is ROS-free AND Qt-free (offline-tested); replay_panel stays a thin ROS-free face; offline suite green at >=80%.
</success_criteria>

<output>
Create `.planning/phases/23-rviz-launch-compact-overlay-and-rerun-live-fixes/23-03-SUMMARY.md` when done.
</output>
