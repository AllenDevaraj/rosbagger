---
phase: 21-replay-cli-parity-flags
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - packages/rosbagger-replay/src/rosbagger_replay/cli.py
  - tests/test_replay_unit.py
autonomous: true
requirements: [REP-05]
must_haves:
  truths:
    - "rosbagger-replay --help exposes --clock, --delay, --remap, --start-paused (and -p), --region-start, --region-end alongside the existing flags; --help exits 0 and builds with NO ROS import (offline-guard discipline)"
    - "--clock forwards publish_clock=True; --delay SECONDS forwards delay=<float>; --start-paused/-p forwards start_paused=True; --region-start SEC / --region-end SEC forward region_start/region_end=<float>; each is forwarded to the monkeypatched replay_bag spy (no ROS runs)"
    - "--remap is repeatable and each value is parsed old:=new into a dict: --remap a:=b --remap c:=d forwards remap={'a':'b','c':'d'}; with no --remap the CLI forwards remap=None"
    - "a malformed --remap value (no := , or an empty side) raises a clean typer error (non-zero exit, a teaching message, NO traceback escaping) — not a silent accept"
    - "the deferred runtime ROS services (~/seek, ~/set_rate, ~/play_next, ~/burst, ~/toggle_paused) AND the deferred single-pass bounded-region stop are documented as out-of-scope in the command docstring/help (SC3 — not silently missing)"
    - "cli.py stays THIN: each new flag maps to a replay()/build_publish_sink param forwarded to replay_bag; NO publish logic, NO rclpy, NO create_publisher in cli.py; the top level stays typer+stdlib (the ROS import stays behind the lazy from rosbagger_replay import replay_bag)"
    - "tests/test_offline_guard.py stays green (cli.py top level adds no ROS/Qt import); the existing CLI tests (--rate/--loop/--topics/--duration/--max-messages forwarding, no-messages clean Exit(1)) still pass"
  artifacts:
    - path: "packages/rosbagger-replay/src/rosbagger_replay/cli.py"
      provides: "typer options --clock (bool), --delay (float seconds), --remap (repeatable str parsed old:=new -> dict, malformed pair rejected with a clean typer error), --start-paused/-p (bool), --region-start/--region-end (float seconds), each forwarded to replay_bag(...) as publish_clock/delay/remap/start_paused/region_start/region_end; the command docstring/help documents the deferred runtime ROS services + the deferred single-pass region stop as out-of-scope"
      contains: "--start-paused"
    - path: "tests/test_replay_unit.py"
      provides: "CliRunner unit tests (mirror the existing cli tests ~:758): --help exposes every new flag (SC1) + documents the deferred services (SC3); each flag forwards the right kwarg to a monkeypatched rosbagger_replay.replay_bag spy; --remap a:=b --remap c:=d parses to {'a':'b','c':'d'}; a malformed --remap foo errors cleanly"
      contains: "start-paused"
  key_links:
    - from: "cli.py replay() command (--remap parsing)"
      to: "a dict forwarded as remap="
      via: "for each --remap value: split on ':=' into exactly two non-empty sides else raise typer.BadParameter; build {old: new}; forward remap=parsed or None"
      pattern: "\\.split\\(\":=\""
    - from: "cli.py replay() command"
      to: "replay_bag(..., publish_clock=clock, delay=delay, remap=..., start_paused=start_paused, region_start=region_start, region_end=region_end)"
      via: "forward each parsed flag as the matching keyword arg to the lazy replay_bag front door"
      pattern: "start_paused=start_paused"
    - from: "cli.py replay() command docstring"
      to: "the deferred runtime ROS services + single-pass region stop"
      via: "a docstring/help paragraph naming ~/seek, ~/set_rate, ~/play_next, ~/burst, ~/toggle_paused + the single-pass [in,out] stop as out-of-scope (the GUI provides interactive control)"
      pattern: "toggle_paused"
---

<objective>
Add the Phase-21 CLI-parity flags to the THIN `rosbagger-replay` CLI — `--clock`, `--delay`, `--remap` (repeatable `old:=new`), `--start-paused`/`-p`, `--region-start`, `--region-end` — each parsed and FORWARDED to the `replay_bag()` front door (the 21-01 library params). The CLI gains a `--remap` parser (`old:=new` → dict, malformed rejected cleanly) and a docstring/help paragraph documenting the deferred runtime ROS services + the deferred single-pass region stop as out-of-scope. NO publish logic enters `cli.py`; the ROS import stays behind the lazy `from rosbagger_replay import replay_bag`.

Mechanism (RESEARCH §2/§4, validated against cli.py): add `typer.Option`s to the single `replay()` command (`cli.py:92-172`) — `--clock` (bool → `publish_clock`), `--delay` (float seconds → `delay`), `--remap` (`list[str] | None`, repeatable → parse each `old:=new` into a dict → `remap`), `--start-paused`/`-p` (bool → `start_paused`), `--region-start` / `--region-end` (float|None seconds → `region_start`/`region_end`). Parse `--remap`: split each value on `:=` into exactly two non-empty sides, else `raise typer.BadParameter` (clean error, no traceback — caught as a usage error by typer); build `{old: new}`; forward `remap=parsed or None`. Forward every flag in the existing `replay_api(...)` call (`cli.py:160-171`). Document the deferred runtime services (`~/seek`, `~/set_rate`, `~/play_next`, `~/burst`, `~/toggle_paused`) + the single-pass `[in,out]` region stop as out-of-scope in the command docstring/help (SC3 — the GUI provides interactive control; mirrors the existing `--end`-folded-into-`--duration` documentation discipline, `cli.py:24-34`).

Purpose: This is the CLI surface of Phase 21 — the parity flags become visible (`--help`, SC1), map to the library (SC2), and the deferred controls are documented (SC3). The CLI stays a pure parser/forwarder over `replay_bag` (SC4 — thin). This is the phase gate.
Output: an extended `cli.py` (typer+stdlib only, thin) + CliRunner unit tests + the blended phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-replay-cli-parity-flags/21-CONTEXT.md
@.planning/phases/21-replay-cli-parity-flags/21-RESEARCH.md
@.planning/phases/21-replay-cli-parity-flags/21-01-SUMMARY.md

# The THIN CLI this plan extends (the ONLY production file changed). The single replay() command
# (~:92-172) holds the existing typer options; the lazy front-door import is at ~:158; the
# forwarding call is ~:160-171. Top level is typer+stdlib only (~:37-42) — keep it that way.
# The --end-folded-into-duration documentation precedent (~:24-34, ~:125-131) is the model for
# documenting the deferred region single-pass stop + the deferred runtime services.
@packages/rosbagger-replay/src/rosbagger_replay/cli.py
# The library params this plan forwards (21-01): replay() gained delay/start_paused/publish_clock/
# remap/region_start/region_end; build_publish_sink gained remap. Re-confirm the kwarg names.
@packages/rosbagger-replay/src/rosbagger_replay/replay.py
# The lazy front door the CLI calls (replay_bag = replay, ~:132) — forwards **kwargs to replay().
@packages/rosbagger-replay/src/rosbagger_replay/__init__.py
# Offline/ROS-free guard that MUST stay green (cli.py top level adds no ROS/Qt import).
@tests/test_offline_guard.py
# The EXACT CliRunner tests to MIRROR: app + _runner (~:758-764); test_cli_replay_help_* (~:767);
# test_cli_replay_parses_and_forwards_flags monkeypatching rosbagger_replay.replay_bag (~:786);
# test_cli_replay_topics_omitted_forwards_none (~:814); the clean-Exit(1) test (~:831). The new
# flag tests are the SAME shape: a fake_replay_bag spy recording **kwargs, invoke app, assert.
@tests/test_replay_unit.py

<interfaces>
<!-- Contracts: the 21-01 library params this CLI forwards + the existing CLI test recipe. -->

From 21-01 (rosbagger_replay.replay — the params this CLI forwards via replay_bag):
```python
def replay(bag_paths, *, topics=None, rate=1.0, loop=False, start=0.0,
           duration=None, max_messages=None, default_typestore=None,
           delay: float = 0.0, start_paused: bool = False, publish_clock: bool = False,
           remap: dict[str, str] | None = None,
           region_start: float | None = None, region_end: float | None = None) -> int: ...
# replay_bag = replay (front door) forwards **kwargs straight through.
```

EXISTING cli.py forwarding call (this plan APPENDS the new kwargs):
```python
n = replay_api(
    bag,
    topics=set(topics) if topics is not None else None,
    rate=rate, loop=loop, start=start, duration=duration, max_messages=max_messages,
    # APPEND: publish_clock=clock, delay=delay, remap=parsed_remap_or_none,
    #         start_paused=start_paused, region_start=region_start, region_end=region_end
)
```

NEW typer options (added to the single replay() command):
```python
clock: Annotated[bool, typer.Option("--clock", help="Publish /clock per message ...")] = False
delay: Annotated[float, typer.Option("--delay", help="Sleep N seconds before publishing ...")] = 0.0
remap: Annotated[list[str] | None, typer.Option("--remap", help="Remap old:=new (repeatable).")] = None
start_paused: Annotated[bool, typer.Option("--start-paused", "-p", help="Build transport, publish nothing until resumed (GUI).")] = False
region_start: Annotated[float | None, typer.Option("--region-start", help="Play from this SEC (with --region-end + --loop to repeat the snippet).")] = None
region_end: Annotated[float | None, typer.Option("--region-end", help="Play to this SEC (snippet repeat with --loop; single-pass stop deferred).")] = None
```

The --remap parse (in the command body — clean error, no traceback):
```python
parsed_remap: dict[str, str] | None = None
if remap:
    parsed_remap = {}
    for pair in remap:
        old, sep, new = pair.partition(":=")
        if not sep or not old or not new:
            raise typer.BadParameter(f"--remap expects old:=new, got {pair!r}")
        parsed_remap[old] = new
```

EXISTING CliRunner test recipe to MIRROR (tests/test_replay_unit.py ~:786):
```python
def test_cli_replay_parses_and_forwards_flags(monkeypatch):
    calls = {}
    def fake_replay_bag(bag, **kwargs):
        calls["bag"] = bag; calls.update(kwargs); return 3
    monkeypatch.setattr(rosbagger_replay, "replay_bag", fake_replay_bag)
    result = _runner.invoke(app, ["/tmp/bag", "--rate", "5", ...])
    assert result.exit_code == 0, result.output
    assert calls["rate"] == 5.0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --clock/--delay/--remap/--start-paused(-p)/--region-start/--region-end to the CLI; parse remap; forward to replay_bag; document deferred services</name>
  <files>packages/rosbagger-replay/src/rosbagger_replay/cli.py</files>
  <action>Extend the single `replay()` command in `cli.py` per RESEARCH §2 — THIN parser/forwarder only, NO publish logic. Add the new `typer.Option` params to the `replay()` signature (keep `Annotated`, mirror the existing options' style): `clock: Annotated[bool, typer.Option("--clock", help=...)] = False` (help: "Publish /clock (rosgraph_msgs/Clock) per message for use_sim_time downstream nodes."); `delay: Annotated[float, typer.Option("--delay", help=...)] = 0.0` (help: "Sleep N seconds after building the transport but BEFORE publishing, so subscribers can discover."); `remap: Annotated[list[str] | None, typer.Option("--remap", help=...)] = None` (help: "Publish a topic under a new name: --remap old:=new (repeatable)."); `start_paused: Annotated[bool, typer.Option("--start-paused", "-p", help=...)] = False` (help: "Build the transport but publish nothing (begins PAUSED). Non-interactive: this CLI has no resume; interactive control is in the GUI."); `region_start: Annotated[float | None, typer.Option("--region-start", help=...)] = None` and `region_end: Annotated[float | None, typer.Option("--region-end", help=...)] = None` (help: region-start "Play from this many SECONDS in (bounded region in-point)."; region-end "Play to this many SECONDS in (region out-point); pair with --loop to REPEAT the [in,out] snippet — a single-pass [in,out] stop is a deferred enhancement (same as --end folded into --duration)."). In the command BODY, before the `replay_api(...)` call, parse `--remap`: `parsed_remap: dict[str, str] | None = None; if remap: parsed_remap = {}; for pair in remap: old, sep, new = pair.partition(":="); if not sep or not old or not new: raise typer.BadParameter(f"--remap expects old:=new, got {pair!r}"); parsed_remap[old] = new` (typer renders BadParameter as a clean usage error + non-zero exit — NO traceback). APPEND to the existing `replay_api(...)` call the new kwargs: `publish_clock=clock, delay=delay, remap=parsed_remap, start_paused=start_paused, region_start=region_start, region_end=region_end`. Update the `replay()` command DOCSTRING (and/or the app help) to: (a) describe the new flags briefly; (b) add an "OUT OF SCOPE (deferred)" paragraph documenting that the runtime ROS playback services `~/seek`, `~/set_rate`, `~/play_next`, `~/burst`, `~/toggle_paused` are NOT exposed (they need a long-lived spinning node; interactive control lives in the GUI) and that a single-pass bounded `[in,out]` stop is deferred (the region repeats with --loop; mirrors the --end-folded-into-duration precedent) — SC3, NOT silently missing. Keep cli.py top level typer+stdlib ONLY (no new import beyond what's there — `typer` + `typing.Annotated` + stdlib; the ROS import stays behind the lazy `from rosbagger_replay import replay_bag`). Keep lines/docstrings under 100 cols. Per CONTEXT §Hard invariants: CLI stays THIN (each flag maps to a replay()/build_publish_sink param; no publish logic in cli.py); the ROS import stays behind _require_ros() via the lazy front door.</action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_offline_guard.py -x -q && PYTHONPATH="" uv run ruff check packages/rosbagger-replay/src/rosbagger_replay/cli.py && PYTHONPATH="" uv run ruff format --check packages/rosbagger-replay/src/rosbagger_replay/cli.py</automated>
  </verify>
  <done>cli.py's replay() command exposes --clock/--delay/--remap/--start-paused(-p)/--region-start/--region-end; --remap is parsed old:=new into a dict (malformed -> typer.BadParameter, clean error); every flag is forwarded to replay_bag as publish_clock/delay/remap/start_paused/region_start/region_end; the docstring/help documents the deferred runtime services + the single-pass region stop as out-of-scope; cli.py top level stays typer+stdlib (offline guard green); ruff check + format clean.</done>
</task>

<task type="auto">
  <name>Task 2: CliRunner unit tests (flag exposure + kwarg forwarding + remap parse + malformed reject) + phase gate</name>
  <files>tests/test_replay_unit.py</files>
  <action>Add CliRunner unit tests to the CLI section of `tests/test_replay_unit.py` (~:758, reuse the existing `app` / `_runner` / `import rosbagger_replay` + the `fake_replay_bag` spy pattern at ~:786). (1) `test_cli_replay_help_exposes_parity_flags` (SC1) — `_runner.invoke(app, ["--help"])` exits 0 and `result.output` contains `--clock`, `--delay`, `--remap`, `--start-paused`, `-p`, `--region-start`, `--region-end`. (2) `test_cli_replay_help_documents_deferred_services` (SC3) — `--help` output mentions the deferred runtime services (assert at least one of the service names, e.g. `~/seek` or `toggle_paused`, AND a single-pass/region-stop deferral phrase) so the deferral is documented, not silent. (3) `test_cli_replay_forwards_parity_flags` (SC2) — invoke `app, ["/tmp/bag", "--clock", "--delay", "2", "--start-paused", "--region-start", "1", "--region-end", "3"]` with the monkeypatched `rosbagger_replay.replay_bag` spy; assert `calls["publish_clock"] is True`, `calls["delay"] == 2.0`, `calls["start_paused"] is True`, `calls["region_start"] == 1.0`, `calls["region_end"] == 3.0`; also invoke with `-p` and assert `start_paused is True` (the short alias). (4) `test_cli_replay_remap_parses_pairs` — invoke `app, ["/tmp/bag", "--remap", "a:=b", "--remap", "c:=d"]`; assert `calls["remap"] == {"a": "b", "c": "d"}`; and a no-`--remap` invoke forwards `remap is None`. (5) `test_cli_replay_remap_malformed_errors_cleanly` — invoke `app, ["/tmp/bag", "--remap", "foo"]` (no `:=`); assert `result.exit_code != 0` and `result.exception is None or isinstance(result.exception, SystemExit)` (a clean usage error, NOT a NoMessagesToReplayError/raw traceback escaping). Use the existing `fake_replay_bag` spy shape (`def fake_replay_bag(bag, **kwargs): calls.update(kwargs); return N`) + `monkeypatch.setattr(rosbagger_replay, "replay_bag", fake_replay_bag)`. Keep lines under 100 cols. Per CONTEXT: these are the SC1 (exposure) + SC2 (mapping) + SC3 (deferral documented) offline proofs via CliRunner — no ROS runs (the front door is mocked).</action>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_replay_unit.py -x -q -k cli && PYTHONPATH="" uv run ruff check tests/test_replay_unit.py && PYTHONPATH="" uv run ruff format --check tests/test_replay_unit.py</automated>
  </verify>
  <done>tests/test_replay_unit.py has CliRunner tests proving --help exposes every new flag (SC1) + documents the deferred services (SC3); each flag forwards the right kwarg to the monkeypatched replay_bag spy (SC2); --remap a:=b --remap c:=d parses to {"a":"b","c":"d"} and no --remap forwards None; a malformed --remap foo exits non-zero cleanly with no traceback escaping; all CLI tests green; ruff check + format clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (untrusted) → replay_bag params | a malformed `--remap` value crosses from the shell into the parser; it must be rejected with a clean usage error, never a raw traceback or a silently-dropped pair |
| cli.py ↔ ROS runtime | adding the new options must keep cli.py top level typer+stdlib only; the ROS import stays behind the lazy `from rosbagger_replay import replay_bag` (offline-guard discipline) |
| documented surface ↔ actual capability | the deferred runtime services + single-pass region stop must be documented as out-of-scope (SC3), not implied present then silently missing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-A | Tampering | a malformed `--remap` value (no `:=`, or an empty side) silently accepted or crashing with a traceback | mitigate | `partition(":=")` + a `typer.BadParameter` raise on a missing separator / empty side → typer renders a clean usage error + non-zero exit; proven by test_cli_replay_remap_malformed_errors_cleanly |
| T-21-02-B | Elevation/Isolation | a stray module-top ROS/Qt import added to cli.py while wiring the flags | mitigate | the new options are typer + stdlib only; the front door stays the lazy `from rosbagger_replay import replay_bag`; test_offline_guard.py stays green |
| T-21-02-C | Information disclosure | the deferred runtime services / single-pass region stop presented as present then silently absent (a misleading surface) | mitigate | the command docstring/help documents `~/seek`/`~/set_rate`/`~/play_next`/`~/burst`/`~/toggle_paused` + the single-pass stop as out-of-scope (SC3); proven by test_cli_replay_help_documents_deferred_services |
| T-21-02-D | Tampering | a flag mis-forwarded (wrong kwarg name) so the library param never receives it | mitigate | each flag's forwarding is asserted against the monkeypatched replay_bag spy (publish_clock/delay/remap/start_paused/region_start/region_end) — a wrong name fails the test |
| T-21-02-SC | Tampering | npm/pip/cargo installs | accept | ZERO new packages this plan — typer is the existing locked CLI dep; no new dependency; no Package Legitimacy Audit needed; nothing to slopcheck |
</threat_model>

<verification>
- `PYTHONPATH="" uv run pytest tests/test_replay_unit.py -x -q -k cli` — the CliRunner tests (help exposure SC1, deferred-services documentation SC3, kwarg forwarding SC2, remap parse, malformed reject) are green; no ROS runs (the front door is mocked).
- `PYTHONPATH="" uv run pytest tests/test_offline_guard.py -x -q` — `import rosbagger_replay` / the CLI module top level stays ROS-free AND Qt-free (the new options add no ROS import).
- `PYTHONPATH="" uv run ruff check ...` + `ruff format --check ...` on cli.py + tests/test_replay_unit.py — clean (line length 100; docstrings under 100).
- Phase gate (blended, OFFLINE): `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest --cov=rosbagger_core --cov=bagq --cov=rosbagger_desktop --cov-fail-under=80` — green at ≥80% (re-run once if the benign offscreen-exit Bus-error appears at teardown — Qt teardown artifact, not a failure; confirm the clean pass line or use --junitxml).
- ROS-sourced lane (the 21-01 -m live remap/clock proofs, NOT part of the offline gate): `source /opt/ros/humble/setup.bash && uv run --with pyyaml pytest tests/test_replay_fidelity_live.py -m live -v`.
</verification>

<success_criteria>
- `rosbagger-replay --help` exposes `--clock`, `--delay`, `--remap`, `--start-paused` (+ `-p`), `--region-start`, `--region-end` alongside the existing flags; `--help` exits 0 (builds with no ROS import) — SC1.
- Each flag forwards the right library kwarg (`publish_clock`/`delay`/`remap`/`start_paused`/`region_start`/`region_end`) to `replay_bag`; `--remap a:=b --remap c:=d` → `{"a":"b","c":"d"}`; a malformed `--remap` errors cleanly — proven by the CliRunner tests — SC2 (mapping).
- The deferred runtime ROS services (`~/seek`, `~/set_rate`, `~/play_next`, `~/burst`, `~/toggle_paused`) + the deferred single-pass region stop are documented as out-of-scope in the command help/docstring — SC3.
- The CLI stays THIN (each flag maps to a `replay()`/`build_publish_sink` param; no publish logic in cli.py; top level typer+stdlib); the offline/Qt-free guard is green; the blended headless suite passes at ≥80% — SC4.
</success_criteria>

<output>
Create `.planning/phases/21-replay-cli-parity-flags/21-02-SUMMARY.md` when done.
</output>
