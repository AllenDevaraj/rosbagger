---
phase: 19-replay-snippet-loop-and-advanced-controls-panel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-desktop/src/rosbagger_desktop/widgets/scrubber.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/theme/tokens.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/theme/qss.py
  - tests/test_desktop.py
autonomous: true
requirements: [REP-03]
must_haves:
  truths:
    - "Scrubber.set_loop_region(in_frac, out_frac) sets a region and Scrubber.clear_loop_region() clears it, both programmatic (repaint, NO region_changed emit), and the loop_region prop reads back (in,out)|None"
    - "A USER handle drag updates the dragged handle's fraction (clamped, in<=out kept) and emits region_changed(in, out) — proven via the widget's internal handle-update helper"
    - "A programmatic set_loop_region does NOT emit region_changed (only a user handle drag does)"
    - "paintEvent with a region set draws a shaded region rect + two handle glyphs without crashing, using NEW theme tokens (region_fill / region_handle) — NO literal hex in scrubber.py"
    - "A click/drag NOT near a handle falls through to the base QSlider playhead seek so the existing seeked path is preserved (Phase-18 live scrubbing intact)"
    - "tokens.py gains region_fill / region_handle and build_qss exposes/derives them; the scrubber reads the token VALUES (never inline hex)"
    - "import rosbagger_desktop.widgets.scrubber stays PySide6+stdlib only (offline/Qt-clean) and tests/test_offline_guard.py stays green"
  artifacts:
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/widgets/scrubber.py"
      provides: "Dual In/Out region handles: _loop_in/_loop_out fractions + an active-drag-handle flag, programmatic set_loop_region/clear_loop_region + a loop_region read prop, a region_changed = Signal(float,float) emitted ONLY on a user handle drag, a paintEvent extension drawing a shaded region rect + two handle glyphs from token-derived QColors (no inline hex), and mousePress/mouseMove handling that hit-tests near a handle (drag mode) else falls through to the base playhead seek"
      contains: "region_changed = Signal"
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/theme/tokens.py"
      provides: "region_fill + region_handle color fields on Tokens, with DARK/LIGHT baked sRGB values (authored in OKLCH per the module convention)"
      contains: "region_fill"
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/theme/qss.py"
      provides: "build_qss surfaces the region tokens (a token accessor / objectName-keyed selector) so the scrubber resolves the region/handle colors from the token module, never an inline literal"
      contains: "region"
    - path: "tests/test_desktop.py"
      provides: "Headless pytest-qt (offscreen) tests: programmatic set/clear + loop_region prop, region_changed via a direct handle-update helper, programmatic set does NOT emit, and paint-with-a-region-set does not crash"
      contains: "loop_region"
  key_links:
    - from: "scrubber.py paintEvent"
      to: "tokens.region_fill / tokens.region_handle"
      via: "QColor resolved from the token VALUES (token module import / build_qss accessor) — never a literal hex in scrubber.py"
      pattern: "region_fill"
    - from: "scrubber.py mousePressEvent / mouseMoveEvent"
      to: "region_changed + the base QSlider seek"
      via: "hit-test near a handle -> handle-drag mode + region_changed; NOT near -> super() playhead-seek (existing seeked path preserved)"
      pattern: "region_changed\\.emit"
    - from: "scrubber.py set_loop_region / clear_loop_region"
      to: "self._loop_in / self._loop_out + update()"
      via: "programmatic set repaints, does NOT emit region_changed"
      pattern: "def set_loop_region"
---

<objective>
Give the `Scrubber` two draggable In/Out handles + a shaded region paint so the user can mark a snippet-loop region directly on the timeline (CONTEXT locked decision: region markable via handles AND buttons). The widget gains region STATE + a `region_changed(in, out)` signal emitted ONLY on a user handle drag, plus programmatic `set_loop_region`/`clear_loop_region` (no emit) that the panel (19-03) drives from the Set-In/Out buttons. Independent of 19-01 — this is the pure Qt-presentation tier.

Mechanism (RESEARCH §2b, the decided design): `_loop_in` / `_loop_out` fractions + an active-drag-handle flag; `set_loop_region(in_frac, out_frac)` / `clear_loop_region()` (programmatic, repaint, NO emit) + a `loop_region` read prop; a new `region_changed = Signal(float, float)`; a `paintEvent` extension (after the base slider + markers) that draws a semi-transparent shaded region rect + two handle glyphs using NEW theme tokens (`region_fill` / `region_handle` added to `tokens.py` + surfaced via `qss.py` — NO literal hex in the widget); and `mousePressEvent`/`mouseMoveEvent` that hit-test near a handle (within a pixel tolerance) to enter handle-drag mode (clamp + keep `in<=out`, emit `region_changed`) else FALL THROUGH to the base QSlider behavior so the existing `seeked` playhead-seek path is untouched.

Purpose: This proves SC2 (shaded region + two draggable handles) and SC4 (offline/Qt-free, token-driven, no inline color) at the widget tier. The Scrubber stays a THIN presentation+input face — no transport/analysis/ROS logic added. 19-03 wires the handles + buttons to the scheduler region.
Output: an updated `scrubber.py` + the new `region_fill`/`region_handle` tokens in `tokens.py`/`qss.py` + headless pytest-qt tests in `tests/test_desktop.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-replay-snippet-loop-and-advanced-controls-panel/19-CONTEXT.md
@.planning/phases/19-replay-snippet-loop-and-advanced-controls-panel/19-RESEARCH.md

# The Scrubber being extended (the playhead+markers paint + the seeked seam to PRESERVE).
@packages/rosbagger-desktop/src/rosbagger_desktop/widgets/scrubber.py
# The token palette — add region_fill / region_handle here (baked sRGB, authored in OKLCH per
# the module convention); NO Qt import in this module (it stays offline/Qt-free pure data).
@packages/rosbagger-desktop/src/rosbagger_desktop/theme/tokens.py
# The QSS builder — surface the region tokens so the scrubber resolves colors from the token
# module (a token accessor / objectName selector), NEVER an inline hex literal in the widget.
@packages/rosbagger-desktop/src/rosbagger_desktop/theme/qss.py
# The desktop tests — new headless scrubber-region tests land here (offscreen at module top).
@tests/test_desktop.py
# Offline/Qt-free guard that MUST stay green.
@tests/test_offline_guard.py

<interfaces>
<!-- Contracts the executor implements against. Extracted from the current codebase.
     Use these directly — no codebase exploration needed. -->

Scrubber (widgets/scrubber.py) — existing surface to PRESERVE, plus NEW region API:
  EXISTING (unchanged):
    seeked = Signal(float)                    # USER click/drag playhead seek (KEEP — fall through to it)
    set_position(fraction)/position           # programmatic playhead (_suppress_emit guard)
    set_markers(marks)/markers                # event-marker overlay
    _clamp01(value)                           # module helper — reuse for fraction clamping
    paintEvent(event)                         # base slider + marker ticks (EXTEND, do not replace)
    _RESOLUTION = 1000                         # value()/_RESOLUTION == fraction
  NEW (this plan adds):
    region_changed = Signal(float, float)     # emitted ONLY on a USER handle drag (in_frac, out_frac)
    set_loop_region(in_frac: float, out_frac: float) -> None
        # programmatic: clamp01 both, keep in<=out, store _loop_in/_loop_out, update() (repaint),
        # NO region_changed emit.
    clear_loop_region() -> None               # _loop_in=_loop_out=None, update(), NO emit.
    property loop_region -> tuple[float, float] | None  # (in,out) when set, else None.
    self._loop_in: float | None ; self._loop_out: float | None ; an active-drag-handle flag
        # (e.g. self._drag_handle in {None, "in", "out"}).

  Mouse contract (RESEARCH §2b):
    mousePressEvent(event): hit-test the press x against in_frac*width and out_frac*width within
        a pixel tolerance (~handle width); if near a handle -> set the active-drag-handle flag and
        accept (do NOT call super for the playhead). If NOT near a handle -> super().mousePressEvent
        (the base QSlider playhead-seek -> existing seeked path).
    mouseMoveEvent(event): if a handle is active, map x->fraction (clamp01), keep in<=out (clamp the
        dragged handle against the other), store it, update(), and region_changed.emit(in, out).
        If no handle active -> super() (base drag seek).
    mouseReleaseEvent(event): clear the active-drag-handle flag; super() otherwise.
    A small internal helper (e.g. _update_drag_handle(handle, fraction) or _set_region_from_drag)
        does the clamp + in<=out + store + emit so a headless test can drive it WITHOUT a real
        offscreen mouse drag.

Tokens (theme/tokens.py) — add two fields:
  region_fill: str    # semi-transparent shade base (baked sRGB hex; alpha applied in the painter)
  region_handle: str  # the two handle glyph color (baked sRGB hex)
  Add concrete DARK + LIGHT values (authored in OKLCH per the module docstring convention).

build_qss (theme/qss.py): surface the region tokens so the widget resolves colors from the token
  module (a small accessor returning the region colors, OR an objectName-keyed selector the widget
  reads a QColor from). The VALUES live in tokens.py; the widget must NOT contain a literal hex.

Existing desktop tests in tests/test_desktop.py to MIRROR for style (offscreen set at module top;
  Scrubber/ReplayPanel imported inside each test; assert OBSERVABLE state): see the scrubber test at
  ~line 186 and the replay-panel tests at ~574/622/1169 for the sentinel-stub + qtbot pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Region tokens (tokens.py + qss.py) + Scrubber region state, signal, and programmatic setters</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/theme/tokens.py, packages/rosbagger-desktop/src/rosbagger_desktop/theme/qss.py, packages/rosbagger-desktop/src/rosbagger_desktop/widgets/scrubber.py, tests/test_desktop.py</files>
  <behavior>
    - RED first (headless, QT_QPA_PLATFORM=offscreen): `test_scrubber_set_and_clear_loop_region` — a fresh `Scrubber()` has `loop_region is None`; after `set_loop_region(0.2, 0.6)` the `loop_region` prop returns `(0.2, 0.6)`; after `set_loop_region(0.6, 0.2)` (reversed) it returns `(0.2, 0.6)` (in<=out kept); after `clear_loop_region()` it returns `None`. Out-of-range fractions clamp to [0,1].
    - `test_scrubber_programmatic_set_does_not_emit_region_changed` — connect a spy/list to `region_changed`; `set_loop_region(0.1, 0.9)` and `clear_loop_region()` emit NOTHING (programmatic path is silent).
    - `test_tokens_have_region_fields` (pure, no QApplication needed for tokens.py): `DARK.region_fill` / `DARK.region_handle` / `LIGHT.region_fill` / `LIGHT.region_handle` are non-empty `#rrggbb`-shaped strings; `build_qss(DARK)` returns a string referencing the region token(s) (or a token accessor returns them) — proving the colors live in the token module, not the widget.
    - Regression: the existing scrubber test (~line 186) and the offline/Qt-free guard stay green.
  </behavior>
  <action>Add the region tokens then the Scrubber region state + setters. In `theme/tokens.py`: add `region_fill: str` and `region_handle: str` fields to the `Tokens` dataclass and concrete values to BOTH `DARK` and `LIGHT` (baked sRGB hex with an OKLCH trailing comment per the module convention — e.g. a low-chroma shade for the fill and the existing `accent` family for the handle; the executor picks calm values consistent with the palette). In `theme/qss.py`: surface the region colors so the widget can resolve them from the token module — either add a tiny accessor (e.g. a function returning `(t.region_fill, t.region_handle)`) OR an objectName-keyed selector the widget reads — KEEP the actual values in tokens.py (RESEARCH §3 Pitfall 6: NO inline hex anywhere in scrubber.py). In `widgets/scrubber.py`: add `self._loop_in: float | None = None`, `self._loop_out: float | None = None`, and an active-drag-handle flag (e.g. `self._drag_handle: str | None = None`) in `__init__`; declare `region_changed = Signal(float, float)` as a class attribute alongside `seeked`; add `def set_loop_region(self, in_frac, out_frac)`: clamp01 both via the existing `_clamp01`, keep in<=out (swap if reversed), store `_loop_in`/`_loop_out`, call `self.update()` (repaint) and DO NOT emit; add `def clear_loop_region(self)`: set both `None`, `self.update()`, no emit; add `@property def loop_region` returning `(self._loop_in, self._loop_out)` when both set else `None`. Import the region token VALUES from the theme module at the TOP of scrubber.py (theme/tokens.py and theme/qss.py are pure-Python, PySide6+stdlib-clean — importing them keeps the Scrubber offline/Qt-clean) so paintEvent (Task 2) can build QColors from them. Do NOT add the paint or mouse handling yet (Task 2). Add the tests above mirroring the offscreen pattern at tests/test_desktop.py ~line 186. NO inline color (RESEARCH §3 Pitfall 6).</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -k "set_and_clear_loop_region or programmatic_set_does_not_emit or tokens_have_region" -x -q</automated>
  </verify>
  <done>tokens.py has region_fill/region_handle on Tokens + DARK + LIGHT; qss.py surfaces them (accessor or selector); the Scrubber holds _loop_in/_loop_out + a drag-handle flag, declares region_changed, and set_loop_region/clear_loop_region/loop_region work programmatically WITHOUT emitting; the region color values live in the token module (no inline hex in scrubber.py); tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: paintEvent region rect + handle glyphs, and mouse handling (handle drag emits region_changed; else playhead-seek fall-through)</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/widgets/scrubber.py, tests/test_desktop.py</files>
  <behavior>
    - RED first (offscreen): `test_scrubber_paint_with_region_does_not_crash` — `set_loop_region(0.25, 0.75)` then force a paint (e.g. `scrubber.resize(200, 30); scrubber.repaint()` or call `paintEvent` via a `QPaintEvent` on a shown offscreen widget) completes without raising — the shaded rect + two handle glyphs draw using the token-derived QColors.
    - `test_scrubber_handle_drag_emits_region_changed` (SC2) — connect a spy/list to `region_changed`; drive the internal handle-update helper directly (e.g. `_update_drag_handle("out", 0.8)` after `set_loop_region(0.2, 0.6)`), assert `region_changed` fired with `(0.2, 0.8)` (the dragged OUT handle moved, IN unchanged, in<=out kept) and `loop_region == (0.2, 0.8)`. Also drive the IN handle below OUT and assert the swap/clamp keeps in<=out.
    - `test_scrubber_click_far_from_handle_falls_through_to_seek` — connect a spy to `seeked`; with NO region set (or a press far from both handles), a base playhead interaction still emits `seeked` (the existing path is preserved — only a near-handle press intercepts). Assert via the hit-test helper returning "no handle" for a far x, OR via `set_position`/value-change still emitting `seeked` as before.
    - Regression: the existing scrubber test (~line 186) — markers + playhead `seeked` + `set_position` suppression — stays green UNCHANGED.
  </behavior>
  <action>Implement RESEARCH §2b's paint + mouse handling. Extend `paintEvent` (AFTER the existing `super().paintEvent` + marker ticks): when a region is set (`self._loop_in is not None and self._loop_out is not None`), open a `QPainter(self)`, compute `width = max(1, self.width() - 1)`, `x_in = round(self._loop_in * width)`, `x_out = round(self._loop_out * width)`, fill a semi-transparent rect from `x_in` to `x_out` across the groove height with a `QColor` built from the `region_fill` TOKEN (apply an alpha via `QColor.setAlpha(...)` in the painter — keep the base hex in the token), then draw two thin vertical handle bars at `x_in` and `x_out` with a `QColor` from the `region_handle` TOKEN; `painter.end()` in a `finally` (mirror the existing marker paint structure). Add `mousePressEvent(self, event)`: compute the press x->fraction, hit-test against `x_in`/`x_out` within a pixel tolerance constant (e.g. `_HANDLE_GRAB_PX = 8`); if within tolerance of a handle, set `self._drag_handle` to "in"/"out" and `event.accept()` (do NOT call super — suppress the playhead jump); else `super().mousePressEvent(event)` (the base QSlider playhead-seek -> existing `seeked`). Add `mouseMoveEvent(self, event)`: if `self._drag_handle` is set, map x->fraction and call the internal helper `_update_drag_handle(self._drag_handle, fraction)`; else `super().mouseMoveEvent(event)`. Add `mouseReleaseEvent(self, event)`: clear `self._drag_handle`; `super().mouseReleaseEvent(event)`. Add the internal helper `def _update_drag_handle(self, handle: str, fraction: float)`: `fraction = _clamp01(fraction)`; if `handle == "in"`: `self._loop_in = min(fraction, self._loop_out)` (keep in<=out); else `self._loop_out = max(fraction, self._loop_in)`; `self.update()`; `self.region_changed.emit(self._loop_in, self._loop_out)`. Add a hit-test helper (e.g. `def _handle_at(self, x_px: int) -> str | None`) the far-from-handle test can call. KEEP the existing `seeked`/`_suppress_emit`/marker logic UNCHANGED — only intercept a press when a handle is actually grabbed (RESEARCH §3 Pitfall 5). Build all QColors from the imported region TOKEN values — NO literal hex in scrubber.py (Pitfall 6). Add the tests above (offscreen); prefer driving `_update_drag_handle` / `_handle_at` directly over a real offscreen mouse drag (RESEARCH §2b headless note).</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -k "paint_with_region or handle_drag_emits or click_far_from_handle" -x -q</automated>
  </verify>
  <done>paintEvent draws a shaded region rect + two handle glyphs from token-derived QColors (no inline hex); a near-handle press enters drag mode and `_update_drag_handle` clamps + keeps in<=out + emits region_changed; a press far from a handle falls through to the base QSlider playhead-seek (existing `seeked` path preserved); the existing scrubber test stays green; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Offline/Qt-free guard + lint + phase gate</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/widgets/scrubber.py, packages/rosbagger-desktop/src/rosbagger_desktop/theme/tokens.py, packages/rosbagger-desktop/src/rosbagger_desktop/theme/qss.py, tests/test_desktop.py</files>
  <action>Confirm `widgets/scrubber.py` top level imports ONLY PySide6 + stdlib + the local theme token module (the theme modules are pure-Python PySide6/stdlib-clean — `tokens.py` imports only `dataclasses`, `qss.py` imports only `tokens`), with NO `rclpy`/`rosbag2_py`/`rosbagger_replay`. Confirm `tokens.py` still imports ONLY `dataclasses` (no PySide6) and `qss.py` imports ONLY `tokens`. Grep scrubber.py for any literal `#` hex color — there must be NONE for the region/handles (the existing `_MARKER_COLOR` literal is pre-existing and out of scope; the NEW region/handle colors must come from tokens). Confirm `import rosbagger_core`/`import bagq` still pull no PySide6 (Qt-free guard) and `import rosbagger_desktop.widgets.scrubber` pulls no ROS. Run ruff lint + format-check on the changed files. Run the blended phase gate at ≥80%. This is verification/glue (no tdd block).</action>
  <verify>
    <automated>PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -x -q && PYTHONPATH="" uv run pytest tests/test_offline_guard.py -x -q && PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src/rosbagger_desktop/widgets/scrubber.py packages/rosbagger-desktop/src/rosbagger_desktop/theme/tokens.py packages/rosbagger-desktop/src/rosbagger_desktop/theme/qss.py tests/test_desktop.py && PYTHONPATH="" uv run ruff format --check packages/rosbagger-desktop/src/rosbagger_desktop/widgets/scrubber.py packages/rosbagger-desktop/src/rosbagger_desktop/theme/tokens.py packages/rosbagger-desktop/src/rosbagger_desktop/theme/qss.py tests/test_desktop.py && PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest --cov=rosbagger_core --cov=bagq --cov=rosbagger_desktop --cov-fail-under=80 -q</automated>
  </verify>
  <done>scrubber.py top level is PySide6+stdlib+local-theme only (offline/Qt-clean); tokens.py stays dataclasses-only, qss.py stays tokens-only; the NEW region/handle colors carry no inline hex in scrubber.py; offline AND Qt-free guard green; the full desktop suite passes; ruff check + ruff format --check clean; blended coverage ≥80%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user mouse input ↔ region vs playhead | A handle drag and a playhead seek share the same QSlider surface; mis-routing a click would either break region marking or break the existing live-scrub seek |
| widget paint ↔ theme tokens | A region/handle color hardcoded in the widget would breach the no-inline-color invariant (D-03 / Phase-17 token-driven system) |
| desktop widget import graph ↔ ROS/Qt | scrubber.py must stay PySide6+stdlib+local only; pulling ROS would breach the offline boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-A | Tampering | a handle drag clobbering the existing playhead seek (or vice versa) | mitigate | mousePressEvent only intercepts when the press is within `_HANDLE_GRAB_PX` of a handle; otherwise it calls super() so the base QSlider playhead-seek + existing `seeked` path is untouched — proven by the far-from-handle test |
| T-19-02-B | Information Disclosure | inline hex region/handle color in the widget (breaks the token system) | mitigate | region_fill/region_handle live in tokens.py + are surfaced via qss.py; the widget imports the token VALUES and builds QColors from them; Task 3 greps for a stray hex |
| T-19-02-C | Tampering | programmatic set_loop_region accidentally emitting region_changed (feedback loop with the panel) | mitigate | set_loop_region/clear_loop_region repaint but never emit; only `_update_drag_handle` (a user drag) emits — proven by the programmatic-no-emit test |
| T-19-02-D | Denial of Service | paint crash when a region is set (e.g. zero-width widget) | mitigate | `width = max(1, self.width() - 1)`; painter opened/closed in a try/finally mirroring the marker paint; proven by the paint-with-region test |
| T-19-02-E | Elevation/Isolation | a stray ROS/Qt-incompatible import in scrubber.py / tokens.py | mitigate | tokens.py stays dataclasses-only, qss.py tokens-only, scrubber.py PySide6+stdlib+local; tests/test_offline_guard.py stays green |
| T-19-02-SC | Tampering | npm/pip/cargo installs | accept | ZERO new packages (PySide6 is already a dep; tokens are plain strings); nothing to slopcheck |
</threat_model>

<verification>
- `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py -x -q` — new scrubber-region tests + all pre-existing desktop tests green (re-run once if the benign offscreen-exit Bus-error appears at teardown — Qt teardown artifact, not a failure; confirm the clean pass line).
- `PYTHONPATH="" uv run pytest tests/test_offline_guard.py -x -q` — offline AND Qt-free guard green.
- `PYTHONPATH="" uv run ruff check ...` + `ruff format --check ...` on scrubber.py + tokens.py + qss.py + tests/test_desktop.py — clean.
- Phase gate (blended): `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest --cov=rosbagger_core --cov=bagq --cov=rosbagger_desktop --cov-fail-under=80` — green at ≥80%.
</verification>

<success_criteria>
- The `Scrubber` shows a shaded loop region with two draggable In/Out handles; a user handle drag emits `region_changed(in, out)` and the programmatic `set_loop_region`/`clear_loop_region` (no emit) drive the same region — SC2.
- The region/handle colors come from NEW Phase-17 theme tokens (`region_fill`/`region_handle`), NEVER an inline hex in the widget — SC4.
- A click/drag not near a handle still drives the existing playhead `seeked` seek (Phase-18 live scrubbing preserved) — SC2/SC4.
- scrubber.py/tokens.py/qss.py stay offline/Qt-clean; offline guard green; full headless suite passes at ≥80% — SC4.
</success_criteria>

<output>
Create `.planning/phases/19-replay-snippet-loop-and-advanced-controls-panel/19-02-SUMMARY.md` when done.
</output>
