---
phase: 10-query-ergonomics
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - packages/bagq/src/bagq/cli.py
  - tests/test_cli_query.py
autonomous: true
requirements: [QURY-08]

must_haves:
  truths:
    - "bagq query gains a --no-alias boolean option (default False = aliases ON, D-11); it forwards alias=not no_alias to run_query(sql, reader, alias=...) — a thin pass-through, no business logic in the CLI"
    - "bagq query \"SELECT vx FROM cmd_vel\" BAG exits 0 and renders rows (aliases on by default); bagq query --no-alias \"SELECT vx FROM cmd_vel\" BAG exits 1 with a clean UnknownColumnError teaching message (no traceback, via @teaching_errors)"
    - "Projection pushdown stays transparent (no CLI flag — D-11): the existing query command behavior (table/-o/--format/--plot routing) is unchanged"
    - "cli.py top level stays typer/rich-only; run_query is still lazy-imported inside the query body (offline-guard discipline intact)"
  artifacts:
    - path: "packages/bagq/src/bagq/cli.py"
      provides: "--no-alias option on the query command, forwarded to run_query(..., alias=not no_alias)"
      contains: "no-alias"
    - path: "tests/test_cli_query.py"
      provides: "CliRunner tests for the --no-alias surface (aliases-on default exits 0 + renders; --no-alias exits 1 cleanly)"
      contains: "no_alias"
  key_links:
    - from: "bagq/cli.py query()"
      to: "rosbagger_core.backend.query.query(sql, reader, alias=not no_alias)"
      via: "the --no-alias boolean forwarded to the orchestrator"
      pattern: "alias=not no_alias"
---

<objective>
Surface the alias feature on the `bagq query` command with a `--no-alias` escape hatch (D-11). Aliases are ON by default; `--no-alias` forwards `alias=False` to the orchestrator (Plan 10-03). Projection pushdown gets NO flag — it is transparent (changes only what is loaded, not results — D-11). This is a thin pass-through: one boolean option added to an existing typer command body, plus CliRunner tests. After this, the phase is complete and the full suite + lint gate runs.

Purpose: give users the documented control surface for the alias pack while keeping the CLI a renderer over `rosbagger_core` (API-first — design decision 1).

Output: edit to `bagq/cli.py` (the `query` command), `--no-alias` tests appended to `tests/test_cli_query.py`, and a green full-suite + ruff phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-query-ergonomics/10-CONTEXT.md
@.planning/phases/10-query-ergonomics/10-RESEARCH.md
@.planning/phases/10-query-ergonomics/10-03-SUMMARY.md

<interfaces>
<!-- The query command + the orchestrator signature this wires. Read cli.py (read_first). -->

From packages/bagq/src/bagq/cli.py (the existing query command — a typer command with `@app.command(cls=_PlotCommand)` + `@teaching_errors`, body lazy-imports `from rosbagger_core.backend.query import query as run_query` and calls `result = run_query(sql, reader)` inside a `with RosbagsReader(bags) as reader:` block; options use `Annotated[..., typer.Option(...)]`):
```python
@app.command(cls=_PlotCommand)
@teaching_errors
def query(
    sql: Annotated[str, typer.Argument(...)],
    bags: Annotated[list[Path], typer.Argument(...)],
    out: Annotated[Path | None, typer.Option("-o", "--output", ...)] = None,
    fmt: Annotated[str, typer.Option("--format", ...)] = "table",
    plot: Annotated[str | None, typer.Option("--plot", ...)] = None,
) -> None:
    ...
    with RosbagsReader(bags) as reader:
        result = run_query(sql, reader)   # ← add alias=not no_alias
```

From packages/rosbagger-core/src/rosbagger_core/backend/query.py (Plan 10-03):
```python
def query(sql, reader, *, alias: bool = True, backend=None) -> pyarrow.Table
```

`@teaching_errors` (existing) already catches `UnknownColumnError` → clean stderr message + `Exit(1)`, no traceback (so `--no-alias "SELECT vx ..."` surfaces cleanly).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --no-alias to bagq query and forward it</name>
  <files>packages/bagq/src/bagq/cli.py, tests/test_cli_query.py</files>
  <read_first>
    - packages/bagq/src/bagq/cli.py (the FULL `query` command: its `Annotated[..., typer.Option(...)]` option style, the `@app.command(cls=_PlotCommand)` + `@teaching_errors` decorators, the lazy `run_query` import in the body, and the `with RosbagsReader(bags) as reader: result = run_query(sql, reader)` call — you add ONE option and ONE keyword on that call; do NOT touch the `_PlotCommand`/`--plot` machinery)
    - tests/test_cli_query.py (the harness: repo-root sys.path, `from tools.make_fixtures import write_ros1_bag`, the `runner = CliRunner()` + `ros1_bag` session fixture, the `PYTHONPATH=""` LOCAL-RUN note, and the existing assertion style — stable data only, NO rich box-drawing assertions; mirror `test_query_default_prints_table` for the new tests)
    - .planning/phases/10-query-ergonomics/10-CONTEXT.md D-11 (alias ON by default + `--no-alias` escape hatch; projection always-on/transparent — NO projection flag)
    - .planning/phases/09-tf-debugger/09-03-SUMMARY.md (the prior CLI pattern note — how a boolean/option was added to a typer command and tested via CliRunner asserting stable data + exit code, the no-traceback `SystemExit` convention for clean Exit(1))
  </read_first>
  <behavior>
    - Test 1 (`-k no_alias`): `runner.invoke(app, ["query", "SELECT vx FROM cmd_vel", str(ros1_bag)])` exits 0 and renders rows (aliases ON by default — `vx` expands to `linear.x`; assert a stable signal like the result column/value, NOT box-drawing).
    - Test 2 (`-k no_alias`): `runner.invoke(app, ["query", "--no-alias", "SELECT vx FROM cmd_vel", str(ros1_bag)])` exits 1 (aliases OFF → `vx` is not a real column → `UnknownColumnError` surfaced cleanly by `@teaching_errors`). Assert `result.exit_code == 1` and that the output mentions the unknown column (the teaching message), and that NO traceback escaped (mirror the 07/09 convention: `isinstance(result.exception, SystemExit)` and not a raw `ValueError`).
    - Test 3 (`-k no_alias`): `--no-alias` appears in `bagq query --help` output (the option is registered).
  </behavior>
  <action>
    Add a `no_alias` boolean option to the `query` command in bagq/cli.py, matching the existing `Annotated[..., typer.Option(...)]` style: `no_alias: Annotated[bool, typer.Option("--no-alias", help="Disable the built-in alias pack (e.g. vx → twist.twist.linear.x).")] = False`. In the body, forward it to the orchestrator: change `result = run_query(sql, reader)` to `result = run_query(sql, reader, alias=not no_alias)`. Make NO other change — projection pushdown is transparent and gets no flag (D-11); the `-o`/`--format`/`--plot` routing, the `_PlotCommand` class, and the lazy `run_query` import all stay exactly as they are. Update the `query` command docstring with a one-line note that aliases are on by default and `--no-alias` disables them. Then append Tests 1-3 from <behavior> to tests/test_cli_query.py (tagged `-k no_alias`), reusing the existing `ros1_bag` session fixture and `runner`.
  </action>
  <verify>
    <automated>PYTHONPATH="" .venv/bin/python -m pytest tests/test_cli_query.py -k no_alias -x</automated>
  </verify>
  <acceptance_criteria>
    - The `query` command accepts `--no-alias` (default False) and forwards it: `grep -nE "alias=not no_alias" packages/bagq/src/bagq/cli.py` matches; `grep -n "no-alias" packages/bagq/src/bagq/cli.py` shows the option declaration.
    - `bagq query "SELECT vx FROM cmd_vel" BAG` exits 0 and renders rows (Test 1 green); `bagq query --no-alias "SELECT vx FROM cmd_vel" BAG` exits 1 with a clean teaching message and no traceback (Test 2 green).
    - `--no-alias` is listed in `bagq query --help` (Test 3 green).
    - cli.py top level is unchanged (still typer/rich/click only): `grep -nE "^import pyarrow|^import duckdb|^from rosbagger_core" packages/bagq/src/bagq/cli.py` returns nothing — `run_query` stays lazy-imported in the body.
  </acceptance_criteria>
  <done>`bagq query --no-alias` exists, forwards `alias=False`, surfaces a clean teaching error when used, and the offline/import discipline of cli.py is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full suite green + ruff clean + offline guard</name>
  <files>tests/test_cli_query.py</files>
  <read_first>
    - .planning/phases/10-query-ergonomics/10-RESEARCH.md § "Validation Architecture" → "Sampling Rate" (the per-wave-merge + phase-gate commands: full suite via `PYTHONPATH="" .venv/bin/python -m pytest -q`, baseline 274 passed ~97.8%, ≥80% gate; then `ruff check` + `ruff format --check`)
    - .planning/STATE.md (the baseline this session: 274 passed, ~97.8% coverage — the new tests + code should keep the gate green and add coverage for alias.py / the restrict params / the query wiring)
  </read_first>
  <action>
    Run the FULL test suite and the lint gate to confirm Phase 10 lands clean and regresses nothing. Sequence:
    (1) `PYTHONPATH="" .venv/bin/python -m pytest -q` — must be green with the ≥80% coverage gate met (the suite should now exceed the 274 baseline by the Phase-10 tests added in Plans 10-01..10-04). The new `backend/alias.py` and the `restrict=`/alias wiring must be covered (driven by the tests in 10-01/10-02/10-03 and the CLI tests here).
    (2) `ruff check .` and `ruff format --check .` — must be clean (fix any lint/format issues introduced in this plan's edits; mirror the bugbear-conscious style notes from prior phases, e.g. `zip(..., strict=False)` if applicable).
    (3) Re-run the offline-guard subset to reconfirm the invariant end-to-end: `PYTHONPATH="" .venv/bin/python -m pytest tests/test_offline_guard.py -x` — `import rosbagger_core` / `rosbagger_core.backend` / `rosbagger_core.backend.alias` all leak no forbidden stack.
    No new production code in this task unless a regression/lint issue requires a fix; if a coverage dip below 80% appears, ADD a focused test (do NOT add a coverage pragma or lower the gate). This task is the phase's green-bar checkpoint.
  </action>
  <verify>
    <automated>PYTHONPATH="" .venv/bin/python -m pytest -q && ruff check . && ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH="" .venv/bin/python -m pytest -q` exits 0 with the ≥80% coverage gate met (`--cov-fail-under=80` in addopts is satisfied); the passed-count is ≥ the 274 baseline plus the Phase-10 tests.
    - `ruff check .` and `ruff format --check .` both exit 0 (clean).
    - `tests/test_offline_guard.py` is green — `import rosbagger_core` / `rosbagger_core.backend` / `rosbagger_core.backend.alias` leak no duckdb/pyarrow (sqlglot allowed only where alias.py/resolve.py import it directly).
    - No coverage pragma was added and the gate was not lowered (any dip is fixed by a real test).
  </acceptance_criteria>
  <done>The full suite + ruff + offline-guard all pass; Phase 10 is green-bar and regresses nothing. QURY-08 + QURY-09 are delivered end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → orchestrator (T-05-04) | `--no-alias` is a boolean parsed by typer/click; it forwards a Python bool to `run_query(alias=...)`. No string interpolation, no new untrusted-data path. The user's SQL argument remains the trusted interface (Phase 5 T-05-04), forwarded unchanged through the CLI to the orchestrator. |
| error rendering → stderr (CLI-01..03) | `@teaching_errors` (existing) renders typed errors as a single stderr line + `Exit(1)`; the `--no-alias` path that raises `UnknownColumnError` reuses that vetted mechanism — no new rendering/format surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Tampering | `--no-alias` flag handling | mitigate | The flag is a typer boolean; it forwards `alias=not no_alias` (a Python bool) — no string built, no SQL touched. The CLI remains a thin pass-through (API-first, decision 1). The SQL argument is forwarded byte-for-byte to the orchestrator, which owns the (already-vetted) rewrite/forward boundary. |
| T-10-12 | Information disclosure | error message via the `--no-alias` path | mitigate | A `--no-alias` query naming a non-column raises `UnknownColumnError`, surfaced by the existing `@teaching_errors` as a clean teaching line (no traceback, no internal paths) — the same CLI-03 boundary already shipped in Phase 7. Tested for a clean `Exit(1)` (no raw exception escapes). |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (10-RESEARCH § Package Legitimacy Audit). cli.py adds no import at module top; `run_query` stays lazy-imported. No install task, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint applies. |

**Residual risk after mitigations:** No HIGH-severity threats. The CLI change is a single boolean option forwarded as a Python bool to the orchestrator; it builds no string, touches no SQL, and reuses the already-vetted `@teaching_errors` rendering and trusted-SQL boundary. No new injection, disclosure, or resource surface is introduced.
</threat_model>

<verification>
- `PYTHONPATH="" .venv/bin/python -m pytest -q` is green with the ≥80% coverage gate met (full suite, ≥ baseline + Phase-10 tests).
- `bagq query --no-alias "SELECT vx FROM cmd_vel" BAG` exits 1 cleanly; without `--no-alias` it exits 0 and renders (D-11).
- `ruff check .` + `ruff format --check .` clean; `tests/test_offline_guard.py` green.
</verification>

<success_criteria>
- `bagq query` carries `--no-alias` (default off = aliases on, D-11), forwarded as `alias=not no_alias` to the orchestrator; projection stays transparent (no flag).
- Aliases-on default renders `SELECT vx FROM cmd_vel`; `--no-alias` surfaces a clean teaching error — both via CliRunner (QURY-08 CLI surface).
- Full suite + ruff + offline-guard green; Phase 10 (QURY-08 + QURY-09) delivered end-to-end with no regression.
</success_criteria>

<output>
Create `.planning/phases/10-query-ergonomics/10-04-SUMMARY.md` when done.
</output>
