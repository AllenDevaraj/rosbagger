---
context: phase
phase: 10-query-ergonomics
task: 0
total_tasks: 10
status: paused
last_updated: 2026-05-23T04:32:39Z
---

# Phase 10 — Query Ergonomics — Paused at the plan→execute boundary

Fully **planned and verified**; **execution not started**. No executor ran, no source
changed, working tree clean. The autonomous `discuss → plan → execute --auto` chain
reached the execute step and was paused here by `/gsd-pause-work`.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Bare `pytest` on this box | Local `pytest`/`ruff` crash because the dev shell leaks ROS onto `PYTHONPATH`; the offline-import guard then fails | advisory | Prefix every local test/lint run with `PYTHONPATH=""` (e.g. `PYTHONPATH="" uv run pytest`). CI is already ROS-free. |
| Spawning `gsd-verifier` | `gsd-verifier` is NOT installed (in `missing_agents`); `verifier_enabled` is also `false`. Trying to spawn it fails | advisory | Verify the phase **inline**: run the suite + ruff + offline-guard yourself and check the 3 success criteria. Do NOT spawn `gsd-verifier`. (Core agents `gsd-executor`/`gsd-planner`/`gsd-plan-checker`/`gsd-phase-researcher` ARE installed.) |
| Worktree isolation for these plans | The plans form a dependency chain (10-03 reads 10-01+10-02 outputs); `parallelization=false` | advisory | Run executors **sequentially on the main tree** (omit `isolation="worktree"`) so each plan's commits are visible to the next. |

<current_state>
Phase 10 delivers two ergonomics features on the EXISTING `bagq` query pipeline:
**QURY-08** alias pack (`vx` → `"twist.twist.linear.x"`) and **QURY-09** column projection
pushdown (load only referenced columns). Discuss ✓, research ✓, plan ✓ (4 plans, 3 waves),
plan-checker ✓ (VERIFICATION PASSED, iteration 2, 0 blockers after W1/W2/W3 fixes).
STATE.md status = ready to execute. Next step is Wave 1 executor dispatch — not yet done.
</current_state>

<completed_work>
- discuss-phase (`--auto`): `10-CONTEXT.md` — 11 locked decisions D-01..D-11 (commit `2e3914d`)
- plan research: `10-RESEARCH.md` — HIGH confidence; sqlglot 30.8.0 `tree.transform()` rewrite + `exp.column(name, quoted=True)` verified, `restrict=` projection signatures, alias-pack table, SC3 Arrow-assertion shape, no new dependency (commit `a3ea7fd`)
- plan-phase: 4 `PLAN.md` files in 3 waves (commit `001ed55`)
- revision per plan-checker warnings W1 (cite D-09), W2 (SC3 uses a recording `QueryBackend` so it's a true integration proof), W3 (move `typestore` binding with the hoisted schema build) — commit `004f583`
- plan-checker re-check: VERIFICATION PASSED; STATE planning-complete (commit `019e636`)
</completed_work>

<remaining_work>
Execute the 4 plans **sequentially** (parallelization=false), in dependency order:
- **Wave 1 → 10-01** (3 tasks, QURY-08): new `packages/rosbagger-core/src/rosbagger_core/backend/alias.py` — `ALIAS_PACK` (per-msgtype) + `expand_aliases` sqlglot-AST rewrite (existence-gated, offline-safe) + extend `tests/test_offline_guard.py`
- **Wave 1 → 10-02** (3 tasks, QURY-09): generalize the schema filter — add a `restrict=` projection set to `schema/model.py` (`arrow_schema`/`column_names`) + `schema/flatten.py` (`build_arrow_table`/`flatten_message`); `flatten_message` SKIPS `reduce(getattr,…)` for non-projected columns (the actual pushdown)
- **Wave 2 → 10-03** (2 tasks, QURY-08+09 — the integration crux): wire both into `backend/query.py` `query()` — hoist per-topic schema build (and the `typestore = reader.typestore` binding) ahead of resolution (D-02), single-base-topic alias gate (RESEARCH Open Q1), compute/thread `restrict` (D-06/07/08), per-topic over-include (D-09), forward the rewritten SQL; SC3 proof parametrized across ROS1 + ROS2-sqlite + MCAP via a recording `QueryBackend`
- **Wave 3 → 10-04** (2 tasks, QURY-08): `bagq query --no-alias` surface (D-11, default alias ON) + full-suite/ruff/offline-guard phase gate
- **Then verify INLINE** (see anti-patterns): `PYTHONPATH="" uv run pytest` (≥80% cov gate; baseline was 274 passed ~97.8%), `ruff`, offline-guard; confirm SC1 (alias resolves `vx`), SC2 (projection loads only referenced columns), SC3 (single-column query does not materialize unreferenced/heavy columns).
- **`--no-transition`:** after verify, report `PHASE COMPLETE` / `GAPS FOUND` and STOP — do NOT auto-advance to Phase 11.
</remaining_work>

<decisions_made>
- Sequential execution on the main tree (parallelization=false + dependency chain) — no worktree isolation.
- Verify the phase inline (gsd-verifier missing + verifier_enabled=false).
- gsd-executor is available and is the right agent for each plan; model `inherit` → omit `model=`.
- No UI-SPEC (UI gate "ui" match was inside "Requirements" — false positive); pattern-mapper skipped (missing agent; CONTEXT already carries code_context).
</decisions_made>

<blockers>
- None. Clean pause; resume is safe.
</blockers>

## Required Reading (in order)
1. `.planning/phases/10-query-ergonomics/10-CONTEXT.md` — the 11 locked decisions (D-01..D-11) the plans implement
2. `.planning/phases/10-query-ergonomics/10-RESEARCH.md` — verified sqlglot mechanics, alias-pack table, `restrict=` signatures, SC3 proof shape
3. `.planning/phases/10-query-ergonomics/10-01..10-04-PLAN.md` — the 4 executable plans (start with 10-01)
4. `CLAUDE.md` — offline-import invariant, Python stack, GSD atomic-commit discipline

## Infrastructure State
- Working tree: clean (everything committed). Branch: `main` (branching_strategy=none).
- Baseline test suite (pre-phase-10): 274 passed, ~97.8% coverage, ROS-free fixtures.
- No running services/watchers. No new dependency is introduced by Phase 10.

<context>
The four plans are high-fidelity and independently verified. The only real design choice
(JOIN/CTE alias ambiguity, RESEARCH Open Q1) is locked as a single-base-topic expansion gate
with a JOIN no-op test. Hard constraints baked into acceptance criteria: offline-import
invariant (grep-gated + offline-guard test extension), trusted-SQL boundary (AST-only rewrite
via `exp.column(quoted=True)`, no f-strings), no-ROS fixture tests, ≥80% coverage. 10-03 is
where execution attention should concentrate (it reorders `query()`); `node_repair_budget` gives
recovery margin there.
</context>

<next_action>
Start with: re-enter execute-phase for Phase 10 and dispatch **Wave 1 plan 10-01** (backend/alias.py)
as a `gsd-executor` on the main tree (no worktree). Then 10-02 → 10-03 → 10-04, one at a time,
then inline verification. Resume with `/gsd:resume-work` or `/gsd:execute-phase 10 --auto`.
</next_action>
