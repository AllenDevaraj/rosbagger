---
phase: 10-query-ergonomics
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - packages/rosbagger-core/src/rosbagger_core/backend/query.py
  - tests/test_backend_query.py
autonomous: true
requirements: [QURY-08, QURY-09]

must_haves:
  truths:
    - "query(sql, reader, *, alias=True, backend=None) hoists per-topic TableSchema construction AHEAD of resolution: order is parse → build per-topic schemas (keyed by table name) → (if alias) expand_aliases on the rewritten tree → referenced_tables/columns/has_star on the REWRITTEN tree → load → execute (D-02 / Pattern 4)"
    - "Alias expansion is gated to the single-base-topic case (Open Q1): when exactly one distinct base topic is referenced, expand_aliases runs against that topic's msgtype+schema_names; with >1 distinct base topic OR alias=False, short tokens are left untouched (safe no-op)"
    - "The schemas built up front are REUSED in the load loop (no double-build); the rewritten SQL (tree.sql('duckdb')) is what is forwarded to backend.execute (D-02)"
    - "Per referenced topic: under has_star → include=heavy, restrict=None (D-08, today's full materialization); else include=heavy&columns, restrict=(columns & schema_names) | {t,t_ns,stamp,topic} applied per-topic / over-include on JOIN, never under-include (D-06 + D-07 + D-09; table-qualified projection deferred)"
    - "SC3: SELECT vx FROM cmd_vel loads a pyarrow.Table whose column_names == exactly {linear.x, t, t_ns, stamp, topic} and excludes angular.z / linear.y (unreferenced light) and any heavy blob — proven on a ROS-free fixture across ROS1 + ROS2-sqlite + MCAP (D-10) by observing the table query() actually registers via a recording QueryBackend (the existing backend= seam)"
  artifacts:
    - path: "packages/rosbagger-core/src/rosbagger_core/backend/query.py"
      provides: "alias rewrite + projection wiring in query(): hoisted schemas, single-base-topic gate, restrict computation, rewritten-SQL forwarding"
      contains: "expand_aliases"
    - path: "tests/test_backend_query.py"
      provides: "alias integration (vx end-to-end, all 3 formats) + SC3 projection proof (recording-backend observes query()'s registered table) + SELECT * disables projection + JOIN no-op safety"
      contains: "def test_query_alias"
  key_links:
    - from: "backend/query.py query()"
      to: "rosbagger_core.backend.alias.expand_aliases(tree, msgtype, schema_names)"
      via: "single-base-topic gated AST rewrite before referenced_* (D-02)"
      pattern: "expand_aliases\\("
    - from: "backend/query.py query()"
      to: "build_arrow_table(msgs, schema, include=include, restrict=restrict)"
      via: "restrict = (columns & schema_names) | STANDARD when not star, else None (D-09 per-topic over-include)"
      pattern: "build_arrow_table\\(.*restrict="
    - from: "backend/query.py query()"
      to: "backend.execute(rewritten_sql)"
      via: "tree.sql('duckdb') after expansion is forwarded (D-02)"
      pattern: "\\.sql\\("
---

<objective>
Wire both Phase-10 features into the query orchestrator (QURY-08 + QURY-09). `query()` gains an `alias: bool = True` keyword, hoists per-topic `TableSchema` construction ahead of SQL resolution (so the alias existence-gate and the projection both see dotted names — D-02), runs `expand_aliases` (from Plan 10-01) gated to the single-base-topic case (Open Q1), recomputes `referenced_tables/columns/has_star` on the rewritten tree, computes a per-topic `restrict` set (from Plan 10-02), and forwards the REWRITTEN SQL to the backend. This is where the two halves meet and become observable end-to-end — including the SC3 proof.

Purpose: deliver the user-visible behavior the success criteria name — `SELECT vx FROM cmd_vel` returns the right rows (SC1), and a single-column query materializes only the referenced column plus the four standard columns (SC2/SC3).

Output: edits to `backend/query.py`, plus alias-integration + SC3-projection + `SELECT *`-opt-out tests appended to `tests/test_backend_query.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-query-ergonomics/10-CONTEXT.md
@.planning/phases/10-query-ergonomics/10-RESEARCH.md
@.planning/phases/10-query-ergonomics/10-01-SUMMARY.md
@.planning/phases/10-query-ergonomics/10-02-SUMMARY.md

<interfaces>
<!-- The seams this plan consumes. resolve.py + query.py are existing; alias.py + the restrict= params land in Waves 1 (Plans 10-01/10-02). Read query.py in full (read_first). -->

From packages/rosbagger-core/src/rosbagger_core/backend/resolve.py (UNCHANGED — run these on the REWRITTEN tree, D-02):
```python
def parse(sql, dialect="duckdb") -> exp.Expression
def referenced_tables_in(tree) -> set[str]      # CTE-subtracted base-table names
def referenced_columns(tree) -> set[str]        # {c.name for c in tree.find_all(exp.Column)}
def has_star(tree) -> bool                       # qualified t.* counts too
```

From packages/rosbagger-core/src/rosbagger_core/backend/alias.py (Plan 10-01):
```python
ALIAS_PACK: dict[str, dict[str, str]]
def expand_aliases(tree, msgtype: str, schema_names: set[str]) -> exp.Expression  # NEW tree
```

From packages/rosbagger-core/src/rosbagger_core/schema/flatten.py (Plan 10-02):
```python
def build_arrow_table(messages, schema, *, include=None, restrict=None) -> pa.Table
def build_table_schema(msgtype, typestore, *, topic) -> TableSchema   # O(1) metadata, no reader.read()
```

From packages/rosbagger-core/src/rosbagger_core/backend/base.py (the QueryBackend ABC — the seam the SC3 recording-backend implements):
```python
class QueryBackend(abc.ABC):
    @abc.abstractmethod
    def register_table(self, name: str, table: object) -> None: ...   # table is a pyarrow.Table
    @abc.abstractmethod
    def execute(self, sql: str) -> object: ...                        # returns a pyarrow.Table
    @abc.abstractmethod
    def close(self) -> None: ...
    # __enter__/__exit__ inherited (with-managed by query())
```

The current query() pipeline (existing, to be reordered) — parse once, derive tables/columns/star, invert topic↔table maps via `_topic_table_maps` (gives `topic_to_msgtype`), then a load loop that builds `schema = build_table_schema(...)` per topic and calls `build_arrow_table(msgs, schema, include=include)`. `query()` already accepts a `backend: QueryBackend | None = None` keyword (defaults to a fresh `DuckDBBackend` via `_default_backend()`); a caller-supplied backend is the caller's to close. The `columns_by_table` accumulation (for `UnknownColumnError`) and the `duckdb_binder_exception()` catch must be PRESERVED unchanged.

VERIFIED orchestrator restrict computation (10-RESEARCH Code Example 4):
```python
STANDARD = {"t", "t_ns", "stamp", "topic"}
schema_names = {c.name for c in schema.columns}
heavy = {c.name for c in schema.columns if c.is_heavy_blob}
if star:
    include, restrict = heavy, None                 # D-08: star → today's full materialization
else:
    include  = heavy & columns                      # existing QURY-07 rule (unchanged)
    restrict = (columns & schema_names) | STANDARD  # D-06 + D-07; applied per-topic = D-09 over-include
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hoist schema build + wire alias expansion (single-base-topic gated)</name>
  <files>packages/rosbagger-core/src/rosbagger_core/backend/query.py, tests/test_backend_query.py</files>
  <read_first>
    - packages/rosbagger-core/src/rosbagger_core/backend/query.py (the FULL current `query()` + `_topic_table_maps` + `_default_backend` + `duckdb_binder_exception` — you are reordering the pipeline, NOT rewriting it; the lazy-import discipline, the `UnknownTableError` pre-load raise, the `columns_by_table` accumulation, the try/finally backend lifecycle, the BinderException→UnknownColumnError catch, AND the `typestore = reader.typestore` binding currently in Step 4 must all survive intact — see step (3) for the move)
    - packages/rosbagger-core/src/rosbagger_core/backend/alias.py (Plan 10-01: `expand_aliases(tree, msgtype, schema_names)` — the single helper this calls; lazy-import it inside query() like the other backend imports)
    - packages/rosbagger-core/src/rosbagger_core/backend/resolve.py (`referenced_tables_in` returns CTE-subtracted base tables — use it to count distinct base topics for the gate)
    - .planning/phases/10-query-ergonomics/10-RESEARCH.md § Pattern 4 (the hoist requirement: build per-topic schemas BEFORE resolution; reuse them in the load loop — no double-build), § Pitfall 2 (CTE over-rewrite; the gate is the safety net), § Open Q1 / Assumption A3 (the single-base-topic expansion rule to LOCK)
    - tests/test_backend_query.py (the harness + the existing `test_query_join_across_two_topics` JOIN test you must keep green; mirror its fixture/format pattern for the new alias tests)
  </read_first>
  <behavior>
    - Test 1 (`-k alias`): `query('SELECT vx FROM cmd_vel', reader)` on the /cmd_vel (Twist) fixture returns rows whose values equal the fixture's `linear.x` series (0.0,1.0,2.0) — proving `vx` expanded to `linear.x` and the query executed. Parametrize over ROS1 + ROS2-sqlite + MCAP.
    - Test 2 (`-k alias`): `query('SELECT vx FROM cmd_vel', reader, alias=False)` raises the unknown-column teaching error (`UnknownColumnError`) — `vx` is NOT a real column, so with expansion off DuckDB's binder rejects it (proves the `--no-alias`/`alias=False` path threads through).
    - Test 3 (`-k alias`): a JOIN referencing two distinct base topics with an unqualified short token that is NOT a real column is left untouched (single-base-topic gate → no expansion); the existing `test_query_join_across_two_topics` still passes (no regression).
    - Test 4 (`-k alias`): the rewritten SQL is forwarded — `query('SELECT vx AS speed FROM cmd_vel', reader)` returns a result whose column is named `speed` (output alias preserved through the rewrite; D-02 forwards `tree.sql("duckdb")`).
  </behavior>
  <action>
    Reorder `query()` per D-02 / Pattern 4, preserving every existing behavior:
    (1) `tree = parse(sql)` (unchanged).
    (2) Build the topic↔table maps via the existing `_topic_table_maps(reader)` → `topic_to_table, table_to_topic, topic_to_msgtype`.
    (3) HOIST schema construction: build `schemas_by_table: dict[str, TableSchema]` for every mapped topic up front via `build_table_schema(topic_to_msgtype[topic], typestore, topic=topic)` (O(1) metadata, no `reader.read()` — cheap; Pattern 4). This REPLACES the per-topic `build_table_schema` call inside the load loop — reuse `schemas_by_table` there (no double-build). Move the `typestore = reader.typestore` binding UP alongside this hoisted schema build (it is currently bound in Step 4, ~line 166, AFTER this point) — without the move the up-front `build_table_schema(..., typestore, ...)` hits a use-before-assign / NameError.
    (4) Alias step (only when `alias` is True): compute the distinct referenced base topics from `referenced_tables_in(tree)` mapped through `table_to_topic` (ignore names that map to no topic — those raise later). LOCK Open Q1: if exactly ONE distinct base topic resolves, call `tree = expand_aliases(tree, topic_to_msgtype[that_topic], {c.name for c in schemas_by_table[that_table].columns})`; if zero or >1 distinct base topics, SKIP expansion (leave `tree` as-is — safe no-op, A3). Lazy-import `expand_aliases` inside `query()` (offline discipline).
    (5) Recompute `tables = referenced_tables_in(tree)`, `columns = referenced_columns(tree)`, `star = has_star(tree)` on the (possibly rewritten) `tree` (D-02 — these MUST run after expansion).
    (6) Keep the `UnknownTableError` pre-load raise, the load loop (now using `schemas_by_table`), the `columns_by_table` accumulation, the try/finally lifecycle, and the BinderException catch.
    (7) Forward the REWRITTEN SQL: change `backend.execute(sql)` to `backend.execute(tree.sql("duckdb"))` so the expanded dotted columns reach DuckDB (D-02). NOTE: the BinderException catch still works — it inspects the exception, not the SQL string.
    Add `alias: bool = True` as a keyword-only parameter to `query()` and document it (default on, D-11; `alias=False` disables expansion). Do NOT change `_topic_table_maps`/`_default_backend`/`duckdb_binder_exception`. Then append Tests 1-4 from <behavior> to tests/test_backend_query.py (tagged `-k alias`).
  </action>
  <verify>
    <automated>PYTHONPATH="" .venv/bin/python -m pytest tests/test_backend_query.py -k "alias or join" -x</automated>
  </verify>
  <acceptance_criteria>
    - `query(sql, reader, *, alias=True, backend=None)` — the new `alias` keyword exists and defaults True: `grep -nE "alias: bool = True|alias=True" packages/rosbagger-core/src/rosbagger_core/backend/query.py` matches the signature.
    - `SELECT vx FROM cmd_vel` returns the `linear.x` series across all three formats (Test 1 parametrized green); `alias=False` makes the same query raise `UnknownColumnError` (Test 2 green).
    - The rewritten SQL is forwarded: `grep -nE "backend\\.execute\\(.*\\.sql\\(" packages/rosbagger-core/src/rosbagger_core/backend/query.py` matches (NOT `backend.execute(sql)` with the raw string).
    - Schemas are built once: `build_table_schema(` appears EXACTLY once in query.py (the hoisted build), not also inside the load loop — `grep -c "build_table_schema(" packages/rosbagger-core/src/rosbagger_core/backend/query.py` == 1.
    - The `typestore = reader.typestore` binding is hoisted ahead of the up-front `build_table_schema` call (no use-before-assign): in `packages/rosbagger-core/src/rosbagger_core/backend/query.py` the line number of `typestore = reader.typestore` is LESS than the line number of the (single) `build_table_schema(` call — confirm via `grep -nE "typestore = reader.typestore|build_table_schema\\(" packages/rosbagger-core/src/rosbagger_core/backend/query.py` (typestore line precedes the build_table_schema line).
    - `expand_aliases(` is called and lazy-imported inside `query()`: `grep -n "expand_aliases" packages/rosbagger-core/src/rosbagger_core/backend/query.py` shows both an import and a call; the existing `test_query_join_across_two_topics` still passes (single-base-topic gate prevents JOIN mis-expansion).
  </acceptance_criteria>
  <done>`query()` is reordered (typestore + schemas hoisted, alias-gated, rewritten SQL forwarded), the `alias` flag threads through, and the `-k alias`/JOIN tests are green with all prior behavior intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute + thread the projection restrict set (SC2/SC3 + SELECT * opt-out)</name>
  <files>packages/rosbagger-core/src/rosbagger_core/backend/query.py, tests/test_backend_query.py</files>
  <read_first>
    - packages/rosbagger-core/src/rosbagger_core/backend/query.py (the load loop after Task 1 — you add the `restrict` computation per topic and thread it into `build_arrow_table`; note `query()` already exposes the `backend=` seam the SC3 recording-backend rides on)
    - packages/rosbagger-core/src/rosbagger_core/backend/base.py (the `QueryBackend` ABC — `register_table(name, table)` / `execute(sql)` / `close()` + the inherited `__enter__`/`__exit__`; the SC3 recording-backend is a test double implementing this ABC)
    - packages/rosbagger-core/src/rosbagger_core/schema/flatten.py (Plan 10-02: `build_arrow_table(..., restrict=)` — the parameter you now pass)
    - .planning/phases/10-query-ergonomics/10-RESEARCH.md § Code Example 4 (the EXACT star/non-star include+restrict computation), § Pitfall 3 (standard-column union — `| {t,t_ns,stamp,topic}`), § Pitfall 4 (`SELECT *` → empty `columns`; MUST pass `restrict=None`, not `columns | STD`), § Pitfall 5 (`o.*` → has_star true routes to restrict=None first), § Code Example 5 / Assumption A4 (the SC3 assertion shape: a `pyarrow.Table.column_names` set assertion is sufficient proof — a column absent from the Table cannot have had its `reduce(getattr)` run)
    - tests/test_backend_query.py § the existing `test_query_select_star_includes_heavy_blob` / `test_query_projection_omits_heavy_blob` (the heavy-blob seam tests that MUST stay green — projection composes with, not replaces, that behavior) AND the existing swappable-backend test (it shows how a `QueryBackend` subclass is constructed + passed via `backend=` — mirror it for the recording spy)
  </read_first>
  <behavior>
    - Test 1 (SC3, `-k projection`): observe what `query()` ACTUALLY materializes end-to-end via a recording `QueryBackend` supplied through the existing `backend=` seam. The recording backend is a test double implementing the `QueryBackend` ABC: its `register_table(name, table)` captures the registered `pyarrow.Table` (e.g. stores `table` keyed by `name`); `execute(sql)` and `close()` are present to satisfy the ABC. Run `query('SELECT vx FROM cmd_vel', reader, backend=spy)`, then assert on the REGISTERED table for `cmd_vel`: `set(registered.column_names) == {"linear.x", "t", "t_ns", "stamp", "topic"}` and `"angular.z" not in registered.column_names` and `"linear.y" not in registered.column_names`. This proves the integrated wiring alias-expansion → restrict computation → `build_arrow_table` as it runs inside `query()` (NOT a reproduction of the computation in the test). Parametrize over ROS1 + ROS2-sqlite + MCAP (D-10, project norm). To keep `execute` from needing DuckDB on this assertion path, the spy may return an empty/passthrough object from `execute` (the assertion is on the registered table, not the result); if running `execute` for real is preferred, the spy may instead WRAP a real `DuckDBBackend` (forwarding `register_table`/`execute`/`close`) while still capturing the registered table — either is acceptable, but capture-on-register is the load-bearing part. Document the chosen spy shape in the test docstring (A4: the registered-column-set assertion is the sufficient proof).
    - Test 2 (`-k projection`): `query('SELECT t_ns, "linear.x" FROM cmd_vel', reader)` still returns the correct rows (regression — the existing filtered-rows test must hold with projection on).
    - Test 3 (`-k star`): `SELECT * FROM cmd_vel` materializes ALL non-heavy columns (projection disabled under star, D-08) — assert (via the same recording-backend, observing the registered table) that it has `angular.z` and `linear.y` present (NOT dropped to just standard cols — Pitfall 4); the existing `test_query_select_star_includes_heavy_blob` (heavy `data` under `SELECT *`) still passes.
    - Test 4 (`-k star`): `SELECT o.* FROM cmd_vel AS o` also disables projection (qualified star, Pitfall 5) — observed registered table is the full non-heavy set, not just standard cols.
  </behavior>
  <action>
    In the load loop of `query()` (after Task 1), add the per-topic projection computation from Code Example 4, composed with the EXISTING heavy-blob `include` logic (do NOT replace it):
    - Define `STANDARD = {"t", "t_ns", "stamp", "topic"}` (module constant or local).
    - Per topic: `schema_names = {c.name for c in schema.columns}`; `heavy = {c.name for c in schema.columns if c.is_heavy_blob}`.
    - If `star`: `include, restrict = heavy, None` (D-08 — restores today's full non-heavy + blob materialization; CRITICAL per Pitfall 4 — under a star `columns` is empty, so `restrict` MUST be None, never `columns | STANDARD`).
    - Else: `include = heavy & columns` (the UNCHANGED QURY-07 rule); `restrict = (columns & schema_names) | STANDARD` (D-06 + D-07 — the standard-column union is the Pitfall 3 safety net). This restrict is computed and applied PER referenced topic independently against that topic's own `schema_names` — over-include on a JOIN, never under-include (D-09); a name present in two joined topics is materialized in both. Table-qualified projection stays a deferred refinement (D-09 / CONTEXT Deferred Ideas).
    - Pass BOTH to the build: `arrow = build_arrow_table(msgs, schema, include=include, restrict=restrict)`.
    Leave registration/execute/finally exactly as Task 1 left them. Then append Tests 1-4 from <behavior> to tests/test_backend_query.py (tagged `-k projection` / `-k star`). For the SC3 materialized-column assertion (Test 1), implement the recording `QueryBackend` test double described in <behavior> and assert on the table it captures at `register_table` — this observes the column set `query()` actually computed and passed to `build_arrow_table`, not a re-derivation of the restrict expression in the test (research A4 / Code Example 5: an Arrow `column_names` set assertion is sufficient proof). Document the chosen spy shape in the test docstring.
  </action>
  <verify>
    <automated>PYTHONPATH="" .venv/bin/python -m pytest tests/test_backend_query.py -k "projection or star or blob" -x</automated>
  </verify>
  <acceptance_criteria>
    - The restrict computation matches Code Example 4 and is applied per-topic (D-09): `grep -nE "restrict = \\(columns & schema_names\\) \\| " packages/rosbagger-core/src/rosbagger_core/backend/query.py` matches, and the star branch sets `restrict = None` (`grep -nE "restrict.*=.*None" packages/rosbagger-core/src/rosbagger_core/backend/query.py` present in the star branch).
    - `build_arrow_table` is called with BOTH filters: `grep -nE "build_arrow_table\\(.*include=.*restrict=" packages/rosbagger-core/src/rosbagger_core/backend/query.py` matches.
    - SC3 holds across all three formats (Test 1 parametrized green) AND is proven by OBSERVING query()'s real wiring: the test supplies a recording `QueryBackend` via `backend=` and asserts on the table captured at `register_table` (NOT on a restrict expression re-derived in the test). The captured `column_names` for a single-column `vx` query is exactly `{linear.x, t, t_ns, stamp, topic}`, excluding `angular.z`/`linear.y`.
    - `SELECT *` (Test 3) and `o.*` (Test 4) disable projection — the captured registered table keeps the full non-heavy column set (NOT just the four standard cols; Pitfall 4/5); the existing heavy-blob star test still passes.
    - The existing filtered-rows test (`SELECT t_ns, "linear.x" FROM cmd_vel WHERE "linear.x" > 0.5`) still returns the VERIFIED `[1.0, 2.0]` (Test 2 / no regression).
  </acceptance_criteria>
  <done>The projection restrict set is computed and threaded into `build_arrow_table` per D-06/D-07/D-08 and applied per-topic per D-09; SC3 is proven across all three formats by a recording backend observing query()'s actual registered table; `SELECT *`/`o.*` correctly opt out; heavy-blob and filtered-row behavior are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user SQL → query() rewrite + forward (T-05-04) | The user's SQL is the trusted interface (Phase 5 T-05-04). query() now rewrites it inside the sqlglot AST and forwards `tree.sql("duckdb")` to `backend.execute` — still no raw-string interpolation by the orchestrator. The expansion only ever substitutes author-controlled dotted names as quoted identifiers (Plan 10-01's boundary). |
| bag-derived names → table/column resolution (T-05-06) | Topic→table resolution and the `UnknownTableError` pre-load raise are unchanged from Phase 5 (the `TableNameResolver` allow-list, T-03-01). The hoisted schema build reads only O(1) reader metadata (no `reader.read()`), preserving the constant-time-before-load property. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | forwarding rewritten SQL to execute | mitigate | The forwarded string is `tree.sql("duckdb")` regenerated by sqlglot from an AST whose only injected nodes are `exp.column(target, quoted=True)` (Plan 10-01). No orchestrator-built string concatenation. Same trusted-SQL boundary as Phase 5 (T-05-04) — the rewrite changes WHAT is forwarded, not HOW (still sqlglot-rendered). |
| T-10-08 | Information disclosure | alias mis-expansion in a JOIN/CTE exposing a wrong column | mitigate | Open Q1 LOCKED: expansion runs ONLY when exactly one distinct base topic resolves; >1 base topic or any unresolved name → no expansion (safe no-op, A3 / Pitfall 2). The existence-gate (Plan 10-01) is the second net. A CTE re-exposing an alias-named column is left untouched. Integration-tested (JOIN no-op). |
| T-10-09 | Information disclosure | projection dropping a standard column needed for ordering/plot | mitigate | D-07 enforced HERE: the non-star branch unions `{t,t_ns,stamp,topic}` into `restrict` (Pitfall 3); the star branch passes `restrict=None`. SC3 + the star tests pin both. A bare `restrict` could never drop a standard column because the union is unconditional in the non-star path. The restrict is applied per-topic / over-includes on JOINs (D-09), never under-includes. |
| T-10-10 | Denial of Service | `SELECT *` collapsing to standard-cols-only (silent data loss) | mitigate | Pitfall 4 LOCKED: `has_star(tree)` true → `restrict=None` BEFORE the empty `columns` set can produce `columns | STANDARD`. The qualified-star `o.*` case (Pitfall 5) routes through the same `has_star` short-circuit. Both integration-tested (observed via the recording backend). |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (10-RESEARCH § Package Legitimacy Audit). query.py's only new dependency is the in-repo `backend.alias` (Plan 10-01, sqlglot-only). No install task, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint applies. |

**Residual risk after mitigations:** No HIGH-severity threats. The orchestrator continues to forward sqlglot-rendered SQL (never hand-built strings), resolves names through the existing allow-list, and the two Phase-10 features only ever (a) substitute author-controlled quoted identifiers gated to an unambiguous single topic and (b) REDUCE materialization. The JOIN/CTE ambiguity (Open Q1) is closed by the single-base-topic gate and tested; the standard-column and `SELECT *` safety nets (D-07/D-08) are pinned by SC3 + star tests.
</threat_model>

<verification>
- `PYTHONPATH="" .venv/bin/python -m pytest tests/test_backend_query.py -x` is green (alias, projection, star, blob, JOIN, unknown-table, empty-result, swappable-backend all pass).
- SC3 proven across ROS1 + ROS2-sqlite + MCAP (parametrized `-k projection`), observing the table query() actually registers via the recording backend.
- `import rosbagger_core.backend` still leaks no heavy stack (the existing `test_import_backend_package_does_not_pull_heavy_stack` in this file stays green — query.py's new `expand_aliases` import is lazy, inside `query()`).
- `ruff check` + `ruff format --check` on query.py + the test file are clean.
</verification>

<success_criteria>
- `query(sql, reader, *, alias=True)` expands `vx`→dotted per the single-base-topic gate and returns correct rows (QURY-08 / SC1) end-to-end across all three formats.
- A single-column query materializes exactly the referenced column ∪ the four standard columns, excluding unreferenced + heavy columns — SC3 proven on a ROS-free fixture across all three formats by observing query()'s registered table (QURY-09 / SC2 / SC3).
- `SELECT *` / `o.*` disable projection (D-08); the standard-column union (D-07) and heavy-blob include (QURY-07) are preserved; the restrict is applied per-topic / over-includes on JOINs (D-09); the rewritten SQL is forwarded (D-02).
</success_criteria>

<output>
Create `.planning/phases/10-query-ergonomics/10-03-SUMMARY.md` when done.
</output>
