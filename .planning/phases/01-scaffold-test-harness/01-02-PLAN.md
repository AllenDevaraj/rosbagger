---
phase: 01-scaffold-test-harness
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/test_offline_guard.py
  - tests/test_smoke.py
  - .github/workflows/ci.yml
  - uv.lock
autonomous: true
requirements:
  - "SC2: pytest runs green in CI with no ROS installed"
  - "DoD: offline packages import without rclpy (actively guarded, not assumed)"
  - "DoD: test suite runs with no ROS install; coverage >= 80%"

must_haves:
  truths:
    - "ruff check and ruff format --check pass on the repo"
    - "pytest runs green locally and the >=80% coverage gate is enforced via pyproject addopts"
    - "Under the meta-path blocker, importing rclpy/rosbag2_py raises ImportError while rosbagger_core and bagq still import"
    - "Importing rosbagger_core and bagq does not populate sys.modules with any ROS module"
    - "A GitHub Actions workflow runs ruff + pytest on a runner with NO ROS installed"
  artifacts:
    - path: "tests/conftest.py"
      provides: "no_ros pytest fixture using a sys.meta_path import blocker that blocks ROS modules and purges them from sys.modules"
      contains: "meta_path"
    - path: "tests/test_offline_guard.py"
      provides: "the load-bearing offline-invariant tests (blocked-import + no-leak)"
      contains: "no_ros"
    - path: "pyproject.toml"
      provides: "centralized [tool.ruff] and [tool.pytest.ini_options] with --cov-fail-under=80"
      contains: "cov-fail-under=80"
    - path: ".github/workflows/ci.yml"
      provides: "no-ROS CI: setup-uv -> uv sync --locked -> ruff check -> ruff format --check -> pytest; matrix [3.10, 3.12]"
      contains: "astral-sh/setup-uv"
    - path: "uv.lock"
      provides: "committed lockfile for reproducible, tamper-evident installs"
  key_links:
    - from: "tests/conftest.py"
      to: "sys.meta_path"
      via: "monkeypatch installs a _ROSBlocker finder ahead of the real finders"
      pattern: "meta_path"
    - from: "pyproject.toml [tool.pytest.ini_options]"
      to: "coverage gate"
      via: "addopts --cov-fail-under=80"
      pattern: "cov-fail-under=80"
    - from: ".github/workflows/ci.yml"
      to: "uv run pytest"
      via: "CI step on a ROS-free ubuntu-latest runner"
      pattern: "uv run pytest"
---

<objective>
Add the dev tooling and the load-bearing offline guarantee: `ruff` (lint + format) and `pytest` + `pytest-cov` config centralized in the workspace root, a `sys.meta_path`-based offline-import guard that proves the offline packages never pull in ROS, and a GitHub Actions CI workflow that runs the suite green on a runner with no ROS installed.

Purpose: This satisfies Success Criterion #2 (`pytest` runs green in CI with no ROS) and the DoD items "offline packages import without `rclpy`" and "test suite runs with no ROS install; coverage >= 80%". The offline guard is the single most load-bearing test in the project — it protects the architectural "universal / no-ROS" promise for the life of the repo, and must be meaningful even though the dev machine has ROS 2 Humble on its PYTHONPATH.

Output: A green `pytest` run behind an enforced 80% coverage gate, a meaningful offline-import guard, and a no-ROS CI workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-scaffold-test-harness/01-CONTEXT.md
@.planning/phases/01-scaffold-test-harness/01-RESEARCH.md
@CLAUDE.md
@pyproject.toml

<interfaces>
This plan depends on 01-01 having created the workspace. The contracts it builds against (from 01-01):
- Import package names: `rosbagger_core` and `bagq` (underscore import names; hyphen distribution names).
- The root `pyproject.toml` already has `[tool.uv.workspace]`, `[tool.uv.sources]`, and `[dependency-groups] dev = [...]`. This plan ADDS the `[tool.ruff]`, `[tool.ruff.lint]`, `[tool.ruff.format]`, and `[tool.pytest.ini_options]` blocks to that same file.
- `bagq --help` already runs; both packages already editable-install via `uv sync`.
- `bagq.cli:app` is the typer application symbol (console-script target) — the smoke test imports it as `from bagq.cli import app`.
</interfaces>

<notes>
The verified, runnable config and test code for THIS plan live in 01-RESEARCH.md:
- "Code Examples 5" — the EXACT, verified `conftest.py` `_ROSBlocker` + `no_ros` fixture and the two guard tests (`test_core_imports_without_ros`, `test_no_ros_leaked_into_sys_modules`). Transcribe these; they were executed and pass with ROS blocked.
- "Code Examples 6" — the verified `[tool.ruff]`/`[tool.ruff.lint]`/`[tool.ruff.format]`/`[tool.pytest.ini_options]` blocks (line-length 100; select E,F,I,UP,B,SIM; quote-style double; addopts cover rosbagger_core and bagq, term-missing report, cov-fail-under=80; testpaths ["tests"]).
- "Code Examples 7" — the verified no-ROS GitHub Actions workflow (matrix ["3.10","3.12"]; astral-sh/setup-uv@v8; uv python pin; uv sync --locked --dev; uv run ruff check .; uv run ruff format --check .; uv run pytest).

CRITICAL — why the naive guard is wrong (01-RESEARCH.md Pitfall 4): a bare `with pytest.raises(ImportError): import rclpy` passes for the WRONG reason on clean CI (rclpy genuinely absent) and proves nothing on the dev box (rclpy genuinely present). The meta-path blocker makes the test meaningful in BOTH environments. Do NOT substitute the naive version.

Coverage reality: Phase 1 ships almost no domain code (light `__init__` + a no-command typer app). To hit >=80% honestly, the guard tests already exercise the `rosbagger_core`/`bagq` imports; ADD a tiny `tests/test_smoke.py` that invokes `bagq --help` via typer's CliRunner and asserts exit code 0 plus `__version__` presence, so the small surface that exists is covered. Do NOT lower the gate below 80; if coverage is short, cover the real surface (imports, `--help`, `--version`) rather than weakening the threshold.

PYTHONPATH hazard (01-RESEARCH.md Runtime State Inventory): the dev shell has ROS on PYTHONPATH. The uv venv is largely isolated, and the meta-path blocker neutralizes the leak inside the guard test; CI is ROS-free so it is moot there. The guard test must remain valid regardless.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add centralized ruff + pytest/coverage config to the workspace root</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current root state from 01-01 — read it before editing so the uv blocks are preserved)
    - .planning/phases/01-scaffold-test-harness/01-RESEARCH.md ("Code Examples 6" — the verified ruff + pytest config blocks)
  </read_first>
  <action>
    Edit the existing root `pyproject.toml` (created by 01-01) and ADD, without removing the existing `[tool.uv.*]` or `[dependency-groups]` blocks:
    - `[tool.ruff]` with `line-length = 100` and `src = ["packages/rosbagger-core/src", "packages/bagq/src", "tools", "tests"]`.
    - `[tool.ruff.lint]` with `select = ["E", "F", "I", "UP", "B", "SIM"]` (pycodestyle, pyflakes, isort, pyupgrade, bugbear, simplify).
    - `[tool.ruff.format]` with `quote-style = "double"`.
    - `[tool.pytest.ini_options]` with `addopts = "--cov=rosbagger_core --cov=bagq --cov-report=term-missing --cov-fail-under=80"` and `testpaths = ["tests"]`.
    Note the `src` list includes `tools` and `tests` so ruff lints the fixture generator (plan 01-03) and the test files too.
  </action>
  <verify>
    <automated>grep -q 'cov-fail-under=80' pyproject.toml && grep -q 'tool.ruff.lint' pyproject.toml && grep -q 'tool.pytest.ini_options' pyproject.toml && grep -q 'tool.uv.workspace' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - Root `pyproject.toml` now contains `[tool.ruff]`, `[tool.ruff.lint]` (with the six rule families), `[tool.ruff.format]`, and `[tool.pytest.ini_options]`.
    - `[tool.pytest.ini_options].addopts` contains `--cov-fail-under=80` and `--cov=rosbagger_core --cov=bagq`.
    - The pre-existing `[tool.uv.workspace]`, `[tool.uv.sources]`, and `[dependency-groups]` blocks from 01-01 are still present (not clobbered).
  </acceptance_criteria>
  <done>The workspace root centralizes ruff (lint+format) and pytest (with the >=80% coverage gate) config, and the 01-01 workspace blocks remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Write the offline-import guard (conftest fixture + guard tests) and a smoke test</name>
  <files>tests/conftest.py, tests/test_offline_guard.py, tests/test_smoke.py</files>
  <read_first>
    - .planning/phases/01-scaffold-test-harness/01-RESEARCH.md ("Code Examples 5" — verified `_ROSBlocker`, `no_ros` fixture, and the two guard tests; Pitfall 4 — why the naive guard is wrong; "Pattern 2: Offline-import guard via sys.meta_path blocker")
    - .planning/phases/01-scaffold-test-harness/01-CONTEXT.md (offline invariant — actively guarded, not assumed)
  </read_first>
  <action>
    Create `tests/conftest.py` with a `_ROSBlocker` meta-path finder whose `find_spec` raises `ImportError` for any module whose top-level name is in the set `{"rclpy", "rosbag2_py", "rosidl_runtime_py", "ament_index_python"}`, and a `no_ros` pytest fixture that (a) `monkeypatch`-prepends a `_ROSBlocker()` instance to `sys.meta_path` and (b) deletes any already-imported blocked modules from `sys.modules` via `monkeypatch.delitem(..., raising=False)`. Transcribe the verified version from 01-RESEARCH.md Code Examples 5 exactly — do NOT use the naive try/except form (Pitfall 4).
    Create `tests/test_offline_guard.py` with the two verified tests:
    - `test_core_imports_without_ros(no_ros)`: asserts `importlib.import_module("rclpy")` and `importlib.import_module("rosbag2_py")` each raise `ImportError`, then asserts `importlib.import_module("rosbagger_core")` and `importlib.import_module("bagq")` succeed.
    - `test_no_ros_leaked_into_sys_modules()`: imports `rosbagger_core` and `bagq`, then asserts no module whose top-level name is `rclpy`/`rosbag2_py` appears in `sys.modules`.
    Create `tests/test_smoke.py` covering the small real surface so the >=80% gate is met honestly: use `typer.testing.CliRunner` to invoke the `bagq` app (`from bagq.cli import app`) with `--help` and assert exit code 0 and that usage text appears; assert `rosbagger_core.__version__` and `bagq.__version__` are non-empty strings. Do NOT lower the coverage gate to compensate.
  </action>
  <verify>
    <automated>uv run pytest tests/test_offline_guard.py tests/test_smoke.py -x -q -p no:cov</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` defines a `no_ros` fixture that mutates `sys.meta_path` (grep finds `meta_path`) and purges blocked modules from `sys.modules`.
    - `test_core_imports_without_ros` asserts `ImportError` is raised for `rclpy` AND `rosbag2_py` under the blocker, AND that `rosbagger_core` and `bagq` still import.
    - `test_no_ros_leaked_into_sys_modules` asserts `rclpy`/`rosbag2_py` are absent from `sys.modules` after importing the offline packages.
    - `test_smoke.py` asserts `bagq --help` exits 0 via CliRunner.
    - `uv run pytest tests/test_offline_guard.py tests/test_smoke.py` exits 0.
    - `tests/conftest.py` does NOT contain a bare `with pytest.raises(ImportError): import rclpy` as the sole guard (the naive anti-pattern).
  </acceptance_criteria>
  <done>The offline guard proves — meaningfully on both clean CI and a ROS-equipped dev box — that importing the offline packages raises ImportError for ROS modules under the blocker and leaks no ROS module into sys.modules, and the smoke test covers the CLI `--help` surface.</done>
</task>

<task type="auto">
  <name>Task 3: Lock dependencies, add the no-ROS CI workflow, and prove the full gate is green</name>
  <files>.github/workflows/ci.yml, uv.lock</files>
  <read_first>
    - .planning/phases/01-scaffold-test-harness/01-RESEARCH.md ("Code Examples 7" — verified CI workflow; Open Question O-3 — matrix ["3.10","3.12"]; Security Domain — `uv sync --locked` for reproducible/tamper-evident builds; Pitfall 6 — pin the interpreter)
  </read_first>
  <action>
    Generate/refresh the lockfile by running `uv lock` (or `uv sync`, which writes it), producing `uv.lock` at the repo root — this is committed for reproducible, tamper-evident installs (threat T-01-DRIFT / T-01-SC).
    Create `.github/workflows/ci.yml` (transcribe 01-RESEARCH.md Code Examples 7): `name: ci`; triggers `on: [push, pull_request]`; one `test` job on `runs-on: ubuntu-latest` (a runner with NO ROS — that is the whole point); `strategy.matrix.python-version: ["3.10", "3.12"]`; steps in order: `actions/checkout@v4`; `astral-sh/setup-uv@v8` with `enable-cache: true`; `uv python pin ${{ matrix.python-version }}`; `uv sync --locked --dev`; `uv run ruff check .`; `uv run ruff format --check .`; `uv run pytest`. The coverage gate is enforced through the pytest `addopts` from Task 1, so no extra `--cov` flag is needed in CI. Set the minimum `permissions:` (`contents: read`) at the workflow level and add NO secrets — there are none to add (least-privilege per threat T-01-CI).
    Finally, run the FULL local gate to prove green: `uv run ruff check .`, then `uv run ruff format --check .`, then `uv run pytest` (which enforces `--cov-fail-under=80`). Fix any lint/format/coverage failures by correcting code/config — NOT by weakening the gate.
  </action>
  <verify>
    <automated>test -f uv.lock && grep -q 'astral-sh/setup-uv' .github/workflows/ci.yml && grep -q '"3.10"' .github/workflows/ci.yml && grep -q 'uv sync --locked' .github/workflows/ci.yml && grep -q 'contents: read' .github/workflows/ci.yml && uv run ruff check . && uv run ruff format --check . && uv run pytest</automated>
  </verify>
  <acceptance_criteria>
    - `uv.lock` exists at the repo root.
    - `.github/workflows/ci.yml` uses `astral-sh/setup-uv`, a `["3.10", "3.12"]` matrix, `uv sync --locked --dev`, and runs `uv run ruff check .` + `uv run ruff format --check .` + `uv run pytest` on `ubuntu-latest` with NO ROS install step.
    - The workflow declares least-privilege `permissions: contents: read` and adds no secrets.
    - Locally: `uv run ruff check .` exits 0, `uv run ruff format --check .` exits 0, and `uv run pytest` exits 0 with the `--cov-fail-under=80` gate satisfied (coverage >= 80%).
  </acceptance_criteria>
  <done>The lockfile is committed, the no-ROS CI workflow runs lint+format+tests on a 3.10/3.12 matrix with least-privilege permissions, and the full local gate (ruff + format + pytest at >=80% coverage) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 1 is dev infrastructure: no auth, no network service, no untrusted input parsing, no data persistence, no cryptography. The boundaries this plan touches:

| Boundary | Description |
|----------|-------------|
| repo build config → uv resolver | locked vs. unlocked dependency resolution determines build reproducibility |
| GitHub Actions runner → repo | CI workflow executes with whatever token/permissions it is granted |
| import graph → host ROS install | the dev host has ROS on PYTHONPATH; the offline packages must not pull it in |

## STRIDE Threat Register

Surface area is intentionally narrow; nothing here is rated `high`.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-DRIFT | Tampering | dependency resolution | mitigate | Commit `uv.lock`; CI uses `uv sync --locked` so installs are reproducible and tamper-evident; upper-bounded pins (set in 01-01) freeze verified APIs. |
| T-01-CI | Elevation of Privilege | `.github/workflows/ci.yml` | mitigate | Least-privilege `permissions: contents: read`; pinned action versions (`actions/checkout@v4`, `astral-sh/setup-uv@v8`); no secrets referenced (none exist in this phase). |
| T-01-ROS | Tampering (architectural invariant) | offline import graph | mitigate | The `sys.meta_path` offline guard test actively proves ROS modules raise ImportError under the blocker and never leak into `sys.modules` — meaningful on both clean CI and the ROS-equipped dev box (Pitfall 4). |
| T-01-SC | Tampering / Spoofing | dev-group dependency installs | accept (verified upstream) | The supply-chain legitimacy gate is owned by plan 01-01's blocking-human checkpoint before first install; the dev group here (ruff/pytest/pytest-cov/rosbags) was covered by that audit. No re-gate needed for the same verified set. |
</threat_model>

<verification>
- `uv run ruff check .` and `uv run ruff format --check .` both exit 0.
- `uv run pytest` exits 0 with the `--cov-fail-under=80` gate satisfied.
- Under the `no_ros` fixture, `rclpy`/`rosbag2_py` raise `ImportError` while `rosbagger_core`/`bagq` import; no ROS module leaks into `sys.modules`.
- `.github/workflows/ci.yml` runs the gate on a ROS-free `ubuntu-latest` across the 3.10/3.12 matrix with least-privilege permissions.
- `uv.lock` is present for reproducible installs.
</verification>

<success_criteria>
- SC2 satisfied: `pytest` runs green and the no-ROS CI workflow enforces it (lint + format + tests + 80% coverage) on a runner with no ROS installed.
- The offline-import guard is meaningful (not the naive form) and protects the universal/no-ROS invariant.
- DoD items "offline packages import without rclpy" and "test suite runs with no ROS install; coverage >= 80%" are met.
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-test-harness/01-02-SUMMARY.md` when done.
</output>
