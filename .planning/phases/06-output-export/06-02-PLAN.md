---
phase: 06-output-export
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - pyproject.toml
  - uv.lock
  - packages/rosbagger-core/src/rosbagger_core/output/__init__.py
  - packages/rosbagger-core/src/rosbagger_core/output/plot.py
  - packages/bagq/src/bagq/cli.py
  - tests/test_output_plot.py
autonomous: true
requirements: [OUT-04]

must_haves:
  truths:
    - "bagq query \"SELECT t_ns, ...numeric... FROM topic\" BAG --plot writes a non-empty PNG line chart of the numeric columns vs t_ns"
    - "bagq query ... --plot chart.png writes to that exact path; bare --plot writes the default plot.png"
    - "matplotlib is set to the headless Agg backend BEFORE pyplot is imported, so plotting succeeds with DISPLAY unset"
    - "A result with no numeric columns (only topic/string/LIST) gives a clear 'nothing to plot' error, not a blank chart"
    - "A 0-row result gives a clear 'nothing to plot' message rather than an empty figure"
    - "On a base install without matplotlib, --plot raises a teaching error telling the user to install bagq[plot] (not a bare ModuleNotFoundError)"
    - "import rosbagger_core / .backend / .output still pull no duckdb/sqlglot/pyarrow/matplotlib into sys.modules"
  artifacts:
    - path: "packages/rosbagger-core/src/rosbagger_core/output/plot.py"
      provides: "plot_table(table, path) — lazy matplotlib, Agg, numeric-cols vs t_ns; ImportError->teaching message"
      exports: ["plot_table"]
    - path: "pyproject.toml"
      provides: "matplotlib>=3.8 added to root [dependency-groups] dev so CI/tests can exercise --plot"
      contains: "matplotlib"
    - path: "packages/bagq/src/bagq/cli.py"
      provides: "--plot [FILE] optional-value flag on the query command + plot routing"
      contains: "flag_value"
    - path: "tests/test_output_plot.py"
      provides: "pytest.importorskip('matplotlib') + assert a non-empty PNG is written; no-numeric / 0-row / default-filename cases"
      contains: "importorskip"
  key_links:
    - from: "packages/bagq/src/bagq/cli.py"
      to: "rosbagger_core.output.plot_table"
      via: "lazy import inside the query command body, called when --plot is set"
      pattern: "plot_table"
    - from: "packages/rosbagger-core/src/rosbagger_core/output/plot.py"
      to: "matplotlib Agg backend"
      via: "matplotlib.use('Agg') before import matplotlib.pyplot"
      pattern: "matplotlib.use\\(.Agg."
---

<objective>
Add the minimal `--plot [FILE]` capability to the `bagq query` command: a headless matplotlib line chart of the NUMERIC result columns (int/float Arrow types; excluding `topic`/string/LIST/STRUCT) plotted against `t_ns`. Delivers OUT-04. Make it testable by adding `matplotlib>=3.8` to the root dev group (the base `bagq` install stays lean behind the `bagq[plot]` extra).

Purpose: OUT-04 is the last of the four output forms — an intentionally-minimal plot (not a PlotJuggler rebuild). It is gated behind a lazy, optional matplotlib import so the offline invariant and the lean base install both hold; a missing matplotlib gives a teaching message, not a crash.

Output: `output/plot.py` (lazy-import `plot_table`), the `--plot` optional-value flag + routing on the existing `query` command, matplotlib added to the dev group (so `uv.lock` changes — commit it), and a `pytest.importorskip`-guarded plot test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-output-export/06-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. The query command + output module already exist from 06-01. -->

From 06-01 — packages/bagq/src/bagq/cli.py now has:
```python
@app.command()
def query(sql: ..., bags: ..., out: Path | None = None, fmt: str = "table") -> None: ...
# Body: with RosbagsReader(bags) as reader: result = query(sql, reader)
# then routes on out / fmt. ADD a `--plot` option + a plot branch here.
```

From 06-01 — rosbagger_core.output re-exports `rows_for_display`, `to_json`, `write_table`.
ADD `plot_table` to that re-export.

Result column types (Phase 3 QURY-04): `t` timestamp[ns], `t_ns` int64, `stamp` timestamp[ns],
`topic` string, plus dotted body columns and LIST/STRUCT. Numeric = int/float Arrow types,
detected via pyarrow.types.is_integer / is_floating. `t_ns` is the x-axis (excluded from y).

Root manifest — pyproject.toml [dependency-groups] dev currently:
```toml
dev = ["ruff>=0.15,<0.16", "pytest>=8,<10", "pytest-cov>=6", "rosbags>=0.11,<0.12"]
```
matplotlib is ALREADY declared as the bagq[plot] extra (packages/bagq/pyproject.toml:
`[project.optional-dependencies] plot = ["matplotlib>=3.8"]`) — same package, no new name.
</interfaces>

<read_first>
- packages/bagq/src/bagq/cli.py (the `query` command from 06-01 — ADD the `--plot` option + plot routing here; the lazy-import-in-body discipline)
- packages/rosbagger-core/src/rosbagger_core/output/__init__.py and output/plot.py-neighbors render.py/export.py (the established lazy-import shape for the new plot.py; the re-export to extend)
- pyproject.toml (the root [dependency-groups] dev to extend with matplotlib>=3.8)
- packages/bagq/pyproject.toml (matplotlib is already the `plot` extra — same constraint, do not change it)
- tests/test_offline_guard.py (the heavy-stack invariant; note matplotlib is NOT in `_HEAVY_STACK` today — the plot module must still be import-light so `import rosbagger_core.output` from 06-01's guard test stays green)
- tests/test_cli_query.py (the 06-01 CliRunner pattern this plot test mirrors)
- .planning/phases/06-output-export/06-RESEARCH.md sections: Pattern 3 (--plot headless numeric-vs-t_ns), Pattern 4 (--plot [FILE] optional-value flag idiom), Pitfall 3 (empty result), Pitfall 4 (no numeric columns), Pitfall 5 (matplotlib not installed -> teaching message + importorskip in tests), Pitfall 7 (close figures), Assumption A1 (default filename plot.png), Installation note (add matplotlib to dev group)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: plot_table — headless matplotlib line chart, numeric vs t_ns; matplotlib in dev group</name>
  <files>pyproject.toml, uv.lock, packages/rosbagger-core/src/rosbagger_core/output/plot.py, packages/rosbagger-core/src/rosbagger_core/output/__init__.py, tests/test_output_plot.py</files>
  <read_first>
    - .planning/phases/06-output-export/06-RESEARCH.md: Pattern 3 (the verified plot_table body — Agg before pyplot, x=t_ns fallback t, y=numeric excluding t_ns), Pitfall 3/4/5/7, Installation note, Assumption A1
    - packages/rosbagger-core/src/rosbagger_core/output/export.py (the sibling lazy-import shape: stdlib-only top level, heavy import inside the function)
    - pyproject.toml (the dev group to extend)
    - tools/make_fixtures.py (the fixtures: /imu has numeric scalar fields + the orientation_covariance LIST; /cmd_vel has numeric Twist scalars and no header)
  </read_first>
  <behavior>
    - plot_table(table, path): a result with t_ns plus ≥1 numeric column (e.g. SELECT t_ns, "orientation_covariance" is a LIST so excluded — use a real numeric Twist/Imu scalar) writes a non-empty PNG to `path` (file exists and size > 0).
    - The matplotlib backend is Agg (set BEFORE importing pyplot) so the write succeeds with DISPLAY unset / headless.
    - plot_table on a result with NO numeric columns (e.g. only `topic`/a string/a LIST column, or only t_ns which is the x-axis) raises a clear error/ValueError whose message says nothing is plottable (no numeric y-columns).
    - plot_table on a result with neither `t`/`t_ns` raises a clear error (no x column).
    - plot_table on a 0-row result raises/returns a clear "0 rows — nothing to plot" signal rather than writing a blank chart.
    - The figure is closed after savefig (no lingering open-figure warning across repeated calls).
  </behavior>
  <action>
    Add `"matplotlib>=3.8"` to the root `pyproject.toml` `[dependency-groups] dev` list (keep the existing entries; this is the same package already declared as the `bagq[plot]` extra — RESEARCH Installation note + Package Legitimacy Audit: Approved). Then run `uv lock` (or `uv sync`) so `uv.lock` resolves matplotlib into the dev group — `uv.lock` WILL change and MUST be committed (downstream-consumer note).

    Create `packages/rosbagger-core/src/rosbagger_core/output/plot.py` with a `from __future__ import annotations`, a `TYPE_CHECKING`-guarded `import pyarrow` for annotations, and a docstring stating the offline invariant (stdlib-only top level; matplotlib + pyarrow imported INSIDE the function). Implement `plot_table(table, path)` per RESEARCH Pattern 3 (verified headless this session): wrap `import matplotlib; matplotlib.use("Agg"); import matplotlib.pyplot as plt` in a try/except `ImportError` that re-raises `RuntimeError("Plotting needs matplotlib. Install with: pip install 'bagq[plot]'")` (Pitfall 5 teaching message). `matplotlib.use("Agg")` MUST precede the pyplot import (Pitfall 6-of-plot / headless). Then `import pyarrow.types as pat`. Guard 0 rows first (`table.num_rows == 0` -> raise ValueError "0 rows — nothing to plot"; Pitfall 3). Pick `x_name = "t_ns" if present else ("t" if present else None)`. Build `y_cols` = column names where `pat.is_integer(col.type) or pat.is_floating(col.type)`, EXCLUDING `t_ns` (the x-axis) — note this naturally excludes string/LIST/STRUCT and topic. If `x_name is None or not y_cols` raise `ValueError("Nothing to plot: need a t/t_ns column and ≥1 numeric result column.")` (Pitfall 4). Get `x = table.column(x_name).to_numpy(zero_copy_only=False)`; `fig, ax = plt.subplots()`; for each y col `ax.plot(x, table.column(yc).to_numpy(zero_copy_only=False), label=yc)`; `ax.set_xlabel(x_name); ax.legend(); fig.savefig(str(path)); plt.close(fig)` (Pitfall 7 — always close). Accept `path` as `str | os.PathLike`.

    Extend `output/__init__.py` to also re-export `plot_table` from `.plot` (alongside the 06-01 re-exports), keeping the top level stdlib-only (the re-export is safe because `.plot`'s top level imports no matplotlib/pyarrow).

    Create `tests/test_output_plot.py` mirroring tests/test_cli_query.py's harness (repo-root sys.path insert for `tools`, the `PYTHONPATH=""` local-run docstring note) and guard the WHOLE module with `pytest.importorskip("matplotlib")` at module top (Pitfall 5 — a contributor without the dev group is skipped, not errored). Build real results via `RosbagsReader` + `query(...)`: a numeric case (`SELECT t_ns, "linear.x" FROM cmd_vel` or another numeric Twist scalar — confirm the exact dotted column name from `bagq tables`/the schema, do NOT guess) -> assert the PNG exists and `os.path.getsize(path) > 0`; a no-numeric case (`SELECT topic FROM cmd_vel`) -> assert `plot_table` raises ValueError; a 0-row case (`WHERE 1=0`) -> assert it raises with a nothing-to-plot message. Write PNGs under `tmp_path`.
  </action>
  <verify>
    <automated>cd /home/the2xman/Desktop/rosbagger && PYTHONPATH="" uv run pytest tests/test_output_plot.py -q</automated>
  </verify>
  <done>matplotlib is in the dev group and `uv.lock` is updated; `plot_table` writes a non-empty headless PNG for a numeric-vs-t_ns result, raises clear errors on no-numeric / no-x / 0-row, sets Agg before pyplot, and closes the figure; the `importorskip`-guarded test passes.</done>
</task>

<task type="auto">
  <name>Task 2: --plot [FILE] optional-value flag + routing on the bagq query command</name>
  <files>packages/bagq/src/bagq/cli.py, tests/test_output_plot.py</files>
  <read_first>
    - packages/bagq/src/bagq/cli.py (the `query` command from 06-01 — add the `--plot` option + the plot branch)
    - .planning/phases/06-output-export/06-RESEARCH.md: Pattern 4 (the EXACT optional-value flag idiom: is_flag=False, flag_value=<sentinel>, default=None), Assumption A1 (default filename plot.png), Pitfall 5 (the ImportError-as-RuntimeError teaching message surfacing)
    - tests/test_cli_query.py (the CliRunner test pattern from 06-01 to extend)
  </read_first>
  <action>
    Add the `--plot [FILE]` option to the `query` command in `packages/bagq/src/bagq/cli.py` using the verified optional-value idiom from RESEARCH Pattern 4. Define a module-level sentinel distinct from any real path (e.g. `_PLOT_DEFAULT = "\x00bagq-default-plot"`). Declare the option with the click kwargs passed through typer: `plot: Annotated[str | None, typer.Option("--plot", is_flag=False, flag_value=_PLOT_DEFAULT, help="Plot numeric columns vs t. Bare = write plot.png; --plot FILE = that file.")] = None`. Semantics: omitted -> `None` (no plot); bare `--plot` -> the sentinel (plot to the default filename); `--plot FILE` -> `"FILE"`.

    In the command body, add a plot branch: when `plot is not None`, resolve the target path = `"plot.png"` if `plot == _PLOT_DEFAULT` else `plot` (Assumption A1 default), lazy-`from rosbagger_core.output import plot_table` (inside the body, alongside the 06-01 lazy imports), call `plot_table(result, target)`, and print a one-line confirmation (the written path). Place the plot branch so `--plot` and `-o`/`--format` routing compose sensibly: `--plot` is its own output sink — if `--plot` is set, plot (it may coexist with a default table render or not; keep it simple: when `--plot` is set, do the plot and return, ignoring table render). Let the `RuntimeError` from `plot_table` (matplotlib-missing teaching message) and any `ValueError` (nothing-to-plot) PROPAGATE — Phase 7 owns formatting errors; do NOT swallow them.

    Extend `tests/test_output_plot.py` (still under the module-level `pytest.importorskip("matplotlib")`) with CliRunner cases on `app`: (a) `bagq query "SELECT t_ns, <numeric-col> FROM cmd_vel" BAG --plot <tmp>.png` exits 0 and the named PNG exists (size > 0); (b) bare `bagq query "SELECT t_ns, <numeric-col> FROM cmd_vel" BAG --plot` (no value) exits 0 and writes `plot.png` — invoke with the CliRunner's isolated filesystem or `monkeypatch.chdir(tmp_path)` so the default `plot.png` lands in tmp, then assert it exists; (c) `bagq --help` shows the `--plot` option on the query command. Assert stable data only.
  </action>
  <verify>
    <automated>cd /home/the2xman/Desktop/rosbagger && PYTHONPATH="" uv run pytest tests/test_output_plot.py -q</automated>
  </verify>
  <done>`bagq query ... --plot FILE` writes that PNG and bare `--plot` writes `plot.png` (OUT-04); the optional-value flag distinguishes omitted/bare/with-value; matplotlib-missing and nothing-to-plot errors propagate; `--help` shows `--plot`. The plot tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: full-suite green at coverage gate + offline guard intact + ruff clean</name>
  <files>tests/test_output_plot.py</files>
  <read_first>
    - tests/test_offline_guard.py (the existing output-subpackage guard test from 06-01 — confirm it still passes; the heavy stack is duckdb/sqlglot/pyarrow)
    - .planning/phases/06-output-export/06-RESEARCH.md: Validation Architecture (the >=80% coverage gate, exercise real behavior; the importorskip note), Pitfall 6
  </read_first>
  <action>
    Run the FULL suite and confirm: (1) the 06-01 offline-guard test `test_import_output_subpackage_does_not_pull_heavy_query_stack` still passes — adding `plot.py` (lazy matplotlib) must NOT make `import rosbagger_core.output` pull duckdb/sqlglot/pyarrow; (2) the `--cov-fail-under=80` gate (root pyproject addopts) holds with the plot module + `--plot` flag covered. The plot module is exercised only under `pytest.importorskip("matplotlib")`; since matplotlib is now in the dev group, CI and local `uv run` both have it, so the plot code is covered there. If any `plot.py` branch (no-numeric, no-x, 0-row, the ImportError path) is uncovered, add the missing assertion to tests/test_output_plot.py rather than weakening the gate (the ImportError path can be covered by monkeypatching the matplotlib import to raise, if needed — otherwise leave it as a documented defensive line).

    Confirm `uv.lock` carries the matplotlib dev-group change from Task 1 (it is part of this plan's deliverable and must be committed). Finally run `PYTHONPATH="" uv run ruff format .` then `ruff check .` to clear the recurring new-test-file format drift (downstream-consumer reminder).
  </action>
  <verify>
    <automated>cd /home/the2xman/Desktop/rosbagger && PYTHONPATH="" uv run ruff format --check . && PYTHONPATH="" uv run ruff check . && PYTHONPATH="" uv run pytest -q</automated>
  </verify>
  <done>Full suite green with `--cov-fail-under=80` satisfied; the output-subpackage offline-guard test still passes (matplotlib/duckdb/pyarrow not leaked on `import rosbagger_core.output`); `uv.lock` reflects matplotlib in the dev group; ruff format-check and lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI `--plot [FILE]` → filesystem | The user-supplied (or default `plot.png`) path is where matplotlib writes the PNG |
| bag numeric data → plotted chart | Untrusted bag-derived numeric values (already in Arrow from Phase 5) become y-series |
| new dev dependency (matplotlib) → install set | matplotlib is added to the root dev group |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | `--plot FILE` path → `fig.savefig(path)` | accept | Local single-user CLI; the user chose the output path (or the `plot.png` default in CWD) and can only overwrite their own files. No SQL is built on the plot path (unlike `COPY`), so no injection surface; plain filesystem write. Documented disposition |
| T-06-05 | Denial of service | matplotlib figure lifecycle (plot.py) | mitigate | `plt.close(fig)` after every `savefig` (RESEARCH Pitfall 7) so repeated `--plot` calls / test loops do not leak figures or grow memory |
| T-06-06 | Information disclosure | bag numeric values → PNG | accept | The chart visualizes the user's own bag data; no new exposure. Disposition: accept |
| T-06-SC | Tampering | pip/uv install (matplotlib) | mitigate | matplotlib is added to the dev group — but it is a pre-declared, locked, mature package (already the `bagq[plot]` extra; RESEARCH Package Legitimacy Audit: Approved — PyPI, since 2003, github.com/matplotlib/matplotlib, very high downloads). NOT `[ASSUMED]`/`[SUS]`/`[SLOP]`, so no blocking human-verify checkpoint is required; `uv.lock` pins the resolved version |
</threat_model>

<verification>
- `PYTHONPATH="" uv run pytest tests/test_output_plot.py -q` — the plot suite passes (skipped only if matplotlib absent, but it is in the dev group).
- `PYTHONPATH="" uv run pytest tests/test_offline_guard.py -q` — `import rosbagger_core.output` still leaks no heavy stack after `plot.py` is added.
- `PYTHONPATH="" uv run pytest -q` — full suite green at `--cov-fail-under=80`.
- `PYTHONPATH="" uv run ruff format --check . && PYTHONPATH="" uv run ruff check .` — clean.
- `git status` shows `uv.lock` modified (matplotlib resolved into the dev group) — commit it.
</verification>

<success_criteria>
- OUT-04: `bagq query "<SQL>" BAG --plot [FILE]` produces a minimal headless line chart of numeric result columns vs `t_ns` (Agg backend; numeric = int/float, excluding `t_ns`/topic/string/LIST). Bare `--plot` writes `plot.png`; `--plot FILE` writes that file.
- No-numeric / no-x / 0-row results give clear "nothing to plot" errors, not blank charts.
- A base install without matplotlib surfaces a teaching "install `bagq[plot]`" message (not a bare ModuleNotFoundError); tests guard with `pytest.importorskip("matplotlib")`.
- matplotlib added to the root dev group; `uv.lock` updated and committed.
- Offline invariant intact (`import rosbagger_core.output` pulls no duckdb/sqlglot/pyarrow/matplotlib); full suite green at the 80% gate; ruff clean.
</success_criteria>

<output>
Create `.planning/phases/06-output-export/06-02-SUMMARY.md` when done.
</output>
