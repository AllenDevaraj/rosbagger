---
phase: 16-native-desktop-gui-pyside6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-desktop/pyproject.toml
  - packages/rosbagger-desktop/src/rosbagger_desktop/__init__.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/cli.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/capabilities.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/panels/__init__.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/panels/inspect_panel.py
  - packages/rosbagger-desktop/src/rosbagger_desktop/panels/tf_panel.py
  - pyproject.toml
  - uv.lock
  - tests/test_offline_guard.py
  - tests/test_desktop.py
autonomous: true
requirements: [SC1, SC3, SC4, SC5]
user_setup: []

must_haves:
  truths:
    - "rosbagger-desktop --help exits 0 without constructing any Qt object"
    - "rosbagger-desktop <BAG> opens a QMainWindow on that bag (the shell builds, no crash)"
    - "The Inspect panel renders real collect_bag_info topic rows + collect_table_schemas columns from the shared reader"
    - "The TF panel renders real collect_tf_report edges/gaps (and a teaching line on NoTransformsError)"
    - "import rosbagger_core / import bagq pull no PySide6 (the Qt-free invariant)"
    - "import rosbagger_desktop.cli stays Qt-free AND ROS-free (QApplication imported inside main())"
  artifacts:
    - path: "packages/rosbagger-desktop/pyproject.toml"
      provides: "the new isolated package manifest (PySide6-only Qt dep, sibling pins, console script)"
      contains: "rosbagger-desktop = \"rosbagger_desktop.cli:main\""
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/cli.py"
      provides: "argparse front door; QApplication imported inside main()"
      exports: ["main", "_build_parser"]
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/capabilities.py"
      provides: "ros_available() rclpy probe inside a function body"
      exports: ["ros_available"]
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py"
      provides: "QMainWindow shell: QListWidget nav + QStackedWidget; App-owned reader; capability gate"
      contains: "class MainWindow"
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/panels/inspect_panel.py"
      provides: "Inspect QWidget over collect_bag_info + collect_table_schemas"
      contains: "class InspectPanel"
    - path: "packages/rosbagger-desktop/src/rosbagger_desktop/panels/tf_panel.py"
      provides: "TF QWidget over collect_tf_report + NoTransformsError"
      contains: "class TfPanel"
    - path: "tests/test_desktop.py"
      provides: "headless pytest-qt SC1/SC3 proof (shell + inspect + tf) against a fixture bag"
      contains: "QT_QPA_PLATFORM"
  key_links:
    - from: "packages/rosbagger-desktop/src/rosbagger_desktop/cli.py"
      to: "main_window.MainWindow"
      via: "import inside main() after parse_args"
      pattern: "from .main_window import MainWindow"
    - from: "packages/rosbagger-desktop/src/rosbagger_desktop/panels/inspect_panel.py"
      to: "rosbagger_core.inspect"
      via: "lazy import inside refresh_view"
      pattern: "from rosbagger_core.inspect import collect_bag_info, collect_table_schemas"
    - from: "packages/rosbagger-desktop/src/rosbagger_desktop/panels/tf_panel.py"
      to: "rosbagger_core.tf"
      via: "lazy import inside refresh_view"
      pattern: "collect_tf_report"
    - from: "tests/test_offline_guard.py"
      to: "sys.modules PySide6 scan"
      via: "fresh-subprocess assertion"
      pattern: "PySide6"
---

<objective>
Increment A foundation (D-18): stand up the new isolated `rosbagger-desktop` workspace
package and ship the offline shell + the two simplest offline panels (Inspect, TF) with
a native `QMainWindow`, the capability gate, the Qt-free offline guard, and a re-locked
`uv.lock`. This is a thin presentation PORT of the already-shipped Textual TUI — every
panel calls the named `rosbagger_core` module APIs verbatim (D-10/D-12) and the GUI
contains zero analysis/bag/SQL/ROS logic.

Purpose: prove SC1 (native window launches via the console script), SC4 (offline+Qt-free
import guard green), and SC5 (headless pytest-qt tests pass), plus the first half of SC3
(inspect/query panels drive real core output — this plan covers inspect; query is Plan 02).
Output: a launchable `rosbagger-desktop [BAG]` window with working Inspect + TF panels,
the Qt-free guard extension, and the re-locked lockfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-native-desktop-gui-pyside6/16-CONTEXT.md
@.planning/phases/16-native-desktop-gui-pyside6/16-RESEARCH.md
@docs/superpowers/specs/2026-05-25-rosbagger-desktop-gui-design.md

# The thin-frontend precedent ported one-to-one (Qt analog of these):
@packages/rosbagger-gui/src/rosbagger_gui/cli.py
@packages/rosbagger-gui/src/rosbagger_gui/app.py
@packages/rosbagger-gui/src/rosbagger_gui/__init__.py
@packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py
@packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py
@packages/rosbagger-gui/pyproject.toml
@tests/test_offline_guard.py

<interfaces>
<!-- Module APIs the panels call verbatim (no new logic). Signatures confirmed from codebase. -->

rosbagger_core.inspect (D-10):
  collect_bag_info(reader) -> BagInfo
    BagInfo: .duration_ns (int|None), .message_count (int), .size_bytes (int), .topics (list[TopicInfo])
    TopicInfo: .topic (str), .msgtype (str|None), .count (int), .hz (float|None)
  collect_table_schemas(reader) -> list[TableSchema]
    TableSchema: .table_name (str), .columns (list[ColumnDef])
    ColumnDef: .name (str), .arrow_type (object), .is_heavy_blob (bool)

rosbagger_core.tf (D-12):
  collect_tf_report(reader) -> TfReport
    TfReport: .edges (list[EdgeReport]), .gaps (list[GapReport]), .start_ns (int|None), .end_ns (int|None)
    EdgeReport: .parent, .child (str), .static (bool), .samples (int), .max_gap_ns (int|None), .gap_count (int)
    GapReport: .parent, .child (str), .gap_ns (int), .at_rel_ns (int), .at_ns (int)
rosbagger_core.errors.NoTransformsError  (ValueError subclass; raised when bag has no /tf nor /tf_static)

rosbagger_core.reader.RosbagsReader(path, default_typestore=...) -> .open() / .close() / .paths
  The ROS-2-Humble typestore (for the legacy sqlite3 fixture) comes from the ROS-FREE
  rosbags.typesys: get_typestore(Stores.ROS2_HUMBLE) — carry over the TUI's _ros2_humble_typestore helper.

Console-script + manifest precedent: packages/rosbagger-gui/pyproject.toml
  version 0.2.0; sibling pins rosbagger-core>=0.2,<0.3; [project.scripts]; hatchling build backend.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package skeleton + manifest + root deps/config + re-lock uv.lock</name>
  <files>packages/rosbagger-desktop/pyproject.toml, packages/rosbagger-desktop/src/rosbagger_desktop/__init__.py, packages/rosbagger-desktop/src/rosbagger_desktop/panels/__init__.py, pyproject.toml, uv.lock</files>
  <read_first>
    - packages/rosbagger-gui/pyproject.toml (the manifest shape to mirror: version, sibling pins, console script, hatchling)
    - packages/rosbagger-gui/src/rosbagger_gui/__init__.py (the __version__ + import-light top-level pattern)
    - pyproject.toml (root: [tool.uv.sources], [dependency-groups] dev, [tool.ruff].src, [tool.pytest.ini_options])
  </read_first>
  <action>
    Create `packages/rosbagger-desktop/pyproject.toml` mirroring `packages/rosbagger-gui/pyproject.toml`'s shape (D-02): `[project]` name `rosbagger-desktop`, version `0.2.0`, `requires-python = ">=3.10"`, dependencies = `["rosbagger-core>=0.2,<0.3", "rosbagger-record>=0.2,<0.3", "rosbagger-replay>=0.2,<0.3", "PySide6>=6.10,<6.12"]` (PySide6 pin per RESEARCH Standard Stack — PySide6 declared ONLY here, never elsewhere, D-01); `[project.scripts]` `rosbagger-desktop = "rosbagger_desktop.cli:main"`; `[build-system]` requires `["hatchling"]`, build-backend `"hatchling.build"`. Create `src/rosbagger_desktop/__init__.py` with `from __future__ import annotations` and `__version__ = "0.2.0"`, Qt-free + ROS-free at top level (mirror the rosbagger-gui __init__ discipline — but note `_detect_ros` will live in capabilities.py for this package, NOT __init__). Create an empty `src/rosbagger_desktop/panels/__init__.py`. Edit root `pyproject.toml`: (a) add `PySide6>=6.10,<6.12` and `pytest-qt>=4.5,<5` to the `[dependency-groups]` `dev` list (so CI exercises the GUI; PySide6 in dev is for test execution only — it is NOT added to any package dep but rosbagger-desktop's); (b) add `packages/rosbagger-desktop/src` to `[tool.ruff].src`; (c) add `qt_api = "pyside6"` under `[tool.pytest.ini_options]`. Do NOT add `rosbagger-desktop` to `[tool.uv.sources]` unless `uv sync` fails to resolve (A3 — expected unnecessary; the members glob auto-discovers it and it depends only on already-sourced siblings). Do NOT add rosbagger_desktop to the `--cov` set (the GUI is a thin face, exempt like the TUI). Re-lock: `PYTHONPATH="" uv sync` to regenerate `uv.lock` with the new member + PySide6/pytest-qt dev deps; review the diff is additive only.
  </action>
  <verify>
    <automated>cd /home/the2xman/Desktop/rosbagger && PYTHONPATH="" uv sync 2>&1 | tail -5 && PYTHONPATH="" uv run python -c "import rosbagger_desktop; print(rosbagger_desktop.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH="" uv sync` exits 0 and `uv.lock` now lists the `rosbagger-desktop` member + `PySide6` + `pytest-qt`.
    - `PYTHONPATH="" uv run python -c "import rosbagger_desktop; print(rosbagger_desktop.__version__)"` prints `0.2.0`.
    - `grep -c 'PySide6' packages/rosbagger-desktop/pyproject.toml` >= 1 AND PySide6 appears in NO other `packages/*/pyproject.toml` (`grep -rl 'PySide6' packages/*/pyproject.toml` lists only rosbagger-desktop).
    - `packages/rosbagger-desktop/pyproject.toml` contains `rosbagger-desktop = "rosbagger_desktop.cli:main"`.
    - root `pyproject.toml` `dev` group contains `pytest-qt` and `PySide6`; `[tool.pytest.ini_options]` `addopts` still lists only `--cov=rosbagger_core --cov=bagq` (rosbagger_desktop NOT added to coverage).
  </acceptance_criteria>
  <done>The new package installs via uv, imports Qt-free at top level, declares PySide6 in isolation, and the lockfile is re-locked additively.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cli.py argparse front door + capabilities.py + main_window.py shell with inspect/tf panels</name>
  <files>packages/rosbagger-desktop/src/rosbagger_desktop/cli.py, packages/rosbagger-desktop/src/rosbagger_desktop/capabilities.py, packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py, packages/rosbagger-desktop/src/rosbagger_desktop/panels/inspect_panel.py, packages/rosbagger-desktop/src/rosbagger_desktop/panels/tf_panel.py</files>
  <read_first>
    - packages/rosbagger-gui/src/rosbagger_gui/cli.py (argparse front door; App imported inside main)
    - packages/rosbagger-gui/src/rosbagger_gui/app.py (shell: nav + switcher, _ros2_humble_typestore, _open_reader, capability gate in on_mount)
    - packages/rosbagger-gui/src/rosbagger_gui/__init__.py (_detect_ros probe body)
    - packages/rosbagger-gui/src/rosbagger_gui/panels/inspect.py (the exact collect_bag_info + collect_table_schemas calls + presentation formatting to mirror)
    - packages/rosbagger-gui/src/rosbagger_gui/panels/tf.py (the exact collect_tf_report call + NoTransformsError teaching path to mirror)
    - 16-RESEARCH.md Patterns 1, 2, 4, 5 + Pitfalls 4, 5, 6
  </read_first>
  <behavior>
    - `main(["--help"])` raises `SystemExit(0)` (argparse prints help and exits) and NO `PySide6` module is in `sys.modules` on that path.
    - `main([])` with no bag and `main([str(bag)])` both construct a `MainWindow` without raising (under `QT_QPA_PLATFORM=offscreen`).
    - `MainWindow(bag_path=str(fixture_bag))` exposes five nav entries (inspect, query, tf, record, replay are the panel registry slots — but only inspect+tf+query are real panels this increment; record/replay slots exist with placeholder/disabled until Plan 03). For THIS plan the registry holds at least the inspect and tf panels as live QWidgets; defer the query/record/replay registry rows to their plans (Plan 02 wires query; Plan 03 wires record/replay). Keep the registry a single editable structure later plans append to.
    - `capabilities.ros_available()` returns a bool; the rclpy import lives inside the function body.
    - The Inspect panel, given a fixture bag, populates a Qt table with > 0 rows from `collect_bag_info`.
    - The TF panel, given a fixture bag with no /tf, shows the `NoTransformsError` message text (no crash); given a /tf bag, populates the edges table with > 0 rows.
  </behavior>
  <action>
    Create `cli.py` as a one-to-one port of the TUI `cli.py` (Pattern 1, D-16): stdlib `argparse` `_build_parser()` with `prog="rosbagger-desktop"` and an optional positional `bag_path` (`nargs="?"`, `default=None`, `metavar="BAG"`); `main(argv=None) -> int` calls `parse_args(argv)` FIRST (so `--help`/bad args exit before any Qt), THEN imports `QApplication` from `PySide6.QtWidgets` and `MainWindow` from `.main_window` INSIDE the body, constructs `QApplication`, builds `MainWindow(bag_path=args.bag_path)`, calls `window.show()`, returns `app.exec()`. Create `capabilities.py` with `ros_available() -> bool` — a direct port of `_detect_ros` (try `import rclpy` inside the body, return False on ImportError, D-09). Create `main_window.py` (Patterns 2/5, D-06/D-07/D-08/D-09): a `class MainWindow(QMainWindow)` whose `__init__(self, bag_path=None)` lazily imports `RosbagsReader` from `rosbagger_core.reader` inside the body, carries over a `_ros2_humble_typestore()` helper (`from rosbags.typesys import Stores, get_typestore; return get_typestore(Stores.ROS2_HUMBLE)` — ROS-FREE, Pitfall 5), computes `self._ros_available = ros_available()` ONCE, builds a `QListWidget` nav + `QStackedWidget` in a `QHBoxLayout` central widget, drives `nav.currentRowChanged -> stack.setCurrentIndex`, exposes `self.reader` (None until opened) and an `_open_reader(path)` that closes any prior reader, constructs `RosbagsReader(path, default_typestore=self._ros2_humble_typestore())`, `.open()`s it inside a try/except surfacing a `QMessageBox.warning` on failure (never crash construction — mirror the TUI WR-05 path), and stores it on `self.reader`. Add a File menu with "Open Bag File…" (`QFileDialog.getOpenFileName` filter `"Bags (*.bag *.mcap);;All files (*)"`) and "Open Bag Directory…" (`QFileDialog.getExistingDirectory`) wired to `_open_reader` then refreshing the active panel (Pattern 5). Define a panel registry as an editable list of `(label, panel_widget, is_live)` rows; this plan registers Inspect (is_live False) and TF (is_live False). Apply the capability gate: for each `is_live` row, when `not self._ros_available`, disable that nav item (`item.setFlags(item.flags() & ~Qt.ItemIsEnabled)`) with a teaching tooltip — there are no live rows yet this plan, but the gate logic must be in place for Plans 02/03 to populate. Expose a public accessor each panel test can use (e.g. a `panels` dict keyed by id, or named attributes `inspect_panel`/`tf_panel`) so headless tests reach the widgets. Create `panels/inspect_panel.py` (`class InspectPanel(QWidget)`, D-10): a constructor building a header `QLabel` + two `QTableWidget`s (bag-info: columns topic/msgtype/count/hz; table-schemas: columns table/column/type/lazy), and a `refresh_view()` that reads the window's shared `reader`, lazily imports `from rosbagger_core.inspect import collect_bag_info, collect_table_schemas` INSIDE the method, calls both against the reader, and fills the tables exactly as the TUI inspect panel does (presentation-only `_human_size`/duration formatting carried over; `<mixed>`/em-dash placeholders). With no reader, show the empty-state header and clear the tables. Trigger `refresh_view()` when the panel becomes visible (override `showEvent`) and after a bag opens. Create `panels/tf_panel.py` (`class TfPanel(QWidget)`, D-12): a status `QLabel` + two `QTableWidget`s (edges, gaps), and a `refresh_view()` that reads the shared reader, lazily imports `from rosbagger_core.tf import collect_tf_report` and `from rosbagger_core.errors import NoTransformsError` INSIDE the method, calls `collect_tf_report(reader)`, catches `NoTransformsError` to render `str(exc)` on the status line (no crash, T-16-TF), and fills the edge-summary + gap-timeline tables mirroring the TUI tf panel's columns and `_human_dur` presentation formatting. EVERY `rosbagger_core` / `rosbags` / `rclpy` import in these modules stays inside method bodies (D-08, Pitfall 4) — module top level imports ONLY `PySide6` + stdlib. Use `QTableWidgetItem(str(value))` for cells (Pattern 4 — temporal-safe).
  </action>
  <verify>
    <automated>cd /home/the2xman/Desktop/rosbagger && PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src && PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run python -c "from rosbagger_desktop.cli import main; import sys; 
try:
    main(['--help'])
except SystemExit as e:
    assert e.code == 0, e.code
assert not [m for m in sys.modules if m.split('.')[0]=='PySide6'], 'Qt leaked on --help'
print('help-clean OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src` exits 0.
    - `rosbagger-desktop --help` (via `main(["--help"])`) raises `SystemExit(0)` and leaves NO `PySide6` in `sys.modules` (the help-clean assertion above prints `help-clean OK`).
    - `grep -rn "^from PySide6\|^import PySide6" packages/rosbagger-desktop/src/rosbagger_desktop/cli.py` returns nothing (QApplication import is inside `main()`).
    - `grep -rn "^from rosbagger_core\|^from rosbags\|^import rclpy\|^from rosbagger_record\|^from rosbagger_replay" packages/rosbagger-desktop/src/rosbagger_desktop/panels/inspect_panel.py packages/rosbagger-desktop/src/rosbagger_desktop/panels/tf_panel.py packages/rosbagger-desktop/src/rosbagger_desktop/main_window.py` returns nothing (all such imports are inside method bodies).
    - `panels/inspect_panel.py` contains `from rosbagger_core.inspect import collect_bag_info, collect_table_schemas`; `panels/tf_panel.py` contains `collect_tf_report` and `NoTransformsError`.
    - `capabilities.py` `ros_available` has `import rclpy` inside the function body, not at module top.
  </acceptance_criteria>
  <done>`rosbagger-desktop --help` exits 0 Qt-free; a `MainWindow` builds on a bag with working Inspect + TF panels that call core APIs verbatim, all heavy/ROS imports lazy.</done>
</task>

<task type="auto">
  <name>Task 3: Qt-free offline guard extension + headless pytest-qt SC1/SC3 tests</name>
  <files>tests/test_offline_guard.py, tests/test_desktop.py</files>
  <read_first>
    - tests/test_offline_guard.py (the _ros_modules_after_import / fresh-subprocess technique + test_import_gui_does_not_pull_ros to mirror)
    - tests/test_gui.py (the headless GUI proof harness: self-contained sys.path insert, fixture-bag write, pause-before-assert convention — the Qt analog uses qtbot instead of Pilot)
    - 16-RESEARCH.md Code Examples 2 + 3, Pitfall 3 (offscreen) + Pitfall 4 (Qt-free guard)
    - tools/make_fixtures.py (write_ros2_sqlite_bag / write_tf_bag fixture writers the tests use)
  </read_first>
  <action>
    Extend `tests/test_offline_guard.py` with two fresh-subprocess assertions (Code Example 2, D-04), each spawned with `env={"PYTHONPATH": ""}` like the existing guards: (1) `test_import_core_does_not_pull_pyside6` — `import rosbagger_core; import bagq` then assert no `sys.modules` entry whose top-level name is in `{"PySide6", "shiboken6"}` (the Qt-free invariant); (2) `test_import_desktop_cli_does_not_pull_pyside6_or_ros` — `import rosbagger_desktop.cli` then assert no module top-level name in `{"PySide6", "shiboken6", "rclpy", "rosbag2_py"}` leaked (QApplication is inside `main()`, ROS imports are lazy). Verify the `shiboken6` module name after first install (A2); if PySide6 ships a differently-named binding-runtime module, keep `PySide6` in the blocklist (it alone catches a leak) and adjust the second name. Create `tests/test_desktop.py` as the headless pytest-qt proof (Code Example 3): set `os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")` at the TOP of the module BEFORE any PySide6 import (Pitfall 3); add the self-contained repo-root + package-src `sys.path` inserts (mirror test_gui.py); import the fixture writers from `tools.make_fixtures`. Write tests using the `qtbot` fixture: (a) `test_app_has_offline_panels` — construct `MainWindow()` (no bag), `qtbot.addWidget(window)`, assert the inspect and tf panels are reachable via the public accessor and their nav items are enabled (SC1 partial — the five-panel completeness is asserted in Plan 03 once all rows exist; this plan asserts inspect+tf present and enabled); (b) `test_inspect_panel_shows_real_topics` — write a ROS2 sqlite fixture bag, `MainWindow(bag_path=str(bag))`, drive the inspect panel's `refresh_view()` (or select it), assert its bag-info `QTableWidget.rowCount() > 0` (SC3 — real collect_bag_info rows landed); (c) `test_tf_panel_renders_or_teaches` — with a `write_tf_bag` fixture assert the edges table has > 0 rows; with a plain (no-/tf) fixture assert the status label shows the NoTransformsError teaching text and the panel did not crash. For capability-gating coverage on this ROS-equipped dev box, monkeypatch `rosbagger_desktop.capabilities.ros_available` -> `False` in a test and assert the gate path is exercised (no live rows yet, so assert the gate code runs without error and offline panels stay enabled). Follow the offscreen-event caveat: assert observable state (rowCount, label text, widget enabled) not pixels/tooltips.
  </action>
  <verify>
    <automated>cd /home/the2xman/Desktop/rosbagger && PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py tests/test_offline_guard.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py tests/test_offline_guard.py -x -q` exits 0.
    - `tests/test_offline_guard.py` contains `test_import_core_does_not_pull_pyside6` and `test_import_desktop_cli_does_not_pull_pyside6_or_ros`, both passing (PySide6/shiboken6/ROS leak lists empty).
    - `tests/test_desktop.py` sets `QT_QPA_PLATFORM` to `offscreen` BEFORE importing PySide6 (`grep -n "QT_QPA_PLATFORM" tests/test_desktop.py` shows it near the top).
    - `test_inspect_panel_shows_real_topics` asserts `rowCount() > 0` against a real `collect_bag_info` over a fixture bag.
    - `test_tf_panel_renders_or_teaches` covers both the populated-edges and the NoTransformsError teaching path.
    - No real window appears during the run (offscreen platform applied).
  </acceptance_criteria>
  <done>The Qt-free invariant is regression-locked in two fresh-subprocess guards, and headless pytest-qt tests prove the shell + inspect + tf drive real core output — all ROS-free and display-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desktop GUI → rosbagger_core (offline) | The GUI hands a user-chosen bag path to core APIs; core owns all IO bounds |
| package dep graph → install (npm/pip boundary) | PySide6 + pytest-qt are the only new external packages, added via `uv sync` |
| offline import graph → Qt/ROS | The load-bearing isolation invariant — core/bagq must stay Qt-free AND ROS-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-ISO | Tampering | offline import graph | mitigate | Two fresh-subprocess guards (Task 3) assert `import rosbagger_core`/`bagq` pulls no PySide6/shiboken6 and `import rosbagger_desktop.cli` pulls no Qt/ROS; PySide6 declared only in rosbagger-desktop deps (D-01/D-04) |
| T-16-01-DOS | Denial of Service | Inspect/TF panels on a hostile/huge/corrupt bag | accept (inherited) | Core APIs already bound work (O(1) metadata in inspect; collect_tf_report bounded); the GUI re-derives nothing and surfaces open errors as a QMessageBox, never a crash |
| T-16-01-HELP | Information disclosure / crash | cli.py --help on a headless box | mitigate | argparse front door (Pattern 1); QApplication imported inside main() so --help exits 0 building no Qt (Pitfall 6); regression-locked by the help-clean assertion + the Qt-free cli guard |
| T-16-01-SC | Tampering | PySide6 / pytest-qt installs | accept | RESEARCH Package Legitimacy Audit verified both first-party on PyPI (PySide6 = Qt Company, pytest-qt = pytest-dev); no [ASSUMED]/[SUS]/[SLOP] packages; Python wheels run no postinstall. No human checkpoint required. |
</threat_model>

<verification>
- `PYTHONPATH="" uv sync` re-locks; `uv.lock` lists the new member + PySide6 + pytest-qt.
- `PYTHONPATH="" uv run ruff check packages/rosbagger-desktop/src` exits 0.
- `PYTHONPATH="" QT_QPA_PLATFORM=offscreen uv run pytest tests/test_desktop.py tests/test_offline_guard.py -x -q` exits 0.
- `rosbagger-desktop --help` exits 0 with no PySide6 in sys.modules.
</verification>

<success_criteria>
- SC1: `rosbagger-desktop [BAG]` launches a native QMainWindow via the console script (window builds; --help exits 0 Qt-free).
- SC3 (partial): the Inspect panel drives real `collect_bag_info`/`collect_table_schemas` against a fixture bag; TF drives real `collect_tf_report`. (Query panel = Plan 02.)
- SC4: `import rosbagger_core`/`bagq` stays Qt-free AND ROS-free; the new Qt-free guards pass.
- SC5: headless pytest-qt tests pass under `QT_QPA_PLATFORM=offscreen`.
</success_criteria>

<output>
Create `.planning/phases/16-native-desktop-gui-pyside6/16-01-SUMMARY.md` when done.
</output>
