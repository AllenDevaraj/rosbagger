---
phase: 11-edit-events
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-core/src/rosbagger_core/events.py
  - tests/test_events.py
autonomous: true
requirements: [EVNT-01]

must_haves:
  truths:
    - "sidecar_path(bag) derives <bag>.events.parquet file-vs-dir-aware (D-11 / Pitfall 7): for a ROS1 .bag file or an .mcap file it strips the real extension (with_suffix), for a ROS2 DIRECTORY bag it appends '.events.parquet' to the full name (NEVER with_suffix — a dotted dir like v1.2/ must NOT lose its last segment)"
    - "add_event(bag, *, t_start_ns, t_end_ns, label, note=None) writes/grows the sidecar with the fixed v1 schema (D-12): columns t_start_ns BIGINT, t_end_ns BIGINT, label VARCHAR, note VARCHAR (nullable); a point event has t_start_ns == t_end_ns; append = read existing → pa.concat_tables → rewrite the whole file (D-13, files are tiny)"
    - "list_events(bag) reads the sidecar back as a pyarrow.Table (the SC2 round-trip: a written event is read back with the same values), and returns an EMPTY table with the fixed v1 schema when no sidecar exists (so callers never crash on a missing sidecar)"
    - "The sidecar WRITE reuses output/export.write_table (DuckDB COPY, the T-06-01 quote-escaped path literal) — NOT a second pyarrow.parquet.write_table and NOT a hand-built COPY (the trusted-input boundary: the output path is the one SQL-literal surface)"
    - "import rosbagger_core.events pulls in NONE of {duckdb, sqlglot, pyarrow} (offline invariant): pyarrow/the export writer are imported lazily inside function bodies; events.py top level is stdlib-light — proven by the extended offline guard (Plan 11-04 owns the guard test; this plan keeps the module importable-light)"
  artifacts:
    - path: "packages/rosbagger-core/src/rosbagger_core/events.py"
      provides: "sidecar_path + add_event + list_events over <bag>.events.parquet, reusing output/export.write_table for the write"
      contains: "def add_event"
      min_lines: 40
    - path: "tests/test_events.py"
      provides: "sidecar write→read-back (SC2) + append grows the row count + point event (t_start==t_end) + dotted-directory-name path derivation + absent-sidecar empty table"
      contains: "add_event"
  key_links:
    - from: "events.py add_event"
      to: "rosbagger_core.output.export.write_table"
      via: "DuckDB COPY Parquet write of the concatenated event table (T-06-01 quote-escape reused; D-13)"
      pattern: "write_table"
    - from: "events.py add_event/list_events"
      to: "pyarrow.parquet.read_table (read existing sidecar) + the fixed v1 pa.schema"
      via: "read existing → pa.concat_tables → rewrite; list reads back (lazy pyarrow import)"
      pattern: "read_table|concat_tables"
    - from: "events.py sidecar_path"
      to: "<bag>.events.parquet derivation"
      via: "file (.bag/.mcap) → with_suffix; directory bag → name + '.events.parquet' (D-11 / Pitfall 7)"
      pattern: "events.parquet"
---

<objective>
Build the event sidecar I/O module (`rosbagger_core/events.py`): derive the `<bag>.events.parquet` path file-vs-dir-aware (D-11), and `add_event` / `list_events` over it with the fixed v1 event schema (D-12). The write reuses the locked DuckDB-`COPY` Parquet writer (`output/export.write_table` — D-13), so the one SQL-literal quote-escape boundary (T-06-01) stays shared and no second COPY is built. This is the EVNT-01 write/read half (SC2: "an event sidecar is written and read back"); the `events`-table query hook (SC3) is Plan 11-04, which depends on `sidecar_path` from this plan.

Purpose: deliver the sidecar storage layer the success criterion SC2 names — `bagq events add` writes a row and `bagq events list` reads it back — proven by a write→read-back test plus the dotted-directory-name edge (Pitfall 7) and the absent-sidecar case. The events table queryability (SC3) builds on this.

Output: a new `rosbagger_core/events.py`, a new `tests/test_events.py` (the SC2 write/read/append suite + the path-derivation edges). No CLI changes (the `bagq events add`/`list` verbs land in Plan 11-04 alongside the query hook, so the events CLI surface lives with the events-table wiring). No new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-edit-events/11-CONTEXT.md
@.planning/phases/11-edit-events/11-RESEARCH.md

<interfaces>
<!-- The locked Parquet writer this plan reuses (UNCHANGED) + the verified sidecar pattern. -->

From packages/rosbagger-core/src/rosbagger_core/output/export.py (UNCHANGED — read it; reuse write_table):
```python
def write_table(table: pyarrow.Table, path: str | os.PathLike[str]) -> None
# DuckDB COPY: con.register("result", table); COPY result TO '<path>' (FORMAT PARQUET).
# The path literal is single-quote-escaped ('→'') before interpolation (threat T-06-01) —
# this is the ONE SQL-literal surface; reuse it, never build a second COPY (D-13).
# Format chosen by the path extension; a '.parquet' path writes Parquet whose schema +
# rows round-trip via pyarrow.parquet.read_table. pyarrow/duckdb are imported INSIDE the
# function body (offline invariant) — calling write_table is what pulls them in.
```

VERIFIED sidecar I/O (RESEARCH Pattern 4 — VERIFIED end-to-end against output/export.write_table + pyarrow 24.0.0):
```python
import pyarrow as pa
import pyarrow.parquet as pq
from rosbagger_core.output.export import write_table

_EVENT_SCHEMA = pa.schema([            # D-12 fixed v1 schema
    ("t_start_ns", pa.int64()),
    ("t_end_ns",   pa.int64()),
    ("label",      pa.string()),
    ("note",       pa.string()),       # nullable
])

def sidecar_path(bag: Path) -> Path:   # D-11 — robust against dotted directory names (Pitfall 7)
    bag = Path(bag)
    if bag.suffix in {".bag", ".mcap"}:        # FILE bags: strip the real extension
        return bag.with_suffix(".events.parquet")
    return bag.parent / (bag.name + ".events.parquet")   # DIR bag: append, never strip

def add_event(bag, *, t_start_ns, t_end_ns, label, note=None):
    path = sidecar_path(bag)
    row = pa.table({"t_start_ns": [t_start_ns], "t_end_ns": [t_end_ns],
                    "label": [label], "note": [note]}, schema=_EVENT_SCHEMA)
    if path.exists():
        existing = pq.read_table(path)
        combined = pa.concat_tables([existing, row])
    else:
        combined = row
    write_table(combined, str(path))           # rewrite whole file (events are tiny) — D-13

def list_events(bag):
    path = sidecar_path(bag)
    return pq.read_table(path) if path.exists() else _EVENT_SCHEMA.empty_table()
```
This is the VERIFIED reference. Module top stays stdlib-light: `import pyarrow`/`pyarrow.parquet` and `from ...output.export import write_table` go INSIDE the function bodies (offline invariant — Pitfall 5; mirror output/export.py and backend/query.py). `sidecar_path` is stdlib-only (pathlib) and may stay at module top.
</interfaces>

<test_harness>
- Mirror tests/test_backend_query.py / tests/test_tf.py for fixtures, but events tests need only a bag PATH (the sidecar sits next to it) — no AnyReader read of the bag content is required for SC2 (write/read-back is pure sidecar I/O). Use a `tmp_path` for the sidecar; you can use any of the make_fixtures bags for a realistic adjacent path, or just a synthetic path for the path-derivation tests.
- For the dotted-directory edge (Pitfall 7): assert `sidecar_path(Path("/data/v1.2"))` (a directory-style name with no .bag/.mcap suffix) == `Path("/data/v1.2.events.parquet")` (the `.2` is NOT lost), and `sidecar_path(Path("/data/run.bag"))` == `Path("/data/run.events.parquet")`, and `sidecar_path(Path("/data/run.mcap"))` == `Path("/data/run.events.parquet")`.
- SC2: `add_event(bag, t_start_ns=1_000_000_000, t_end_ns=1_100_000_000, label="turn", note="left")`, then `list_events(bag)` returns a 1-row table whose values match; a second `add_event` grows it to 2 rows; a point event (`t_start_ns == t_end_ns`) round-trips; `list_events` on a path with no sidecar returns a 0-row table with the four columns.
</test_harness>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: events.py sidecar I/O — sidecar_path + add_event + list_events</name>
  <files>packages/rosbagger-core/src/rosbagger_core/events.py, tests/test_events.py</files>
  <read_first>
    - packages/rosbagger-core/src/rosbagger_core/output/export.py (the FULL file — `write_table(table, path)` is the DuckDB-COPY Parquet writer you REUSE; understand the T-06-01 quote-escape so you do NOT build a second COPY, and that pyarrow/duckdb are imported inside its body so the import discipline is to call it, not re-implement it)
    - .planning/phases/11-edit-events/11-RESEARCH.md § Pattern 4 (the VERIFIED `_EVENT_SCHEMA` / `sidecar_path` / `add_event` / `list_events` reference — copy its structure), § Pitfall 7 (the dotted-directory `with_suffix` bug — the file-vs-dir branch), § Pitfall 5 (offline invariant — lazy pyarrow/export imports), § D-11/D-12/D-13 (path derivation, fixed v1 schema, reuse the COPY writer + read→concat→rewrite append)
    - packages/rosbagger-core/src/rosbagger_core/tf.py (the placement precedent — a flat `rosbagger_core/<module>.py` NOT in `__init__`, stdlib-light top, consumed via `from rosbagger_core.<module> import ...`; mirror its offline discipline for events.py)
    - tests/test_tf.py (the self-contained-harness pattern: repo-root sys.path insert if `tools` is needed, per-format/`tmp_path` fixtures; mirror it in tests/test_events.py — though events tests need only paths + tmp_path, not bag reads)
    - .planning/phases/11-edit-events/11-CONTEXT.md § Claude's Discretion (function/parameter names are yours; the fixed v1 schema D-12, the COPY-writer reuse D-13, the path derivation D-11, and the offline invariant are hard constraints)
  </read_first>
  <behavior>
    - Test (path derivation, `-k path`): `sidecar_path(Path("/data/run.bag"))` == `/data/run.events.parquet`; `sidecar_path(Path("/data/run.mcap"))` == `/data/run.events.parquet`; `sidecar_path(Path("/data/v1.2"))` (a ROS2-dir-style name, NO .bag/.mcap suffix) == `/data/v1.2.events.parquet` (the `.2` is PRESERVED — Pitfall 7); `sidecar_path(Path("/data/ros2_sqlite"))` == `/data/ros2_sqlite.events.parquet`.
    - Test (write→read-back = SC2, `-k sidecar`): `add_event(bag, t_start_ns=1_000_000_000, t_end_ns=1_100_000_000, label="turn", note="left")` then `list_events(bag)` returns a 1-row table; assert the four column values match (t_start_ns/t_end_ns/label/note) and the schema is the fixed v1 schema (the `_ns` columns are int64/BIGINT).
    - Test (append grows, `-k append`): two `add_event` calls grow the sidecar to 2 rows in insertion order (read→concat→rewrite — D-13).
    - Test (point event, `-k point`): `add_event(..., t_start_ns=X, t_end_ns=X, label="mark")` round-trips with `t_start_ns == t_end_ns` (D-12 — an instant event).
    - Test (nullable note, `-k note`): `add_event(..., note=None)` round-trips with a null `note` (the column is nullable — D-12).
    - Test (absent sidecar, `-k absent`): `list_events(bag)` on a bag whose sidecar does not exist returns a 0-row table carrying the four columns (callers never crash on a missing sidecar).
  </behavior>
  <action>
    Create `rosbagger_core/events.py` (a flat module, NOT in `__init__` — mirror `tf.py`'s placement so `import rosbagger_core` stays light and the existing `--cov=rosbagger_core` gate covers it; D-01). Implement per the VERIFIED RESEARCH Pattern 4 reference:
    - `_EVENT_SCHEMA`: the fixed v1 `pa.schema([("t_start_ns", pa.int64()), ("t_end_ns", pa.int64()), ("label", pa.string()), ("note", pa.string())])` (D-12). Build it inside a function (or a lazily-initialized module-level helper) so `pyarrow` is NOT imported at module top (offline invariant).
    - `sidecar_path(bag) -> Path`: stdlib-only (pathlib). For `.bag`/`.mcap` FILE bags use `bag.with_suffix(".events.parquet")`; for a DIRECTORY bag (any other suffix, incl. dotted dir names) return `bag.parent / (bag.name + ".events.parquet")` — NEVER `with_suffix` on a directory (Pitfall 7 / D-11). This may live at module top (no heavy import).
    - `add_event(bag, *, t_start_ns, t_end_ns, label, note=None)`: build the 1-row `pa.table(..., schema=_EVENT_SCHEMA)`; if the sidecar exists, `pq.read_table` it and `pa.concat_tables([existing, row])`, else use the row alone; `write_table(combined, str(sidecar_path(bag)))` (REUSE `output/export.write_table` — D-13; do NOT build a second COPY). Lazy-import `pyarrow`/`pyarrow.parquet` and `write_table` inside the body.
    - `list_events(bag) -> pyarrow.Table`: `pq.read_table(sidecar_path(bag))` if it exists, else `_EVENT_SCHEMA.empty_table()` (lazy pyarrow import).
    Keep the module top stdlib-light (only `from __future__ import annotations`, `pathlib`, maybe `typing`) so `import rosbagger_core.events` pulls no duckdb/sqlglot/pyarrow — Plan 11-04 adds the offline-guard test for this; this plan must keep the module passing it.
    Then write `tests/test_events.py` with the Tests from <behavior> (a self-contained harness like tests/test_tf.py; events tests need only paths + `tmp_path`).
    Do NOT add any CLI verb (`bagq events add`/`list` land in Plan 11-04 with the query hook). Do NOT touch `output/export.py`, `backend/query.py`, or the offline guard.
  </action>
  <verify>
    <automated>PYTHONPATH="" .venv/bin/python -m pytest tests/test_events.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `rosbagger_core/events.py` exists with `sidecar_path`, `add_event`, `list_events`: `grep -nE "def sidecar_path|def add_event|def list_events" packages/rosbagger-core/src/rosbagger_core/events.py` shows all three.
    - The write reuses the locked COPY writer: `grep -n "write_table" packages/rosbagger-core/src/rosbagger_core/events.py` matches AND there is no second hand-built COPY — `grep -ci "COPY " packages/rosbagger-core/src/rosbagger_core/events.py` is 0 (the COPY + T-06-01 escape stay in output/export.py).
    - The fixed v1 schema has the four D-12 columns as int64/string: `grep -nE "t_start_ns|t_end_ns|label|note" packages/rosbagger-core/src/rosbagger_core/events.py` matches all four.
    - The dotted-directory path edge is correct: the `-k path` test green (`sidecar_path(Path("/data/v1.2"))` keeps the `.2`); `grep -n "with_suffix" packages/rosbagger-core/src/rosbagger_core/events.py` shows `with_suffix` used ONLY in the file branch (the dir branch appends to `.name`).
    - SC2 holds: the `-k sidecar`/`-k append`/`-k point`/`-k note`/`-k absent` tests are green (write→read-back with matching values, append grows to 2 rows, point event round-trips, null note round-trips, absent sidecar → empty 4-column table).
    - Heavy imports are lazy: `import pyarrow` / `import pyarrow.parquet` / the `write_table` import appear INSIDE function bodies, not at module top — confirm the module-top import block contains none of `pyarrow`/`duckdb`/`output.export` (`grep -nE "^import pyarrow|^from .*output.export|^import duckdb" packages/rosbagger-core/src/rosbagger_core/events.py` returns nothing).
    - `PYTHONPATH="" .venv/bin/python -m pytest tests/test_events.py -x` is green.
  </acceptance_criteria>
  <done>`rosbagger_core/events.py` derives the sidecar path file-vs-dir-aware (D-11), writes/appends/reads the fixed v1 event schema (D-12) reusing the locked DuckDB-COPY writer (D-13), and SC2 (write→read-back, append, point/null events, absent sidecar) is proven; the module top stays stdlib-light (offline-ready for the Plan 11-04 guard).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar path → write_table COPY (T-06-01) | The `<bag>.events.parquet` path is interpolated into the DuckDB `COPY ... TO '<path>'` literal — but ONLY via the reused `output/export.write_table`, whose single-quote-escape (`'`→`''`) is the existing T-06-01 defense. This plan builds no second COPY. |
| existing sidecar Parquet → read-back | A `<bag>.events.parquet` the user owns (same trust level as the bag); read via the standard `pyarrow.parquet.read_table`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Tampering | malicious sidecar path breaks/injects the COPY statement | mitigate | Reuse `output/export.write_table` — its `_copy_to` already single-quote-escapes the path literal (T-06-01). Never build a second COPY (D-13); the events module passes the path to the existing escaped writer. |
| T-11-10 | Tampering | hand-crafted untrusted `<bag>.events.parquet` read at list time | accept | A local single-user file the user owns (same trust level as the bag) — disposition *accept*, consistent with the project's local-CLI threat model. `pyarrow.parquet.read_table` is the standard reader; the fixed v1 schema (D-12) means unexpected columns simply are not referenced. |
| T-11-11 | Information disclosure | sidecar path traversal (events file written outside the bag's dir) | accept | The sidecar path is derived deterministically NEXT TO the user-supplied bag path; the user already controls that location (local single-user CLI — consistent with T-06-02). No mitigation code needed. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH § Package Legitimacy Audit: N/A — no install surface). No install task → no `[ASSUMED]`/`[SUS]` legitimacy checkpoint. |

**Residual risk after mitigations:** No HIGH-severity threats. The sidecar write reuses the single, already-hardened SQL-literal boundary (T-06-01) rather than introducing a second COPY; the read is the standard pyarrow reader over a file at the user's own trust level; path traversal is *accept* for a local single-user CLI (consistent with T-06-02). The fixed v1 schema bounds what is ever read.
</threat_model>

<verification>
- `PYTHONPATH="" .venv/bin/python -m pytest tests/test_events.py -x` is green.
- Full suite + coverage gate: `PYTHONPATH="" .venv/bin/python -m pytest` passes at ≥80%.
- The sidecar write reuses `output/export.write_table` (no second COPY); the dotted-directory path edge (Pitfall 7) is correct.
- `import rosbagger_core.events` stays importable-light (heavy imports lazy) — Plan 11-04 adds the formal offline-guard test; this plan keeps the module compliant.
- `ruff check` + `ruff format --check` on `events.py` + `tests/test_events.py` are clean.
</verification>

<success_criteria>
- The event sidecar `<bag>.events.parquet` is written and read back (SC2): `add_event` grows it, `list_events` reads it; a written event's values round-trip.
- The sidecar path is derived file-vs-dir-aware (D-11) — a dotted ROS2 directory name does NOT lose its last segment (Pitfall 7).
- The v1 schema is the fixed `t_start_ns/t_end_ns/label/note` (D-12); the write reuses the locked DuckDB-COPY writer (D-13) so the T-06-01 quote-escape stays the single SQL-literal boundary.
- `import rosbagger_core.events` pulls in none of duckdb/sqlglot/pyarrow (offline invariant; heavy imports lazy).
</success_criteria>

<output>
Create `.planning/phases/11-edit-events/11-03-SUMMARY.md` when done.
</output>
