---
context: phase
phase: 13-live-replay
task: 1
total_tasks: 3
status: paused
last_updated: 2026-05-23
---

# Phase 13 — Live Replay — Paused mid-execution (after a laptop crash + low context)

**1 of 3 plans executed.** 13-01 done & committed (`4e7ae49`), working tree clean. Paused at a clean
plan boundary because the orchestrator context was low — resume by executing 13-02 next.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Bare `pytest` on this box | Dev shell leaks ROS onto `PYTHONPATH`; offline-guard then fails | advisory | Prefix offline test/lint with `PYTHONPATH=""` (`PYTHONPATH="" uv run pytest -q`). |
| Spawning `gsd-verifier` | Not installed (`missing_agents`); `verifier_enabled=false` | advisory | Verify the phase **inline** (run suite + check SCs, hand-write `13-VERIFICATION.md`). Core agents `gsd-executor`/`gsd-planner`/`gsd-plan-checker`/`gsd-code-reviewer`/`gsd-code-fixer` ARE available. |
| Worktrees for these plans | `parallelization=false`, dependency chain | advisory | Run executors **sequentially on the main tree** (omit `isolation="worktree"`). |
| Live test only "collected-and-skipped" | The SC1 live test skips in the offline env | blocking-for-SC1 | The orchestrator MUST actually RUN the live lane in a ROS-sourced shell (W4) — see 13-03 note below — and confirm it PASSES, not skips. |

<current_state>
Phase 13 (`rosbagger-replay`): publish a bag to live ROS topics with transport controls. Plans
verified (checker PASSED iteration 2; BLOCKER-1 fixed → SC1 live test must drive the production
`replay_bag()` front door + an EXTERNAL subscriber subprocess). Execution: sequential, main-tree,
verify inline, `--no-transition`. STATE status=executing; begin-phase already run (plan_count 3).
</current_state>

<completed_work>
- 13-01 ✓ (commits `9232215`/`369eb28`/`cd31a7d`/`ddc30a6`/`4e7ae49`): `packages/rosbagger-replay/`
  scaffold + teaching errors (`RosNotAvailableError`, `NoMessagesToReplayError`) + PURE `source.py`
  raw-CDR seam (`load_items` over the v1 `rosbags` AnyReader; ROS2 passthrough + ROS1→CDR bridge; no rclpy).
  Offline suite 439 passed/1 skipped @ 97.37%; `import rosbagger_replay` ROS-free; `uv sync` green.
</completed_work>

<remaining_work>
Execute sequentially, spot-check each (SUMMARY + key files + commits + tree clean), then inline-verify:
- **Wave 2 → 13-02** (2 tasks): PURE `scheduler.py` `Replayer` — play/pause/step/seek/rate/loop +
  monotonic pacing + bounded stop (`duration is not None`, `time.monotonic()`). NO `start=` index
  param (seek is sole position-setter); seek truth = first item `t_ns >= items[0].t_ns + t_offset_ns`.
  Loop+max_messages exact-end test (DONE wins over loop-reset). Unit tests with fake clock + recording
  sink prove SC2/SC3. ROS-free. Depends on 13-01.
- **Wave 3 → 13-03** (3 tasks): lazy `_require_ros` boundary, rclpy publish SINK
  (raw CDR → `rclpy.serialization.deserialize_message(raw, get_message(type))` → `create_publisher().publish()`),
  thin `rosbagger-replay` CLI (`--rate/--loop/--start|--seek/--topics/--duration`; `--end` folded into
  `--duration`, documented), offline-guard extension, and the LIVE SC1 test. **SC1 test MUST call the
  production `replay_bag(...)` in-process and use an EXTERNAL subscriber subprocess** (grep-pinned: shows
  `replay_bag(`, NO `Replayer(`/hand-built `create_publisher` in the test path). Depends on 13-01+13-02.
- **Then inline verify**: `PYTHONPATH="" uv run pytest -q` (≥80% on core+bagq, ruff clean, offline-guard
  green) AND **actually run the live lane** (W4):
  `bash -lc 'source /opt/ros/humble/setup.bash && PYTHONPATH="$PWD/packages/rosbagger-replay/src:$PWD/packages/rosbagger-core/src:$PWD/tests:$PYTHONPATH" python3 -m pytest tests/test_replay_live.py -m live -q'`
  → confirm SC1 PASSES (not skipped). Then code-review gate (`gsd-code-review 13`), fix any critical/material
  warnings (scoped `gsd-code-fixer`), write `13-VERIFICATION.md` (status: passed), `phase.complete 13`,
  evolve PROJECT.md, commit. `--no-transition` → report PHASE COMPLETE and STOP (do NOT chain to Phase 14).
</remaining_work>

<decisions_made>
- D-05 resolved: raw-CDR source is the v1 rosbags reader (rosbag2_py SequentialReader fails on rosbags-written fixtures).
- Pure scheduler / rclpy-sink split; monotonic pacing; rclpy isolated + lazy in rosbagger-replay; core/bagq stay ROS-free.
- SC1 via production front door + external subscriber subprocess (checker BLOCKER-1 fix).
</decisions_made>

<blockers>None. Clean boundary; resume is safe.</blockers>

## Required Reading (in order)
1. `.planning/phases/13-live-replay/13-CONTEXT.md` (D-01..D-12; D-05 resolved)
2. `.planning/phases/13-live-replay/13-RESEARCH.md` (verified publish path + bridges; live-lane recipe)
3. `.planning/phases/13-live-replay/13-02-PLAN.md` then `13-03-PLAN.md` (execute in that order)
4. `.planning/phases/12-live-record/12-VERIFICATION.md` (the live-lane pattern + W3/W4 lessons this phase reuses)

## Infrastructure State
- Working tree clean; branch `main`. Offline baseline: 439 passed/1 skipped @ 97.37%.
- ROS-sourced live lane reachable on this box (`source /opt/ros/humble/setup.bash`); MCAP plugin absent (sqlite3/ROS1/ROS2 fixtures fine for replay).

<next_action>
Resume execution at Wave 2 / plan 13-02 (spawn a `gsd-executor`, sequential main-tree). Then 13-03,
then inline verification incl. the actually-run live lane. Resume with `/gsd:resume-work` or
`/gsd:execute-phase 13 --auto --no-transition`.
</next_action>
