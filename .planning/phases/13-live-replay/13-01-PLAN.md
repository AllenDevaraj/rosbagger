---
phase: 13-live-replay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rosbagger-replay/pyproject.toml
  - packages/rosbagger-replay/src/rosbagger_replay/errors.py
  - packages/rosbagger-replay/src/rosbagger_replay/source.py
  - tests/test_replay_unit.py
  - pyproject.toml
  - uv.lock
autonomous: true
requirements: [REP-01]

must_haves:
  truths:
    - "import rosbagger_replay.source pulls no rclpy/rosbag2_py (offline-clean, rosbags only)"
    - "load_items yields time-ordered (t_ns, topic, msgtype, cdr) ReplayItems for a ROS 2 sqlite3 fixture bag, cdr is raw CDR bytes"
    - "load_items bridges a ROS 1 bag (ROS 1 wire bytes -> reader.deserialize -> typestore.serialize_cdr) so cdr is CDR"
    - "load_items honors a topics= subset filter and returns [] for an empty/unmatched selection"
    - "uv sync --locked --dev exits 0 with the new rosbagger-replay member"
  artifacts:
    - path: "packages/rosbagger-replay/pyproject.toml"
      provides: "uv workspace member; deps=rosbagger-core+typer only; console script rosbagger-replay"
      contains: "rosbagger-replay = \"rosbagger_replay.cli:app\""
    - path: "packages/rosbagger-replay/src/rosbagger_replay/errors.py"
      provides: "RosNotAvailableError + NoMessagesToReplayError teaching capability errors (RuntimeError subclasses, stdlib-only)"
      contains: "class NoMessagesToReplayError"
    - path: "packages/rosbagger-replay/src/rosbagger_replay/source.py"
      provides: "ReplayItem dataclass + load_items raw-CDR seam (rosbags only, no rclpy)"
      contains: "class ReplayItem"
    - path: "tests/test_replay_unit.py"
      provides: "source unit tests over real fixture bags (ROS2 sqlite3/MCAP + ROS1 bridge + topics filter + empty)"
      contains: "load_items"
  key_links:
    - from: "packages/rosbagger-replay/src/rosbagger_replay/source.py"
      to: "rosbags.highlevel.AnyReader"
      via: "AnyReader.messages() raw bytes + reader.deserialize/typestore.serialize_cdr ROS1 bridge"
      pattern: "AnyReader|serialize_cdr"
    - from: "packages/rosbagger-replay/pyproject.toml"
      to: "uv.lock"
      via: "members=[packages/*] globs the new member; only external dep typer (already locked)"
      pattern: "rosbagger-replay"
---

<objective>
Scaffold the `rosbagger-replay` workspace package (mirroring `rosbagger-record`) and build the PURE, offline-clean raw-CDR source seam plus the teaching capability errors. This is the offline foundation: it reads a bag through the v1 `rosbags` `AnyReader` and yields an ordered stream of `(t_ns, topic, msgtype, cdr_bytes)` `ReplayItem`s that the pure scheduler (Plan 02) consumes and the rclpy sink (Plan 03) publishes. No `rclpy` anywhere in this plan — `source.py` imports only `rosbags`, exactly like `reader/rosbags_reader.py`.

Purpose: establish the package boundary + the verified raw-CDR read path (D-01/D-03/D-05) so downstream plans build against a defined `ReplayItem` contract with no scavenger hunt.
Output: `packages/rosbagger-replay/` member (pyproject + errors.py + source.py), the `ReplayItem` contract, source-layer unit tests over real fixtures, re-locked `uv.lock`, ruff src entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-live-replay/13-CONTEXT.md
@.planning/phases/13-live-replay/13-RESEARCH.md

<interfaces>
<!-- The v1 reader the source seam wraps. load_items uses AnyReader DIRECTLY (mirrors source.py in 13-RESEARCH Pattern 2), not RosbagsReader, because it needs raw (connection, t_ns, rawdata) tuples + the ROS1 deserialize/serialize_cdr bridge — which RosbagsReader.read() hides behind deserialized Message records. -->

From rosbags.highlevel.AnyReader (used by reader/rosbags_reader.py):
  AnyReader(paths: list[Path], *, default_typestore=None)
  reader.open() / reader.close()        # context-manager capable: `with AnyReader([...]) as r:`
  reader.connections                    # Sequence; each conn has .topic, .msgtype, .ext
  reader.messages(connections=...)      # yields (connection, t_ns: int, rawdata: bytes), time-ordered across bags
  reader.deserialize(rawdata, msgtype)  # ROS1 wire/CDR -> rosbags object
  reader.typestore.serialize_cdr(obj, msgtype)  # rosbags object -> CDR bytes
  # ROS1 detection: type(conn.ext).__name__ == "ConnectionExtRosbag1" (13-RESEARCH Pattern 2)

From tools/make_fixtures.py (the no-ROS fixture writers the tests replay):
  write_ros2_sqlite_bag(dest_dir) -> Path   # 3 topics x 3 msgs = 9; /cmd_vel /imu /image
  write_ros2_mcap_bag(dest_dir) -> Path     # same, self-describing (no default_typestore needed)
  write_ros1_bag(dest_dir) -> Path          # same topics, ROS1 wire format
  # ROS2 sqlite3 needs default_typestore=get_typestore(Stores.ROS2_HUMBLE)

From rosbags.typesys (for the ROS2 sqlite3 fixture):
  from rosbags.typesys import Stores, get_typestore
  get_typestore(Stores.ROS2_HUMBLE)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the rosbagger-replay member (pyproject + re-lock + ruff src)</name>
  <read_first>
    - packages/rosbagger-record/pyproject.toml (the EXACT template — copy its shape, swap record->replay)
    - pyproject.toml (root: [tool.uv.workspace] members, [tool.uv.sources], [tool.ruff] src list, [tool.pytest.ini_options] markers/addopts)
    - .planning/phases/13-live-replay/13-RESEARCH.md (Installation block lines ~99-119; "Build artifacts" line ~431)
  </read_first>
  <files>packages/rosbagger-replay/pyproject.toml, pyproject.toml, uv.lock</files>
  <action>
    Create `packages/rosbagger-replay/pyproject.toml` mirroring `rosbagger-record/pyproject.toml` exactly: `[project]` name `rosbagger-replay`, version `0.1.0`, requires-python `>=3.10`, `dependencies = ["rosbagger-core", "typer>=0.15,<1"]` (NOTHING ROS — rclpy/rosidl_runtime_py are environment-provided per D-03, never declared, so uv never tries to fetch a nonexistent wheel). `[project.scripts]` `rosbagger-replay = "rosbagger_replay.cli:app"` (cli.py lands in Plan 03; hatchling does not import it at build time). `[build-system]` requires hatchling. Add a top-of-file comment matching record's, swapping record->replay semantics. Add `packages/rosbagger-replay/src` to the root `[tool.ruff] src` list (after the rosbagger-record entry). Re-lock: run `uv lock` so the `members=["packages/*"]` glob picks up the new member (its only external dep `typer` is already locked, so this is the same clean change Phase 12 made). Do NOT touch the root `[tool.pytest.ini_options] addopts` `--cov=` line — `rosbagger_replay` stays OUT of the coverage gate (D-12). The `live` marker is already registered (confirmed) — no markers change needed.
  </action>
  <acceptance_criteria>
    - ENV offline: `PYTHONPATH="" uv sync --locked --dev` exits 0 (re-locked lockfile is consistent).
    - `packages/rosbagger-replay/pyproject.toml` declares ONLY `rosbagger-core` + `typer>=0.15,<1` as dependencies; grep finds no `rclpy`/`rosbag2_py`/`rosidl_runtime_py` token in it: `grep -v '^#' packages/rosbagger-replay/pyproject.toml | grep -c -E 'rclpy|rosbag2_py|rosidl' ` returns 0.
    - The console script line is present: `grep -c 'rosbagger-replay = "rosbagger_replay.cli:app"' packages/rosbagger-replay/pyproject.toml` returns 1.
    - Root `pyproject.toml` ruff src list contains `packages/rosbagger-replay/src`.
    - The root addopts `--cov=` line is unchanged (still `--cov=rosbagger_core --cov=bagq ...`).
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH="" uv sync --locked --dev && grep -c 'rosbagger_replay.cli:app' packages/rosbagger-replay/pyproject.toml</automated>
  </verify>
  <done>The new member resolves under uv (`uv sync --locked --dev` exit 0), declares no ROS deps, declares the console script, and is in the ruff src list; the coverage gate is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Teaching capability errors (errors.py) — RosNotAvailableError + NoMessagesToReplayError</name>
  <read_first>
    - packages/rosbagger-record/src/rosbagger_record/errors.py (the EXACT pattern — RuntimeError subclasses, stdlib-only top, teaching message in __init__, carries structured data)
    - .planning/phases/13-live-replay/13-RESEARCH.md (Anti-Patterns "Empty selection ... -> raise NoMessagesToReplayError"; Pattern 1 RosNotAvailableError)
  </read_first>
  <files>packages/rosbagger-replay/src/rosbagger_replay/errors.py</files>
  <action>
    Create `errors.py` mirroring `rosbagger_record/errors.py`: stdlib-only (`from __future__ import annotations`, no other imports), with a module docstring stating the offline invariant (imports no rclpy/rosbag2_py/rosidl_runtime_py; `test_offline_guard.py` will assert `import rosbagger_replay` leaks no ROS). Define `RosNotAvailableError(RuntimeError)` — its `__init__` builds the same teaching message as record's ("rclpy ... not importable — source your ROS 2 environment (e.g. `source /opt/ros/humble/setup.bash`) before replaying ..."). Define `NoMessagesToReplayError(RuntimeError)` carrying structured data `.bag_paths` and `.topics`, with a teaching `__init__` message naming what was requested and the remedy (e.g. "No messages to replay (bag_paths=..., topics=...). Check the bag path and that --topics names a topic that exists; run `rosbagger-replay <bag> --topics` with no filter to replay everything."). Both build their message in `__init__` (API-first; the CLI only presents). Do NOT add an `__all__` here that references the lazy `replay` function (that lives in `__init__.py`, Plan 03).
  </action>
  <acceptance_criteria>
    - ENV offline: `PYTHONPATH="" uv run python -c "import rosbagger_replay.errors as e; assert issubclass(e.RosNotAvailableError, RuntimeError) and issubclass(e.NoMessagesToReplayError, RuntimeError)"` exits 0.
    - `NoMessagesToReplayError(bag_paths=['/x'], topics={'/a'})` carries `.bag_paths == ['/x']` and `.topics == {'/a'}` and `str(e)` is non-empty teaching text.
    - errors.py imports only stdlib: `grep -c -E '^(import|from) ' packages/rosbagger-replay/src/rosbagger_replay/errors.py` shows only `from __future__` (no rclpy/rosbags/rosbagger_core import).
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH="" uv run python -c "import rosbagger_replay.errors as e; x=e.NoMessagesToReplayError(bag_paths=['/x'], topics={'/a'}); assert x.bag_paths==['/x'] and x.topics=={'/a'} and str(x); assert issubclass(e.RosNotAvailableError, RuntimeError)"</automated>
  </verify>
  <done>Both capability errors exist as stdlib-only RuntimeError subclasses with teaching messages built in __init__; NoMessagesToReplayError carries its structured data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Raw-CDR source seam (source.py) + ReplayItem contract + source unit tests over real fixtures</name>
  <read_first>
    - .planning/phases/13-live-replay/13-RESEARCH.md (Pattern 2 source.py listing lines ~244-287 — the VERIFIED seam; Pitfall 2 ROS1 bridge; Pitfall 6 empty selection)
    - packages/rosbagger-core/src/rosbagger_core/reader/rosbags_reader.py (the connection-filter + ROS1/ROS2 raw handling precedent: read() lines 121-165, connections filter)
    - tools/make_fixtures.py (write_ros2_sqlite_bag/write_ros2_mcap_bag/write_ros1_bag — the fixtures the test replays; 9 msgs, 3 /imu)
    - tests/test_record_unit.py (the unit-test harness shape: self-contained, real fixtures via tmp_path)
  </read_first>
  <files>packages/rosbagger-replay/src/rosbagger_replay/source.py, tests/test_replay_unit.py</files>
  <behavior>
    - Test (source-ros2-sqlite3): load_items(write_ros2_sqlite_bag(tmp_path), default_typestore=ROS2_HUMBLE) returns 9 ReplayItems, time-ordered by t_ns (non-decreasing), each .cdr is non-empty bytes, .topic in {/cmd_vel,/imu,/image}, .msgtype matches the topic.
    - Test (source-ros2-mcap): load_items(write_ros2_mcap_bag(tmp_path)) (NO default_typestore — self-describing) returns 9 ReplayItems; cdr is bytes.
    - Test (source-ros1-bridge): load_items(write_ros1_bag(tmp_path)) returns 9 ReplayItems; for a ROS1 conn the cdr came through reader.deserialize -> serialize_cdr (assert .cdr is bytes and the run does not raise — the bridge produced CDR, not raw ROS1 wire).
    - Test (topics-filter): load_items(ros2_sqlite_bag, topics={"/imu"}, default_typestore=ROS2_HUMBLE) returns exactly 3 items, all .topic == "/imu".
    - Test (empty-selection): load_items(ros2_sqlite_bag, topics={"/nope"}, default_typestore=ROS2_HUMBLE) returns [].
    - Test (offline): source.py imports no rclpy (asserted in this file by checking sys.modules after import, plus the dedicated offline-guard test lands in Plan 03).
  </behavior>
  <action>
    Create `source.py` exactly as 13-RESEARCH Pattern 2 specifies (VERIFIED end-to-end on box): `from __future__ import annotations`; a frozen, slotted `@dataclass ReplayItem` with fields `t_ns: int`, `topic: str`, `msgtype: str`, `cdr: bytes`. `load_items(bag_paths, *, topics=None, default_typestore=None) -> list[ReplayItem]` — import `AnyReader` from `rosbags.highlevel` INSIDE the function body (mirrors edit/pipeline.py lazy-rosbags discipline so the module top stays clean; rosbags is allowed either way, but lazy keeps the offline-guard rosbags check trivially green for the package's other modules). Normalize `bag_paths` to a `list[Path]` (accept a single str/Path or an iterable, like RosbagsReader.__init__). Open AnyReader; if `topics is not None`, filter `reader.connections` to those whose `.topic` in `topics` and return `[]` when the filtered list is empty (the QURY-05 empty-selection short-circuit — do NOT pass an empty connections list, which rosbags treats as all). For each `(conn, t_ns, rawdata)` from `reader.messages(connections=conns)`: detect ROS 1 via `type(getattr(conn, "ext", None)).__name__ == "ConnectionExtRosbag1"`; for ROS 1 bridge `obj = reader.deserialize(rawdata, conn.msgtype)` then `cdr = bytes(reader.typestore.serialize_cdr(obj, conn.msgtype))` (ROS 1 wire is NOT CDR — Pitfall 2); else `cdr = bytes(rawdata)` (ROS 2 rawdata IS CDR — VERIFIED). Append `ReplayItem(t_ns, conn.topic, conn.msgtype, cdr)`. Close the reader in a `finally`. Return the list (already time-ordered across bags — A2: materialize a list for v1, fixtures are tiny; document streaming as a deferred enhancement in the docstring). Then add the source tests to a NEW `tests/test_replay_unit.py` (self-contained harness with the belt-and-suspenders src-path insert like test_record_live.py is unnecessary here since offline tests run under uv; mirror test_record_unit.py's plain shape, building fixtures into `tmp_path`). Use `-k source` selectable test names. Write the tests FIRST (they fail until source.py exists), then implement source.py to green.
  </action>
  <acceptance_criteria>
    - ENV offline: `PYTHONPATH="" uv run pytest tests/test_replay_unit.py -k source -q` passes (all source tests green).
    - load_items over a ROS 2 sqlite3 fixture returns 9 items, t_ns non-decreasing, each .cdr non-empty `bytes`.
    - The ROS 1 fixture path produces 9 items without raising (the deserialize->serialize_cdr bridge ran).
    - topics={"/imu"} returns exactly 3 items all on /imu; topics={"/nope"} returns [].
    - source.py imports no rclpy/rosbag2_py: `grep -v '^#' packages/rosbagger-replay/src/rosbagger_replay/source.py | grep -c -E 'rclpy|rosbag2_py|rosidl'` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH="" uv run pytest tests/test_replay_unit.py -k source -q</automated>
  </verify>
  <done>load_items reads all three fixture formats into time-ordered ReplayItems with CDR bytes (ROS1 bridged), honors the topics filter, returns [] on empty selection, and imports no ROS; source tests pass offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> source.py | local bag path read (trusted user-owned data) via rosbags AnyReader |
| pyproject -> uv resolver | a new workspace member; only external dep is the already-locked typer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | bag path read in load_items | accept | The bag is the user's own local data, read read-only via the same rosbags AnyReader the offline tier already uses; no privilege boundary crossed. No path written. |
| T-13-02 | Information Disclosure | source.py import surface | mitigate | source.py imports rosbags only, never rclpy/rosbag2_py; the offline-guard test (Plan 03) regression-locks that import rosbagger_replay leaks no ROS. |
| T-13-SC | Tampering | uv member add (no new PyPI install) | accept | No new registry package introduced (RESEARCH Package Legitimacy Audit: only rosbagger-core [local, vetted] + typer [pre-vetted]). rclpy/rosidl_runtime_py are env-provided ROS distro packages, not pyproject deps — slopcheck N/A. |
</threat_model>

<verification>
- ENV offline: `PYTHONPATH="" uv sync --locked --dev` exits 0.
- ENV offline: `PYTHONPATH="" uv run pytest tests/test_replay_unit.py -k source -q` passes.
- `packages/rosbagger-replay/{pyproject.toml,src/rosbagger_replay/errors.py,src/rosbagger_replay/source.py}` exist; errors.py + source.py import no rclpy/rosbag2_py/rosidl.
- The root coverage gate line is unchanged.
</verification>

<success_criteria>
- The `rosbagger-replay` member resolves under uv (re-locked, clean) with no ROS deps and the console script declared.
- `ReplayItem` + `load_items` provide a time-ordered raw-CDR stream across ROS1/ROS2-sqlite3/MCAP (ROS1 bridged to CDR), with a topics subset filter and a clean empty-selection result.
- Teaching capability errors (RosNotAvailableError + NoMessagesToReplayError) exist, stdlib-only.
- No `rclpy`/`rosbag2_py`/`rosidl_runtime_py` is imported anywhere in this plan's code.
</success_criteria>

<output>
Create `.planning/phases/13-live-replay/13-01-SUMMARY.md` when done.
</output>
