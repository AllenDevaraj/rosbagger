"""Schema seam — reserved for Phase 3 (message -> table schema mapping).

Intentionally empty in Phase 1: this package reserves the layout so the schema
mapper plugs in without restructuring. It ships no logic.
"""
