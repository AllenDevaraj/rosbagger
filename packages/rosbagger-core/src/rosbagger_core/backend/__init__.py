"""Backend seam — reserved for Phase 5 (the swappable QueryBackend; DuckDB default).

Intentionally empty in Phase 1: this package reserves the QueryBackend seam so
the backend plugs in without restructuring. It ships no logic.
"""
